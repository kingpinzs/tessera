<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Package visibility (API 30+): lets the client resolve the shell's Live Tile API provider. Merged into the app. -->
    <queries>
        <provider android:authorities="app.tileshell.livetile" />
    </queries>
</manifest>
