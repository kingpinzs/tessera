# metro-launcher build state
- 2026-09-16: Project started via phased-build. Stage A step 1: PLAN.md drafted at docs/plan/PLAN.md.
  Next: Q1 (tech stack) is waiting on Jeremy. No code yet. No INDEX.md yet (created at split).
- 2026-09-16: Q1 ruled A (Kotlin + Compose, run in emulator, sideload the APK). Cortana simulation added to scope.
  Assumption A6: WP8.1 look (Cortana was 8.1-only). Next: Q2 (what powers Cortana) is waiting on Jeremy.
- 2026-09-16: Q2 ruled C first (offline fixed commands), with an on-device tiny LLM as a later layer. No cloud AI.
  Assumption A7: layers are permanent (Hard Rule 16). Next: Q3 (build order) is waiting on Jeremy.
- 2026-09-16: Q3 ruled A (Start screen first). Target look changed to Windows 10 Mobile (was the WP8.1 assumption).
  R1 (W10M fidelity reference) and R2 (reuse check) added as agent research before the split. Next: Q4 (phone model).
- 2026-09-16: Q4 ruled Galaxy S25 Ultra. Next: Q5 (what live tiles show).
- 2026-09-16: Q5 ruled A (notification-driven live tiles plus weather). Jeremy expanded scope to replacing all of Android's UI. Verified: S25U bootloader can't be unlocked on One UI 8, so no ROM. Next: Q6 (how deep).
- 2026-09-16: Q6 ruled A (full shell built from apps, no root; ROM stays open for later). Single-phase questions moved to per-phase interviews. Next: Q7 (build order).
- 2026-09-16: Q7 ruled A (home screen first, full order in PLAN.md). Next: Q8 (font), the last plan question, then R1/R2 research and the cross-model review.
- 2026-09-16: Q8 ruled A (Selawik). All plan questions are done. Dispatched R1 (w10m-reference.md) and R2 (r2-reuse-check.md) research agents. Next: Stage A step 2, the cross-model review (Fable + codex) on PLAN.md plus the research.
- 2026-09-16: R2 done (docs/plan/r2-reuse-check.md). Headlines: no W10M launcher worth building on; MangoTile (MIT) for Compose Metro controls; FlorisBoard for the keyboard; OpenDialer, not Fossify (GPL); Vosk then sherpa-onnx; LiteRT-LM. R1 still running.
- 2026-09-16: R1 done (docs/plan/w10m-reference.md, 24 NOT FOUND gaps: tile motion durations, gutters, launch animation, Cortana ring timings). Next: cross-model review.
- 2026-09-16: Cross-model review round 1: codex (MCP) done, 7 BLOCKING / 9 MAJOR, saved to docs/plan/review/2026-09-16-codex.md. Fable reviewer still running. Next: merge and triage, then re-interview Jeremy on the real gaps, one at a time.
- 2026-09-16: Fable review done (8 BLOCKING / 12 MAJOR / 9 MINOR). Triage in docs/plan/review/2026-09-16-triage.md: 3 ASK (Q9-Q11), 8 agent resolutions (RV1-RV8 in PLAN.md), the rest go to per-phase interviews. Next: Q9 (first real feed for the tile engine).
- 2026-09-16: Q9 ruled C (all live tiles in phase 1; 8 phases now). Principle: quality over speed. Next: Q10 (fidelity source for NOT FOUND values).
- 2026-09-16: Q10 ruled A (measure from real W10M video). R3 measurement agent dispatched -> docs/plan/w10m-measurements.md; scratch video files under /tmp/claude-1000/.../scratchpad/r3-*. Next: Q11 (scaling).
- 2026-09-16: Q11 ruled A (950 proportions, 360-epx canvas mapped to screen width). All pre-split questions ruled. Dispatching review round 2 (Fable + codex).
- 2026-09-16: Review round 2 codex: 2 BLOCKING (R2R2-01 Shizuku vs one-APK rule, needs Jeremy; R2R2-02 source order, resolved as RV9 + R3 scope extended to check legacy motion values). Waiting on round 2 Fable before asking Jeremy.
- 2026-09-16: Review round 2 Fable: 0 BLOCKING / 7 MAJOR / 5 MINOR. Round-2 triage in docs/plan/review/2026-09-16-r2-triage.md: ASK Q12 build, Q13 toggles/Shizuku, Q14 phase-1 layout; RV3/RV4/RV5 amended, RV10 added, CANNOT list widened. R3 scope extended twice. Next: Q12.
- 2026-09-16: Q12 ruled C (final W10M release, all updates included); R3 told. Post-plan: Jeremy's own updates come as a new plan. Next: Q13 (toggles / Shizuku).
- 2026-09-16: Q13 ruled A (Shizuku-style helper inside our APK). Principles P1 (quality over speed) and P2 (all in one, seamless) recorded. Next: Q14 (phase 1 layout source).
- 2026-09-16: Jeremy correction P3: phases are dev chunks, not rollouts; phase placement is the agent's call. Q14 withdrawn and resolved (default layout in phase 1, pinning in phase 2). Dispatching review round 3 (final allowed round).
- 2026-09-16: Review round 3 codex: 0 BLOCKING / 0 MAJOR / 1 MINOR (stale round-1 triage rows, fixed: marked superseded, round-2 topics merged into the per-phase list). Waiting on round 3 Fable.
- 2026-09-16: Review round 3 done. Both reviewers 0 BLOCKING, so the PLAN is REVIEW-CLEARED. Clerical fixes applied (feature list helper, Cortana waveform, R3 scope, RV1/RV9/RV10 wording, R4 helper spike added). Triage: docs/plan/review/2026-09-16-r3-triage.md. Next: Jeremy's go to split (Stage A step 3). R3 still running.
- 2026-09-16: Stage A step 3 SPLIT done: 8 phase docs (DRAFT) + INDEX.md (master). Toggle commands go to phase 04 with the helper. Phase 1 agent resolutions: 3 columns, light+dark (dark default), W10M bars on Start, missing app = W10M behaviour. Next: step 4, phase 01 interview Q1 (default tile stand-ins).
- 2026-09-16: Phase 01 interview Q1 ruled C with assignable slots (unassigned unless Android has a category default). Layout store moved from phase 02 into phase 01. Next: phase 01 Q2 (notification semantics).
- 2026-09-16: Phase 01 Q2 ruled B plus a public Live Tile API modelled on Windows. R5 research dispatched (docs/plan/r5-live-tile-api.md). Next: phase 01 Q3 (Weather tap).
- 2026-09-16: Phase 01 Q3 ruled A (W10M-style Weather app in the shell, in phase 01). R3 told to measure MSN Weather screens. Next: phase 01 Q4 (nav mode).
- 2026-09-16: Phase 01 Q4 ruled B (3-button nav). Long-press-Back recents question added to phase 04's queue. Next: phase 01 Q5 (Secure Folder).
- 2026-09-16: R5 done; precedence + Live Tile API design recorded in phase 01 (secondary tiles in phase 02). Still waiting on phase 01 Q5 (Secure Folder).
- 2026-09-16: R3 done (w10m-measurements.md). Launch transition corrected to W10M's measured scale+fade (no turnstile). Conflict found: no press tilt on Start tiles in final W10M vs feature list; queued as phase 01 Q6 for Jeremy. Still waiting on Q5.
- 2026-09-16: Phase 01 Q5: Jeremy set principle P4 (Android gaps get a Microsoft-style design); profile groups designed and recorded. Next: phase 01 Q6 (press feedback on Start tiles, R3 conflict).
- 2026-09-16: Phase 01 Q6 ruled (press effect setting: none default / WP8 tilt / P4 press). Phase 01 interview DONE. Next: phase 02 Q1 (layout backup).
- 2026-09-16: Phase 02 Q1: no backup/export-import (removed). Jeremy: 'this is called a launcher so lets keep it sort of that direction' is ambiguous against Q6 (full shell); asking whether it's naming only or a re-scope.
- 2026-09-16: Jeremy: 'ignore what I said and keep going', so scope is unchanged (full plan). Phase 02 interview DONE. Phase 03: models bundled in the APK, sound-alike sounds (agent). Next: phase 03 Q1 (wake word).
- 2026-09-16: Phase 03 Q1 ruled C (no wake word for now; engine must support keyword spotting). Next: phase 03 Q2 (command list).
- 2026-09-16: Phase 03 Q2 ruled A (W10M Cortana phone commands); routing, reminders and Notes slot designed (agent). Next: phase 03 Q3 (voice).
- 2026-09-16: Phase 03 Q3 ruled C (bundled voices, pick in settings); engine = sherpa-onnx (agent). Phase 03 interview DONE. Phase 04 parked until R4 (needs phone). Next: phase 05 Q1 (keyboard feature set).
- 2026-09-16: Phase 05 Q1 ruled C (full W10M keyboard + offline voice typing); depends on 03 now. Phase 05 interview DONE. Next: phase 06 Q1 (RCS).
- 2026-09-16: Phase 06 Q1 ruled A (W10M Messaging default, no RCS; verified a third-party app can't do RCS). New principle P5: no Google unless required, applied to the AVD image (AOSP), phase 08 model (non-Google) and the dictionary. MMS in (agent). Next: phase 06 voicemail.
- 2026-09-16: Phase 06 Q2 ruled A (visual voicemail, T-Mobile) + on-device transcription so he doesn't pay for voicemail-to-text. Phase 08: Microsoft Phi first candidate. Phase 06 interview DONE. Next: phase 07 Q1 (glance).
- 2026-09-16: Phase 07 Q1 ruled A (W10M glance, all modes, AOD off). Phase 07 interview DONE. Next: phase 08 Q1 (model delivery).
- 2026-09-16: Phase 08 Q1 ruled B (one-time model download; A11 amended). Next: phase 08 Q2 (confirmations).
- 2026-09-16: Phase 08 Q2 ruled A (confirm texts, calls, calendar, reminders). New requirement: correctable harness (memory/rules/hooks) like his Claude setup. Next: phase 08 Q3 (shape of the tuning harness).
- 2026-09-16: Phase 08 Q3 ruled C; the harness became phase 09 (memory in Jeremy's Claude format + index, Notebook, corrections, rules, scriptable hooks, adb-only file access, replay tests). Interviews done except phase 04 (waits for R4 on the phone). Next: Stage A step 5, cross-model review of all phase docs (Fable agent + codex CLI in background).
- 2026-09-16: Phase-doc review round 1: codex out for the session (CLI exit 1 untrusted dir; MCP usage limit until 09-19). Reviewer 2 = second Fable (testability lens). Fable 1 (design lens) running.
- 2026-09-16: Phase-doc review round 1 done (reviewer 1: 3B/12M/10m; reviewer 2: 3B/16M/15m). Triage: docs/plan/review/2026-09-16-phases-triage.md. Owner questions PQ1 (TTS GPL), PQ2 (visual voicemail), PQ3 (Cortana while locked). R6 measurement pass and a fixer agent dispatched in parallel; owner rulings go into the triage file until the fixer finishes.
- 2026-09-16: Fixer agent applied all AGENT items from the phase triage; spot-verified with grep (no recently added / press tilt / (interview) left; phase 08 depends [03, 04]; pending-R6 tags present). Waiting on PQ1 and R6.
- 2026-09-16: PQ1 ruled A (Kokoro + GPL espeak-ng accepted for personal use). Next: PQ2 (visual voicemail).
- 2026-09-16: PQ2 ruled B (voicemail via helper experiment, probe in R4; phase 06 depends on 04). Next: PQ3 (Cortana while locked).
- 2026-09-16: PQ3 ruled A (safe commands while locked, personal ones need unlock). All review owner questions answered. Waiting on R6, then replace (pending R6) tags, then phase-doc review round 2.
- 2026-09-16: R6 FAILED: agent stopped with a 429 'You've reached your Fable limit' (request id req_011Cf8FuWxnWvHmyM4oHNzgY). Its last note: yt-dlp hit a 403 (no JS runtime). No r6-measurements.md written. Waiting on Jeremy: rerun on Opus or wait for the Fable limit.
- 2026-09-16: Jeremy chose A: rerun on Opus. yt-dlp 403 root cause: system yt-dlp 2026.02.04 too old for YouTube's SABR change; fixed without touching the system copy via uvx yt-dlp 2026.08.19 + --js-runtimes node (probe download rc=0). R6 relaunched on Opus. Phase-doc review round 2 will also run on Opus.
- 2026-09-16: R6 done on Opus (r6-measurements.md, 573 lines). Back on Start resumes the last app; Search tap/hold opens Cortana; New caption doesn't clear on a timer; W10M had a Cortana lock-screen setting. Next: fixer pass replacing (pending R6) tags, then phase-doc review round 2 on Opus.
- 2026-09-16: R6 values applied (0 pending tags, verified by grep). Back history resets on lock (fidelity, agent). Leftover untagged R6 values (caption placement, keyboard handedness/emoticon settings, reminder card, Cortana text box, glance date/notification rows) go to review round 2. Dispatching review round 2: opus + opus.
- 2026-09-16: Phase-doc review round 2 done: design (2B/14M/13m), testability (5B/22M/16m). Two round-1 items still open (F1-M12 ph04, F2-M5 ph09). Opus triage+fix agent applying non-owner fixes (-> review/2026-09-16-phases-r2-triage.md); R7 footage pass (Phone, Messaging, Notebook, action-center motion) on Opus. Owner questions: R2D-06 AI while locked, R2D-12 keyboard languages, R2D-13 place/person reminders. Round 3 (last) follows.
- 2026-09-16: R2D-06 ruled A (AI while locked: general questions only, personal needs unlock). Recorded in review/2026-09-16-phases-r2-owner.md, applied after the fixer finishes. Next: R2D-12 (keyboard languages).
- 2026-09-16: R2D-12 ruled A (keyboard English US only). Next: R2D-13 (place/person reminders).
- 2026-09-16: R2D-13 ruled A (time + place + person reminders; place via LocationManager, not Google). All round-2 owner questions answered; waiting on the fixer and R7, then apply owner rulings, then review round 3.
- 2026-09-16: Round-2 fixer done (68 AGENT, 3 OWNER, 1 R7; all 106 IDs in the triage file). Applied owner rulings R2D-06 (phase 08), R2D-12 (phase 05), R2D-13 (phase 03). Waiting on R7, then review round 3.
- 2026-09-17: R7 done (r7-measurements.md, 202 rows). W10M had no voicemail list (PQ2 list kept, labelled P4 design); Phone app has 3 icon tabs + Contacts button; Notebook is a flat light list; action center is a finger-following drag. Opus fixer applying R7 values + scope notes to phases 04/06/09. Next: review round 3 (last), then Jeremy's finalize.
- 2026-09-17: Phase 03 now owns the Cortana pane/Reminders/Settings values (moved from phase 09). Review round 3 (last allowed) dispatched: opus design + opus test -> review/2026-09-17-phases-r3-opus-{design,test}.md. Known leftover for them: R7 §3.8.1 card.
- 2026-09-17: Review round 3 done (cap reached): design 5B/5M/7m, test 8B/14M/16m. Opus fixer applying all agent-fixable items (-> review/2026-09-17-phases-r3-triage.md). Owner questions: place-reminder locations, person-reminder direction. R3D-10 is a lettering mix-up in the r2 triage file, not a real ambiguity (Jeremy's A = all three kinds).
- 2026-09-17: Place locations ruled C (save on the spot via GPS, or type an address looked up once via Nominatim; amends A11). Next: person reminder direction.
- 2026-09-17: Person reminder direction ruled A (either direction; answered calls and texts both ways; missed calls don't count). All round-3 owner questions answered; apply after the fixer finishes, then Jeremy's finalize.
- 2026-09-17: Round-3 fixer done (51 AGENT + 4 OWNER, 0 rejected, all 55 IDs triaged). Owner rulings (place source C, person direction A) applied to phase 03 + A11; no owner-pending or (pending R) tags left. Stage A step 6 done. Next: Jeremy's 'finalize' for 01-03 and 05-09 (04 stays DRAFT until R4).
- 2026-09-17: FINALIZED (Jeremy 'A'): 01-03, 05-09 FINAL; 04 DRAFT. Stage B done: docs/plan/build-prompt.md written, INDEX updated (steps 7-9, change log). Next: fresh session builds phase 01 ('Load the phased-build skill, read docs/plan/INDEX.md, continue the build.'); R4 phone spike needed before phase 04.
- 2026-09-17: STAGE C started (Jeremy: 'continue'). Phase 01 row -> building. git init in project root, .gitignore added. Next: build task 1 (Android SDK + AOSP AVD).
- 2026-09-17: Phase 01 tasks 1-2 done: SDK + AOSP AVD booted (Android 16, 1080x2340@450), Gradle skeleton builds (rc=0) and installs. Compose BOM 2026.06.01 (newer BOMs need compileSdk 37; RV7 says 36). Emulator pid in scratchpad/emulator.pid. Next: tasks 3-5 (branding, tokens, motion).
- 2026-09-17: Phase 01 tasks 3-6 code done and compiling (brand, tokens, motion, diagnostics, tile model, layout store, default layout, app catalog, slot resolver). Build-start agent calls to add to the INDEX change log: Compose BOM 2026.06.01; default accent 0078D7; default layout arrangement; Calendar as a category slot; Settings hub launcher label. Next: tile content contract + engine, Start screen, bars, launch.
- 2026-09-17: Start screen runs on the emulator (screenshot docs/plan/qa/phase-01/progress/2026-09-17-first-start-screen.png). Fixed insets-controller crash at the helper. Next: feeds (photos/calendar/music/weather), full app list, Weather app, Settings hub, onboarding, HOME role flow, Live Tile API, release signing, then the QA gate.
- 2026-09-17: MERGE NOTE: when the weather branch lands, add WeatherFeed.start(this) to ShellApp.startFeeds (removed a reflection call to avoid a consumer-side workaround).
- 2026-09-17: Done so far in phase 01: tasks 1-7, 9 (photos/calendar/music/notifications), 10, 13-17 on main; early checks pass for E3 (max 0.42 epx diff) and E20 basic path (same task resumed). Parallel worktree agents building task 8 (Live Tile API), 11 (app list), 12 (weather). Fixture APKs downloading to scratchpad/fixtures. Next: merge the three branches, wire WeatherFeed.start + checklist rows for Samsung badge/legacy broadcasts, then run the full QA gate.
- 2026-09-17: Merged weather (Nominatim swap, feed at app start) and app list (+ACCESS_HIDDEN_PROFILES). Build + unit tests rc=0; emulator shows live Calendar and Weather tiles and the W10M app list. Waiting on the Live Tile API branch; then QA gate.
- 2026-09-17: Post-FINAL change (Jeremy): bottom tile row, small tiles up to 6, default Phone/Messaging/Camera. Logged in INDEX Change Log; phase 01 (E21, H35) + phase 02 (E9) docs updated; code done (layout store v2, DefaultLayout without the three, fixed row on Start). Verified on the emulator window. All three agent branches merged (weather, app list, Live Tile API). Next: Live Tile API kill-switch Settings page + checklist rows (Samsung badge, legacy broadcasts), then the full QA gate.
- 2026-09-17 10:10: phase 01 QA: E8, E11 PASS; E20 PASS after BackRules fix (0c29b43; HOME activity-level exclusion, unlock re-show not counted, launchable fallback), evidence 4d371f2. INDEX Change Log: QA-gate agent calls (E20 lock screen method, Back window clarification, E4 fixture swaps: Gramophone, F-Droid Basic; Maps/People one-handler). Next: E4/E4b slots, then E12+E9 (clock), E10 motion, E13, E15-E17 API, E18 profiles, edge cases, reviewers.
- 2026-09-17 10:18: E4/E4b PASS after picker fix (Settings Tile apps crash + Start picker covering bars; commit before evidence). Layout restored (PHOTOS=Aves). Lock screen restored disabled. Next: E12 app list + E9 weather (shared clock move), E10 motion, E13 settings, E15-E17 API, E18 profiles, edge cases.
- 2026-09-17 10:21: OPEN ITEM: X25 build-start keyboard check never recorded (Decisions bar rule requires it). E12 search screenshot shows the system nav bar (IME) visible and the drawn nav bar gone while typing. Investigate after E12 and record in Decisions/INDEX.
- 2026-09-17 10:35: E9/E9b PASS (H9 human), E12 PASS after caption baseline fix 915ef14; Change Log (4) caption wording. Next: X25 keyboard check (open item), E10 motion, E13 settings, E15-E17 API, E18 profiles, edge cases.
- 2026-09-17 10:50: E10 in progress: peek transition built (was missing) + start-to-start tile period fix committed; peek measured in tolerance on the pre-period-fix build (slide code unchanged since). Test client A tile placed at (0,6) via layout JSON (restore layout_before_E10.json at the end). Next: timing capture (3x170 s), flip/crossfade durations, Start exit/return, press styles.
- 2026-09-17 11:05: E10: peek built (f5edc1e), periods start-to-start (f5edc1e), flip single animation (e1dc849); run 1 evidence filed; run 2 (final build) capturing in background. Settings widget semantics (selected/checked/slider %) built, not yet committed (verify in E13). Next: run 2 analysis, exit/return capture, press capture, E13.
- 2026-09-17 11:30: E10 PASS (H2, H8 human) after fixes f5edc1e (peek + periods), e1dc849 (flip), bdbcaed (entrance fade); 18b4103 settings semantics; evidence 6d51988 (large timing videos on disk only, manifest). Emulator: someone clicked the emulator window mid-capture at 11:06 (segment replaced). Next: E13 settings, then E15-E17 API, E18 profiles, edge cases.
- 2026-09-17 12:05: E15/E16/E17/E18 PASS. Fixes: badge over every face (b39173f). Remaining for phase 01 gate: edge cases list, then reviewers (Fable + roster), adversarial review of the Live Tile API, NEEDS-HUMAN rows, phone-only P rows. Jeremy asked about the missing Search key (phase 03, answered).
- 2026-09-17 12:22: Edge cases: slots+row (role-follow fix 9d7a248), notifications, Live Tile API done. Next: clock/locale/timezone, photos, background, calendar, music, weather, density, liveness, app-list scale, taps, nav, theme mid-animation.
- 2026-09-17 13:00: Edge cases done so far: slots/row, notifications, API trust+counts, photos, calendar, music, weather, background, locale/timezone (clock fix 2060d53), interaction, liveness. Running: 300-app install for app-list scale. Left: app list scale checks, profiles-while-open, density cross-ref, then reviewers + NEEDS-HUMAN + P rows.
- 2026-09-17 13:55: Reviews done (codex + opus design + opus adversarial). 12 fixes landed incl. HIGH badge-spoofing hole. Final pass running on one APK (fresh install; out-of-box accent 0078D7 verified, E12 precondition real). Left: finish final pass rows, write verdicts, NEEDS-HUMAN handoff.
- 2026-09-17 14:10: Phase 01 QA gate complete: all rows + edge cases pass on the final APK (fresh install), three reviews triaged, 22 defects fixed. INDEX row = QA passed, NEEDS-HUMAN open. Next: Jeremy H sign-offs + phone P rows, or phase 02 in a fresh session.
- 2026-09-17: Phase 01 QA gate closed. E1-E21 + edge cases re-run on one final APK over a wiped install
  (qa/phase-01/FINAL); three reviews triaged (qa, design, adversarial on the Live Tile API); 22 defects fixed,
  including the legacy-badge spoofing hole. INDEX row = "QA passed, NEEDS-HUMAN open". Jeremy signed off
  H24/H29/H35/H36/H37 and Start-side H1 on the fresh-install capture. 58 commits on main, nothing pushed.
  Next: phase 02 (edit mode + folders) in a FRESH conversation, per the 2026-09-17 ruling that an open
  NEEDS-HUMAN row does not gate the next phase.
- 2026-09-17: PHASE 02 STARTED (fresh session). Scope = edit mode + folders only; hard stop after its gate.
  Carried in from phase 01: (a) the 2-column overflow defect is phase 02's reflow to fix (E9 + edge case);
  (b) MusicFeed's ~3 s republish dedupe is phase 01's engine, OUT of phase 02's scope (Scope "Out"), left recorded.
  INDEX row -> building.
- 2026-09-17..21: PHASE 02 build. Tasks 1, 2, 4, 7 DONE and committed (54b031d ordered layout + GridPack;
  ca88a54 edit mode, folders, bottom-row editing). Unit suite rc=0, 87 tests, 0 failures (LayoutOpsTest 17,
  GridPackTest 7 incl. the 2-column reflow that fixes phase 01's defect).
  Verified live on the emulator before it went down: v2->v3 migration keeps the layout, 783-ms hold enters edit
  mode with the measured contraction/dim, discs draw and resize cycles, drop-during-dwell makes a folder that
  auto-expands into the band, drop-after-dwell moves with reflow. 4 defects found and fixed that way
  (pointerInput keyed on its own state cancelled the drag; drop target re-packed instead of reading the drawn
  grid; forward drag inserted before the target = no-op; resize arrow rotated the wrong way).
  Tasks 3 (pin from app list), 5 (package changes), 6 (secondary tiles) were dispatched to two worktree agents;
  BOTH died on the weekly account limit with 0 commits. Partial uncommitted work sits in
  .claude/worktrees/agent-a00901a5ef0b39ec2 (3+5) and agent-a11fbac22182b9dd6 (6). Emulator not running.
  Next: resume both agents, merge, then the QA gate (E1-E9 + edge cases). Nothing pushed.
- 2026-09-21: PHASE 02 built (7/7 tasks) and QA'd on the emulator; every row passed, 9 defects found and fixed.
  BOTH reviewers then returned the gate NOT CLOSED on evidence integrity; the lead verified every blocking
  claim (E4 cannot fail; E1/E7/E8 logs predate their drivers; E8 has no assertions; the 830-ms bracket is
  fail-open; EDGE verdicts never reach the log; band-drop, folder-on-folder, H8/H10/H12/H13 never run).
  Two PRODUCT defects the reviewers found are FIXED: a drag above the grid left a stale drop target (a release
  there made a folder with a tile the finger had left), and an emptied bottom tile row could never be refilled.
  Triage + the 14-item owed list: docs/plan/review/2026-09-21-phase02-gate-triage.md. INDEX row = "QA returned
  (reviewers blocking)". Jeremy deferred the re-runs to an end-of-build QA pass. 20 commits on main since
  phase 01, nothing pushed. NEXT SESSION = PHASE 03 (its own session, per the hard stop).
- 2026-09-21 (later): GATE REWORK after both reviewers returned phase 02 NOT CLOSED. All 14 owed items fixed
  in code and harness (commits 8b545e1, 7eeb8b4): assert.sh gives every row real comparisons and an exit code;
  run_all.sh runs the unit tests and every row and fails on any failure; layout_restore stops the shell before
  writing and reads the store back; build_guard stamps each log with the driver blob and the installed APK;
  E4 mutates the layout before testing persistence; the 830-ms bracket rejects absence and carries a control;
  e7.sh regenerates E7 across BOTH themes, every tile, three recordings, median + spread; EditValuesTest pins
  the ~30 R6 constants (113 unit tests, 0 failures).
  FOUR product defects fixed by all this: a drag above the grid left a stale drop target; an emptied bottom row
  could not be refilled; the exit started 169-185 ms after touch-up instead of 150 +/- 17 (delay counted from
  the coroutine's scheduling, not the touch-up — now 151-158 ms); a gesture another handler consumed counted
  as a tap. Both "emulator limit" write-offs were wrong: the latency is measurable from the ring buffer, and
  Home-while-showing is testable through the drawn Windows key.
  VERIFIED: unit tests, and E7 standalone (15/15 dark geometry, 17/17 light, 4/4 both brackets, entry + exit
  motion, latency). NOT VERIFIED: E1, E2/E6, E3, E5, E8, E9, EDGE, regress — their drivers were rewritten with
  new assertions and new sub-rows and have not been re-run. Jeremy stopped the suite mid-E8 on purpose: the
  re-runs belong to the END-OF-BUILD QA pass, not mid-build. Partial artifacts from that killed run sit
  uncommitted in E01/ and E08/ — overwrite them, do not trust them.
  INDEX row stays "QA returned (reviewers blocking)". Phase 02 is NOT done. Next session = PHASE 03, its own
  session; the owed list is docs/plan/review/2026-09-21-phase02-gate-triage.md.
- 2026-09-21 (evening): PHASE 03 BUILT, GATE PARTIALLY RUN. All 13 build tasks are in and Cortana
  answers a spoken command on the AVD end to end: microphone -> sherpa-onnx streaming zipformer en
  2023-06-26 int8 (grammar pass + open pass, two streams from one recognizer) -> matcher -> action
  layer -> Kokoro int8 en v0.19 reply, spoken aloud. Verified live: "WHAT TIME IS IT" -> TimeQuery ->
  "It's 8:13 PM."; "OPEN CLOCK" -> OpenApp; "WHAT'S THE WEATHER" -> "It's 64 degrees and sunny in
  Mountain View."; "WHAT DAY IS IT" -> DateQuery. 233 unit tests pass, 0 failures.
  Built with two parallel worktree agents (speech process; the R7 pane/Reminders/Settings pages) plus
  the lead (session, persona, text box, cards, matcher, action layer, lock gate, tile, Search key,
  Cortana Settings, checklist, Places/Nominatim). Both merged.
  MODEL SWAP mid-build, recorded in the phase doc's QA evidence and NEEDS-HUMAN sheet: the 20M ASR
  model ships no bpe.model, so sherpa-onnx cannot tokenize plain-text hotwords and the RULED grammar
  pass is impossible with it. Payload is now ~250 MB (73 MB ASR, 143 MB TTS, 66 MB native, 2 ABIs),
  fetched by tools/fetch-speech.sh and pinned by sha256; the APK is 318 MB, well inside the 600 MB
  budget.
  FIVE DEFECTS the gate found, all fixed: (1) the BPE vocabulary was the sentencepiece binary where
  sherpa wants a text vocab - it aborted the speech process on every bind; (2) the framework REUSES a
  VoiceInteractionSession across show/hide, so binding in onCreate left every open after the first
  with no engine; (3) OfflineTts.generateWithCallback's JNI signature is one Kotlin does not emit and
  ART aborted the process on the first reply; (4) four matcher defects the unit tests caught (six
  forty five, midnight, "is it going to rain", contact-name case); (5) an exported receiver
  (androidx.profileinstaller) missing from the allow-list.
  TWO OPEN QUESTIONS IN THE DOC ANSWERED BY THE DEVICE: the system bars CAN be hidden over a
  voice-interaction window (the doc's fallback is unnecessary), and bpeVocab works as an asset path.
  GATE STATUS - honest: E1 5/5, E3 7/7, E5 12/12 PASS. E11 and E12 have drivers and one fixed
  assertion each, re-running. E2 and E6 drivers written and running. E4, E7, E8, E9, E10, E13, E14,
  E15 and the edge cases are NOT RUN - no driver written for them yet. NEITHER REVIEWER has judged
  the gate, so phase 03 is NOT done and its INDEX row is "QA (partial)".
  The harness answers phase 02's returned gate up front: every log carries the driver's git blob and
  a comparison of the INSTALLED APK's md5 against the built one, every assertion reaches both the log
  and the exit code, and a row that asserts nothing FAILS.
  NEXT: finish E2/E6, write and run E4, E7-E10, E13-E15 and the edge cases, then both reviewers.
- 2026-09-21 (E2, still running at handoff): a FIFTH thing E2 owes, found while it ran. The driver
  skips the fixture setup the row itself specifies (R3T-m9: assign the Music / Maps / Camera / Notes
  slots through Settings > Start and Cortana's Settings before the table runs). Without it those four
  commands get "Choose a <slot> app in Start settings first." - the action layer behaving correctly
  for an unassigned slot, and a driver testing nothing. Those four verdicts are not evidence either
  way until the assignment step exists and E2 is re-run.
- 2026-09-21 (Jeremy, correction): "remember only test your current changes NOT everything." I had
  re-run the whole 14-command E2 row after each of three small fixes - ~10 minutes of emulator time
  per cycle, mostly re-proving assertions my change could not have touched, while E4/E7-E10/E13-E15
  still had no driver at all. Stopped the running full-table E2 (pid 3485994; its EXIT trap restored
  wifi and data, verified wifi_on=1). From here a fix is verified by the narrowest thing that would
  have caught it, and the full row belongs to the end-of-build QA pass - the same ruling he made for
  phase 02. Written into qa/phase-03/README.md and saved to memory. The E2 evidence committed
  alongside is from that STOPPED run: 17 passed / 4 failed with the table incomplete, not a verdict.
- 2026-09-21 (E7): driver written and run. It found the phase's most serious open defect: NOTHING
  INSIDE THE CORTANA SESSION RESPONDS TO TOUCH. The window is focused and full-screen, accessibility
  reports every node clickable at the right bounds, the text field takes focus by itself and adb input
  text reaches it (which is why E5's typed request passed) - but an injected tap AND a full
  motionevent DOWN/UP stream produce no dispatchTouchEvent at all on the content view. That is
  instrumented, not inferred: the content view is wrapped in a FrameLayout that records every DOWN and
  UP to the diagnostics ring, and the ring stays empty.
  Two real causes were found and FIXED on the way, neither being the whole story: (1) the session's
  touchable region was computed from the content frame and came out empty, so every tap was treated as
  a touch outside and dismissed the session - fixed with onComputeInsets -> TOUCHABLE_INSETS_FRAME;
  (2) the lifecycle owner was only CREATED when the ComposeView attached - now RESUMED before
  onCreateContentView, and setUiEnabled re-asserted per show.
  NOT established: whether a real finger works. Input injection into a TYPE_VOICE_INTERACTION window
  may be restricted in a way a real touch is not, and that distinction decides whether this is a
  shipping defect or an emulator limit. It belongs on the phone next to P3.
  Also fixed while getting E7 to its first exchange, each verified by its own unit test only:
  leading-filler stripping ("THE TEXT MAM ON MY WAY"), the hotword boost raised to 4.0 (which did NOT
  rescue the contact name - recorded as a result, not a win), and a tolerant contact lookup that
  matches a misheard name by edit distance or by sounding the same (H31). speak.sh now refuses to
  report a result when the recogniser produced no NEW final, after it reported "CALL MA'AM" for a text
  command by reading the previous utterance's line.
- 2026-09-21 (late, environment): THE TOUCH FINDING WAS WRONG AND IS CORRECTED. "Nothing inside the
  Cortana session responds to touch" was written up as a product defect; it is not. The same failure
  then appeared on Start (whose taps passed phases 01 and 02 and E6 earlier the same day), then
  KEYCODE_HOME stopped returning to Start, and finally a tap on Android's OWN ResolverActivity button
  did nothing either. An app cannot break the Home key or the system chooser. The AVD's input is
  wedged: scroll gestures with travel still work, taps and short presses activate nothing, and it
  survives both adb reboot and a full emulator restart.
  A second environment fault found with it: pressing Home raised the app CHOOSER rather than Start,
  because the HOME role was held but no preferred home ACTIVITY was set. The chooser sits in front of
  Start and absorbs every tap - which is very likely what was happening during parts of E6/E7.
  Two REAL defects were found on the way and are fixed regardless: the session's touchable region was
  computed from the content frame and came out empty (onComputeInsets -> TOUCHABLE_INSETS_FRAME), and
  the lifecycle owner was only CREATED when the ComposeView attached (RESUMED now).
  STILL UNKNOWN: whether Cortana's UI is tappable at all. No card button has ever been tapped
  successfully on any build. It needs a WIPED AVD, not a restart.
  WRITTEN THIS SESSION, NOT YET RUN: drivers for E4, E8, E9, E10, E13, E14, E15 and the edge cases,
  plus persona.py (sub-pixel disc/halo per frame), fade.py (R7 3.2's black hold and fade-up),
  coords.py, and provision.sh - which puts a wiped AVD into the baseline state AND ends by checking
  that a tap really activates something, refusing to hand over a device whose input is wedged. That
  check is the thing that would have saved this session hours.
  ALSO: .github/workflows/apk.yml builds the APK on every push and attaches it to a GitHub Release
  tagged "latest", so it can be downloaded straight onto the phone. It runs the unit tests first,
  caches the 250 MB speech payload, and builds release-signed when the keystore secrets are present -
  saying loudly in the job summary when they are not.
- 2026-09-21: BRAND + ICON. The shell is Tessera, the assistant is Tess; the repo is https://github.com/kingpinzs/tessera and the remote is wired as origin. NOTHING IS PUSHED. Only what a user sees changed (Brand.PRODUCT_NAME/ASSISTANT_NAME, the launcher label, the service labels, rootProject.name, the CI artifact prefix); the package id app.tileshell, every resource id, test tag and driver name is unchanged on purpose. Then Tess's eye became HAL 9000's lens with Cortana as a three-tap easter egg (5 s reveal, 1.5 s tap window) — a paint change only, every A22/R6 number untouched, persona.py switched to the lens hue and checked against real device pixels (100.0 px measured vs 98.74 px geometric). Evidence qa/phase-03/H32; new judgement row H32. 251 unit tests, 0 failures. STILL BLOCKED: this AVD will not open a voice session at all (KEYCODE_ASSIST does nothing, ACTION_ASSIST resolves to nothing, taps activate nothing, cmd voiceinteraction show needs a signature permission), so the 8 written drivers (E4, E8, E9, E10, E13, E14, E15, edge) and the re-runs of E2/E6/E12 still cannot run. provision.sh exists to put a WIPED AVD into the baseline and refuses to hand over a wedged one. Next: wipe the AVD, run provision.sh, then the 8 drivers.
- 2026-09-22: Speech root-caused from the phone's Diagnostics dump. `zip -X` keeps DOS timestamps and writes them
  in the builder's LOCAL timezone, so CI's espeak-ng-data.zip hashed differently from this machine's and every CI
  APK shipped with TTS dead (payloads were identical; only the timezone differed). fetch-speech.sh now builds the
  archive with Python zipfile at fixed date/mode/order/level (one hash under UTC, Denver, Tokyo) and verifies both
  pins against SpeechModels.kt, exiting non-zero on drift. ESPEAK_ZIP_SHA256 = efc829fc… Separately, the speech
  error notice was spoken by the engine that had just failed, looping ~1000x in 50 ms; SpeechError.isSpeakable()
  now shows engine faults instead of speaking them. Verified locally (unit tests, guard both directions, and the
  asset inside a freshly built APK). NOT verified: that Tess speaks on the phone — needs a CI build installed.
- 2026-09-22: Jeremy's rulings — medium weather tile gets its detail line back (WIDE all three, MEDIUM the
  first two); item 3's pause behaviour stands as built (shrinks, drops the strip); and he verified item 3 on
  the phone himself: the music tile grows, white glyphs survive a bright cover, and the buttons drive the
  player. Remaining unverified: item 4 (photo slideshow / picture frame), item 2's scenes + the restored
  detail line, item 8's app-list sections.
- 2026-09-22: Emulator verification pass done. ITEM8 16/16, ITEM2 15/15 (incl. the medium-tile detail-line fix,
  asserted on an actually-MEDIUM tile), ITEM4 7/7 (slideshow grows + 3000 ms median; frame neither grows nor
  flips). Drivers live in docs/plan/qa/phase-01/scripts, evidence in ITEM2/ITEM4/ITEM8. Three driver bugs fixed
  (list scroll position, the weather tile's flip race, and prefs written into the force-stop/service-restart
  gap) — all three had produced false failures against correct code. Open observation: white tile label over a
  near-white picture-frame photo has poor contrast; Jeremy's call.
- 2026-09-22: Jeremy's four asks built + verified (ASKS0922 16/16). Flip eased to abs(cos(pi t));
  tile timers moved onto one clock comb so no two tiles ever flip together - costs one shared rate
  instead of two measured bands, undoable via bandCentreMs; slot tiles keep their own name
  (People/Browser/Maps/Messaging, not the resolved app's); Games/Office folders seeded from
  ApplicationInfo.category, one-shot, refusing to invent a folder from apps that declare nothing.
  Device defect found + fixed: seeding raided an existing folder and H19 dissolved it.
  NEEDS-HUMAN: does the flip feel right, and do the folders populate on the phone.
- 2026-09-22: Uninstall added to the app list hold menu (UNINSTALL row 10/10, 5 unit tests). Offered only for
  non-system, main-profile, non-self apps. Root cause found by the row: without REQUEST_DELETE_PACKAGES the
  uninstall intent starts cleanly and Android's uninstaller finishes silently - the tap did nothing. Permission
  added. NEXT: Jeremy asked for a W10M-style media player; that is phase-sized, drafted as phase 10 (DRAFT),
  interview Q1 waiting on him.
- 2026-09-22: Phase 10 tasks 3-5 committed (playback service + media session + the per-package now-playing
  tile rule). Crossfade is NOT built and is recorded as outstanding in the phase doc; gapless is ExoPlayer's
  default. Then Jeremy's "the last active app goes right above the bottom row in this black space":
  RecentPromotion was moving the tile to the end of the grid order, which is not the band above the bottom
  row; it now lifts the tile out and StartPage pins it one gutter above the bottom row. Promotion moved to
  onResume. Evidence qa/phase-01/RECENT0922 11/11 on the emulator. Next: phase 10 task 6 (collection UI).
- 2026-09-22: App list "Recent (5, seven days)" replaced by "Running (3, since boot)" per Jeremy; ITEM8
  rewritten with a real reboot bracket, 20/20 (note: com.android.settings appeared post-boot — the system
  runs it). Phase 10 task 6 built: the collection pivot, letter groups, jump grid, album art, album/artist
  detail, tap-to-play through the session. NOT on MangoTile — it is not a dependency and R8 disqualified
  its numbers; recorded in INDEX + the phase doc. Evidence qa/phase-01/MUSIC6 37/37. 446 unit tests pass.
  Next: phase 10 task 7 (now-playing, built to R8), then 8/9/10.
- 2026-09-22: Phase 10 task 7 built — the now-playing screen to R8's V-2016 measurements, anchored per
  §1.10 (nav bar / chevron 16 / transport 56 / scrubber 96). Evidence qa/phase-01/MUSIC7 46/46, all
  geometry asserted in epx. Also Jeremy's pivot-header ask: the strip scrolls no further than the
  selected header needs, so songs looks exactly as before and playlists pulls it in. QA fixtures are now
  90-second MP3s (4s ran off the end of the queue between driver steps) and the music rows mute the
  media stream. MUSIC6 re-run 38/38. 456 unit tests pass. Next: phase 10 task 8 (playlists + its store),
  then 9 (sleep timer, equaliser) and 10 (settings + checklist rows). Crossfade still NOT built.
- 2026-09-22: H-M1 signed off by Jeremy ("V-2016 looks right"). Phase 10 task 8 built: playlists, stored
  in music_playlists.json (app-private, temp+rename) because MediaStore playlists are not writable.
  Create/rename/reorder/delete plus add/remove, all on the shell's hold-menu idiom. Evidence
  qa/phase-01/MUSIC8 38/38 with a force-stop persistence bracket; MUSIC6 38/38 and MUSIC7 46/46 re-run.
  479 unit tests. Next: task 9 (sleep timer, equaliser) then task 10 (settings + checklist rows).
  Crossfade still NOT built and is the one outstanding part of Q7's "everything".
- 2026-09-22: Phase 10 tasks 9 and 10 built and verified: MUSIC9 44/44 (equaliser enabled in AudioFlinger on the
  player's session; end-of-track pause at 90 044 ms; real 15-min timer fired 900 001 ms after arming), MUSIC10 20/20
  (checklist Music row + in-app grant + don't-ask-again path), MUSIC6 39/39. Also today: Start mark B replaces the
  Windows logo (STARTMARK 8/8); R10 plan additions ruled (Fluent B, setup wizard, quick actions trigger A, pod bay +
  pods, nine inbox apps, A11 stands); plan review running (fable + fable, codex out). Next: crossfade agent builds E17.
- 2026-09-22: Crossfade built (E17): a fader player under the session's own player, equal-power fade, handback by
  seek + drift check + 200 ms micro-fade; setting Off/2/5/8/12 s in the now-playing ••• menu. MUSIC17 26/26
  (242 two-track samples on one session, fade 4948 ms, handback drift -2 ms), MUSIC7 re-run 46/46, 504 unit tests.
  Q7's "everything" is complete. Open for the phone: screen-off playback (no WAKE_MODE_LOCAL set) is unverified.
- 2026-09-22 SESSION END (handoff). Phase 10: ALL build tasks + crossfade built and verified on the emulator (MUSIC6 39,
  MUSIC7 46, MUSIC8 38, MUSIC9 44, MUSIC10 20, MUSIC17 26; 504 unit tests). Gate still owes: phone rows (E7, E9-E13,
  screen-off playback — no WAKE_MODE_LOCAL), H-M2, and the two-reviewer QA gate. Start mark B replaced the Windows logo
  (STARTMARK 8/8). R10 plan additions ruled and reviewed (fable + fable; triage in docs/plan/review/2026-09-22-r10-triage.md):
  Fluent B, setup wizard, quick actions (re-ruled A: burst appears at 783 ms WITH edit mode, 4 shortcut satellites), pod bay
  + pods + "open the pod bay doors", nine inbox apps, A11 stands (no Mail/browser/Maps). STILL OPEN for Stage A: triage Q2
  (acrylic vs the already-built, measured menus and phase 04's "as captured" colours) and Q3 (real Android widgets in the
  pod bay: in or out); then split the additions into phases 11+ (order in the triage), per-phase interviews, review, FINAL.
  NEXT SESSION (Jeremy: "after this we start a new session for a new phase"): the next buildable phase is 05 (keyboard,
  FINAL, emulator-buildable); phase 04 waits on R4 (phone over USB). 36 commits unpushed — push needs Jeremy's approval.
- 2026-09-22: PHASE 05 (keyboard) BUILD STARTED (Jeremy: "continue metro-launcher from docs/plan/INDEX.md — build phase 05").
  INDEX row -> building. Surveying the codebase (IME absent), R6 §2, FlorisBoard licence, phase 03 speech process binding.
- 2026-09-22: Phase 05 build in progress. Committed: text engine (agent, 4 commits, 73 tests), QA tooling
  (agent: testapps/ime-fixture + gesture driver, docs/plan/qa/phase-05/TOOLING.md), build-start checks
  (agent: SCOWL+12dicts dictionary 124,847 words, Fluent Emoji MIT 1,595 PNGs, FlorisBoard nothing ported;
  docs/plan/qa/phase-05/BUILD-START.md, tools/fetch-keyboard.sh), ShellApp main-process guard, speech
  multi-client/one-mic-owner, and the keyboard itself (KeyboardService in :ime). Verified on the AVD: renders,
  typed "hello" via taps (mirror [hello] 5,5), Word Flow swipe decodes hello first, alternates popup shows.
  Fixing: popups drew under the strip text (moved to an overlay canvas). NEXT: probe emoji/symbols/dot/move/
  one-handed, then write QA drivers E1-E12 + edge cases under docs/plan/qa/phase-05/scripts.
- 2026-09-22: Phase 05 QA progress (emulator): E1 10/10, E2 11/11, E3 static 135/135, E3M motion 4/4 (IME cannot own
  the slide: system show 167 ms + fade-in, hide 117 ms + fade-out -> H10), E4 11/11 Word Flow, E5 17/17, E6 9/9.
  Defects fixed from QA: &123 label width, strip ink placement, popup under strip, checklist selected-row read.
  Frames extracted from captures are gitignored (bulk). NEXT: E7 E10 E11, then E9, E8 (audio route), E12, edge.
- 2026-09-22: Phase 05 QA continued (rows now run as a background queue per Jeremy: "do all testing in the background").
  Passing: E1 E2 E3 E3M E4 E5 E6 E7 E9 E10 E11 E12. Fixed from QA: space-bar raise reset (E9), lift point ignored
  (E9), VoicesTest fake + smoke-test premise (594 unit tests, 0 fail). OPEN: the strip's tap ("– word") never reached
  the keyboard in EDGE1 — probe pending (mic is in the same strip, E8 depends on it); EDGE1 long-text now uses the
  fixture's --ei fill. Queue next: EDGE1 re-run, E8, EDGE3, EDGE4 (reboot); then the two Fable reviewers
  (brief docs/plan/review/2026-09-22-phase05-gate-brief.md); Change Log draft in the scratchpad (changelog_draft.md).
- 2026-09-22: Phase 05 gate: final pass 1 (bef3d55) 17/17 rows pass; two Fable reviewers' PART 1 (design: no Decision
  deviations, MAJOR two-thumb routing + minors; evidence: B1 password text in the :ime log, B2 popup-timing pixel could
  not fail, M1-M7). All fixed (commits ecbf8ea, 06f6250, 897bd2f): password redaction, per-pointer routing, MicArbiter +
  device refusal via the fixture recogniser, driver rework, Change Log + BUILD-START records. Final pass 2 running on
  a28c7b58 @ 897bd2f (clean tree). NEXT: triage pass 2, then SendMessage PART 2 to both reviewers (design a7b8dd19...,
  evidence ae73c8b1...), then handoff (phone rows P1-P3 and H1-H23 are Jeremy's).
- 2026-09-22 SESSION END (Jeremy: "this is going on 3 hours. lets push the changes"). Phase 05 gate NOT closed.
  Final pass 2 (897bd2f): 13/17 rows pass. Open items are in the INDEX row (E3M touch reference, EDGE1 log-grep
  driver bug, EDGE2 switcher dump, EDGE3 fixture recogniser not reaching the shell's recognition service), then
  resume both reviewers for PART 2 (their PART 1 files: docs/plan/review/2026-09-22-phase05-{design,evidence}.md).
- 2026-09-22: Mic-prompt bug (Jeremy, phone) fixed: 3 producer defects (Tess never asked; stale permission task swallowed requests; listen dropped before :speech bind). MICPERM 15/15, phase-03 E1 5/5, phase-05 E8 5/5. Commits 3b6ea1f + ee7b64b, pushed to origin/main 2026-09-22. Open: helper-vs-no-helper answer for R4; phase 05 gate items unchanged.
- 2026-09-22: Jeremy reconfirmed Q13 (keep the in-APK helper): "keep it". Noted under Q13 in PLAN.md.
- 2026-09-22: Jeremy: "I know you are missing phases". INDEX.md gained an "In scope but NOT split" table: proposed 11 quick actions, 12 setup wizard, 13 Fluent engine, 14 pod bay, 15-17 inbox apps, 18 Files, 19 Settings front, plus the unruled Tessera bar. Not split yet (Stage A needed). INDEX + PLAN Q13 note uncommitted.
- 2026-09-22: SPLIT done (Jeremy: "split"): phases 11-19 DRAFT docs written by 4 Fable writers, 29 interview questions total. Cross-phase: 15/16 need 17 task 2 first; 16 task 1 fixes assignSlotOnce overwrite; phase 04 queue gained Q11 (AC acrylic vs captured black) + Q12 (All settings target). Live QA ledger docs/plan/qa/JEREMY-QA.md: J1 fixed (20414e5 X clears; c75688d auto-clear on send per Jeremy). Interviews for 11 held until Jeremy finishes QA. Nothing pushed since ee7b64b.
- 2026-09-22 (late): Live QA J1-J4 fixed (J1 Tess text box auto-clear, J2 music headerless-MP3 total/scrub, J3 Start end/folder clears bottom row, J4 hold on promoted tile). Phase 02 suite driver faults fixed; all rows pass except E7 motion timings (method problem, screenrecord). Commits since last push ee7b64b: 20414e5..824f71f. NOT pushed. Next: push on Jeremy approval; E7 method; phase 11 interview.
- 2026-09-23: Stage A step 4 DONE for phases 11-19 (all interview questions answered; A11 amended; P6 added; scope adds: theme presets, TV channels; music streaming+radio back). Next: step 5 cross-model review of 11-19; research R11/R12/R13 pending; Jeremy owes 4 theme pictures (docs/plan/theme-art-brief.md); phase docs 20 (music streaming/radio) and 21 (TV channels, after R13) to write.
- 2026-09-23: PUSHED ee7b64b..f8032a0 (46 commits: J1-J7 fixes, phase 02 driver fixes, split 11-19 + interviews, phase 20, R11, R12, reviews). Round 2 review of 11-20 running (opus+opus). Waiting on Jeremy: music apps list (phase 20 Q5), 4 theme pictures.
- 2026-09-23 (evening): round 2 of the 11-20 review triaged and applied; all questions answered (Q-A2 B TMDB key PC-only; Q-D A http media-only; Q-E A alarm banner; R12 wallpapers committed publicly; Tess asked-line built). Build order line in INDEX (C-14). Next: round 3 (last) review of 11-20, then FINAL per doc whose gates are met (phase 12 also waits on the 4 theme pictures; phase 21 waits on R13 on the phone).
- 2026-09-23 (session 1, phase 11): Jeremy chose this session = phase 11 (main checkout, emulator-5554); phase 15 runs elsewhere. Stage A round 3 (last) of phase 11 dispatched: reviewer 1 fable (design lens) -> review/2026-09-23-phase11-r3-design.md; reviewer 2 codex CLI (MCP not loaded; testability lens) -> review/2026-09-23-phase11-r3-codex.md. Brief: review/2026-09-23-phase11-r3-brief.md. Next: triage, apply, FINAL, row 11 building.
- 2026-09-23 (session 1, phase 11): roster fell through: Fable 429 (req_011CfMGHFajJmUyzSFBp6vza), codex CLI rc=1 usage limit until 22:50. Round 3 re-dispatched as opus + opus under Jeremy's standing Opus approval: design -> review/2026-09-23-phase11-r3-design.md, testability -> review/2026-09-23-phase11-r3-testability.md.
- 2026-09-23 (session 1, phase 11): round 3 done (opus+opus): design 2B/11S/6N, testability 7B/9S/5N; triage review/2026-09-23-phase11-r3-triage.md (34 items, all applied, 0 questions, 5 agent calls listed). Phase 11 doc FINAL; INDEX row 11 = building; Change Log line added. Carried to phase 13 round 3: E7 satellite sample point + tolerance (T11-31). Next: Stage C build tasks 1-7 on emulator-5554.
- 2026-09-23 (session 1, phase 11 build): code in (d34d7bb consumed-DOWN fix, 4657886 source+rules+21 JVM tests, bd2256c burst, 939c86f shell shortcuts) + fixtures/ring helpers cherry-picked (fa0ab56..e89700f). QA harness in docs/plan/qa/phase-11/scripts (q.sh, baselines via make_baselines.py). E1 18/18 on emulator-5554. Findings: (1) cold first hold janks (maxGapMs 50) in phase 02's edit entry itself (gfxinfo: folder hold no burst 30-36% janky) — E6 measures warm, P2 cold on phone; (2) a full-width WIDE tile mid-grid keeps a clamped corner (literal "never the held tile" rule) — wide baseline puts it in row 1, Change Log line owed; (3) driver dump: no --no-restart, retries on null root. Next: E2..E16.
- 2026-09-23 23:10 (session 1, phase 11): all 16 rows written and run individually; pass except E10 (BLOCKED L11-1: TileNotificationListener republishes pkg:<package> as null on every media notification update, wiping MusicFeed's now-playing face + transport strip on the AVD; phases 01/10's part, not fixed). Product fixes from the gate: a53a51e (row tiles "off screen"), 1950b89 (quiet profile), be98994 (pivot Role.Tab). Findings: phase 03 allow-list lacks phase 10's Music exports (pre-existing); cold-entry jank in phase 02 (debug build); assistant role = shortcut host. Doc corrections applied in 9312f47, INDEX Change Log line still owed. Running: final one-APK pass (run_all.sh -> qa/phase-11/SUITE.txt) + gate reviewer 1 (design, fable). Next: reviewer 2 (evidence) after the pass; README totals; INDEX row 11 + Change Log; report; HARD STOP (row cannot be done: E10 blocked, NEEDS-HUMAN, phone rows).
- 2026-09-24 10:05 (session 1, phase 11): GATE RUN, ROW BLOCKED (L11-1). Pass 3 on apk 8e2f6479 (code e26d4fe, drivers 005d42d): unit 639/639, E1-E9 + E11-E16 PASS, E10 blocked by L11-1 (notification feed nulls pkg:<package> and wipes the playing tile's face/strip; proven on Fossify = phase 01 E8's fixture and on the shell's player; phases 01/10's part; not fixed). Both gate reviewers ran (fable+fable): design 0B (fixed f0284db), evidence 1B/6S (applied; pass 3). L11-2: one ANR under screenrecord with two emulators (environment). Doc re-cuts + Change Log lines written. HARD STOP: row stays blocked until Jeremy plans the L11-1 fix; then E10 + reviewers' re-judge + NEEDS-HUMAN H1-H10 + phone rows. Nothing pushed.
- 2026-09-24 12:50 (session 1, phase 11, L11-1 fix per Jeremy "(a)"): fix in (ca73663 arbiter + 12 JVM tests, 7cb3742 fix-review findings F-1..F-5); unit 651/651. L11-1 device row 50/50 on apk a982b189 (aefe4b9: F-2 uninstall/reinstall from the device's own APK, F-1 phase 02 E5 from its own baseline, phase 01 E15/E17 asserted; smokes 2-3 kept, smoke 2's reinstall PASS was vacuous and left Fossify uninstalled until restored from E04's hash-matched APK). E6 re-cut committed (4769695: shell clock asserted only with nothing recording; 36/36). Pass 5 (full run_all incl. L11-1, E14 last) RUNNING on emulator-5554. Next: pass 5 verdict -> ledger L11-1 fixed -> README/INDEX row 11 -> both reviewers re-judge (fable+fable, codex out this session) -> HARD STOP; done still needs Jeremy's H1-H10 + phone rows. Nothing pushed.
- 2026-09-24 14:05 (session 1, phase 11): PASS 5 SUITE PASSED (aaf7adc; apk 114511e7, code 7cb3742): unit 651/651, E1-E16 + L11-1 all PASS, E10 13/13 -> T11-38 verified on device. L11-1 recorded FIXED in INDEX Change Log (ca73663 + 7cb3742; TileSourcePrecedenceTest + qa/phase-11/L11-1). README + row 11 updated. Reviewers' re-judge NOT RUN: both Fable subagents hit HTTP 429 Fable usage limit on first request; codex out this session; skill forbids Opus/Sonnet substitute -> briefs saved at docs/plan/review/2026-09-24-phase11-rejudge-briefs.md for the next session. HARD STOP. Row 11 NOT done: owes re-judge + Jeremy NEEDS-HUMAN H1-H10 + phone rows P1-P3/P5/P6. Nothing pushed.
- 2026-09-24 (session 1, phase 11): Jeremy ruled "use opus for the re-judge" (Fable at its usage limit). Both lenses dispatched as Opus subagents from docs/plan/review/2026-09-24-phase11-rejudge-briefs.md (roster ruling recorded there); reports -> review/2026-09-24-phase11-rejudge-{design,evidence}.md. Next: triage, fix anything BLOCKING/SHOULD-FIX, record in INDEX row 11 + Change Log, HARD STOP.
- 2026-09-24 15:05 (session 1, phase 11): Opus re-judge of pass 5: R1 0B/1S/5N, R2 2B/6S/8N (triage review/2026-09-24-phase11-rejudge-triage.md). Fixed: product 9f790b7 (R1-1 forget race, R1-2, R1-6; unit 651/651); fixture c78f488 (labels verb); drivers 50e0f30 (E10 art over a showing strip), 585e6c3 (L11-1 (d) playing uninstall + one pid + app-list re-pin, (e) F-1 device rescan, JVM results, quoting), 19a9ffa (E6 reason/clipped windows/close settle), 239e74e (NEW EDGE row: 43 Edge Cases mapped; flip, fling, 2nd finger via sendevent+pressure, call via ANSWER, keyguard, listener, death, show more, folder dissolve, labels, theme, X5 via photo picker + backdrop model, RV10), f8cd00a (E13 Contacts filter). Readings to flag to Jeremy: a ringing call is a heads-up (burst stays; answering closes, stop); an in-place listener rebind leaves the burst open (Settings path closes). Next: pass 6 (run_all incl. edge), then doc QA evidence + INDEX lines + README, then Opus re-judge round 2. Nothing pushed.
- 2026-09-24 (session 1, phase 11): pass 7 SUITE PASSED end to end on one driver set (9805f5e; apk 50129ec4): unit 651, E1-E16, L11-1 70/70, EDGE 84/84 (commits db1f364 + 01f891c). Round-2 re-judge (opus+opus) fixed in 9805f5e. Round 3 (cap's last) next. PUSH: Jeremy's push of main was rejected — origin/main moved (his PR #1 merged phase-15, which carried 46 of our commits); 9+ phase-11 QA/docs commits unpushed; asked him: push to a phase-11 branch (recommended) or merge origin/main after pass 7.
- 2026-09-24 (session 1, phase 11): round-3 re-judge 0B/0S (3+3 NOTE, recorded). Row 11: QA passed, awaiting Jeremy's H1-H11, phone rows, two readings. Merged origin/main (phase 15 PR #1) into main as fab60a5 (clean; INDEX auto-merged, both sides verified). HARD STOP. Push of main pending Jeremy's command (agent push blocked by the auto-mode classifier).
- 2026-09-24 (session 1, phase 11): Jeremy ruled the two readings 1(a), 2(a) (a ringing heads-up call keeps the burst, answering closes it; a listener rebind with Start in front changes nothing, the Settings path closes it). Recorded in INDEX Change Log + edge_index.tsv. Row 11 now waits only on H1-H11 + phone rows.
- 2026-09-25: QA RULING (Jeremy): no agent runs a whole phase gate any more — specific tests only; Jeremy runs every gate once at the end of the project and feeds the result doc. Recorded in INDEX Change Log + memory feedback_test_only_your_changes.
- 2026-09-25 (phase 11): H1 rejected (satellites vanish on drag, don't come back) -> Jeremy (a): hide during drag, back at drop. Built 548082c; specific tests: unit 805, QuickBurstDragTest 5/5, DRAG 21/21, E2 20/20, E13 drag sub-step 6/6 (apk 749be3b2). No review round (cap used). Row 11 waits on H1 re-check + H2-H11 + phone rows.
- 2026-09-25 (phase 11): Jeremy's one-tap ruling (a): a tap elsewhere with a burst open closes it AND exits edit mode; discs stay. Built b3fe189; specific tests: E5 74/74, E6 35/35 (first try: L11-2 ANR again, kept), EDGE tap sub-steps 25/25 (apk fdc76d9f). Still asked: are H2-H11 accepted?
- 2026-09-25 (phase 11): Jeremy accepted H2-H11. Owed for done: H1 re-check (drag + one tap) and the phone rows P1-P3, P5, P6 (asked whether his phone session covered them).
