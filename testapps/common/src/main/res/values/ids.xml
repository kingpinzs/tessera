<?xml version="1.0" encoding="utf-8"?>
<!-- Shared by the three test APKs (each build.gradle.kts adds this res dir, as it adds the shared Kotlin dir).
     Phase 11 (T11-47): ShortcutActivity's TextView carries this id, so a row reads which shortcut ran as
     resource-id="app.tileshell.testclient.a:id/shortcut_id" in a uiautomator dump. -->
<resources>
    <item name="shortcut_id" type="id" />
</resources>
