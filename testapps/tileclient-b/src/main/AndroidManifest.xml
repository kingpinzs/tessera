<?xml version="1.0" encoding="utf-8"?>
<!-- Shares a uid with tileclient-b2 (E16 shared-uid case). Both are signed with the same debug key. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:sharedUserId="app.tileshell.testclient.shared">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Package visibility for ShortcutBadger-style receiver resolution (queryBroadcastReceivers). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.BADGE_COUNT_UPDATE" />
        </intent>
        <intent>
            <action android:name="me.leolin.shortcutbadger.BADGE_COUNT_UPDATE" />
        </intent>
    </queries>

    <application
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="Tile client B">

        <activity
            android:name="app.tileshell.testclient.VerbActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="app.tileshell.testclient.TestImageProvider"
            android:authorities="${applicationId}.images"
            android:exported="false"
            android:grantUriPermissions="true" />

        <!-- Phase 11 (T11-15): the shortcut verbs (reset, disable, badicon, deadtarget) with no window, so a verb sent
             while a burst is open leaves Start resumed: am broadcast -n with this component and the string extra
             verb. Exported for the adb shell; tileclient-b only (the verbs act only in this package, T11-30). -->
        <receiver
            android:name="app.tileshell.testclient.ShortcutVerbReceiver"
            android:exported="true" />
    </application>
</manifest>
