# Phase 15 — review triage, round 3 (the last allowed), 2026-09-23

Inputs: review/2026-09-23-phase15-r3-design.md (Reviewer 1, ids `D1`–`D25`: 4 BLOCKING · 16 SHOULD-FIX · 5 NOTE) and
review/2026-09-23-phase15-r3-testability.md (Reviewer 2, ids `V1`–`V25`: 7 BLOCKING · 15 SHOULD-FIX · 3 NOTE), brief
review/2026-09-23-phase15-r3-brief.md. Roster: opus + opus — codex MCP not loaded; codex CLI rc=1 "You've hit your usage
limit … try again at 10:50 PM"; the two Fable reviewers died at the Fable limit (HTTP 429) before writing, so both ran on
Opus under Jeremy's standing approval (2026-09-16). Continues round 1 / 2's ids: new items are `T15-21`… and `C-33`….
Class rule as rounds 1–2 (DOC-UPDATE · AGENT-CALL · QUESTION · REJECT); the scope rule of r2 (A8 + the 2026-09-22 scope add +
Q12 + A4: an in-scope W10M app's own offline, buildable features are in) decides "is this W10M control in".

**Checked by the triage (today, this worktree, emulator-5556 read-only):**
- `pactl list source-outputs`: BOTH emulators' capture streams (5554 pid 388227, 5556 pid 3728035) are on source 374
  `easyeffects_source`, not `vmic.monitor`, and share one stream-restore key `source-output-by-application-name:qemu-system-x86_64` (V7).
- `lib.sh` defines none of `record` / `ring_mark` / `ring_since` / `reply_since` / `fill_volume`; no wizard code; provision.sh
  writes no marker (V1 / D1). `exported.py` on this APK → rc 1, MusicActivity / MusicService not allowed (V12 / D8).
- The manifest gives Music no `ACTION_VIEW` filter, but Jeremy's own rulings make shell apps the phone's handlers for their
  kind: phase 17 Q5 A ("the shell's Camera answers IMAGE_CAPTURE and VIDEO_CAPTURE", phase-17 l.115) and phase 18's
  interview ("shell apps open their own types", INDEX row 18). That precedent decides D20 (T15-37).
- `qa/phase-01/scripts/e12_part1.sh` exists (V13's replacement for regress.sh).

**Counts (merged, de-duplicated): 41 items — 10 BLOCKING · 24 SHOULD-FIX · 7 NOTE. Classes: 30 DOC-UPDATE · 11 AGENT-CALL
· 0 QUESTION · 0 REJECT.** No finding changes what is built for the user without a ruling that covers it (§2), so nothing
goes to Jeremy as a question; §2b lists the agent "in / out" calls he can prune at sign-off.

---

## 1. Merged findings

| Id | Sev | Source | Class | Resolution (the doc text carries the full wording) |
|---|---|---|---|---|
| T15-21 | BLOCKING | D1, V1 | AGENT-CALL | **Parallel build.** New Decision "Parallel build": every build task is built on branch `phase-15`; the GATE is one full run of E0–E32 on main after the rebase onto phase 11. Drivers are written once against phase 11's helper API; the branch may cherry-pick phase 11's `lib.sh` helper commit from local main as that exact commit (the rebase drops the duplicate patch) to run rows as development checks, never an interim copy. Branch-runnable rows (V1's first column) are development checks with no gate verdict. `record` moves to THIS phase's task 8 (15 is now its first user in time; 11 has no recorded row) — phase 13's task 7 drops it (C-33). E28 and the two wizard steps move to phase 12's gate (C-34); task 3 keeps the Setup rows, provision lines and the ring mechanics. depends-on → `[01, 02, 03, 05, 10, 11]` (05: the gesture driver and EDGE3's recogniser route, V1 fix 3 / V5). Decisions l.342's stale `[01, 02, 03, 10]` struck. E25 measures the rebased main without / with this phase. INDEX: a new Change Log line corrects the 2026-09-23 clearance's "needs nothing from 11" (its build does not; its gate does). |
| T15-22 | BLOCKING | D2 | AGENT-CALL | **Direct boot.** The alarm receiver, the ring service and the ring activity are `directBootAware`; alarms.json / timers.json / stopwatch.json live in device-protected storage; re-arm also on `LOCKED_BOOT_COMPLETED`; `ShellApp.onCreate` runs the launcher start-up only once the user is unlocked (else on `ACTION_USER_UNLOCKED`) — an ADD to phase 01's `ShellApp`, Change Log; "Pick from my music" before unlock rings the default sound with `[alarms] sound <uri> locked -> default`. New row E6c (PIN set, reboot, no unlock, the alarm rings over the keyguard). Reason: the Goal's "after a reboot" fails on the phone (FBE + a PIN) and passes on the AVD's no-lock baseline. Phase 03's reminders have the same latent gap — recorded in the Change Log line as OBSERVED, NOT FIXED (phase 03's part). |
| T15-23 | BLOCKING | D3 | AGENT-CALL | **Two ring-notification forms.** Chosen at fire time: locked / screen off / no overlay grant → the alarm channel (high importance, full-screen intent, category alarm, Snooze / Dismiss); in use WITH the overlay → a second channel `alarm_ringing_quiet` (low importance, no full-screen intent, the same actions, still the service's foreground notification), so SystemUI's sticky heads-up never covers the toast. `ACTION_SCREEN_OFF` while the overlay rings → remove it, re-post the full-screen form; `ACTION_USER_PRESENT` while the locked toast rings → finish it and add the overlay. E4b gains the no-heads-up assertions and the sleep-mid-ring step. |
| T15-24 | BLOCKING | D4, V12, D8 | DOC-UPDATE | **E2 / exported surface.** Exported ADDs: the three launcher activities and the `AlarmClock` API handler (T15-37, guarded by `com.android.alarm.permission.SET_ALARM`); the ring activity, ring service, recorder service and alarm receivers are `exported="false"` and must be absent. Task 1 also adds phase 10's two owed allow-list lines (MusicActivity, MusicService; legitimately exported — a launcher activity and a MediaSessionService) with a Change Log line naming phase 10's debt, so E2 / phase 03 E5 can pass. |
| T15-25 | BLOCKING | V2, D22 | DOC-UPDATE | **E4's keyguard evidence.** The locked reference screencap is taken after sleep + wake with `isKeyguardShowing=true` asserted; under the toast the row asserts `isKeyguardShowing=true` and `mKeyguardOccluded=true` with the ring activity on top, and the pixels below the toast equal the build-start check's outcome — the lock screen (reference) or the wallpaper (the ring activity sets `windowShowWallpaper`) — asserted either way, never `record`ed; any Start / app window beneath fails. Bars l.263's "the lock screen's own nav bar shows below it" becomes conditional on the same check. |
| T15-26 | BLOCKING | V3, D15 | AGENT-CALL | **Orphaned takes.** MediaProvider clears `OWNER_PACKAGE_NAME` on uninstall and on `pm clear`; T15-3 then makes those takes another app's: listed, play and share, no rename / delete / trim, no markers (recordings.json is gone). Stated in T15-3's Decision and H18; E14's uninstall sub-row uses its own throwaway take, runs LAST among the recorder rows (after E30) and ASSERTS the orphaning; the wiped-state rule notes it; rows that need an own take make it after any clear. (The consent-dialog alternative is a P2 seam; §2b lists it.) |
| T15-27 | BLOCKING | V4, D24 | DOC-UPDATE | **Storage floor.** `getAllocatableBytes` excludes Android's low-storage reserve (`lowBytes`, 500 MB on this AVD), so E19 fills to `lowBytes + 54 MB` free and asserts the self-stop within a computed window; the floor is checked before a take and every 5 s during it; the take is AAC-LC, mono, 44.1 kHz, 64 kbps (agent; R11 records no encoding), written into the mechanics Decision. `fill_volume`'s helper text gains the `lowBytes` note. |
| T15-28 | BLOCKING | V5, D11 | AGENT-CALL | **The microphone hold carries a name.** `holdMicrophone(cb, who): String?` (null = held, else the holder's name) / `releaseMicrophone(cb)`; `cb` must already be `register`ed (else refused) so `onCallbackDied` frees it; `MicArbiter` keeps `who` beside the binder; every refusal (startListening's too) reports the holder's name and the `:speech` line reads `… refused: held by <who>`; Tess's MICROPHONE_BUSY notice and the keyboard's busy text pick their wording from it ("The keyboard …" / "The voice recorder …"); `acquire`'s third argument is stated. E17's converse step runs phase 05 EDGE3 (a)'s route (the ime-fixture's recogniser takes the microphone through the shell's recognition service while the Voice Recorder page stays in front); its first half reads `speech_status mic_owner_pid` and the new refusal line. |
| T15-29 | BLOCKING | V6 | DOC-UPDATE | **E8's network check can fail.** `dumpsys netstats --poll` (asserting "Forced poll") before each read, and a positive control first: a forced Weather refresh moves the uid's counters after a poll; then the world-clock window must not. |
| T15-30 | BLOCKING | V7 | DOC-UPDATE | **Audio isolation (two emulators, one PipeWire).** `audio.sh` becomes per-emulator: `AUDIO_SINK` (default `vmic`; this phase uses `vmic5556`) names its own null sink; `setup` moves THIS emulator's qemu capture stream (the source-output whose `application.process.id` is this emulator's qemu pid) onto `<sink>.monitor` and asserts it; replies are captured from this emulator's own qemu sink-input (`parecord --monitor-stream=<id>`), never the default sink's monitor. Every microphone / reply-audio step asserts those two preconditions first. A qa/phase-03 ADD owned by task 8. The shared stream-restore key is recorded as a hazard (moving one qemu stream can re-target the other emulator's NEXT stream). |
| T15-31 | SHOULD-FIX | V8, D5 | DOC-UPDATE | Q-E ruled A: every "pending Q-E" / B / C / "(lean)" / "every answer" text replaced with the A-only text (D5's list, V8's table); the ruling line's paraphrase fixed (an app overlay cannot show over the keyguard); `[alarms] surface: toast-locked \| toast-overlay \| heads-up \| lockscreen-notification <id>` (`page` cannot occur; FSI denied + locked is a lock-screen notification); the C-4 why line loses "its page"; H5 re-cut, keeping the heads-up FALLBACK's [accept]. |
| T15-32 | SHOULD-FIX | D6, V16 | AGENT-CALL | **In-use toast form and the unwritten motions.** Over an app that shows its status bar the overlay toast is laid out below it (an app overlay cannot cover SystemUI — a recorded seam, H5); over the shell's own pages (bars hidden) from y 0; the window is full-screen, dims what is beneath (8.12) with `FLAG_DIM_BEHIND` at 0.5 (approximation, H5) and consumes touches outside the toast (a ring is never ended by a stray touch). Motion values, each tagged from r11 U12 / calculator M.1 and each with a `[motion]` row: toast entrance grow-down 217 ms (`[motion] ring_toast`), flyout / dropdown grow 233 ms (`[motion] flyout`), Calculator pane slide ≈167 ms (`[motion] calc_pane`, LOW). A "jump" (tab tap, record-state cut) is graded by a screenrecord under phase 05's frame-spacing rule — no intermediate frame — with the `[motion]` line secondary. The clock's tab swipe logs `[motion] clock_swipe` (not `pivot`, which phase 13's task 7 owns for the shared pivot; V25). |
| T15-33 | SHOULD-FIX | D7 | DOC-UPDATE | **Trust.** New Decision listing (a)–(i) plus the `AlarmClock` API handler (T15-37) for an adversarial team review before `done`; task 8 line; build-prompt.md's trust list gains phase 15's ring surfaces, microphone hold, recorder and alarm API handler (C-35). |
| T15-34 | SHOULD-FIX | D9 | DOC-UPDATE | **Session ids.** Media3's platform tag is `androidx.media3.session.id.<id>`; task 0 ADDs `.setId("music")` to phase 10's `MusicService` (Change Log) and routes on the id with that prefix removed (`[music] session <pkg> id=<id> -> …`); Voice Recorder's playback runs a Media3 session with `setId("recorder")` (r11/voice-recorder.md 4.9, LOW). Build-start check: `dumpsys media_session` shows the tag. |
| T15-35 | SHOULD-FIX | D10, V22 | DOC-UPDATE | Task 0 also routes the COUNT: the shell's own notifications give no badge to any in-APK tile. E0 gets its own driver `qa/phase-15/scripts/e0.sh` (phase 10 E10–E13's tile rule has no emulator driver — "re-run" struck), the baseline pins Auxio, and the notification negative has its positive control (`listener connected=true`, `badges=` holds `app.tileshell` ≥ 1). |
| T15-36 | SHOULD-FIX | D12, V19 | AGENT-CALL | Call detection through the audio mode (`AudioManager.getMode()` / `addOnModeChangedListener`, API 31+, no permission): RINGTONE = ringing, IN_CALL / IN_COMMUNICATION = in a call (VoIP too). No READ_PHONE_STATE, no new Setup row. |
| T15-37 | SHOULD-FIX | D20 | AGENT-CALL | **Alarms & Clock answers the `AlarmClock` API** (precedent: phase 17 Q5 A, phase 18's "shell apps open their own types"): an exported handler activity, `android:permission="com.android.alarm.permission.SET_ALARM"` as AOSP DeskClock's, for `ACTION_SET_ALARM`, `ACTION_SET_TIMER`, `ACTION_SHOW_ALARMS`, `ACTION_SHOW_TIMERS`; with Samsung Clock also installed Android asks once which clock (a one-time P2 seam, H25). Tess stays in-process (Q1). `SET_ALARM` leaves the manifest's `uses-permission` (task 5) — the handler's guard does not need it held. New row E33. Trust review item (T15-33). |
| T15-38 | SHOULD-FIX | D13 | DOC-UPDATE | `[recorder] paused: call\|silenced\|user` — another app's package is not visible to the shell. |
| T15-39 | SHOULD-FIX | D14 | DOC-UPDATE | `RecorderService.onStartCommand` returns START_NOT_STICKY; recovery runs in `onCreate` of the next start or bind. |
| T15-40 | SHOULD-FIX | D16 | DOC-UPDATE | Secondary timer / stopwatch tiles: `SecondaryTiles.launch` targets the Alarms & Clock component explicitly for tiles the shell owns (ADD to phase 01 / 02, Change Log); the activity reads `EXTRA_LAUNCH_TILE_ID`; the in-process request applies the provider's checks itself. |
| T15-41 | SHOULD-FIX | D17 | AGENT-CALL | Alarms due in the same minute ring together on one toast (`ring_name` lists them; Snooze / Dismiss act on all); a LATER alarm firing while one rings supersedes it (`ring ended <id>: superseded`, the first missed). |
| T15-42 | SHOULD-FIX | D18 | DOC-UPDATE | E13: the History glyph in Standard AND Scientific (r11/calculator.md 6.1, gap 5), not in Programmer or the Converter. |
| T15-43 | SHOULD-FIX | D19 | AGENT-CALL | W10M controls, each built or ruled out (scope rule): Clock **Select** = multi-select delete (Alarm and Timer tabs); **More** = About (in), Notification settings → the shell's Setup page (in; no Android page), Send feedback OUT (a Microsoft online service); **Pick from ringtones** → a shell-drawn Sounds page per 4.6 (the branding sound-alikes + `RingtoneManager.TYPE_ALARM` tones); the **timer editor** per 4.8; Voice Recorder's **level rings** (2.7, LOW); every "…" / pane **Settings** = an About page (r11/voice-recorder.md U13), **Feedback** OUT; the `converter` App Shortcut opens the last-used category, Volume the first time. H rows H26–H27. |
| T15-44 | SHOULD-FIX | V9 | DOC-UPDATE | `[alarms] surface: <form> <id>`; a row that moved the clock ends, after RV12's clock restore, with `am force-stop app.tileshell` + Home (every ring reset); a step after a BACKWARD jump starts from that reset and reads the whole fresh ring. No change to phase 11's helper. |
| T15-45 | SHOULD-FIX | V10 | DOC-UPDATE | Baseline: no alarm, timer or running stopwatch in the shell (asserted at row start); every row deletes what it creates in its restore; E5 dismisses before its restore. |
| T15-46 | SHOULD-FIX | V11 | DOC-UPDATE | Task 8 helpers `push_fixture_recordings` / `remove_fixture_recordings`; each row pushes at its start and removes at its restore; E0 plays the pushed other.m4a; E20 re-runs MUSIC6 after `rm song.m4a` + scan. |
| T15-47 | SHOULD-FIX | V13 | DOC-UPDATE | The in-APK app-list rows share `applist_row:app.tileshell`; task 1 ADDs `applist_name:<component short form>` on the label (phase 01's app list, Change Log); E1 finds rows by it; jump grid "V becomes available"; phase 01 E12's walk (`qa/phase-01/scripts/e12_part1.sh`) replaces "regress.sh's pattern". |
| T15-48 | SHOULD-FIX | V14 | DOC-UPDATE | E22 over every alternative of every line, edge-case slices included, with a producer table; recorder rows save `ring-recorder.txt` before leaving the page. |
| T15-49 | SHOULD-FIX | V15 | DOC-UPDATE | New row E32: an alarm left unanswered stops at 600 ± 5 s (ring timeout), logs `timeout`, and posts "Missed alarm h:mm". |
| T15-50 | SHOULD-FIX | V17 | DOC-UPDATE | E15 reads `types=0x00000080` (the microphone bit), not `foregroundServiceType=microphone`. |
| T15-51 | SHOULD-FIX | V18 | DOC-UPDATE | `display_name` → `_display_name` in E14 / E16. |
| T15-52 | SHOULD-FIX | V20 | DOC-UPDATE | Tess's reply rule: numbers as the engine displays them, operator words as said except "percent" → "%" and "square root of <a>" → "√<a>"; unit names from the en-US table ("kilometers"); the `tess` column is generated from that rule on the host; the matcher accepts both spellings. |
| T15-53 | SHOULD-FIX | V21 | DOC-UPDATE | E6's third pass reboots when the timer reads ≤ 10 s remaining and asserts the first post-boot clock is past the deadline. |
| T15-54 | SHOULD-FIX | D21 | DOC-UPDATE | Ring FGS: `FOREGROUND_SERVICE_SYSTEM_EXEMPTED` added if the check picks it; the ring service starts only from the alarm's broadcast, never from a boot receiver (a past-due alarm is armed at now). |
| T15-55 | NOTE | D23 | AGENT-CALL | Timers arm `ELAPSED_REALTIME_WAKEUP` (a wall-clock change never moves a running timer), with the wall-clock deadline stored for re-arm after a reboot; E6 reads the ELAPSED entry. |
| T15-56 | NOTE | V23 | DOC-UPDATE | Kill / reboot rows save the pre-kill slice to `ring-launcher-prekill.txt`; continuity = post − pre `elapsed` vs their `wall=` difference. |
| T15-57 | NOTE | V24 | DOC-UPDATE | E3 `alarm_repeat:<id>` and `dumpsys alarm`; E5 names its subset; E9 uses utterance `alarm` ("… seven twenty AM", reply "Alarm set for 7:20 AM."); E11 paste route through Fossify Notes and `calc_paste`, "80 + 15 % =" ; E16 `rec_position` and the URI from `_id`; E21 reopen after each revoke, the SCHEDULE_EXACT_ALARM arm asserted; E30 `rec_elapsed`; E4b grants DeskClock POST_NOTIFICATIONS; edge in-call level from `dumpsys media.audio_flinger`; FSI op reads default→allow after provisioning (both recorded); E1's positive control. |
| T15-58 | NOTE | V25, D25 | DOC-UPDATE | microsoft/calculator paths at 4fd3fc5 (`src/Calculator.ViewModels/DataLoaders/UnitConverterDataLoader.cs`, `src/Calculator.ViewModels/Common/DateCalculator.cs`); j7.sh cite `:16-25`; one QWORD form; the two Setup rows' `detail` strings; `acquire`'s argument list; "no NEW instrumentation APK — phase 05's gesture driver is the only one used". |
| C-33 | NOTE | V1, D1 | DOC-UPDATE | phase-13 task 7: `record` is built by phase 15 (parallel build, T15-21); 13 uses it. If 13 builds before 15 merges, the lead keeps one copy at the merge. |
| C-34 | NOTE | D1, D5 | DOC-UPDATE | phase-12: the why table gains `setup:full_screen_alarms` and fills `setup:overlay`'s line; E14's instance list gains phase 15's two steps, run in phase 12's gate because 15 was built first. |
| C-35 | NOTE | D7 | DOC-UPDATE | build-prompt.md's trust list gains phase 15's items (T15-33). |

(Rows T15-22 … T15-58 cover D2–D25 and V2–V25 one-to-one or merged as the Source column says; every reviewer id appears once.)

## 2. Questions for Jeremy — none

D20 (does the shell's clock take other apps' alarm requests) was the one candidate: it changes what Android does when another
app asks for an alarm. Jeremy has ruled the same shape twice — phase 17 Q5 A (the shell's Camera answers capture intents) and
phase 18 (shell apps open their own types) — so it is an AGENT-CALL by precedent (T15-37), listed in §2b.

## 2b. Agent "in / out" calls Jeremy can prune at sign-off

- IN: Alarms & Clock answers other apps' set-alarm / set-timer / show requests (T15-37; one "which clock" prompt, H25).
- IN: Clock multi-select delete; Sounds page; the timer editor; Voice Recorder's level rings; About pages; Notification settings
  → the shell's Setup page (T15-43).
- OUT: Send feedback / Feedback everywhere (Microsoft's online service; no offline equivalent).
- Orphaned takes after an uninstall or "Clear storage" are read-only in Voice Recorder (T15-26); the alternative is one
  Android consent dialog per edit (a seam).
- Direct boot (T15-22) is not prunable: without it an alarm after an overnight restart is silent on the phone.

## 3. Cross-doc edits made with this triage

- phase-13-fluent-materials.md task 7 (C-33); phase-12-setup-wizard.md why table + E14 instance list (C-34);
  build-prompt.md trust list (C-35); INDEX.md row 15, the reviewers line, and new Change Log lines only.
