BLOCKING: 1 · SHOULD-FIX: 4 · NOTE: 10

# Phase 11 re-judge, round 2: Reviewer 2 (testability and evidence), 2026-09-24

Scope: pass 6 (`qa/phase-11/SUITE.txt`: the main run on f8cd00a, plus E13 / E14 appended on a45b2ad and 1477e75), the
fix commits since pass 5 (9f790b7 through 8ff76de), the EDGE row and its index, the kept smokes, and my round-1 findings.
This review was read-only. I did not run adb or an emulator, did not touch metro-launcher-p15, and pushed nothing. Locally
I ran `python3` over the committed dumps, rings and PNGs, `strings` over the built APK's dex, and `git show` / `git diff`.
I read every commit message, README line and INDEX line as a claim to check, not as evidence. Paths are relative to
`docs/plan/` unless they start with `app/`.

## Findings

id | sev | file:line | finding | evidence | exact fix
---|---|---|---|---|---
EV-1 | BLOCKING | qa/phase-11/scripts/edge.sh:129-141 (keyguard), :73-87 (fling), :89-106 (second finger) · INDEX.md:59 · qa/phase-11/README.md:32 · phase-11 doc :687 ("Edge cases (every bullet, indexed) PASS") | **R2-1's fix is incomplete. Three of the EDGE row's new sub-steps pass without showing their precondition, and for one of them the files show the precondition did not hold.** These sub-steps are the only evidence for their bullets, so "every Edge Cases bullet" is not evidenced. **(a) The keyguard never appeared.** The sub-step sends SLEEP, then WAKEUP, captures, dismisses and asserts `burst closed: stop`. That close comes from the screen going off, which is E5's screen-off sub-step. keyguard.png, taken after the wake, shows Start in edit mode with no burst and no lock screen. The "keyguard showing" note (:135) matched only a dream flag. No assertion checks for a keyguard. **(b) The fling sub-step shows neither that a fling ran nor that the press arrived.** "The page moved (the fling ran)" is also satisfied by a plain drag. In pass 6 and in smoke2, the page stopped at the same offset: 927 px, which is the 950-px drag minus the ≈22.5-px touch slop (E7's slow drag shows the same slop: a 120-px swipe moved the page 98 px, E7.txt:80). So neither run shows any travel after release. Smoke1, where nothing touched, shows that "no hold", "no burst" and "no edit mode" all PASS when no press reaches the app. The pass-6 ring holds no line from the press. **(c) Nothing shows the second finger arrived.** "The burst stays" is also what an undelivered slot-1 contact produces, and there is no control. The first-finger check does prove that slot-0 raw touches arrive. | (a) EDGE/keyguard.png; EDGE.txt:51 (`mShowingDream=false mDreamingLockscreen=false`), :52-54; E5.txt:62-65 (the same stop from screen-off) · (b) EDGE/fling-before.xml vs fling-after.xml: CALENDAR top 795 → −132 (clipped [9,0][708,211]); EDGE-smoke2-picker/fling-after.xml identical; EDGE-smoke1-nopressure/EDGE.txt:31-34 (page did not move, yet no hold / no burst / no edit mode PASS); EDGE ring 15:51:10.473-15:51:20.146 has only tile_anim / weather lines · (c) EDGE.txt:38-42; finger1/finger2/finger-released.xml identical in burst, satellites and discs | (a) Give the AVD a real keyguard for this sub-step: `adb shell locksettings set-pin 1111` before it, and after the wake assert a keyguard-showing flag from dumpsys (KeyguardController's keyguard-showing line) reads true. Unlock with `input text 1111` + `KEYCODE_ENTER`, assert Start comes back plain, then `locksettings clear --old 1111`. Or re-map edge_index.tsv:17 to "E5 screen-off; this AVD has no lock screen (keyguard.png); phone row", with an INDEX Change Log line. (b) Add a control: the same swipe with no press, recording the page's resting offset. Assert that the control travelled further than drag − slop (a fling ran) and that the run with the press stopped short of the control (the press landed during the fling and stopped it). If the control also rests at 927, the swipe makes no fling and has to change. (c) Add a control after the finger sub-step: with a burst open and no finger down, send the same slot-1 down/up at (EX,EY) and assert `burst closed: tap elsewhere`. That proves the contact reaches the app and would close the burst if it were not a second pointer. Then re-run EDGE and E14 in an appended section, and until then word INDEX:59, README:32 and doc :687 without "every bullet".
EV-2 | SHOULD-FIX | qa/phase-11/scripts/l11_1.sh:103-104 · L11-1/L11-1.txt:51 · README.md:31 ("(F-2, R1-1)") | **(d) does not exercise R1-1's window, and its "(R1-1)" check passes on the pre-9f790b7 code for the sequence that actually ran.** R1-1 is a MusicFeed publish landing between the engine's forget and MusicFeed's forget. In this run, the session's dying publish came 109 ms BEFORE the forget: `from=music … -> shows music (paused)` at 29.985, then `[engine] forget` at 30.094. With 7cb3742 alone, the last engine line would be the forget line, which contains no "-> shows music", so the check would pass. 9f790b7 changed no test (unit stays 651), so no JVM test fails without it either. What the files DO show is 9f790b7's own publish running: `from=music faces=0 … -> shows nothing` at 30.145, after the forget. F-2 (7cb3742) is proved: without the forget, the paused face published at 29.985 would still be there when the player is re-pinned. | L11-1/uninstall-sequence.txt:2 (29.985 paused face), :4 (30.094 forget), :5 (30.145 MusicFeed's null publish) · `git show --stat 9f790b7` (MusicFeed.kt, LiveTileEngine.kt only) | In l11_1.sh (d), assert a `[engine] publish pkg:org.fossify.musicplayer from=music faces=0 … -> shows nothing` line AFTER the `[engine] forget` line. That line exists only with 9f790b7, so the row fails without the fix. Re-word the assertion name and README:31 to: "9f790b7's own null publish is seen; the race window itself did not occur in this run (uninstall-sequence.txt)". Optionally add an engine-level JVM case: forget, then a MUSIC face, then MUSIC null → nothing shows.
EV-3 | SHOULD-FIX | qa/phase-11/scripts/edge.sh:231-263 (the Light test at :255) · EDGE-smoke1-nopressure/EDGE.txt:94-95 | **The "theme Light" check passes on a Dark screen, and it was loosened into that shape after it failed.** Light is judged by "black ink present": min luma in the label rectangle < 60. In Dark mode the label sits over a dimmed neighbouring tile whose luma is 51 (EDGE.txt:99 `bg=51`), so a label rectangle with no black ink in it at all still reads min ≤ 51 < 60. The switch to Light is never asserted either: `settings_tap` result, page ground and `bg` are only printed. The first run failed this check (smoke1 `bg=122 -> FAIL`), and the check was then cut to "the ink alone". Pass 6's result stands: the ring has `[settings] start theme changed … theme=LIGHT` at wall=1790286925819, and theme-light.png shows the Light page with black labels. | EDGE.txt:96-100 · edge.sh:255 · EDGE/theme-light.png, theme-dark.png · EDGE ring (theme=LIGHT line) | Assert the theme from the ring (`start theme changed … theme=LIGHT` / `DARK` after the tap). Assert the page ground from a clear page patch (Light > 200, Dark < 30). Judge the ink against its own ground: Light passes if min luma < bg − 60, Dark if max luma > bg + 120, both measured inside the label's text row only.
EV-4 | SHOULD-FIX | qa/phase-11/scripts/edge_index.tsv:7,16,18,21,25 · phase-11 doc :630-633, :636, :638, :641 · e5.sh:143-149 | **Some index lines cite evidence that does not prove the case as the doc words it.** (i) Bullet 1(e): "a press in edit mode, **with or without a burst open**, has no hold timer … a still press **of any length** on another tile … (with a burst open it only closes the burst)". No row runs a long still press while a burst is open. E5 closes the burst first (e5.sh:146) and only then does its 1.0-s press, and its burst-open cases are taps. The held-tile press with no burst open (exits edit mode) is not cited at all. (ii) "a tap on a satellite that slides off … under every press style": E5 runs tilt and p4 only (e5.sh:209), not none. (iii) "a full row of six": no row has six. E7's bottom row has four tiles, and the JVM test clamps a row's leftmost tile. (iv) The call and listener lines state the doc's "closes" as proved, but the sub-steps assert that ringing keeps the burst open and record that an in-place rebind keeps it too. Those are the two readings that still wait on Jeremy (INDEX:90). | E5.txt:66-72 (the press runs after the burst is closed) · E7.txt:40 (a 4-tile row) · QuickRulesTest.kt:171-178 · EDGE.txt:46, :59 | (i) Add an EDGE sub-step: burst open, then a 1.0-s still press on another tile (Tess) and release. Assert `burst closed: tap elsewhere`, no `[edit] hold`, no second `burst on`, selection unchanged. Cite phase 02's own row for "held tile, no burst → exits edit mode". (ii) Add none to E5's loop, or cite only tilt and p4. (iii) Say "clamping at both row ends (E7 right end, JVM left end); a row of six not run". (iv) Mark edge_index.tsv:16 and :18 "reading (a) / (b), Jeremy's call pending (INDEX:90)".
EV-5 | SHOULD-FIX | qa/phase-11/scripts/edge.sh:357-371 | **RV10's density and font-scale cases would pass unchanged if the change never applied.** `rv10` discards the command's output (:359) and never reads the setting back. Both results are `0.00 epx`, and every node with an id or text in rv10-d560.xml and rv10-font13.xml has the same bounds as the base dump, which is exactly what an unapplied change gives. `wm size` checks itself, because its dump is divided by 720 (0.50 epx), so a size that did not apply would fail. Phase 01 E3's method has the same gap (phase-01 final_rows2.sh:11-18). | EDGE.txt:123-126 · my diff of rv10-base vs rv10-d560 / rv10-font13: 0 differing id / text nodes | After each apply, record and assert the setting: `adb shell wm density` → `Override density: 560`, and `settings get system font_scale` → `1.3`. As a positive control, assert one node that is known to scale with dp or sp, or the system bar inset, changed.
EV-6 | NOTE | edge.sh:221-223, :225-227 · app/…/start/QuickBurst.kt:300 | **Two label checks cannot fail.** (1) "The long label keeps the label box's size" compares two nodes whose size is fixed (`.size(labelW, labelHeightPx)`), so it holds whether or not the text is ellipsised. The ellipsis rests on labels.png ("An unusu…"), which the index cites honestly. (2) "The blank-labelled satellite still runs" looks for `qa_blank` in the `[quick]` lines, and `tap satellite 1 …/qa_blank: startShortcut failed …` would match too. The ring reads `startShortcut ok`. | EDGE ring :1263 · EDGE/labels.png · E3.txt:30 (E3 asserts `startShortcut ok`) | Assert `qa_blank: startShortcut ok`, as E3 does. Drop the box-size check or re-name it "the label box is fixed-size"; the ellipsis stays H9 / labels.png.
EV-7 | NOTE | edge.sh:60-68 | **The flip sub-step's strongest proof is in the ring and is not asserted.** The two dumps are compared at rest before and after the flip, so a satellite that moved mid-flip and came back would pass. The ring shows it did not happen: one set of `satellite i rest=` lines at 48.533, flips at 48.909 and 53.862 under the burst, and no second rest set. A second set is what a tracked-bounds change produces (doc :203). | EDGE ring :136-157 | Assert zero `[quick] satellite [0-3] rest=` lines in the slice after the second MARK.
EV-8 | NOTE | edge.sh:166-169 · q.sh:12-19 (fresh_burst writes B.xml) | **Process death: neither the burst before the kill nor the ring from the sub-step is kept.** The only sign a burst was open is that `fresh_burst` wrote no "no burst" note. Its dump B.xml is overwritten by the next default `fresh_burst`. There is no `ring_save` before `kill -9`, so the killed process's ring is lost: the saved ring jumps from 15:52:58.945 to the restart at 15:53:19.743 and has no `burst on` for this sub-step. | EDGE ring :754 → :771 | Dump to `death-before.xml` and assert `quick_burst`, and call `ring_save` before `kill -9` (T11-21 covers force-stop; process death is the same class).
EV-9 | NOTE | qa/phase-11/scripts/e6_frames.py:56-58, e6.sh:97-98 | **The E6 retake rule accepts a recording in which no entry was detected.** With `entry=None` the window becomes frames 0-19 (`(entry or 1)`). Any recording of 20 or more frames then prints `window_gaps 19` and is measured on idle frames before the hold. Pass 6's accepted attempt did detect its entry (`entry_ms 1827.2`, E6.txt:54), so it is unaffected. | E6.txt:54 | Accept only when `entry_ms` is not `none`, and print `window_gaps 0` when there is no entry.
EV-10 | NOTE | qa/phase-11/scripts/e14.sh:10-13 · E14/E14.txt:17,29-32 | **E14's union now counts smoke directories.** It takes any `E*` directory whose log shows the same APK, so E13-presssmoke and EDGE-x5smoke..4 (non-gate drivers) are counted, while E10-smoke-artstrip and E13-pass6-append-800ms are skipped silently because their log names differ. I recounted the union on the gate rows alone (E1-E13, E15, E16, EDGE): all 25 patterns are still ≥ 1, so pass 6 stands. | E14.txt:12-33 (21 files) · my recount on 16 gate directories | Count only the rows run_all names (E1-E16, EDGE; L11-1 if wanted), or require the log's driver blob to equal the committed driver's.
EV-11 | NOTE | qa/phase-11/UNIT-results/*.xml · L11-1.txt:15 · run_all.sh:49-51 · l11_1.sh:20-23 | **"This run's unit results" are Gradle's cached results from 13:44.** Every XML carries `timestamp="2026-09-24T19:44:4x Z"` (13:44 local, when the APK was built and 18 s before 9f790b7 was committed). The suite's 15:00 `testDebugUnitTest` was up to date and ran nothing, and L11-1.txt:15 prints the copy's mtime (15:00:29) as the results' time. By Gradle's up-to-date rule the inputs equal 9f790b7's (app/ is clean at f8cd00a), so the 651 and the 12 stand; only the label is wrong. | UNIT-results/TEST-…TileSourcePrecedenceTest.xml:2 · app/build/outputs/apk/debug/app-debug.apk mtime 13:44:46 | Print the XML's own `timestamp` and Gradle's task outcome (UP-TO-DATE vs executed), or run `cleanTestDebugUnitTest` first so the suite's unit line is this run's.
EV-12 | NOTE | qa/phase-11/scripts/e13.sh:97, :103, :118 | **In E13's press styles, "no burst" cannot fail, and "pressed N ms" is not the press length.** (1) The pressed tile is `slot:BROWSER`, which has no shortcuts. Pass 6's own race held it (`[edit] hold 783ms on slot:BROWSER`, then `no burst … no shortcuts`) and `press_none: no burst` still PASSED. "No hold" is the check that carries the sub-step, and it caught the race. (2) `t1 − t0` includes the DOWN's own sendevent chain and excludes the MOVE's. In the 800-ms append, presses measured at 791, 803 and 794 ms raised no hold, so the device-clock figure and the hold clock differ by an amount nobody measured. The direction is safe: a press that really runs long fails "no hold". | b2eafb8:qa/phase-11/E13/E13.txt:49-50, ring :572-574 · E13-pass6-append-800ms/E13.txt:50-54, :59-63, :68-72 | Press a tile that has shortcuts (the fixture) so "no burst" can fail. Log the note as "t1−t0 (DOWN chain to MOVE start)", or read DOWN→MOVE from `getevent -t`.
EV-13 | NOTE | qa/phase-11/scripts/edge.sh:305, :312 | **X5's "0 %" and "100 %" are 0.099 % and 99.8 %.** The slider taps land at `sl+1` and `sr−2`, and the assertion reads the slider's rounded label. The ring stores `transparency=9.920635E-4` and `0.9980159`. The colour effect is under 0.4 units against a tolerance of 14, so the result stands. | EDGE ring `[settings] start theme changed` lines (wall=1790287003974, 1790287030619) | Assert the stored value from the ring (≤ 0.01 / ≥ 0.99), or drag the thumb to the ends.
EV-14 | NOTE | README.md:32, INDEX.md:90, commit 239e74e ("43 cases") · review/2026-09-24-phase11-rejudge-triage.md:4, :30; INDEX.md:91; review/…-rejudge2-briefs.md:11 · e10.sh:10 · INDEX.md:99; e6.sh:80-81 | **Record drift.** (1) The index has **42** cases (EDGE.txt:172 `(42 cases)` in pass 6 and in all three smokes; edge_index.tsv has 42 data lines). My count of doc :627-658 gives 42 as well (5+6+6+3+6+3+5+5+2+1), so no case is missing; the "43" is wrong. (2) My round-1 report had 7 NOTEs and no R2-16 (first line `NOTE: 7`; R2-9..R2-15). The triage's "8 NOTE", its R2-16 row, INDEX:91 and the briefs repeat a count that does not exist. (3) E10's row title still says "(L11-1 blocks the strip)" (E10.txt:2). (4) The R2-14 leftovers are listed in the round-1 table below. | as cited | Correct the counts to 42 and 7, drop R2-16, re-title E10, and add a Change Log correction for INDEX:99.
EV-15 | NOTE | qa/phase-11/scripts/run_all.sh:23-31 · qa/phase-11/E13/ | **Two harness gaps in the append path.** (1) The appended verdict is recomputed only from `E*/L11-1/EDGE exit` lines (:27), so a failed unit line in the main section would not stop an appended "SUITE PASSED". Unit passed in pass 6. (2) The append runs `rm -rf E13`, so pass 6's own E13 failure (the race the README describes) survives only in git (b2eafb8), while the 800-ms re-run was kept as a directory. | SUITE.txt:4, :31, :38 · `git show b2eafb8:…/E13/E13.txt` | Include `unit: … 0 failures` in the recomputed verdict. Before an append deletes a row directory, move it to `<row>-pass6-<why>/`.

## Round-1 findings

id | status | evidence
---|---|---
R2-1 (BLOCKING) | **partly** | The EDGE row exists, maps all 42 cases (edge_index.tsv; EDGE.txt:129-172) and passes 61/61 on the gate APK. Keyguard, fling and second finger are not evidenced (**EV-1**), some index citations fall short (**EV-4**), and RV10 density / font are unverified (**EV-5**).
R2-2 (BLOCKING) | **resolved** | E10.txt:26-34: shell player PLAYING, strip present, art point (892,1086) inside the tile [721,973][1064,1316] and above the strip (top 1199) on no control; `[edit] hold`, no `[tile_control]`, edit mode, Songs / Albums / Artists / Playlists. Smoke kept on the previous APK (apk match NO), re-run on 50129ec4.
R2-3 | **resolved for F-2** | l11_1.sh:94,126 one pid (7681, L11-1.txt:55); re-pin from the app list (:111-118); positive "Music Player" and no "An Ending" (L11-1.txt:54-57). The paused face published before the forget (uninstall-sequence.txt:2) would survive without 7cb3742, so the check can fail. The R1-1 part is **EV-2**.
R2-4 | **resolved** | Single-word extras (l11_1.sh:33); the control finds NoteTitle in notif-after-clear-3 (L11-1.txt:25,27).
R2-5 | **resolved** | (e): the pin is asserted, `SecContent` is seen before the rescan (control), `[notif] rescan (connected)`, the pid is the same (10393), and SecContent is seen after (L11-1.txt:86-92). F-1's mechanism (the fix review: the rescan nulls the pseudo-package `<owner>#<tileId>`) means the pre-fix code fails it. Phase 02 E5 is kept as a regression check.
R2-6 | **resolved** | run_all.sh:51 keeps 62 XMLs; I recomputed 651 / 0 / 0. L11-1.txt:16-19 asserts 12 tests and the three cases by name. The label issue is **EV-11**.
R2-7 | **resolved** | INDEX:89 and e6.sh:2-9 now give the facts. `window_gaps` is printed and a recording with fewer than 19 is retaken (e6.sh:97-98); pass 6 accepted attempt 1 with 19. The entry=none hole is **EV-9**.
R2-8 | **resolved** | Doc :660-694 is written. Its "every bullet" row inherits **EV-1**.
R2-9 | **resolved** | The settle assertion is dropped and the value noted (e6.sh:111-113; E6.txt:60).
R2-10 | **resolved** | (a) recorded at INDEX:89 (3); (b) README:20 says drawn motion rests on H1 / H2.
R2-11 | **resolved** | L11-1.txt:35 (`An Ending` asserted in Fossify's dumps).
R2-12 | **resolved** | L11-1.txt:61, :68 (session PLAYING and PAUSED).
R2-13 | **resolved** | E10.txt:23 (`start_page` yes, `quick_burst` no, `edit_disc` no).
R2-14 | **partly** | README:72-78 now matches COLDJANK (25.45 / 27.27 vs 41.67 / 37.50) and the smoke log (attempts 1 and 3 at 33.3). INDEX:99 still says "30-36 %", and e6.sh:80-81 still says "16.7 ms throughout".
R2-15 | **resolved** | Phase 01 blobs stamped (L11-1.txt:73); E13/contacts-shortcuts.txt has one `Package:` section (com.android.contacts).
R2-16 | **n/a** | Not in my report (see **EV-14**).

## What I checked and found right

- **One APK and the code in it.** All 18 row logs read `apk installed 50129ec4bf4de7f8`, `apk match yes`. The APK's mtime is 13:44:46, before 9f790b7's commit (13:45:05), and its classes5.dex holds `forgetRegistered`, 9f790b7's R1-2 field, so the gate APK carries the re-judge fix. `git diff 9f790b7 f8cd00a -- app/` is empty, and SUITE.txt:2 reads 0 uncommitted files.
- **Drivers.** Every row's `driver … blob` equals `git rev-parse f8cd00a:<driver>` (all 17 checked). E13's equals 1477e75's `24e3962`, as SUITE.txt:34 records, and lib.sh is `f296b3d`. `git diff f8cd00a HEAD` on the drivers touches only e13.sh and its smoke. E14 ran last (16:32:17), after E13's second re-run, and SUITE.txt:38 is the correct latest-line verdict.
- **E14 on gate rows alone.** All 25 patterns match at least one line without the smoke directories (e.g. `burst closed: stop` 6, `shortcuts changed` 1, `off screen` 1).
- **The EDGE sub-steps that hold.** Flip: the precondition is asserted and the ring shows no second rest set (EV-7). Call: the ringing dump has `ANSWER` from SystemUI and `quick_burst` present; after answering, InCallActivity is on top with `stop` (EDGE.txt:44-49). Listener: the Settings path gives `stop` then `listener connected`; the in-place rebind is recorded (1 connect, burst open, 0 closes). Process death: new pid, Start plain. Show more tiles: the toggle took, because the restore reads `before: checked="false"` (EDGE.txt:73). Two-tile folder: the baseline holds exactly MAPS + fixture (baseline_layout-folder2.json), then `unpin`, `folders []`, MAPS on Start. Labels: labels.png shows "An unusu…", the blank satellite has no text and no id, and the ring has `startShortcut ok`. X5: the model is StartPage.kt:418 (`1 − 0.8 × transparency`) × QuickBurst.kt:284 (`accent × tileAlpha`), independent of the data; it matches to 0.4 at 100 % and would fail an opaque satellite (|d| ≈ 150); the slider presence is asserted, so smoke2's missing-picture hole is closed. RV10 `wm size` checks itself (0.50 epx).
- **The smokes.** Each fixed the hole it names. Smoke1 (no pressure): slot-0 raw touches land (EDGE.txt:38-39), though the fling and finger checks still could not catch a silent miss (EV-1). Smoke2 and x5smoke1-3 (the picker, then the held-tile comparison): the slider is asserted, and the backdrop model gives 9/9 in x5smoke4 and 4/4 in pass 6. L11-1-smoke5 (the pin accept id): pass 6 asserts the pin (L11-1.txt:86), and the positive control that caught smoke5 (smoke5 :87) is kept. E10-smoke-artstrip ran on the old APK and was re-run on 50129ec4, 19/19.
- **E13's race fix.** The press styles now read 649-696 ms. DIFF for tilt and p4 shows the press registered at the capture. "No hold" can fail, and it did catch the pass-6 race (b2eafb8). Show-touches is off for the sub-step and restored.
- **E6 on pass 6.** Five warm opens, each asserted (peak 100, overshoot 6.5, maxGap 16.7, settle 266 ≤ 266.8, t0 − entry 16-17). The accepted recording covers the whole window (19 gaps, max 17.4).
- **L11-1 (a), (b), (c), (e), and E15 / E17.** Each check reads a non-empty sample with a positive control. (b) and (c) show the race was exercised (5 and 8 notification publishes after the face) and that every publish after the face resolves to the playing session.
- **The EDGE index against the doc.** Every case at :627-658 has a line (42 in total). The cited E3 / E5 / E7 / E8 / E9 / E12 / E16 / JVM evidence exists and says what the line says, except where EV-4 notes otherwise.
