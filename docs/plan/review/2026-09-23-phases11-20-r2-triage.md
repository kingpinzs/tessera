# Phases 11–20 — review triage (Stage A steps 5–6, round 2, 2026-09-23)

Inputs: review/2026-09-23-phases11-20-r2-design.md (Reviewer 1, "R1", ids `F1`–`F45`) and
review/2026-09-23-phases11-20-r2-testability.md (Reviewer 2, "R2", ids `#1`–`#64`), against PLAN.md (P1–P6, RV9–RV13, the
2026-09-22 / 23 rulings incl. A11 AMENDED), INDEX.md (phase table, research R11 / R12 / R13, Change Log), build-prompt.md, the
ten DRAFT docs' Decisions (Jeremy's dated rulings are not reopened; where a finding shows a real contradiction or
impossibility it becomes a question), qa/JEREMY-QA.md, .github/workflows/apk.yml and round 1's triage
(review/2026-09-23-phases11-19-triage.md, whose ids `T<phase>-<n>` / `C-<n>` this triage continues so every id stays unique).

Facts established after the reviews, applied throughout:
1. **kingpinzs/tessera is PUBLIC** (anonymous API, 2026-09-23) and apk.yml attaches every `main` APK to the public rolling
   "latest" Release (apk.yml:3-8, :150-169) plus a 30-day artifact (:126-131). Anything in BuildConfig ships publicly → phase 17's
   Q-A key route goes back to Jeremy (§2 Q-A2).
2. Jeremy is being re-asked separately whether R12's Microsoft wallpapers stay local (uncommitted, gitignored:
   `.gitignore` `docs/plan/r12/*.jpg`). **Not decided here** (T12-13 records it as a pending input).
3. The lead's decision "QA provisioning finishes the wizard once; rows that test the wizard clear the marker first" STANDS
   (phase 12 Decisions, lead call). F39 / #5 are applied as DOC-UPDATEs under it (C-15): those rows clear the marker first
   and restore it (RV12), with a precedence rule for the two "not shown" lines.
4. Phase 12 Q7–Q9 are answered: Cobalt #3E65FF a 49th swatch everywhere; the original preset is ONE entry with two picture
   variants (later Hero img0, first light-streak img0); Tess keeps her lens tinted Cobalt. Phase 17 Q-B: Jellyfin only.
   Phase 20: all five answered, plus Pandora as the first hand-off entry.
5. The build loop builds the first INDEX table row not marked `done` (build-prompt.md:9), and the table is numeric — so
   round 1's order (11 → 13 → 12 → 14; 15's task 0) is not where the loop reads it (C-14 decides the mechanism).

**Checked today by the triage (every cite below is against these):** `qa/phase-03/scripts/lib.sh:24` (`APK=` fixed, no
override), `:47-56` (`wake_device` forces AC power), `:87-91` (`_verdict` counts PASS/FAIL whatever the detail), `:125-137`
(`row_end` fails a zero-assertion row), `:259-267` (`reply_text` = last `text=` of the whole ring; `replies_since` is a tail
count); `qa/phase-03/scripts/provision.sh:35` (`install -r -g`), `:50-51` (HOME / ASSISTANT roles); `qa/phase-03/scripts/j7.sh:13-15`
(the `wall=` slicing lesson); `diag/Diagnostics.kt:14-26` (one in-memory ring per process); `cortana/speech/SpeechService.kt:101`,
`:215-222` (`:speech` has its own ring, printed by its `dump()`), `:326`; `cortana/speech/SpeechClient.kt:107` (`speaking done
<id> cancelled=<b>`); `music/MusicService.kt:155-187` (J5's `onSetMediaItems`; lines `:166` `search "<q>": nothing in the
library`, `:169` `search "<q>": <kind> <label>, n track(s)`); `AndroidManifest.xml:111-120` (`MusicActivity` is the FIRST
MAIN / LAUNCHER activity), no `usesCleartextTraffic`, no `networkSecurityConfig`, `res/xml/` holds only `method.xml`,
`recognition_service.xml`, `voice_interaction_service.xml`; `app/build.gradle.kts:22` (`targetSdk = 36`); `bars/SystemBars.kt:77-80`
(`BarMetrics.STATUS_EPX = 28`, "R3 C4"); `brand/Brand.kt:46` (`LENS_IRIS` #D81810); `StartActivity.kt:187-190` (the pin band is
drawn ungated); the only hard-coded network hosts in `app/src/main/kotlin` are `api.open-meteo.com` and
`nominatim.openstreetmap.org`; INDEX.md has NO Change Log line for J5 / J6 / J7 (grep). **Device (read-only):** on
emulator-5554 `adb shell content query --uri content://com.android.deskclock/alarms` → `SecurityException … ClockProvider …
not exported from UID 10105` (confirms #18). **Network probe:** radio-browser.info, the 2,000 most-clicked non-broken
stations (`/json/stations/search?limit=2000&order=clickcount&reverse=true&hidebroken=true`, 2026-09-23): **700 (35.0 %) have an
`http://` `url_resolved`; 89 of the top 200 (44.5 %)** — R1 F38's "most stations are plain http" is overstated; "about one in
three" is the fact used in §2.

**Counts (merged, de-duplicated): 92 items — 11 BLOCKING · 53 SHOULD-FIX · 28 NOTE.**
**Classes: 68 DOC-UPDATE · 20 AGENT-CALL · 3 QUESTION FOR JEREMY (3 questions + 1 held conditional, §2) · 1 REJECT**
(plus two partial rejects inside T20-2 and T20-7).

Class rule (round 1's): a mechanical re-cut to a dated Decision or a missing row with an obvious form → DOC-UPDATE; a
finding whose fix needs a design choice → AGENT-CALL (decided below, one line of reason); a change to WHAT is built or how it
behaves for the user with no ruling that covers it → QUESTION; wrong or confirmation-only → REJECT. **Scope rule used for
"W10M features the phase neither includes nor excludes" (F11, F16):** PLAN A8 + the 2026-09-22 scope add ("read as A8 reads the
feature list — everything buildable is in") + Q12 (the final release, every update included) + A4 (fidelity beats feature count)
already rule that an in-scope W10M app's own offline, buildable features are in; so those are AGENT-CALLs "in", listed for
Jeremy in §2b so he can prune, not asked.

---

## 1. Merged findings

Severity is the higher of the two reviewers'. Line numbers are the DRAFT docs' as read today.

### Phase 11 — tile quick actions

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T11-11 | NOTE | F1 | DOC-UPDATE | Scope Out (l.29) "any network use (A11 holds: shortcuts are local)" and Decisions "Data source" (l.53) "A11 holds: nothing here touches the network" → "no network (shortcuts are local; offline preferred, A11 as amended 2026-09-23)". |
| T11-12 | NOTE | F2, #1 | DOC-UPDATE | The shortcuts line cannot tell two tiles of one package apart. (1) Harness contracts (l.149): `[quick] shortcuts for <pkg>/<activity>/<userId>: n (k shown: id,…)` (`<activity>` = the tile component's class, short form). (2) Selection rule (l.105-107) marked "SUPERSEDED 2026-09-23 by the per-activity line (last Decisions line)". (3) E15's OPEN parenthetical (l.353-356) struck. (4) Build task 5 names the eight ids (`songs`, `albums`, `artists`, `playlists`; `start_theme`, `tile_apps`, `checklist`, `diagnostics`); E15 asserts `app.tileshell/.music.MusicActivity/0: 4 (4 shown: songs,albums,artists,playlists)` and `app.tileshell/.settings.SettingsActivity/0: 4 (4 shown: start_theme,tile_apps,checklist,diagnostics)`; E1 / E3 / E9 take the form (`app.tileshell.testclient.a/<launcher activity>/0`). (5) The later rows that assert "names only that activity's ids" (15 E27, 16 E25, 17 E23, 18 E2 / E16, 19 E17) assert the activity-keyed line, which is what makes them failable. |
| T11-13 | NOTE | #2 | DOC-UPDATE | Decisions "QA fixtures" (l.158-164): "tileclient-a declares `res/xml/shortcuts.xml` in the `<meta-data android:name="android.app.shortcuts">` of its LAUNCHER activity (the component `baseline_layout.json` pins); `ShortcutActivity` is only the shortcuts' target" — under the per-activity query a declaration on `ShortcutActivity` yields 0 and E1 fails on a correct shell. |
| T11-14 | NOTE | #3 | DOC-UPDATE | Harness contracts gain `[quick] satellite <i> icon failed <pkg>/<id>: <why>`; the edge case (l.398-399) names it; tileclient-b gains a second dynamic shortcut whose `Icon.createWithContentUri` points at a missing URI so E14's coverage includes the line. |

### Phase 12 — setup wizard

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T12-10 | BLOCKING | F3, #6 | AGENT-CALL | Q7 A / Q8 C / Q9 A are ruled but the doc is still conditional, E13 contradicts Q8, and the variant is undesigned. **Agent call — one preset id, one variant key** (#6's form; F3's two-id form rejected because Q8's Decision says "one entry … so the list stays six"): `theme_preset` = `w10m`; new key `theme_preset_variant` = `hero` (default: `img0_w10m_1607-1709.jpg`, the final release's, Q12) \| `streaks` (`img0_w10m_1507-1511.jpg`); the entry shows the Hero; under it two small picture chips pick the variant (P4, H9); tags `preset_variant:<v>` (wizard) / `theme_preset_variant:<v>` (Start + theme); line `[theme] preset w10m variant <v> applied`; changing the variant is not a Custom change, and re-tapping the preset applies the last-chosen variant. **Re-cuts:** preset table's original row → accent `Cobalt` #3E65FF, the 49th named swatch after the 48 (Q7 A); picture = the variant; `tess_lens` / `tess_ring` = the lens on Cobalt's hue line / Cobalt (Q9 A); every "by Q7 / Q8 / Q9: A (lean) … B … C" cell struck. T12-9 line (l.253-257): "Q7–Q9 answered 2026-09-23 (A, C, A)"; "UNANSWERED … written conditionally" struck. E3 (l.489) drops the Q7-C hedge (HAL's accent per T12-11). **E13 re-cut** (#6 (4)): tap `preset:Windows 10 Mobile (original)` → `theme_preset` = `w10m`, `theme_preset_variant` = `hero`, `accent` = 4282279423, `accent:Cobalt` present and `selected="true"`, the accent grid holds 49 `accent:*` nodes on BOTH surfaces, `persona.py` finds the lens on Cobalt's hue, `wizard_presets` holds exactly six `preset:*` entries; tap `preset_variant:streaks` → `background` names the streaks asset, the variant line, `theme_preset` still `w10m`. **Picture branch chosen from the APK, never skipped:** `unzip -l <apk>` lists the img0 asset → T = 0.40 · accent + 0.60 · G ± 4 (T12-16's sampling); not listed → `[theme] preset Windows 10 Mobile (original) applied: no picture` and T = accent ± 2. "Until Q7–Q9 are answered this row is not runnable" struck. H9 drops "if Q7 is A" and gains "the two picture variants and how one is chosen". |
| T12-11 | SHOULD-FIX | F4, #6 (HAL half) | AGENT-CALL | HAL's accent `Brand.LENS_IRIS` #D81810 (`brand/Brand.kt:46`) is off the 48 and Q7 was ruled for Cobalt only. **Agent call: HAL's `accent` = Red #E81123** (the A16 swatch nearest the lens red, RGB distance 25.8); `tess_lens` stays the exact HAL lens, so the lens look is unchanged. Reason: Q7 A added Cobalt for a fidelity reason ("as W10M phones effectively had it") that HAL red does not have; the grid stays one-off (49), the HAL accent is pickable and shows selected in the grid, and the red that matters (the lens) is its own key. Table row and E3 follow (`accent` = 4293398819; T = 0.72 · accent + 0.28 · G with that accent); E11's Custom step names a preset whose accent is not Red before tapping `accent:Red` (else no change occurs). H6 judges. |
| T12-12 | SHOULD-FIX | F5 | AGENT-CALL | Live apply makes every preset tap a preview, and "the user's picture is NOT kept anywhere" (l.295-296, edge l.633) destroys user data on a browse. **Agent call: Custom remembers the last custom set.** When a preset tap replaces a state whose `theme_preset` is `custom` (every user item change makes it so), the full item set — including `background` and its persisted URI grant, which is kept, not released, while referenced — is saved as the Custom snapshot; `preset:Custom` / `theme_preset:Custom` is tappable when a snapshot exists and restores it (`[theme] preset Custom restored`); a newer snapshot replaces the older and releases a grant no longer referenced. The "NOT kept" sentence and edge case are struck; E11 gains: set the checkerboard picture (Custom) → tap HAL → tap Custom → `background` = the checkerboard URI and every key equal the pre-HAL state. H6. Reason: a preview must never cost the user their own picture (P1), and the snapshot costs one saved key set. |
| T12-13 | SHOULD-FIX | F6 | DOC-UPDATE (the ruling is pending with Jeremy — asked separately 2026-09-23; not decided here) | R12's img0 files are gitignored, and Jeremy's phone APKs are the public CI builds (apk.yml), so today no CI APK carries an original picture. Header status and the T12-9 line list the FINAL gates as: the four pictures in `art/themes/` (Q6) AND Jeremy's ruling on committing / shipping R12's Microsoft wallpapers, with "which build carries the original pictures follows that ruling; with none, T12-7's no-picture form ships". E12 / E13 already branch on the APK (T12-10), so both answers are testable without re-cutting. |
| T12-14 | NOTE | F7 | DOC-UPDATE | Build task 2 builds the "Start visible" gate: the `SecondaryTiles` pin band (drawn ungated today, `StartActivity.kt:187-190`) and phase 14's `PodBayRequests` consumption both wait on `[wizard] finished` / `[wizard] skip` / `[wizard] not shown`; the edge case (l.621-623) becomes an E8 sub-row: a pending pin request while the wizard shows → no band until `wizard_skip`, then the band. |
| T12-15 | NOTE | F8 | DOC-UPDATE | (a) R12 §7.1: R3's accent-coloured readings (A19 action-center tile, A20 volume fill, A22 Cortana ring, the keyboard dot) are Cobalt renditions, not "#0078D7 plus a capture offset" — phase 12's Build task 3 writes the INDEX Change Log line R12 asks for when phase 12 lands, and phase 04's DRAFT (A19 / A20 derivations) is re-read before its interview (cross-phase note; phases 03 / 05 FINAL rows are re-checked at their re-runs). (b) Scope Out (l.46) keeps img5 out with the real reason: "a preset must not rewrite Android's lock-screen wallpaper as a side effect of a preview (phase 17's "set as" and phase 19's Lock screen page are the user's routes)". |
| T12-16 | SHOULD-FIX | #4 | DOC-UPDATE | E3, E11, E13's pixel rule samples the picture at two pixels. All three: "G = the mean of the two gutter pixels at the same y immediately left and right of the band (≤ 12 px from T's column); the sample row is where those two gutters differ by ≤ 3 levels per channel, searched down the band in 8-px steps; the chosen y and both gutter values are logged". "Fails if the picture is missing (then α = 1)" stays as the second assertion. |
| T12-17 | SHOULD-FIX | #7 | DOC-UPDATE | Diagnostics gain `[wizard] step <ns>:<id>: action failed <intent action>: <exception>`; the step's button then relabels to "Open Android settings" (Android's Settings home, phase 19's fallback form); a JVM test drives the step runner with an unresolvable intent; P1 records any One UI step that hits it. |
| T12-18 | NOTE | #8 | DOC-UPDATE | E2: "the id list is extended by each later phase's C-4 line (the id at its `Checklist.kt` position) and E2 is re-run with the full list at the end-of-build pass". |
| T12-19 | NOTE | #9 | DOC-UPDATE | E11's keyboard check writes numbers: DARK = phase 05 E3's key-fill RGB ± 4 (the number copied into the row), LIGHT = #E6E6E6 ± 4; add a ring sample (`persona.py` ring colour = the preset's `tess_ring` ± 8). |

### Phase 13 — Fluent materials

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T13-8 | SHOULD-FIX | #10 | DOC-UPDATE | The satellite sub-rows are geometrically impossible under T13-1's sample rule (a 164-px satellite pressed at its centre has no pixel ≥ 126 px away). Phase 13 E7's satellite sub-row and phase 11 E5 / E7's: "press the satellite at (left + 10, top + 10) px; sample the ring on its right and bottom edges ≥ 126 px from that point; the touch-point pixel = F + 0.10·(255 − F) ± 4; the pixel 60 px along the diagonal = F + 0.05·(255 − F) ± 4; the interior 12 px inside the bottom-right corner (≈ 200 px away) = the rest fill ± 2". |
| T13-9 | SHOULD-FIX | #11 | DOC-UPDATE | E1 "within 1 s" → `MARK=$(adb shell date +%s%3N)` immediately before `cmd power set-mode 1` / the toggle tap; the `[fluent] acrylic=off …` line's `wall=` − MARK ≤ 1000. The surface line is also written when a surface's form changes (`[fluent] <surface> form=acrylic\|fallback`); the edge case (l.363-364) asserts that line's `wall=` − the `acrylic=off` line's `wall=` ≤ 34 ms (2 frames), the screencap only corroborating. |
| T13-10 | SHOULD-FIX | #12 | DOC-UPDATE | Diagnostics gain `[fluent] static backdrop failed for <uri>: <why> (fallback)`; edge-case row: set the checkerboard, `adb shell rm` it, open the app list → E2's acrylic-off pixels and the line. |

### Phase 14 — pod bay

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T14-11 | SHOULD-FIX | #14 | AGENT-CALL | Phase 14 ("the same material — phase 13's app-list backdrop, same engine, same parameters", l.65-67) and phase 13's table ("its own P4 fill", l.121) disagree. **Agent call: phase 14's reading.** Phase 13's table row becomes: Pod bay page \| 14 \| static wallpaper \| as the app-list backdrop \| wallpaper \| (0,0,0) \| wallpaper under the tint, unblurred (A18's form); no picture → theme background. Tag `acrylic:pod_bay`; line `[fluent] pod_bay source=static tint=(0,0,0) alpha=0.8 blur=30epx`; E14 written with values: "phase 13 E2's method on the pod bay: in a strip between two pods, edge spread = the blurred width ± 20 % and pixels = 0.2 × blurred checker ± 3; pod text edges sharp; acrylic off → 0.2 × checker ± 3 unblurred; the `[fluent] pod_bay` line". Reason: phase 14's Decision is the explicit one (H10 is written for it) and two sibling pager pages share one material. |
| T14-12 | SHOULD-FIX | #13 | DOC-UPDATE | T14-7 is proven only by an edge-case sentence. Add **E17 Pod-bay request under the wizard**: `pm clear` → `PROVISION_FINISH_WIZARD=0 qa/phase-03/scripts/provision.sh` (C-15) → `cmd notification disallow_listener …` → Home (`wizard_page`); `nav_search` opens Tess, `type_request "open the pod bay"` → reply "Opening the pod bay.", session closed; from a MARK, the ring holds no `[podbay] opened` and no `[start] page=POD_BAY`, the dump still shows `wizard_page`; tap `wizard_skip` → `[wizard] skip`, then `[podbay] opened by voice` with a later `wall=`, `pod_bay` in the dump. Restore `pm clear` → `provision.sh` → Home. depends-on gains 12 (C-23). |
| T14-13 | SHOULD-FIX | #15 | DOC-UPDATE | Diagnostics gain `[podbay] launch <id> failed: <why>` (`slot unassigned` \| `no session: not the assistant` \| `ActivityNotFoundException`); E4 gains two sub-rows: a baseline variant with `slots.MUSIC` removed → title tap → the slot picker or the line, never nothing; `cmd role remove-role-holder android.app.role.ASSISTANT app.tileshell` → Reminders tap → the line; restore the role. |
| T14-14 | NOTE | #16 | DOC-UPDATE | E6: "the `SpeakingDone` line" → the literal `[speech] speaking done <utteranceId> cancelled=false` (`cortana/speech/SpeechClient.kt:107`), both lines read from the launcher ring sliced from the MARK taken before `say` (C-20). |

### Phase 15 — Alarms & Clock, Calculator, Voice Recorder

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T15-14 | BLOCKING | F9 | QUESTION (§2 Q-E) + AGENT-CALL for the mechanics under each answer | R11 found in three sources on two builds that W10M alarms and timers ring as a toast banner pinned to the top (alarm 0 → 248 epx, timer 0 → 216 epx) over the lock screen or the dimmed app, with no full-screen page (r11/clock.md §8.1–8.12; G1 says "a decision for Jeremy"); and the doc has no unlocked-in-use surface at all (a full-screen intent launches its activity only when the screen is off or locked; in use Android shows a heads-up notification). **Whatever the answer, the doc gains:** an in-use surface and **E4b** (alarm fires with DeskClock in front, unlocked → the chosen surface appears, Snooze / Dismiss work, the sound on `USAGE_ALARM`), and the line `[alarms] surface: toast-locked \| toast-overlay \| heads-up \| page`. **Under A (lean):** locked → the FSI starts a translucent `showWhenLocked` + `turnScreenOn` activity drawing §8's toast at the top (build-start check: whether the keyguard stays visible beneath a translucent `showWhenLocked` activity on API 36; if not, the toast draws over the wallpaper, recorded, H5); in use → the ring service adds a `TYPE_APPLICATION_OVERLAY` window with the same toast, which needs "Display over other apps" (`SYSTEM_ALERT_WINDOW`): this phase ADDs the Setup row `setup:overlay`, its wizard step and why line, and the provision line `appops set app.tileshell SYSTEM_ALERT_WINDOW allow` (phase 04 later reuses the row — no second one); without the grant the heads-up with Snooze / Dismiss actions shows and the surface line says so. Timers use the 14393 timer toast (§8.9–8.11, one ✕ button). H1 / H5 become [fidelity] against §8; E4, E10, E21, E23 re-cut to the toast. **Under B:** locked as A; in use → the heads-up (a recorded P2 seam, H row). **Under C:** the full-screen page as written becomes a P4 [accept] design; in use → the heads-up. |
| T15-15 | SHOULD-FIX | F10, #50 (15) | DOC-UPDATE | R11 landed; apply r11/clock.md G1–G13, r11/calculator.md items 1–11, r11/voice-recorder.md's gap bullets as measured values or tagged approximations. Header (l.4) "pending on disk … empty" and Fidelity (l.121-122) "none of the three files exists" struck. **Clock:** the four pivots are icon-over-caption TABS (64.5-epx pitch, centred, accent underline 64.4 × ≈3.7 epx, grey band 24.0 → 92.4 epx), labels Alarm / **World Clock** / Timer / Stopwatch (tags keep `clock_pivot:<id>`); a tab tap does NOT slide (E10 asserts the jump; X13's settle kept for swipes only, a tagged approximation U12); editor order time spinner / Alarm name / Repeats / Sound / Snooze time at a 64-epx pitch, values in accent; snooze choices 5 / 10 / 20 / 30 min and **1 hour**, default 10, and the ring-time "Snooze for" choice (G2); world-clock rows 96.9 epx with "Today, N hours ahead / behind", an accent-filled "Local time" row (G7); empty state "No alarms" grey Light at §2.1's position (G8); tile face per §9 (G11, H14). **Calculator:** the converter label "**Weight and Mass**" (E12's exact list re-cut); **no history in Programmer** (E12); no accent `=` or operator fill; x², ¹⁄x, xʸ are text, not MDL2 glyphs; Standard is immediate-execution (each E11 case names its mode; Tess stays Scientific). **Voice Recorder:** one page, no pivot, no pane (`rec_page:<record\|list>` becomes one page's two states); rows **56 epx** two-line (E24's "44 ± 1 epx (R3 C2)" → 56 per R11's tolerance). Status bar per C-17. H1–H3 [fidelity] only where R11 has HIGH / MEDIUM, [accept] for UNMEASURED; P6 struck (G13: no ≤ 17-ms value exists). |
| T15-16 | SHOULD-FIX | F11 | AGENT-CALL | R11 found W10M features the phase neither includes nor excludes. **Agent call: in** (the Scope rule in the preamble: A8 + Q12 + A4), each with a build-task line, a row and an H row ([fidelity] where R11 measured, else [accept]): Calculator's **Date calculation** mode (a fourth mode; `calc_mode:date`; host-computed cases in calc-cases.tsv); Voice Recorder's **pause** (manual pause / resume, distinct from the call pause), **markers** (tap to mark, listed on playback), list **search / filter**; Clock's **compare mode** (the 48-epx accent hour strip, G7), the **expanded** timer / stopwatch views (G9), **pin a timer or stopwatch to Start** (a shell-owned secondary tile through phase 01's secondary-tile model, 2015 face per clock §9, approximation), stopwatch **Share** (`ACTION_SEND text/plain` of the laps), alarm sound **"Pick from my music"** (a local audio file through MediaStore, G6). "Open file location" is NOT built here: phase 18 ADDs it to Voice Recorder when Files exists (an ADD, not a hook). Listed in §2b. |
| T15-17 | BLOCKING | #17 | DOC-UPDATE | E18's `[speech]` lines live in `:speech`'s ring (`SpeechService.kt:101,326`; `Diagnostics` is per process, `Diagnostics.kt:14-26`) and no dump route exists for `:recorder`'s. Preamble: "Three rings: the launcher's (`diag`), the `:speech` process's (`speech_dump`, `lib.sh:147-149`) and the `:recorder` process's (`adb shell dumpsys activity service app.tileshell/.recorder.RecorderService`, whose `dump()` prints its process's `Diagnostics` as `SpeechService.kt:215-222` does — build task 7). Each `[recorder]` line is listed with its ring; a line needed after the recorder process died (`recovered`) is written by the next recorder start and read from that ring." E18: "the `:speech` ring (`speech_dump`, sliced from a MARK taken just before the `kill -9`) holds `a client died` then `microphone released: owner died`, both `wall=` − MARK ≤ 2000". E14 / E19 / E22 name the recorder ring; E22 greps all three rings' saved slices (C-20). |
| T15-18 | SHOULD-FIX | #18 | DOC-UPDATE | E9's DeskClock negative cannot fail: verified today, the shell uid's `content query --uri content://com.android.deskclock/alarms` throws `SecurityException … not exported`, so "count before = after" is 0 = 0. Replace with `adb shell dumpsys alarm \| grep -c com.android.deskclock` before = after, keep "no `com.android.deskclock` activity resumed", and drop the query form. |
| T15-19 | SHOULD-FIX | #19 | DOC-UPDATE | `[recorder] list: n recordings (m by other apps)` hides the READ_MEDIA_AUDIO-denied case. The line gains `(other apps: hidden, READ_MEDIA_AUDIO denied)` in that state, the list shows one line naming the Music grant; E21 sub-row: with E14's other.m4a present, `pm revoke … READ_MEDIA_AUDIO` → other.m4a absent, the take present, the line and the notice; `pm grant` restores. |
| T15-20 | NOTE | #20 | DOC-UPDATE | E8 keeps its airplane pass (works offline) and adds a pass with the network ON: the shell uid's byte counters in `dumpsys netstats --uid` unchanged across the search and add (proves the world clock makes no request). |

### Phase 16 — Calendar and People

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T16-11 | SHOULD-FIX (trust) | F13 | AGENT-CALL | The write guard as written (l.291-293) refuses the Birthdays writer and the JVM test would enshrine it. **Agent call: the guard's allow set is exactly three cases:** (1) the `Tessera` calendar (`LocalCalendar.id`), every op; (2) the `Tessera Birthdays` calendar, the Birthdays writer's path only (sync-adapter URI, its own LOCAL account; never offered to the editor or Sync); (3) an allowed Sync target, only a push / update / delete of a copy `calendar_sync.json` maps. Everything else → `refused (not allowed)`. The JVM test covers each allowed case and the refusal of every other combination (editor → Birthdays, Birthdays writer → Tessera, Sync → a non-allowed id, any op → an account calendar). Reason: rule 1–4 of Q2 D are about user events; the derived Birthdays calendar is the shell's own and must be writable by exactly one code path. |
| T16-12 | NOTE | F14 | DOC-UPDATE | T16-1 Sync hardening: before an `updated` push or a delete-"both", re-read the copy event's `calendar_id` and require it to equal the mapping's target and that target to be still on the allowed list; otherwise refuse (`failed mapping stale`) and never offer "Delete here and from <calendar>" for an un-allowed target. JVM case added to T16-11's test. |
| T16-13 | SHOULD-FIX | F15, #50 (16) | AGENT-CALL | R11 calendar / people applied; header (l.4) and Fidelity (l.155) "pending / not yet written" struck. **People:** rows 50 epx, 32-epx avatar at x 12, name at x 57.75, accent letter headers (E10 re-cut from the R7 §1.3 stand-in); the card is a full accent page with a 124-epx circular photo and titled action rows ("Call Mobile" + number); editor fields 32 epx with a 2-epx grey border and "+ field" rows; **agent call: People's own jump grid** (72-epx cells, 4 columns at 360 epx — HIGH on two devices), not X8 (E20 re-cut; phase 01's app-list grid is phase 01's matter); H2 [fidelity] against the 10586 captures with a version note (no governing-build capture, U1). **Calendar:** W10M had Agenda / Day / Week plus a month DROP-DOWN (no Month page) — **agent call: the "Month" App Shortcut opens Agenda with the month drop-down open**; tags `cal_view_mode:<agenda\|day\|week>` (Week added, month removed) and `cal_month_dropdown`; build task 4's views and E25 follow; the ≡ calendar list is W10M's show / hide pane with account group headers (`cal_calendar_row:` inside `cal_pane`), and "Can sync to" is a checkbox list styled like K6.4; event rows: an 8-epx colour bar at x 0, the label tinted with the calendar's colour, the title at x 92.5 (E19); the page background #1A1A1A; the 15063 form for the strip / selected day with an H1 note (LOW); day view, event page, editor, reminder toast and settings are UNMEASURED → [accept] rows (H1 [fidelity] narrows to Agenda, Week, the drop-down and the pane). Status bar per C-17. |
| T16-14 | SHOULD-FIX | F16 | AGENT-CALL | People's GROUPS pivot (W10M People 10586: CONTACTS / WHAT'S NEW / GROUPS). **Agent call: in** (Scope rule; What's New stays out as offline-preferred social): create / rename / delete through `ContactsContract.Groups` under the same account rule new contacts use (Decisions "People over the provider"); "Text the group" = `ACTION_SENDTO smsto:<n1>;<n2>…` to the SMS role holder (phase 06's Messaging once it holds the role — works before 06 like the card's Text); tags `people_pivot:groups`, `people_group:<id>`; line `[people] group <create\|rename\|delete> <id>: ok \| failed <err>`; an E-row; H row [accept] (guide wording only); the People shortcut set gains `groups` (rank 2) under phase 11's standing rule. Listed in §2b. |
| T16-15 | NOTE | F17 | AGENT-CALL | WRITE_CONTACTS widens Tess's `contacts` row, so Tess's health page would read PARTIAL for People's need. **Agent call: People gets its own Setup row `setup:people`** (READ_CONTACTS + WRITE_CONTACTS; `partialIsDone` false; why line "People shows and edits your contacts. Without it People can't see them." — approximation, phase 12 H1); Tess's `contacts` row is unchanged (READ). Scope "Manifest ADD" paragraph (l.62-70), build task 2, E13's PARTIAL clause and E26 (→ `wizard_step:setup:people`) re-cut; phase 12's why table gains the row. Reason: 15 / 17 / 18 / 19 each put their app's grant on the Setup checklist; Tess never writes contacts. |
| T16-16 | SHOULD-FIX | #21 | DOC-UPDATE | E18 asserts an undefined line. Harness contracts gain `[calendar] calendars: denied (READ_CALENDAR)`; E18 asserts it with the permission revoked and its absence after `pm grant`. |
| T16-17 | SHOULD-FIX | #22 | DOC-UPDATE | `[calendar] view <name> <from>..<to>: n instances` gains `in <ms> ms` (query + first composed frame, from `withFrameNanos`); E7 asserts that ≤ 3000 and the line's `wall=` − the swipe's device MARK ≤ 3000 (no screenrecord frame count); E4 asserts the `view …` line listing "Dentist" has `wall=` − the insert's MARK ≤ 2000. |
| T16-18 | NOTE | #23 | DOC-UPDATE | E16's policy negative gets a fixture: TestDPC (googlesamples/android-testdpc, Apache-2.0, needs no Play services) in `~/android-fixtures/`, provisioned as the profile owner with `dpm set-profile-owner --user <id> com.afwsamples.testdpc/.DeviceAdminReceiver`; P5 note: a QA-only fixture, never shipped. The negative stays on the AVD (P6's "moves to P6" struck). |
| T16-19 | NOTE | #24 | DOC-UPDATE | Two edge-case failures get lines: `[calendar] birthdays calendar could not be created: <err>` (LocalCalendar's form) and `[people] tile: photo <lookup> skipped: <why>`. |

### Phase 17 — Photos, Camera, Movies & TV

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T17-13 | BLOCKING (trust) | F18, #26 | QUESTION (§2 Q-A2) + DOC-UPDATE (QA route, both answers) | The Q-A Decision puts the TMDB key into BuildConfig through a CI secret, and CI publishes every APK on the public repo's Release page — Q-A's premise ("ships only in your personal build") does not hold. **Back to Jeremy with the corrected premise (§2 Q-A2; lean: typed once into Movies & TV's settings, Keystore-encrypted, never in the repo or any published APK; the lead's local dev builds may read `local.properties`).** **Writable now whatever the answer (#26):** one credential, the v3 read token sent as `Authorization: Bearer` (`tmdb.apiKey` and the `api_key` query parameter removed everywhere); "never logged: the catalogue lines carry the query and status only"; E20's key half re-cut — the QA APK never carries the real key: assembled with `-Ptmdb.readToken=` (empty; the Gradle property overrides `local.properties`, build task 12 reads the property first) → "No catalogue key in this build", `[video] catalogue: no key`, NO request in the fixture log; then the dummy token (typed into the settings field under A; `-Ptmdb.readToken=qa-dummy-token` under B / C) → `catalogue_server.py` asserts every request carries `Authorization: Bearer qa-dummy-token` and no `api_key`, logging only `bearer ok` / `bearer missing`; **leak scan (gated):** a script reads the real values from `local.properties` without echoing them and finds zero matches in `qa/phase-17/**`, the saved `:video` and launcher ring slices and `adb logcat -d`; the same scan over P15's evidence (C-32). Under A, "Trust" names the TMDB token beside the Jellyfin token in the Keystore store, and a "no key" Browse pivot offers the settings field. |
| T17-14 | SHOULD-FIX | F19, #25 | DOC-UPDATE | Collapse to the ruled forms. **Now:** header (l.4) "two questions … still open" → "Q-B answered (Jellyfin); Q-A re-asked 2026-09-23 (review round 2, Q-A2)"; the "Pending Q-B" block → the Jellyfin client as written, B (Plex) / C (both), BS-6 (task 0), the Plex fixture container (l.570-571), E22's "NOT APPLICABLE … Q-B B / C" half and "Under Q-B B …", P14's "under Q-B B / C" all struck; the "Pending Q-A" block's B (keyless) struck, E20's "under Q-A B" and E21's "under Q-A B" struck, H10's "under Q-A A / C" → plain. **After Q-A2 is answered:** the block becomes the one ruled form with ONE no-key string (`[video] catalogue: no key`, "No catalogue key in this build") and ONE attribution placement (agent: the Browse pivot's foot AND Movies & TV's About, since TMDB's terms ask for it where the data shows — `hub_attribution` on Browse; E20 asserts it there). |
| T17-15 | SHOULD-FIX | F20 | AGENT-CALL | TMDB's `/watch/providers` returns provider names / logos and a TMDB link, not the services' own title ids, so `openTitle`'s deep-link branch is almost never reachable and Q3b A's "opening that app at the title" would mostly land on search. **Agent call: resolve per-service title ids from Wikidata** (its streaming-service title-id properties, e.g. "Netflix ID", looked up by the TMDB id through the keyless Wikidata Query Service; the property list and coverage recorded at build start with BS-4); a found id → the title deep link, none → the service's search URL with title and year. Reason: it keeps Jeremy's ruled behaviour wherever public data allows, with no key and an honest fallback. `query.wikidata.org` joins the fixed-endpoint list (C-16); the fixture (`catalogue_server.py`) answers a Wikidata-shaped query for QA-Flix's fixture property through a debug pref `qa_wikidata_base`; E21 asserts both branches; H11's wording and §2b tell Jeremy many titles will land on the service's search. |
| T17-16 | SHOULD-FIX | F21, #29, #50 (17) | AGENT-CALL | R11 photos / camera / movies-tv applied; "none written as of 2026-09-23" (l.161-164) and every Y "pending" struck. **Movies & TV — agent call: the ≡ pane, not pivots** (W10M used the pane in 2015 and 2017; fidelity, H3): pane rows My videos / Browse / Media server (only when set up), the pane's bottom group → the app's settings page; tags `hub_pane:<id>` replace `hub_pivot:<id>`; the library is the **112-epx tile grid, 2 across** at 360 epx with the 2-line clipped caption (1.4); the player's scrubber is its own (1.6.6–1.6.10: 2-epx track inset 12, accent ring Ø 24, HH:MM:SS labels below), not Groove's; E11's 70 % tap = 247.2 epx on the measured track; Browse borrows 1.5.2 / 1.5.3 / 1.7.5 / 1.7.10 — section rows "title + accent Show all", 112-epx strips, **2 posters across with 12-epx gutters** (Y8's 3-across / 2-epx struck); library pages show the status bar (C-17), the player hides it (Y7 closed). **Photos:** **3 columns** at 360 epx (111-epx squares, 2-epx gutters, 11 / 12-epx margins; Y1's 4-across struck); the status bar is hidden on EVERY Photos page (Y7 extended); viewer: 50-epx #171717 date header, #171717 app bar, Share / Favorite / Edit / Delete / More, overflow Slideshow / Set as / File information (Y2); the editor capture is a panel of large tiles with tabs, filter thumbnails and 60-epx buttons with an accent "Save a copy" (Y11, LOW); **P4 marks:** straighten, light & colour, filters and red-eye are P4 additions (the inbox editor did crop / rotate / auto-enhance only) — Q1 C's ruling stands, its reading as fidelity is corrected; H13 splits into H13a crop / rotate / auto-enhance and H13b the four P4 tools [accept]; viewer open modelled on R7 3.2.2's fade (Y6); en-US strings. **Camera:** no horizontal mode strip in any version (Y3 per T17-17); the pro dial is five arcs centred on the nav-bar top at W/2, radii 130.5 + 65·k epx, inner → outer exposure / shutter / ISO / focus / WB, one control alone = one 130-epx arc (Y4, measured); status bar hidden, nav bar drawn (Y7 closed); panorama guide band (Y12, LOW-MEDIUM); the settings page per 1.7 / §2 minus Lenses, OneDrive and "Related settings"; the capture-intent accept / retake UI is a P4 row (**H20** [accept]). E19 written now; H12 → "the sign-in page AND the Media server library and title rows (Y10, P4)" (#29). All motion UNMEASURED (Y6 / H4 stand). |
| T17-17 | SHOULD-FIX | F22, movies-tv G1 | AGENT-CALL | Three version choices R11 hands to the owner. **Agent call, R8 H-M1's precedent (build the governing / later form, a NEEDS-HUMAN row judges it):** Photos — V-2016+ for the collection (the final release's app, native) and V-2015 geometry for the pages V-2016+ does not show (R11's lean); Camera — the V-2017 form (72-epx shutter disc, 32-epx mode discs at ±60 epx, the top toggle capsule, corner cells) as a tagged approximation with a fidelity note against K11, keeping the measured V-2015 pro dial; Movies & TV — the 10586 geometry for everything measured plus the 2017 −10 / +30 skips and the 2017 "•••" menu (cast / zoom to fill / repeat) (R11's lean). New rows **H17** / **H18** / **H19** [accept] "the version choice" per app, judged on the phone. Listed in §2b. |
| T17-18 | NOTE | F23 | DOC-UPDATE | Build task 1 ADDs `SET_WALLPAPER` (install-time; Photos' "set as lock-screen wallpaper", `WallpaperManager` FLAG_LOCK) — phase 17 is its first user; phase 19 reuses it (no second ADD). |
| T17-19 | NOTE | F24 | AGENT-CALL | Build task 1 drops `FOREGROUND_SERVICE_CAMERA`: the same line says recording stops on screen-off and CameraX runs in the activity's lifecycle, so no camera foreground service exists. Reason: no permission without a user. |
| T17-20 | SHOULD-FIX | #27 | DOC-UPDATE | E20's artwork has no fixture route. `catalogue_server.py` serves `/3/configuration` with `images.secure_base_url = "http://10.0.2.2:8090/img/"` and the poster PNGs under `/img/`; E20 asserts the fixture logged `/img/…` requests; C-29's egress guard runs around E13 and E20–E22. |
| T17-21 | SHOULD-FIX | #28 | DOC-UPDATE | E22's `[0-9a-f]{32}` grep can fail on a correct build and the server floats. The driver reads the app's issued token from the server (`GET /Sessions` with the fixture admin's token, the session whose `Client` is the shell) and greps for that exact string (`grep -rlF "$TOKEN" /data/data/app.tileshell/` → no file); the fixture is `jellyfin/jellyfin:<version>@sha256:<digest>`, recorded at BS-5, the same digest in every run's log. |
| T17-22 | SHOULD-FIX | #30 | DOC-UPDATE | E7 "Timer": MARK = device ms before the `input tap`; the `:camera` ring's `[camera] timer 3s -> shutter` and `[camera] saved <uri> …` lines satisfy `saved.wall` − MARK ≥ 3000 and ≤ 4500; `date_added` only corroborates (≥ MARK / 1000 + 2). |
| T17-23 | SHOULD-FIX | #31 | DOC-UPDATE | E18 gains `[photosapp] delete <id>: refused by user`, `[photosapp] edit <tool> failed: <why>`, `[photosapp] trim <id> failed: <why>` and lists `[video] server token cleared`; E5 gains the Deny branch (the dialog's Deny by bounds → count unchanged, the line); E22's "remove the server" step asserts the token line and T17-21's exact-token grep → no file. |

### Phase 18 — Files

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T18-8 | SHOULD-FIX | F25, #32, #50 (18) | AGENT-CALL | R11 files applied; header (l.4), Scope (l.24) and the R11 Decision's "pending" struck. **Agent calls:** (1) **no root page — the ≡ pane** (256-epx overlay, #171717, 48-epx rows, glyph at cx 24, label at x 60): Recent / This Device / one row per volume / Recycle Bin (a fourth row, P4, H2); `files_root:*` tags → `files_pane:*` (E2, E16); (2) volumes named by their label (`StorageVolume.getDescription`), **no drive letter** (Android has none; H4); (3) **sort by name / date / size only** — "type" dropped (W10M had no type sort; nothing ruled it; E3 asserts three orders); (4) **no free-space line** in Files (W10M had none; phase 19's Storage page shows space). Measured values in: 64-epx two-line rows with colour type icons / thumbnails (A10 branding assets; MDL2 Folder / Page as the monochrome fallback) — Scope's "W10M glyphs by type" → "W10M-style type icons"; the location bar with breadcrumb and ↑, and the "Sort by: Name ⌄" line; app bar Select / New folder / Icons-List / Search / More and selection bar Delete / Move to / Copy to / Share; verbs "Move to" / "Copy to" (pick a folder; no cut / paste); Properties is a PAGE (Y4's properties dialog struck); the other dialogs derive from R7 1.3.9's top-anchored dialog (Y4); Back leaves selection mode, ↑ and breadcrumb go up (Y3; a breadcrumb-jump edge case added); status bar drawn (C-17). New rows: **H7** [accept] the zip flows (a zip opened as a folder, extract destination naming, the "password-protected" / "can't be opened" / "storage removed" wording, create naming); **H8** [accept] Recent as "recently changed, newest first, 100 max"; E11 written now against 1.1–1.10. Listed in §2b. |
| T18-9 | SHOULD-FIX | F26 | DOC-UPDATE | The Storage page's bin row is built twice and phase 19 builds after 18 (C-14). Strike "the one-line ADD to phase 19's Storage page" from Decisions T18-1 (l.155-156) and build task 8 (l.261); phase 18 publishes the bin index reader and an `empty(volume)` call; phase 19 builds the row (its table and task 3 already do). |
| T18-10 | NOTE | F27 | DOC-UPDATE | Bin privacy: binned files stay readable on shared storage (`.nomedia` hides them from MediaStore only; any app with storage access and a PC over USB can read them). The bin page carries one line "Deleted files stay on this phone until you empty the Recycle Bin"; H2 covers the wording. |
| T18-11 | NOTE (trust) | F28 | DOC-UPDATE | Build task 3's `FileProvider`: with a `root-path` entry (needed for `/storage/<UUID>`) it would also cover the app's private dirs, so `getUriForFile` is called only after a canonical-path check that the file lies under a `StorageVolume.getDirectory()`; E7 gains the negative (a file under the app's private dir is never shareable — the share is refused with `[files] share refused: outside shared storage`). The provider scope joins the phase's adversarial-review list (the project's rule for access-control changes). |
| T18-12 | NOTE | #33 | DOC-UPDATE | `[files] zip open <path>: failed <why>` added; E13's `qa-corrupt.zip` step asserts it. |

### Phase 19 — Settings front

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T19-11 | SHOULD-FIX | F29, #34, #50 (19) | AGENT-CALL | R11 settings-front applied; header "pending" struck. **Apps is W10M's own category** (15063, R11 1.5 / 1.12 HIGH): home order System · Devices · Network & wireless · Personalization · **Apps** · Accounts · … · Extras (E1's order re-cut; the table said "r11 may reorder, recorded"); Apps & features and Apps for websites are [fidelity]; Default apps and App permissions are the P4 additions Q2 C allows (H9 / Y6 narrow to those two); Offline maps is OUT with its reason (no Maps, R10-Q4). Goal (l.13), the Q2 Decision's description "desktop Windows 10's 2017 addition" and Y6 / H9 get a dated note correcting the description (Jeremy's ruling itself stands). The table follows R11 §1.1–1.10 (System: Battery, not "Battery saver"; Devices "Bluetooth & other devices"; Network order Data usage · Cellular & SIM · Wi-Fi · Airplane mode · Mobile hotspot · VPN; Update & security's 15254 page name and Device encryption; every as-shipped page with an Android counterpart gets its row, the rest go to Scope Out with their reason, as Kid's corner does — Driving mode, Mouse, Navigation bar each decided that way at build start). Category pages are **48-epx one-line rows at label x 44.5** with a grey section title under the home header and box (Y1 closed); leaf pages a **72.6-epx band over black**; the 15063 sidebar hosts the fallback line (Y2 → a measured slot). **Agent call:** the home box is drawn in its measured focused-at-rest style (2.5) without taking IME focus; an [accept] H row. E1 values: box 32.7 epx, text x 56–57, category rows 48 epx at x 44.5, with R11's tolerances. **Motion measured (60 fps, HIGH):** Y4 closed; E15 written now: `[motion] settings_page t0=… first_offset_epx=<n> settle=<ms>` — first-frame offset 9.8 ± 1.2 epx, 90 % of travel by 200 ± 17 ms, settle 250–300 ms (+ one frame), the old page gone in one frame; H1 [fidelity] covers it; H5 narrows to toggle / combo motion (U2, U3). Status bar per C-17. |
| T19-12 | NOTE | #35 | DOC-UPDATE | Reads with usage access revoked are silent: `[settingsfront] read <data_usage\|storage>: denied (usage access)` and a page line naming the checklist row; an E-row sub-step with `appops set app.tileshell GET_USAGE_STATS ignore`, restored to allow. |

### Phase 20 — Music streaming and Radio

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T20-1 | SHOULD-FIX | F30, #36 | DOC-UPDATE | Stale sweep (round 1's C-11 on this doc). Header → "interview DONE 2026-09-23 (Q1 C, Q2 A, Q3 A, Q4 A, Q5 A + Pandora)"; Goal "by whichever forms the interview rules" → the ruled forms; Scope's "per Q2 / Q3 / Q1 / Q5 / Q4" clauses struck; Scope's "Listen on <service> opening the installed service's app at the title" (l.26-27) and P1 (l.431-432) → "opening each installed service's own search for the title (Q1 Decision)"; Decisions "(only if Q2 rules internet radio)", "(only if Q1 rules a catalogue)", "Q4 decides whether a Wi-Fi-only setting exists" (→ "no setting, Q4 A"), "(if Q3 keeps Radio inside Music)" struck; build tasks' *(Qn)* markers struck with the ruled form; task 6's B / C struck; task 9 struck (Q4 A: no setting); E1's Q3 B / Q3 C branches, E12's Q4 B / Q4 C, E18's "(only with Q3 B)" (→ "exactly the allow-list, no ADD"), H3's "under Q4 B" and H9's "under Q3 B" struck. |
| T20-2 | SHOULD-FIX | F31, #37 | DOC-UPDATE (partial correction of #37) | J5 already built what task 7 builds. The "Observed in code, 2026-09-23" Decision → "closed by J5 (qa/JEREMY-QA.md J5, commit 373106a): `MusicSearch` resolves song / artist / album and `MusicService`'s callback answers searches (`music/MusicService.kt:155-187`); this phase EXTENDS `MusicSearch` with stations — favourites, then the cached directory by name / genre — ahead of the library". Task 7: no new override; the station resolution goes into `MusicSearch` and the existing callback. E16's local-track clause cites `qa/phase-03/scripts/j5.sh` re-run on this build (8/8) as a regression; the station miss "play zzqx radio" → J5's miss reply naming it and **J5's own line form** `[music] search "zzqx radio": nothing in the library` (`MusicService.kt:166`) — #37's proposed `-> none` form is not the code's; E19's `search: "<query>" -> <station\|track\|none>` is re-cut to J5's form (`: station <name>` / `: <kind> <label>, n track(s)` / `: nothing in the library`); session unchanged. |
| T20-3 | SHOULD-FIX | F32 | AGENT-CALL | A fifth Music shortcut never bursts (four-satellite cap; phase 11 shows ranks 0–3). **Agent call: keep Jeremy's four** (phase 11 Q1 named Songs / Albums / Artists / Playlists; the cap is his ruling too) and declare no "Radio" shortcut (a rank-4 static shortcut is dead weight). Decision (l.113-114) "A Radio pivot adds 'Radio' to that list" and E1's shortcut clause struck; H9 → [accept] "Music's satellites stay the four; Jeremy can swap Playlists for Radio on the phone". Reason: round 1's T11-10 precedent (keep the ruled four, an H row to swap). Listed in §2b. |
| T20-4 | SHOULD-FIX | F33 | AGENT-CALL | Pin-a-station-to-Start is a new tile kind nobody designed or asked for. **Agent call: struck** (l.184-185): the hold menu on a station offers add to / remove from favourites only. Reason: favourites and the Music tile's live face cover the need; Rule 16 forbids a half form. Listed in §2b. |
| T20-5 | SHOULD-FIX (trust) | F34 | DOC-UPDATE | Untrusted directory URLs reach ExoPlayer, whose `DefaultDataSource` also opens `file://`, `content://`, `asset://`, `rawresource://`, `data:`. Station and playlist-entry URLs are accepted only as `http` / `https` (`[music] stream: unsupported scheme=<s>`; JVM test on the resolver); a `.pls` / `.m3u` entry is resolved only if http / https; the directory response is capped (the cap recorded at build start; larger → `[music] radio: directory too large`); station logos are decoded at bounds with a byte cap. All of it sits under C-16's adversarial review. |
| T20-6 | SHOULD-FIX | F35, #46 | AGENT-CALL | The Acceptance preamble lacks round 1's harness rules and forces a second APK. Preamble gains: **Seeding** — `layout_restore qa/phase-20/baseline_layout.json`, derived from the newest baseline on disk at build (phase 18's under C-14's order; #46's "phase 19's" is wrong under that order) with MUSIC / PHOTOS / CAMERA slots, Auxio pinned and `manualSizes` for every tile, zero `-> assigned` after the restore (C-3); **C-6** force-stop + Home after E13 / E16's stub launches; **C-4** "fresh install" = `pm clear` → `provision.sh` → Home; the **C-5** motion line; C-20 / C-26 lines. **Agent call — fixture route by debug-only prefs** (`qa_radio_base`, `qa_music_catalogue_base`, `qa_coverart_base`, written with `qa/phase-01/scripts/prefs_edit.py`, honoured only when `BuildConfig.DEBUG`), replacing the "debug APK with BuildConfig fields pointed at 10.0.2.2 … and a build with the real defaults" route (l.321-323, l.192-194). Reason: one debug APK serves every row so the evidence's APK stamp matches (`lib.sh` `apk match`), and it is phase 17's route (task 12). |
| T20-7 | NOTE | F36, #49 | DOC-UPDATE (partial reject) | depends-on per C-23 (`[03, 10, 12, 15, 17]`: 15 for task 0 which "must be in first", 12 for C-4's form; 11 is not needed once T20-3 declares no shortcut); Y5 "R11 §Movies & TV pending" → "phase 17's Y8 / Y9 forms (P4)"; E16's phrases get ids built through `utterances.py build` — `radio_genre_jazz`, `radio_station_news1`, `radio_last`, `radio_miss`, `handoff_listen_stub`. **Partial reject:** F36's "Jeremy's list of music apps is an input the doc should name as a FINAL gate" — answered 2026-09-23 (Q5 follow-up: Pandora); the Decision's "his list lands here" → "his answer: Pandora (the follow-up line above)". H5's kind → T20-14. |
| T20-8 | BLOCKING | #38 | DOC-UPDATE | The Pandora Decision and the "plain open hand-off" rule have no row. **E13b:** a second stub (`app.tileshell.testclient.pandorastub`) declaring the Pandora search form recorded at build start (task 1) → "Listen on Pandora" reaches it with title and artist in the URI (`TileShellQa` log), `[music] handoff: pandora "<title>" -> <uri>`; a third stub that is only `CATEGORY_APP_MUSIC` with no recorded form → "Listen on <stub>" opens its launch activity, `[music] handoff: <app> "<title>" -> open (no search link)`. **P9:** Pandora on the S25U — `com.pandora.android` resumed on its search results for the title (screencap), or, if task 1 recorded no search link, Pandora opened and `[music] handoff: pandora: search not passed`; gated on the recorded form, RECORDED for the landing. E19 gains both lines; E16's Tess half: "listen to qa artist on <plain stub>" → the plain open. |
| T20-9 | SHOULD-FIX | #39 | DOC-UPDATE | E16 "'play radio' → the first favourite" contradicts Q5's "the last favourite". Decisions define it: "the favourite played most recently; with none played yet, the first favourite". E16: two favourites, play the second, stop; "play radio" → the second (J5-form line naming the station); after `pm clear` → `provision.sh` → re-favourite both, nothing played → the first. |
| T20-10 | SHOULD-FIX | #40 | DOC-UPDATE | E21 hits the live internet from the AVD: it moves to **P10** (the S25U, a build with the real defaults), RECORDED with the HTTP status and mirror; a JVM test proves the mirror rule (a fake DNS answer for `all.api.radio-browser.info` → a host list → one picked), so "resolve a mirror through DNS" has a failable check with no network. |
| T20-11 | SHOULD-FIX | #41 | DOC-UPDATE | The catalogue has no named fixture or expected rows. `qa/phase-20/scripts/catalogue_server.py` at `10.0.2.2:8081` serves MusicBrainz `/ws/2/` JSON and `/coverart/` PNGs (bases through T20-6's prefs); E13: exactly three `catalogue_row:` rows hand-listed ("QA Song A" / "QA Artist" / "QA Album", …), artwork = the fixture PNG's colour ± 4; airplane → `[music] catalogue "qa artist": offline` and the page's line; `/500` → `error 500`; server stopped → `error connect`; the fixture log shows `User-Agent: Tessera/<version> (…)` on every request (directory and catalogue) and five back-to-back searches arrive ≥ 1.0 s apart (MusicBrainz etiquette). E19 gains the `[music] catalogue …` forms. |
| T20-12 | SHOULD-FIX | #42 | DOC-UPDATE | E14 cites phase 17 E22's container (pinned digest, T17-21) with a Music library of three MUSIC6-style MP3s of known durations in `qa/phase-17/fixtures/jellyfin/`; wrong password → `[music] server 10.0.2.2:8096: unauthorised`; `docker stop` → `unreachable` with the local pivots intact; these lines are in the MAIN ring (`MusicService` is main-process), `[music] server …`, not phase 17's `:video` `[video] server …`. "Once phase 17 writes it … not runnable" struck. |
| T20-13 | SHOULD-FIX (trust) | #43 | DOC-UPDATE | Jellyfin stream URLs carry the token as a query parameter. Stream URLs are logged with the query string removed (`stream: connected http://10.0.2.2:8096/Audio/<id>/stream codec=mp3`); E14 asserts the issued token (read as T17-21) is absent from the saved ring slices, logcat and `qa/phase-20/**` (C-32). |
| T20-14 | SHOULD-FIX | #44, F36 (H5) | DOC-UPDATE | H5 → *accept* (P4, phase 17's Y8 / Y9 forms — R11 measured no Browse half, so no fidelity row can close); add **H10** *accept* the media-server music view (entry point, grouping, the server-name line). |
| T20-15 | SHOULD-FIX | #45 | DOC-UPDATE | Device-clock timing: icy_server.py switches the StreamTitle 20 s after each connection opens; E4 asserts the `[music] now playing … title=QA Song 2` line's `wall=` − the `stream: connected` line's `wall=` = 20 000 ± 5 000; E8: MARK before `airplane-mode enable`, `stream: lost, retrying` `wall=` − MARK ≤ 5000; MARK2 before `disable`, `stream: reconnected after N ms` with `wall=` − MARK2 ≤ 10 000. |
| T20-16 | SHOULD-FIX | #47 | DOC-UPDATE | E19 gains `[music] sleep: end-of-track cleared (live item)`, `[music] stream: unsupported playlist`, `[music] handoff: <service> did not open at the title`, `[music] radio: no cache yet (offline)` and T20-8 / T20-11's lines, each asserted by the row that produces the state (E5, E3 with a `.pls` station in directory_server.py, E13, E2's fresh-install-offline half). |
| T20-17 | NOTE | #48 | DOC-UPDATE | The FM negative rests on web sources. **P11** (S25U, RECORDED): `adb shell cmd package has-feature android.hardware.broadcastradio` → false; `adb shell service list \| grep -ci broadcastradio` → 0; `adb shell pm list packages \| grep -i fm` → none. |

### Cross-phase

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| C-14 | BLOCKING | F37 (T12-3's unapplied half) | AGENT-CALL | **Order is expressed as an explicit line the build loop reads, not by reordering rows.** Reason: the phase numbers are cited across every doc, review and evidence path, so reordered rows would read wrong everywhere; one line is the smallest change, and the loop already reads INDEX first. **INDEX.md** gains, above the Phases table: "**Build order** (the build loop follows this line, not the table's row order): 01 → 02 → 03 → 05 → 10 → 11 → 13 → 12 → 14 → 15 → 16 → 17 → 18 → 20 → 04 → 19 → 06 → 07 → 08 → 09; 21 (TV channels) after 17 and 20 once its doc exists. Phases before 11 are in their QA gates as their rows say (dated clearances, e.g. 01's of 2026-09-17 and 02's deferral to the end-of-build pass, are unchanged)." Placement: 13 before 12 (T12-3); 11–18 and 20 need nothing from 04, so none of them waits on the phone-gated R4; 06 / 07 / 08 depend on 04 and 09 on 08 (their headers), so they follow it, in PLAN Q7 A's ruled order; 19 right after 04 (its only hard dependency, and its five helper ADDs are built while 04's helper and review are fresh); 20 after 17 (its dependency). **Phase 12 is on the critical path:** its FINAL waits on Jeremy's four pictures and his R12 wallpaper ruling, and 14 (T14-7) and 15–19 (E14 instances, C-15) need it built — the lead tells Jeremy when 11 and 13 are built. Row notes: 13 "lands before phase 04" → "builds after 11 and before 12 and 04 (Build order)"; 12 gains "builds after 13 (T12-3)". **build-prompt.md** step 1 (l.9): "Read `docs/plan/INDEX.md`. It says `mode: standard`. Your only scope this session is the first phase in INDEX's **Build order** line that is not marked `done` (subject to the dated clearances its rows record, as today)"; the phase-04 bullet (l.10): "If phase 04 is the next phase in the Build order line and R4 has not run …". **PLAN.md:254** order clause → "order (re-cut 2026-09-23 by review triage T12-3 / C-14): quick actions (11), Fluent engine (13), wizard (12), pod bay (14), inbox apps (15 → 16 → 17), Files (18), Music streaming and Radio (20), then phase 04 and the Settings front (19); INDEX's Build order line is the operative list". The build loop's done-gate semantics are not changed here. |
| C-15 | BLOCKING | F39, #5, #59, #64 | DOC-UPDATE | Under the lead's standing decision (provisioning finishes the wizard). **Phase 12:** (1) the lead's Decision (l.326-331) names ONE mechanism instead of "taps through, or writes the same done marker": "`provision.sh` writes the marker with the shell stopped — `adb shell am force-stop app.tileshell`, `adb push qa/phase-12/fixtures/setup_wizard.xml /data/local/tmp/` (`<map><boolean name="finished" value="true" /></map>`), `adb shell 'run-as app.tileshell sh -c "mkdir -p shared_prefs && cat /data/local/tmp/setup_wizard.xml > shared_prefs/setup_wizard.xml"'`, then `ime enable` + `ime set app.tileshell/.ime.KeyboardService` (the stop deselected it), then Home; `PROVISION_FINISH_WIZARD=0` skips the marker" ("taps through" is impossible — with every grant held the wizard never shows); Build task 4 lists the step. (2) **Precedence** in Diagnostics: "`not shown: core held` when every core row is held (marker or not); else `not shown: finished` when the marker is set; else the run shows." (3) Goal (l.18-20) → "A phone that already holds every grant never sees it. The QA harness provisions every grant and then finishes the wizard once through the marker the wizard's own finish writes, as a user who completed setup would (Decisions, lead 2026-09-23)"; the 2026-09-22 line (l.107-109) "no existing driver needs a test-only bypass" gets "SUPERSEDED in part 2026-09-23 (lead): provisioning also writes the finished marker". (4) **E1(a):** provisioned AVD (marker written): Home → `start_page`, no `wizard_page`, `[wizard] not shown: core held`; then `am force-stop` + Home → still no `wizard_page`, `[wizard] not shown: finished`, `settings get secure default_input_method` ≠ the shell's and `checklist:keyboard_selected:missing` on the Setup page (the case the marker exists for); `ime set` restores. E1(b) unchanged (no marker; `core held`; no `setup_wizard.xml`). (5) **E14 template:** (a) `pm clear` → `PROVISION_FINISH_WIZARD=0 qa/phase-03/scripts/provision.sh` → revoke <grant> → Home: `wizard_step:<ns>:<id>`, its `wizard_why`, "Step 1 of 2"; grant from adb + resume → the step gone, `wizard_presets` shows. (b) `pm clear` → `provision.sh` → Home → no `wizard_page`, `[wizard] not shown: core held` (E1 re-run on that build). (c) the finished-install rule: with the marker set, revoke <grant> → Home → no `wizard_page`, `[wizard] not shown: finished`, the checklist row `missing`; restore the grant. The same three-part edit in **15 E28, 16 E26, 17 E25, 18 E15, 19 E16** (their Skip-based finished-install halves struck), and 14 E17 (T14-12) uses (a)'s form. (6) E11's control step asserts `theme_preset` absent or `default` right after `pm clear` → `provision.sh` (#64: provisioning must not apply a preset). Rows that assert a full-green checklist after a force-stop run `ime set` first (N-01). |
| C-16 | BLOCKING (trust) | F38, F34 (policy half), #43 (part) | QUESTION (§2 Q-D) for the policy + DOC-UPDATE for the guard, both answers | `targetSdk = 36` (`app/build.gradle.kts:22`) with no `usesCleartextTraffic` / `networkSecurityConfig` (manifest; `res/xml/`) → every `http://` connection is refused: about one in three popular radio stations (probe above), a home Jellyfin at `http://host:8096`, `http://` VIEWs and every `http://10.0.2.2` fixture in 17 and 20. **The policy changes what the user can play → Q-D.** **The guard, whatever the answer** (a trust change; the project requires an adversarial review): phase 17 (first builder) creates `res/xml/network_security_config.xml` and the manifest attribute; phase 20 reuses it (no second config). (1) **Fixed endpoints are https-only by the platform:** a `<domain-config cleartextTrafficPermitted="false">` listing every host the shell itself calls (includeSubdomains): `api.open-meteo.com`, `nominatim.openstreetmap.org` (the two in code today), `api.themoviedb.org`, `image.tmdb.org`, `query.wikidata.org` (T17-15), `api.radio-browser.info`, `musicbrainz.org`, `coverartarchive.org` (+ its redirect host, recorded at build start), and phase 08's model host when it exists; plus a `FixedEndpoints` constant set with a JVM test that every URL is `https://`; at process start the shell logs `[net] cleartext permitted for <host>: <bool>` (from `NetworkSecurityPolicy.isCleartextTrafficPermitted(host)`) for each fixed host — rows assert `false`. (2) **Base config per Q-D:** A → cleartext permitted at the base (arbitrary station / server hosts cannot be listed) with (1) holding; B → base permitted, plus a code guard in the media data-source factory that refuses `http://` except to private addresses (RFC 1918, link-local, `.local`) for the media-server client, stations over http refused with `[music] stream: cleartext refused`; C → base not permitted. (3) **Debug-only QA exception:** a debug source-set config adds `<domain-config cleartextTrafficPermitted="true"><domain>10.0.2.2</domain></domain-config>` so host fixtures run under any answer; a row checks the RELEASE APK's config (`aapt2 dump xmltree --file res/xml/network_security_config.xml <release apk>`) holds no `10.0.2.2`. (4) **Schemes:** T20-5's http / https allow-list and phase 17's VIEW scheme list. (5) **Credentials:** under A, a Jellyfin sign-in to a non-private address over `http://` asks first ("This server isn't secure — your password would be sent unencrypted") and needs a tap; under B it is refused; tokens never appear in a logged URL (T20-13, C-32). (6) **Rows:** per doc, an `http://` stream plays (A / B-LAN) or is refused with its line (C / B-station); the fixed-host `[net]` lines read `false`; the release-config check. (7) **GATE:** the adversarial review (team-review, adversarial mode) of the config, the data-source guard and the http-credential path, recorded under `qa/phase-17/` before 17 is `done`; phase 20 re-runs it on its station path as its own gate. |
| C-17 | SHOULD-FIX | F40, #62, F12 | AGENT-CALL (+ a held conditional question, §2 Q-F) | 28 epx (R3 C4, read on Start) vs 24 epx in every in-app measurement since (R8 Groove; R11 clock, calculator ×2 devices, people P0.2, files, movies-tv library, and settings-front on R3's own S1 / S2 footage at 1080p). **Agent call: re-measure before anyone rules.** INDEX's research table gains "R3 C4 re-check" (dev box, no phone): on R3's own S1 / S2 Start frames at 1080p, (i) the status band's bottom edge by column profile over a flat tile column and (ii) the vertical centre of the status glyphs (≈12.4 epx in a 24-epx bar per R8; ≈14 in a 28-epx bar), and the same two readings on an in-app frame of the same video. Start = 24 → R3 C4 was a misread: phase 01's owner sets `BarMetrics.STATUS_EPX = 24` through the INDEX Change Log (E19 re-cut; Start's grid top kept where measured). Start = 28 and apps = 24 → a real per-surface difference → ask Q-F. Meanwhile every 11–20 citation reads "phase 01's drawn status bar (`BarMetrics.STATUS_EPX`)" instead of the literal 28 (phase-11 l.89, E7 l.311; phase-12 E8 l.521; phase-15 l.123-124, E10 l.517, E13, E23, E24; phase-16 l.156-157, E19 l.581; phase-17 l.165; phase-18 l.122; phase-19 E2), and the wrong symbol `SystemBars.STATUS_EPX` (phase-15 l.124, phase-16 l.157) becomes `BarMetrics.STATUS_EPX` (`bars/SystemBars.kt:77-80`). Phase 10's "28 so one app is not the odd one out" rationale is recorded as inverted if the re-check lands on 24. |
| C-18 | BLOCKING | #53 | DOC-UPDATE | Battery saver may not switch on while `wake_device` forces AC power (`lib.sh:47-56`; the AVD reads `mIsPowered=true`). Phase 13's 2026-09-22 "Verified on the AVD" line (l.131-132) is replaced by a build-start probe recorded there: "after `wake_device`: `cmd power set-mode 1`; `settings get global low_power` → (expected) 0; `dumpsys battery unplug`; `cmd power set-mode 1` → 1" (if it reads 1 on AC, this item drops to a NOTE). `lib.sh` gains `battery_saver_on` (`dumpsys battery unplug`; save and raise `screen_off_timeout` to 1800000 so the screen stays on; `cmd power set-mode 1`; ASSERT `low_power` = 1 — a precondition that fails loudly) and `battery_saver_off` (`set-mode 0`, restore the timeout, `wake_device`). Phase 13's Controls line and E1, phase 12's battery-saver edge case and phase 19 E9's battery-saver sub-row (the helper's verb meets the same AC refusal) call them; 13 P3 / 19 P3 run over Wireless debugging with the phone unplugged, or record the charging state and One UI's behaviour (RECORDED). Owner of the helper code: phase 13's build task 7. |
| C-19 | BLOCKING | #54 | DOC-UPDATE | The upgrade legs cannot upgrade: `provision.sh` always installs the current build (`lib.sh:24`, `provision.sh:35`). `lib.sh`: `APK="${TILESHELL_APK:-$REPO/app/build/outputs/apk/debug/app-debug.apk}"`. **16 E1** and **17 E1** upgrade halves: "`adb uninstall app.tileshell`; `TILESHELL_APK=qa/phase-NN/upgrade/<tag>.apk qa/phase-03/scripts/provision.sh` (the old build, every grant, the wizard marker); hand-assign through Settings > Tile apps; `adb install -r app/build/outputs/apk/debug/app-debug.apk` (NOT `provision.sh`, so the upgrade path runs); Home." Precondition assert: `install -r` succeeds (same debug key; `INSTALL_FAILED_UPDATE_INCOMPATIBLE` fails the row loudly); the log records both APK ids (`apk match: NO` on the first leg is expected and noted). 16 E1's "roles … as `provision.sh:50-54` sets them" route is struck. Owner of the `lib.sh` line: phase 16's build task 8. |
| C-20 | BLOCKING | #52 | DOC-UPDATE | Ring slicing by `wall=` is applied nowhere; several rows fail by design or pass on stale lines, and coverage rows grep a ring that `am force-stop` / `pm clear` / `layout_restore` resets (`Diagnostics.kt:14-26`). (1) `lib.sh` gains `ring_mark` (`adb shell date +%s%3N`), `ring_since <mark> [launcher\|speech\|<service component>]` (j7.sh:13-15's `wall=` filter, one function), `reply_since <mark>` (the first `text=` reply after the mark, empty if none), and `row_end` saves `ring_since $ROW_MARK` of each ring the row names to `<row>/ring-<name>.txt`. (2) One line in every 11–20 preamble: "Every ring assertion reads `ring_since` from a MARK taken immediately before the step's action (after any clock jump, so the MARK is on the new clock); absence assertions read the same slice; `reply_text` is `reply_since <MARK>`." (3) Coverage rows (11 E14, 14 E16, 15 E22, 16 E21, 17 E18, 18 E12, 19 E12, 20 E19): "grep the union of `qa/phase-NN/*/ring-*.txt` from this build's run (the rows' APK id matching), each pattern at least once". (4) Named fixes: 15 E26 a MARK before each negative; 13 E1 a MARK per step (the second `acrylic=on`, the Midnight sub-row); 12 E11 per surface and 12 E12 per preset; 14 E7 / E8 `reply_since`. Owner of the helpers: phase 11's build task 7 (first in the order). |
| C-21 | SHOULD-FIX | F41 | DOC-UPDATE | A dynamic shortcut published without `ShortcutInfo.Builder.setActivity(...)` attaches to the package's first MAIN / LAUNCHER activity — `MusicActivity` (`AndroidManifest.xml:111-120`) — so under phase 11's per-activity query 17's "Media server", "Panorama", "Slow motion" and 18's "SD card" would burst on the Music tile. Phase 11's C-9 Decision gains "every dynamic shortcut calls `setActivity(<its app's launcher activity>)`"; 17 build task 15 (`VideoActivity`, `CameraActivity`) and 18 build task 11 (`FilesActivity`) say so; 17 E23 and 18 E2 also assert the Music tile's burst is unchanged (the four, no dynamic ids) after each publication. |
| C-22 | SHOULD-FIX | F42 | DOC-UPDATE (INDEX, the lead's) | J5, J6, J7 changed FINAL parts (03, 10) in code with no Change Log line (INDEX grep: none). Three lines in the 2026-09-22 LIVE-QA entry's form: J5 (phase 03 / 10: `MusicSearch` + `onSetMediaItems`, commit 373106a, J5.txt 8/8), J6 (phase 03: every Tess event into the shell's LOCAL `Tessera` calendar, `writableCalendarId` removed, J6.txt 7/7 — phase 16 cites this line as the record of the ADD), J7 (phase 03: the relative silence gate and the pending card kept on silence). |
| C-23 | NOTE | F43, #49, T14-12, T20-7 | DOC-UPDATE | depends-on headers say what the rows use: 14 → `[01, 03, 12, 13]` (T14-7's wizard gate, E17); 15 → `[01, 02, 03, 10, 11, 12]`; 16 → `[01, 02, 03, 11, 12, 15]`; 17 → `[01, 02, 03, 10, 11, 12, 15, 16]`; 18 → `[01, 02, 10, 11, 12, 17]`; 19 → `[01, 04, 11, 12]`; 20 → `[03, 10, 12, 15, 17]` (the burst rows need 11, the E14 instances and C-15 need 12). C-14's order makes each true. |
| C-24 | NOTE | F44 (+ found by this triage in build-prompt.md) | DOC-UPDATE | **PLAN.md:** A11 in Working assumptions (l.46-48) annotated in place "AMENDED 2026-09-23 — internet where a feature needs it, offline preferred (see Rulings)"; the feature list's "(A11 stands, R10-Q4)" (l.73) → "(R10-Q4: the shell builds no Mail, browser or Maps)"; "19 as of 2026-09-22" (l.98) → "20 as of 2026-09-23; 21 (TV channels) after R13". **build-prompt.md** (the loop reads it every session): l.20 "Internet uses are only those in A11: …" → "Internet: A11 as amended 2026-09-23 — where a feature needs it, offline preferred (PLAN.md Rulings)" (as written the loop would refuse phases 17 and 20's network features); l.21's trust list gains the network security config / cleartext policy (C-16), the TMDB and Jellyfin credential store (phase 17 Trust), the capture-intent guards (T17-4), the FileProvider scope (T18-11), the helper ADDs (T19-1) and the calendar write guard (T16-11); l.11's research list gains `r8-groove-measurements.md`, `r11/<app>.md`, `r12-w10m-stock-theme.md`. |
| C-25 | SHOULD-FIX | #51 | DOC-UPDATE | Reboots and screen-offs mid-row do not re-wake the AVD (`wake_device` runs only in `row_begin`). Each 11–20 preamble: "after any `adb reboot` (boot-completed poll), `dumpsys battery unplug` or `KEYCODE_SLEEP` step, the driver calls `wake_device` and asserts it printed `Awake` before the next tap"; the rows name the call: 12 E4, 15 E6 / E7, 16 E1 / E6, and the reboot edge cases of 15 / 16 / 17 / 18 / 20. |
| C-26 | SHOULD-FIX | #55 | DOC-UPDATE | "RECORDED" (C-13) has no mechanism: `_verdict` counts by the verdict word and `row_end` fails a zero-assertion row (`lib.sh:87-91`, `:125-137`). `lib.sh` gains `record <name> <value>` (prints `RECORD <name> <value>`, increments a RECORDED counter, never PASS / FAIL) and `row_end` reports "`<row>: recorded only (<n> facts)`" with exit 0 when PASS + FAIL = 0 and RECORDED > 0; C-13's line in each preamble becomes "a recorded clause uses `lib.sh` `record`, never an assert". Owner: phase 13's build task 7 (13 E9 is the first recorded row in the order). |
| C-27 | SHOULD-FIX | #56 | DOC-UPDATE | One helper `fill_volume <leave_bytes>` (15 E19's root route to `/data/media/0/fill.bin`) that asserts `adb shell df /sdcard` free ≤ leave + 5 MB after the fill, and `unfill_volume`; 15 E19, 17's storage-full edge case, 18 E4b / E13 and the copy-onto-full edge case call it (17's FUSE `fallocate` form struck). Owner: phase 15's build task 8. |
| C-28 | SHOULD-FIX | #57 | DOC-UPDATE | The baseline chain: **15** derives from phase 02's file (plus any marker 11–14 add — none), never carrying phase 11's fixture tiles, which only phase 11's rows install; **18** keeps `slots` (MUSIC, CALENDAR, PEOPLE, PHOTOS, CAMERA) with `addedOnce` and `manualSizes` from phase 17's; **19**'s rows that read Start restore `qa/phase-19/baseline_layout.json` (18's, unchanged — it adds no marker) and assert zero `-> assigned`; **20** per T20-6. |
| C-29 | SHOULD-FIX | #58 | DOC-UPDATE | No row proves the AVD network rows stayed off the live internet. A shared guard in 17 and 20's preambles, around 17 E13 / E20–E22 and 20 E2–E16: "`adb root`; `UID=$(adb shell pm list packages -U app.tileshell \| sed 's/.*uid://')`; `adb shell iptables -I OUTPUT -m owner --uid-owner $UID ! -d 10.0.2.2 -j REJECT`; at row end `iptables -L OUTPUT -v -n` shows 0 packets on that rule (gated), then the rule is deleted and `adb unroot`". Offline sub-rows keep airplane mode. Owner: phase 17's build task 16. |
| C-30 | NOTE | #60 | DOC-UPDATE | Voice rows (14's five utterances, 15 E26's six, 20's T20-7 ids) built through `utterances.py build` save `speech_dump` sliced from each `say`'s MARK and assert the `:speech` ring's final line for that capture is not "asr: no speech", so a gate drop fails with its own reason, not as a matcher miss. |
| C-31 | NOTE | #61 | DOC-UPDATE | C-5's `[motion]` line self-reports its own animation: it gains `frames=<n> maxGapMs=<ms>` (consecutive frame-time gaps inside the motion) and rows assert `maxGapMs` ≤ 33.4 ms (2 vsync) beside the numbers, so jank fails on the shell's clock. |
| C-32 | NOTE | #63 (instances #26, #43) | DOC-UPDATE | One credential-hygiene Decision in phase 17's "Trust", cited by 20: "no credential (TMDB token, Jellyfin token, the fixture passwords) is ever written to a diagnostics line, logcat, a logged URL or an evidence file; every row that touches one ends with T17-13's leak scan over `qa/phase-NN/**`, the saved ring slices and `logcat -d`". |

**Folded pointers (not counted twice):** R2 #50 → T15-15 / T16-13 / T17-16 / T18-8 / T19-11; R1 F12 → C-17; R2 #62 → C-17; R2
#59 / #64 → C-15; F36's H5 → T20-14; F34's policy half → C-16; #29 → T17-16.

| Id | Sev | Sources | Class | Reason |
|---|---|---|---|---|
| X-1 | NOTE | F45 | REJECT | Confirmation only (the round-1 resolution ledger and today's code re-checks); nothing to change. Its one open item (T12-3's INDEX / PLAN half) is C-14. |

---

## 2. Questions for Jeremy (Stage A shape; load-bearing first)

Three true forks survive, plus one held until a measurement says whether it exists. Order: the two trust questions first
(one would publish something of his; one decides what he can play), then the one that changes what phase 15 builds.
Everything else in this triage is an agent call; the user-visible ones are in §2b so he can overrule any.

**Q-A2 (phase 17 — your TMDB key; this re-asks Q-A with a corrected fact).** Your repo is public, and every build of the app
is posted as a public download on the "latest" release page. A key built into the app can be pulled out of that download by
anyone, so the plan's "built in by the CI build" would publish your personal TMDB key. Which way?
- A. You type the key once into Movies & TV's settings on the phone; it's stored encrypted there, and the repo and the public download never contain it (test builds on your PC may read it from local.properties; they're never published) (lean)
- B. The key stays built in, but only in builds made on your PC; the public download has no key, so film search is off in it
- C. Keep it built into the public download and accept that anyone could copy your free key (you can reset it on TMDB any time)
- D. Other / let me clarify.

**Q-D (phases 17 and 20 — plain "http" addresses).** Android blocks unencrypted "http://" connections unless the app allows
them. About 1 in 3 of the 2,000 most-played internet radio stations are http only (checked today), and a home Jellyfin server
is usually http. Which?
- A. Allow plain http for media only: radio stations, your media server, and videos other apps hand over. Weather, film and music info and the station list stay encrypted-only, enforced by Android; signing in to a media server over plain http outside your home network asks you first (lean)
- B. Allow plain http only for your media server on your home network; radio stations must be encrypted, so about a third of stations won't play
- C. Encrypted only, everywhere: those stations, and a home server without https, won't play
- D. Other / let me clarify.

**Q-E (phase 15 — how alarms and timers ring).** The research found Windows 10 Mobile had no full-screen alarm page: an alarm
or timer rang as a banner across the top of the screen, with Snooze and Dismiss on it, and the lock screen (or the app you were
in) still showing underneath. The plan builds a full-screen page. Which?
- A. W10M's banner everywhere: on the lock screen, and over whatever app is open while you're using the phone (one more setup step: "display over other apps") (lean)
- B. W10M's banner on the lock screen; while you're using the phone, Android's own pop-up notification with Snooze / Dismiss
- C. Keep a full-screen alarm page (not W10M's form), with Android's pop-up while you're using the phone
- D. Other / let me clarify.

**Q-F (HELD — ask only if the C-17 re-check finds Start at 28 and apps at 24; otherwise it is never asked).** Windows 10
Mobile's status bar (the strip with the clock and battery) measured a little taller on Start than inside apps. The shell draws
one height everywhere today. Which?
- A. Taller on Start, shorter inside apps, as W10M drew them (lean)
- B. The taller one everywhere, so no screen looks different
- C. The shorter one everywhere
- D. Other / let me clarify.

**Pending with Jeremy, asked separately (not written here):** whether R12's Microsoft wallpapers stay local or are committed /
shipped (T12-13). Both answers are already testable (E12 / E13 branch on the APK).

### 2b. Agent calls Jeremy can see and overrule (not questions)

1. Phase 15: W10M's own extras are in — Calculator's Date calculation; Voice Recorder pause, markers and search; Clock's compare mode, full-screen timer / stopwatch views, pinning a timer or stopwatch to Start, stopwatch Share, an alarm sound from your music (T15-16).
2. Phase 16: People gets W10M's Groups (create / rename / delete, text a group through your messaging app) (T16-14), and its own setup step for editing contacts, leaving Tess's contacts step alone (T16-15).
3. Phase 12: the HAL preset's accent is Red (the nearest standard swatch); Tess's lens keeps its exact HAL red (T12-11). Trying presets never loses your own picture: "Custom" brings it back (T12-12).
4. Phase 17: "Watch on <service>" opens the title where a public database (Wikidata) knows that service's id, otherwise the service's search for it — expect many to land on search (T17-15).
5. Phase 17: version choices, each judged on the phone like Groove's H-M1: Photos = the final-release collection with 2015 geometry elsewhere; Camera = the final shutter-disc layout; Movies & TV = the 10586 layout with 2017's −10 / +30 skips (T17-17). Movies & TV and Files use W10M's ≡ side pane (no pivots, no root page) (T17-16, T18-8).
6. Phase 18: Files drops "sort by type" and its free-space line (W10M had neither; Settings > Storage shows space); volumes show their label with no drive letter (T18-8).
7. Phase 20: Music's four satellites stay Songs / Albums / Artists / Playlists (Radio is a pivot, not a fifth satellite); a station can't be pinned to Start — favourites cover it (T20-3, T20-4).
8. The status bar: re-measured before anyone decides; Q-F only if Start and apps really differ (C-17).

---

## 3. Per-doc work list (for the phase writers)

Apply the items above; this is the checklist form. Nothing here reopens a dated ruling; T17-13's key block and C-16's base
config wait for Q-A2 / Q-D, T15-14's surface waits for Q-E, T12-13's picture line for the separate wallpaper answer.

**phase-11-tile-quick-actions.md** — T11-11 (A11 wording ×2) · T11-12 (activity-keyed line; Selection rule SUPERSEDED; E15
OPEN struck; ids in task 5; E1 / E3 / E9 / E15 lines) · T11-13 (fixture meta-data on the launcher activity) · T11-14 (icon
line) · T13-8 (E5 / E7 satellite sample points) · C-17 (status bar cites) · C-20 (preamble line; ring helpers in task 7) ·
C-21 (setActivity clause in the C-9 Decision) · C-25 · C-31.

**phase-12-setup-wizard.md** — T12-10 (original row; T12-9 line; variant key, tags, line; E13 re-cut with the APK picture
branch; H9) · T12-11 (HAL accent Red; E3; E11 Custom step) · T12-12 (Custom snapshot; edge case; E11 sub-row) · T12-13 (FINAL
gates line) · T12-14 (Start-visible gate in task 2; E8 sub-row) · T12-15 (Change Log line for R12 §7.1 in task 3; img5 reason)
· T12-16 (pixel sampling in E3 / E11 / E13) · T12-17 (action-failed line + fallback) · T12-18 (E2 list extension) · T12-19
(E11 numbers, ring sample) · C-15 (the marker mechanism, precedence, Goal, E1(a), E14 template, E11 control) · C-17 (E8) ·
C-18 (battery-saver edge case) · C-20 (E11 / E12 MARKs) · C-25 (E4) · C-26 · T15-14 / T16-15 (new why-table rows
`setup:overlay` if Q-E is A, `setup:people`).

**phase-13-fluent-materials.md** — T13-8 (E7 satellite sub-row) · T13-9 (E1 MARK; form line; edge case) · T13-10 (backdrop
failed line + edge row) · T14-11 (the Pod bay table row) · C-18 (build-start probe replaces l.131-132; Controls; E1; the lib.sh
helpers in task 7) · C-20 (E1 MARKs) · C-26 (task 7) · C-31.

**phase-14-pod-bay.md** — T14-11 (tag, line, E14 values) · T14-12 (E17) · T14-13 (launch-failed line; E4 sub-rows) · T14-14
(E6 literal) · C-20 (E7 / E8 `reply_since`; E16 union) · C-23 (depends-on `[01, 03, 12, 13]`) · C-30.

**phase-15-inbox-clock-calculator-recorder.md** — T15-14 (after Q-E: the surface, E4b, the surface line, re-cut E4 / E10 /
E21 / E23 / H1 / H5; under A the `setup:overlay` row, step and provision line) · T15-15 (R11: tabs, World Clock, no slide,
editor order, snooze 1 hour and ring-time choice, world-clock rows, "No alarms", Weight and Mass, no Programmer history,
recorder one page and 56-epx rows; header struck; H kinds; P6 struck) · T15-16 (Date calculation, recorder pause / markers /
search, compare mode, expanded views, timer / stopwatch tiles, Share, Pick from my music — tasks, rows, H rows) · T15-17 (three
rings; RecorderService.dump in task 7; E18 / E22) · T15-18 (E9 negative) · T15-19 (list line + E21 sub-row) · T15-20 (E8
network-on pass) · C-15 (E28 three parts) · C-17 · C-20 (E22 union; E26 MARKs) · C-23 · C-25 (E6 / E7) · C-27 (E19; helper
in task 8) · C-28 (baseline from phase 02) · C-30 (E26).

**phase-16-inbox-calendar-people.md** — T16-11 (guard allow set; JVM cases) · T16-12 (copy-path re-read) · T16-13 (People rows,
card, editor, own jump grid; Calendar Agenda / Day / Week + month drop-down, "Month" shortcut form, the ≡ pane, event rows,
#1A1A1A, UNMEASURED → accept; header struck) · T16-14 (Groups pivot; tags; line; row; H row; `groups` shortcut) · T16-15
(`setup:people`; Tess row unchanged; E13 / E26) · T16-16 (calendars denied line) · T16-17 (`in <ms> ms`; E4 / E7 MARKs) ·
T16-18 (TestDPC fixture) · T16-19 (two lines) · C-15 (E26 three parts) · C-17 · C-19 (E1 upgrade leg; `TILESHELL_APK` in task
8) · C-20 (E21 union) · C-23 · C-25 (E1 / E6).

**phase-17-inbox-photos-camera-video.md** — T17-13 (after Q-A2: the key route; now: bearer-only, one property, never logged,
E20's QA route and leak scan) · T17-14 (strike Plex / keyless / BS-6 / conditional E20–E22 / P14 halves now; one no-key string
and attribution placement after Q-A2) · T17-15 (Wikidata ids; `qa_wikidata_base`; E21 branches; H11) · T17-16 (Movies & TV ≡
pane, tile grid, own scrubber, Browse forms; Photos 3 columns, status bar hidden, viewer, editor panel, P4 marks, H13a / H13b;
Camera no strip, pro dial arcs, settings page, H20; E19 written; H12) · T17-17 (the three versions; H17–H19) · T17-18
(`SET_WALLPAPER`) · T17-19 (drop `FOREGROUND_SERVICE_CAMERA`) · T17-20 (`/3/configuration`, `/img/`) · T17-21 (exact token
grep; pinned digest) · T17-22 (E7 timer MARK) · T17-23 (failure lines; E5 Deny; E22 token line) · C-15 (E25 three parts) ·
C-16 (the network security config, fixed-host list, `[net]` lines, debug exception, release check, http-credential prompt,
the GATE) · C-17 · C-19 (E1 upgrade leg) · C-20 (E18 union) · C-21 (setActivity; E23 Music-burst check) · C-23 · C-29 (egress
guard in task 16) · C-32 (Trust Decision).

**phase-18-files.md** — T18-8 (≡ pane; volume labels; sort without type; no free-space line; rows, icons, location bar, verbs,
Properties page, dialogs, Back; H7 / H8; E11 written; `files_pane:*`) · T18-9 (strike the Storage ADD) · T18-10 (bin privacy
line) · T18-11 (FileProvider canonical-path check; E7 negative; review list) · T18-12 (zip failed line) · C-15 (E15 three
parts) · C-17 · C-20 (E12 union) · C-21 (setActivity; E2 Music-burst check) · C-23 · C-27 (E4b / E13) · C-28 (slots kept) ·
T15-16 ("Open file location" ADD to Voice Recorder).

**phase-19-settings-front.md** — T19-11 (Apps 5th and its pages' kinds; R11 §1 table; category / leaf page forms; sidebar
fallback slot; focused box; E1 values; E15 motion written; H1 / H5 / H9) · T19-12 (usage-access denied lines) · C-15 (E16
three parts) · C-17 (E2) · C-18 (E9 battery-saver sub-row; P3) · C-20 (E12 union) · C-23 · C-28 (the phase-19 baseline) ·
T17-18 (reuse `SET_WALLPAPER`) · T18-9 (the bin row stays here).

**phase-20-music-streaming-radio.md** — T20-1 (stale sweep; "search" in Scope and P1) · T20-2 (J5 closed; `MusicSearch`
extension; E16 J5 regression; J5 line form in E19) · T20-3 (no Radio shortcut; H9) · T20-4 (no station pin) · T20-5 (scheme
allow-list; caps) · T20-6 (preamble: seeding from phase 18's baseline, C-6, C-4, C-5; debug-pref fixture route) · T20-7
(depends-on; Y5; utterance ids; Pandora answered) · T20-8 (E13b; P9; E19 lines) · T20-9 ("play radio" defined; E16) · T20-10
(E21 → P10; mirror JVM test) · T20-11 (catalogue fixture; E13 values; etiquette) · T20-12 (E14 on phase 17's container; main-ring
lines) · T20-13 (query-stripped URLs; token absent) · T20-14 (H5 accept; H10) · T20-15 (E4 / E8 device clock) · T20-16 (silent
lines) · T20-17 (P11) · C-16 (reuses 17's config; station-path review gate) · C-20 (E19 union) · C-23 · C-25 · C-29 (egress
guard) · C-30 · C-32.

**INDEX.md / PLAN.md / build-prompt.md (the lead, when this triage is applied)** — C-14 (the Build order line; row notes 12 / 13;
build-prompt step 1 and the phase-04 bullet; PLAN.md:254) · C-22 (J5 / J6 / J7 Change Log lines) · C-17 (the "R3 C4 re-check"
row in the research table) · C-24 (PLAN A11 annotations and l.98; build-prompt l.11 / l.20 / l.21) · Stage A progress row 5:
round 2 triaged (review/2026-09-23-phases11-20-r2-triage.md) · T12-15 (a) (the R12 §7.1 Change Log line is written when phase 12
lands) · phase 04 DRAFT (outside 11–20): re-read A19 / A20 as Cobalt renditions before its interview (T12-15), and it reuses
phase 15's overlay row if Q-E is A (T15-14).

**Harness (built by the named phase's build task, specified by the docs above)** — `lib.sh` ring helpers + slice saving (C-20,
phase 11) · `provision.sh` marker step with `PROVISION_FINISH_WIZARD` (C-15, phase 12) · `wake_device` calls after reboots (C-25,
phase 12) · `battery_saver_on/off` and `record` (C-18 / C-26, phase 13) · `fill_volume` (C-27, phase 15) · `TILESHELL_APK` (C-19,
phase 16) · the egress guard (C-29, phase 17).

---

## 4. Top cross-phase fixes (in the order they unblock the build)

1. **C-14 build order** — without the Build order line the loop builds 12 before 13 (a preset writes a field only 13 adds) and
   stalls all of 11–18 / 20 behind the phone-gated phase 04.
2. **C-15 provisioning marker** — one mechanism, a precedence rule, and a re-cut E1(a) / E14 template (and its five instances),
   or six docs' wizard rows fail by design under the lead's standing decision.
3. **C-20 ring slicing** — every coverage row greps a ring that restarts wipe, and several negatives pass on stale lines; the
   `wall=` slice helpers already exist in j7.sh.
4. **C-16 cleartext guard** — the fixed endpoints pinned to https by the platform, a debug-only fixture exception, a release
   check and an adversarial-review gate, whichever way Q-D goes.
5. **C-18 / C-19 harness preconditions** — battery saver under forced AC power and upgrade legs that never upgrade.
6. **R11 applied** (T15-15, T16-13, T17-16, T18-8, T19-11) — the measured forms replace stand-ins that R11 contradicts (toast
   ring, icon tabs, 50-epx People rows, ≡ panes, Apps as W10M's own category).
7. **C-17 status bar** — one re-measure decides 28 vs 24; the docs stop hard-coding 28 now.

---

## 5. Counts

- Merged findings: **92** (from R1's 45 and R2's 64; overlaps merged as cited per row; #50 folded into five per-doc items).
- Severity as merged: **BLOCKING 11 · SHOULD-FIX 53 · NOTE 28.**
- Class: **DOC-UPDATE 68 · AGENT-CALL 20 · QUESTION FOR JEREMY 3 (Q-A2, Q-D, Q-E) + 1 held conditional (Q-F) · REJECT 1**
  (X-1; partial rejects inside T20-2 — #37's line form — and T20-7 — F36's music-list gate, already answered).
- BLOCKING by phase: 12 ×1 (T12-10) · 15 ×2 (T15-14, T15-17) · 17 ×1 (T17-13) · 20 ×1 (T20-8) · cross-phase ×6 (C-14, C-15,
  C-16, C-18, C-19, C-20).
- Of the 11 BLOCKING: 3 wait on Jeremy in part (T15-14's surface, T17-13's key route, C-16's base policy — each item's
  answer-independent half is writable now); the other 8 are writable now.
- Checked by the triage beyond the reviews: DeskClock's provider on the AVD (confirms #18); the radio-browser http share
  (corrects F38's "most" to about one in three); INDEX has no J5–J7 Change Log line (confirms F42); J5's real line form
  (corrects #37); build-prompt.md:20 still enforces the pre-amendment A11 (new, C-24).
