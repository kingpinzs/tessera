# Phases 11–20 — design / correctness review, ROUND 2 (Reviewer 1, 2026-09-23)

Lens: design and correctness (trust surfaces, ownership, build order, contradictions, rulings). Testability / evidence is
Reviewer 2's lane and is not repeated here except where a row is wrong because the design under it is wrong.

Inputs read in full: phase-11 … phase-20 (DRAFT), PLAN.md, INDEX.md, review/2026-09-23-phases11-19-triage.md, r11-inbox-apps.md
and all ten r11/*.md (summaries and "gaps for the phase doc" sections in full), r12-w10m-stock-theme.md (§0–§7), build-prompt.md,
qa/JEREMY-QA.md (J5–J7), the FINAL phase-01 / phase-10 passages cited below, phase-04 (DRAFT, cross-refs only). Code claims
were checked on disk today; every cite is file:line.

## Verdict

**BLOCKING REMAINS: YES — 5 BLOCKING** (F9 phase 15 ring surface, F18 phase 17 TMDB key in a public APK, F37 build order /
T12-3 unresolved where the build loop reads it, F38 cleartext HTTP, F39 provisioning marker vs the wizard rows).
Counts: **5 BLOCKING · 24 SHOULD-FIX · 16 NOTE** (45 findings, F1–F45).

Round-1 check (task item 1): every round-1 BLOCKING and SHOULD-FIX is applied in the phase docs **except** T12-3's INDEX /
PLAN half (F37) and T17-1, whose two questions were answered but whose conditional text was never struck (F19). R11 and
R12's new findings are almost entirely **not** applied yet (R11: F9, F10, F11, F15, F16, F21, F22, F25, F29; R12: F3, F6, F8).

---

## Phase 11 — tile quick actions

**F1 · NOTE · §Scope Out, §Decisions "Data source"** — A11 cited in its withdrawn form.
Problem: C-7 re-tagged 14–18 only; 11 still says "any network use (A11 holds: shortcuts are local)" and "A11 holds: nothing
here touches the network". Evidence: phase-11-tile-quick-actions.md:29, :53. Fix: "no network (shortcuts are local; offline
preferred, A11 as amended 2026-09-23)".

**F2 · NOTE · §Decisions "Harness contracts"** — the shortcuts log line predates the per-activity query.
Problem: the lead's 2026-09-23 line made the query per ACTIVITY (`:189-195`), but the line is still
`[quick] shortcuts for <pkg>/<userId>` (`:149`), and four later docs assert "names only that activity's ids" through it
(15 E27, 16 E25, 17 E23, 18 E16), all of them `app.tileshell/0`. Fix: `[quick] shortcuts for <pkg>/<activity>/<userId>: n (k
shown: …)`; the later rows key on the activity.

## Phase 12 — setup wizard

**F3 · SHOULD-FIX · §Decisions T12-9 line, preset table, E3, E13, H9** — Q7–Q9 are answered but the doc is still written
conditionally, and E13 contradicts Q8.
Evidence: Q7 A / Q8 C / Q9 A are ruled (phase-12-setup-wizard.md:49-57), yet T12-9 says "Q7–Q9, UNANSWERED at this writing"
(`:255`), the original row still offers A/B/C per cell (`:286`), E3 keeps "the Q7-C form … if Q7 is answered C" (`:489`), E13
branches on Q7/Q8/Q9 and says "Until Q7–Q9 are answered this row is not runnable" (`:559-562`), H9 "if Q7 is A" (`:599`).
Worse, E13's Q8 branch reads "C: two presets, one per picture" while the Q8 Decision says "one entry with the two pictures as
its variants … so the list stays six" (`:53-54`). The variant mechanism itself is undesigned: no `theme_preset` id per variant
(the id list has one `w10m`, `:292`), no tag, no UI for choosing the variant, no default variant.
Fix: collapse the row to the rulings (Cobalt `accent:Cobalt` as the 49th swatch; both img0 files; lens on Cobalt's hue); design
the variant (e.g. ids `w10m` = Hero 1607–1709 default, `w10m_1511` = light streaks; the preset entry shows the Hero, a second
line / chevron picks the other; tags `preset:w10m:hero` / `preset:w10m:streaks`); E11/E13 assert both ids; strike T12-9's
"unanswered"; H9 drops "if Q7 is A".

**F4 · SHOULD-FIX · preset table, HAL row** — HAL's accent is off-grid and its rule is undefined under the ruling.
Problem: HAL's accent `Brand.LENS_IRIS` #D81810 (brand/Brand.kt:46) is outside the 48; the table says it "follows Q7's answer
the same way (C: Red …)" (`:287`). Q7 was answered A for **Cobalt only** ("Cobalt … becomes a 49th accent", `:55-57`). Under A
nothing says whether HAL red becomes a 50th swatch, stays unpickable (Q7's rejected B form — "choosing another accent loses it
for good"), or snaps to Red #E81123. User-visible. Fix: apply Q7 A's reasoning explicitly (a named `HAL red` swatch after
Cobalt) or ask Jeremy in the next batch; E3's expected `accent` follows.

**F5 · SHOULD-FIX · preset model "A preset replaces a picture the user chose"** — a preview tap destroys user data.
Problem: live-apply makes every preset tap a preview (Jeremy: "auto changes the phone so they can see", `:74-75`), and the agent
call says "the user's picture is NOT kept anywhere (re-chosen from Start + theme)" (`:295-296`, edge `:633`). A user browsing
presets loses their own background permanently (a picked URI's grant cannot be re-derived). Fix (agent lean, H6-visible): the
`Custom` entry remembers the last custom item set (incl. the picture URI and its persisted grant) and re-tapping `preset:Custom`
restores it; or put the trade to Jeremy.

**F6 · SHOULD-FIX · §Decisions T12-7 / Q8, header gates** — the ruled Microsoft pictures cannot reach Jeremy's phone as
planned, and nobody has ruled on shipping them.
Evidence: R12's img0 files are gitignored (`.gitignore`: `docs/plan/r12/*.jpg`) "until Jeremy rules on committing them"
(INDEX.md:39); Jeremy's phone APKs are CI builds published as a GitHub Release asset downloadable from a phone browser
(.github/workflows/apk.yml:3-8). So the CI APK carries no original picture (T12-7's fallback fires on the phone), and
committing them puts Microsoft wallpapers into a publicly downloadable release — A10's "fine on Jeremy's own phone" did not
cover that. Fix: list "commit / ship R12's wallpapers (and in which build)" as an open Jeremy question gating FINAL beside the
four AI pictures; state which build carries them.

**F7 · NOTE · T12-8, Build task 2** — the pin-band wait is asserted but never built.
Problem: T12-8 and the edge case say a pending secondary-pin band waits until the wizard finishes (`:242-245`, `:621-623`) and
phase 14 reuses "the same gate phase 12 puts on the SecondaryTiles pin band" (phase-14-pod-bay.md:119-122), but no phase-12
build task builds that gate, and StartActivity.kt:187-190 draws the band ungated today. Fix: Build task 2 names the "Start
visible" gate (band and pod-bay request both wait on `[wizard] finished|skip`).

**F8 · NOTE · §Scope Out, Build tasks** — two R12 items not carried.
(a) R12 §7.1: R3's accent-coloured readings (A19 action-center tile, A20 volume fill, A22 Cortana ring, the keyboard dot) are
cobalt renditions, not "#0078D7 plus capture offset" (r12-w10m-stock-theme.md:201-207) — phase 12 owes the INDEX Change Log
line R12 asks for and phases 03 / 04 / 05's colour tolerances should be re-read. (b) Scope Out drops R12's lock-screen img5
because "the shell draws no lock screen" (`:46`), but the shell can set Android's lock wallpaper (phase 17 "set as", phase 19
E5, `WallpaperManager` FLAG_LOCK). Keep it out if wanted, but give the real reason (a preset preview should not rewrite the
system lock wallpaper).

## Phase 13 — Fluent materials

No new findings. T13-1…T13-5 are applied; T11-8's drops are applied consistently (`:79-84`, `:120`, `:156`, `:226-227`).

## Phase 14 — pod bay

No finding of its own: depends-on omits 12 for T14-7's wizard gate (phase-14-pod-bay.md:5 vs :119-122), folded into F43.

## Phase 15 — Alarms & Clock, Calculator, Voice Recorder

**F9 · BLOCKING · §Decisions "Ringing", "Bars"; Build task 3; E4, E10, E21, E23; H5** — the ring surface contradicts R11, and
the unlocked case is undesigned.
Problem 1 (ruling needed): R11 found in three sources on two builds that W10M alarms and timers ring as an interactive
**toast pinned to the top (≈216–248 epx) over the lock screen or the dimmed app — no full-screen ring page exists**
(r11/clock.md:27-30, :220-231; G1 at :340 "A decision for Jeremy"). The doc builds a full-screen ring page as if it were W10M's
(phase-15:147-156, :214-218, task 3 :378-384). Building the page and switching later is a second form (Rule 16).
Problem 2 (design gap, either answer): a full-screen intent only launches its activity when the screen is off or locked; on a
phone in use Android shows a **heads-up notification instead** (One UI's own look — a P2 seam). The doc has no unlocked-in-use
behaviour and no row (E4 / E5 fire over the keyguard only).
Fix: put G1 to Jeremy — A. the W10M toast (a translucent `showWhenLocked` activity drawing R11 §8's toast with the lock screen
visible beneath, FSI still the launch path; [fidelity] vs §8.1–8.12) or B. keep the page as a P4 design ([accept]). Then state
the in-use surface: One UI's heads-up with Snooze / Dismiss actions (recorded seam), or a W10M toast in an overlay window
(needs `SYSTEM_ALERT_WINDOW`, which only phase 04 grants — so not before 04). Add a row: alarm fires with DeskClock in front,
unlocked → which surface appears, Snooze / Dismiss work. Timers follow the 14393 timer toast (§8.9–8.11).

**F10 · SHOULD-FIX · header, §Decisions "Fidelity", Build tasks 4/6/7, E10, E12, E13, E24** — R11 landed; the doc still says it
is pending and asserts values R11 contradicts.
Evidence: header "pending on disk … r11/src/clock/ and r11/src/voice-recorder/ are empty" (`:4`); "none of the three files
exists" (`:121-122`). R11 against the rows: Clock's four pivots are **icon-over-caption tabs** (64.5-epx pitch, accent
underline, grey band 24→92.4 epx) and a tab tap **does not slide** (r11/clock.md:14-16, :31-32, G3/G4) — E10 asserts "four pivot
headers" on phase 10's text-pivot geometry and a 250-ms X13 settle (`:515-520`), task 4 says "four pivots on the shell's pivot
geometry" (`:385`); the label is "World **C**lock"; the editor order is spinner / Alarm name / Repeats / Sound / Snooze time at
64 epx (G6); snooze choices include **1 hour** (G2); world-clock rows are 96.9 epx with "Today, N hours ahead" (G7); empty state
"No alarms" (G8). Calculator: the converter label is **"Weight and Mass"** — E12 asserts the exact list with "Weight" and will
fail (`:529-532`; r11/calculator.md:311-313); **no history in Programmer** (E12 expects history generally); no accent `=` /
operator fill; x², ¹⁄x, xʸ … are not MDL2 glyphs; Standard is immediate-execution (E11 cases need their mode, Tess keeps
Scientific — already stated at `:282`). Voice Recorder: one page, no pivot, no pane; rows **56 epx** two-line — E24 asserts
"rows at 44 ± 1 epx (R3 C2)" (`:607-609`; r11/voice-recorder.md:147, :263-266).
Fix: apply R11's gap lists (clock G1–G13, calculator items 1–11, recorder bullets) as measured values / tagged approximations;
strike "pending"; E10 asserts tabs and the tap jump; E12 "Weight and Mass" and no Programmer history; E24 56 epx.

**F11 · SHOULD-FIX (question for Jeremy) · §Scope** — W10M features R11 found that the phase neither includes nor excludes.
Calculator's **Date calculation** mode (a fourth mode on the phone, r11/calculator.md:22, :313-314); Voice Recorder's **pause**
button, **markers**, list **search / filter**, "Open file location" (r11/voice-recorder.md:270-274); Clock's **compare mode**,
**expanded** timer / stopwatch pages, pinning a timer / stopwatch as its own tile, stopwatch **Share**, alarm sound "Pick from my
music" (clock G6, G7, G9–G11). Each changes WHAT is built. Fix: one batched question; agent lean "in" for Date calculation,
recorder pause and markers (offline, deterministic, W10M had them — A4), "out" for secondary tiles.

**F12 · NOTE · §Decisions "Fidelity"** — wrong symbol: "`SystemBars.STATUS_EPX`" (`:124`; also phase-16:157). The constant is
`BarMetrics.STATUS_EPX` in bars/SystemBars.kt:77-79. Cite it by that name (and see F40 on its value).

## Phase 16 — Calendar and People

**F13 · SHOULD-FIX (trust) · §Decisions T16-1 "The write layer's guard"; Birthdays** — the guard refuses the Birthdays writer.
Problem: "every insert, update and delete refuses a calendar id other than `LocalCalendar.id`'s, except a Sync push to an
allowed id" (phase-16:291-293). The Birthdays calendar is a second shell-written calendar under its own LOCAL account
`Tessera Birthdays` (`:202-208`), whose yearly events the shell inserts, updates and deletes from a Contacts observer — every one
of those writes is refused by the guard as written, and the JVM test would enshrine it. Fix: the guard's allow set = {the
`Tessera` calendar (all ops), the `Tessera Birthdays` calendar (writer path only, sync-adapter URI, never offered to the editor
or Sync), an allowed Sync target (push / update / delete of a mapped copy only)}; the JVM test covers each and the refusal of
everything else.

**F14 · NOTE · T16-1 Sync** — harden the copy path. Before `updated` / delete-"both", re-read the copy event's `calendar_id`
and require it to equal the mapping's target and that target to be still allowed; do not offer "Delete here and from
<calendar>" when the target has been un-allowed. (One read, closes the case of a stale mapping writing into the wrong calendar.)

**F15 · SHOULD-FIX · header, §Decisions "Fidelity", E10, E19, E20, Build tasks 4/6, App Shortcuts** — R11 calendar / people
not applied.
Evidence: header "r11/src/calendar/ and r11/src/people/ are still empty" (`:4`), "not yet written: R11 is running" (`:155`).
People: rows are **50 epx with a 32-epx avatar at x 12, name at x 57.75**, not phase 06's 70.5 / 48 stand-in E10 asserts
(`:520-521`; r11/people.md:13-14, :207-210); the card is a full accent page with a 124-epx photo and titled action rows; editor
fields 32 epx with a 2-epx grey border (not R7 §3.7.1's 43.4); the jump grid needs a choice (People 72-epx cells vs X8).
Calendar: W10M had **Agenda / Day / Week plus a month drop-down**, no standalone Month page (r11/calendar.md:9-11, :179-181) — the
"Month" App Shortcut (`:89-90`, task 9) and E25's `cal_view_mode:month` need a stated form (drop-down state, or a P4 page with an
H row); the ≡ calendar list is W10M's show/hide surface (`cal_calendar_row:` should be that pane); event rows have an 8-epx bar at
x 0 and the title at x 92.5; the page is #1A1A1A, not black. Fix: apply both gap lists; strike "pending".

**F16 · SHOULD-FIX (question for Jeremy) · §Scope** — People's **GROUPS** pivot (create / rename / delete, message a group;
r11/people.md:219-220) is neither in nor out. Fix: ask (lean: out — no group messaging surface exists before phase 06).

**F17 · NOTE · §Scope "Manifest ADD: WRITE_CONTACTS"** — People's write grant rides Tess's health row.
Problem: WRITE_CONTACTS is People's need, but the doc widens **Tess's** `contacts` row (`:62-70`), so Tess's Settings page reads
PARTIAL for a permission Tess never uses, and her wizard step asks for People's write. 15, 17, 18, 19 all put their app's grant
on the Setup checklist. Fix (agent): a Setup row `setup:people` (WRITE_CONTACTS) and Tess's row unchanged; the C-4 lines follow.

## Phase 17 — Photos, Camera, Movies & TV

**F18 · BLOCKING (trust) · §Decisions Q-A; Build task 12; E20; P15** — the TMDB key is compiled into an APK that CI publishes.
Problem: the key "is NEVER in the repo … reaches the app as a BuildConfig field … the CI build that makes Jeremy's phone APKs
reads it from a GitHub Actions secret" (phase-17:78-82). apk.yml builds every push to `main` and attaches the APK to the
rolling "latest" **GitHub Release so it can be downloaded straight from a phone browser** (apk.yml:3-8), plus a 30-day artifact
(:126-131). A BuildConfig string is recoverable from the APK with `apktool` / `strings`; whoever can reach that release page has
Jeremy's personal key (if kingpinzs/tessera is public, everyone). "Never in the repo" holds; "ships only in your personal build"
(Q-A's premise, triage §2) does not. Checked: the key is in the gitignored local.properties today (3 `tmdb` lines, not printed)
and appears in no tracked file.
Fix: back to Jeremy with the corrected premise — A'. Q-A's C form: the key typed once into Movies & TV's settings, stored in the
Keystore-encrypted store this phase already builds for the Jellyfin token (Decisions "Trust"), no key in any artifact (lean); B'.
keep BuildConfig but CI publishes a keyless APK and the keyed one is built locally only; C'. accept the exposure (a free,
rotatable personal key) in writing. Whichever: E20's `api_key` / bearer assertions and the "no key" state follow (F19).

**F19 · SHOULD-FIX · header, T17-1 block ("Pending Q-A", "Pending Q-B"), BS-6, E20, E22, H10, P14** — Q-A / Q-B answered but
the conditional design was never struck, and the two halves now disagree.
Evidence: header "two questions to Jeremy still open" (`:4`); "written conditionally below, marked 'pending Q-A' / 'pending
Q-B'" (`:250-252`); "Written for all three forms; one is built" (`:292-301`) and Plex / both forms (`:302-308`); BS-6 (`:440-442`);
E20 "under Q-A B / C" (`:732-739`), E22's Plex half (`:764-766`), H10 "under Q-A A / C" (`:843-844`), P14 "under Q-B B / C"
(`:831`). Contradictions: the Decision uses `tmdb.readToken` "as a bearer header" (`:79-83`) while the pending block and E20 use
`tmdb.apiKey` → `BuildConfig.TMDB_KEY` and assert an `api_key` **query parameter** (`:293`, `:734-735`); the no-key strings differ
("catalogue: no TMDB key in this build" `:82-83` vs "No catalogue key in this build" / `[video] catalogue: no key` `:294`,
`:736`); attribution "in Movies & TV's About" (`:84`) vs "on the Browse pivot" (`:295`) vs `hub_attribution` on the title page
(E20). Fix: one form (per F18's outcome), one diagnostics string, one attribution placement; strike B/C, Plex, BS-6.

**F20 · SHOULD-FIX (expectation to Jeremy) · T17-1 `openTitle`, E21, P13, H11** — "Watch on <service> opening that app at the
title" (Q3b A, `:96-98`) is not reachable for most services with TMDB data.
Problem: `openTitle` deep-links "where the catalogue yields that service's own id for the title, else the service's search URL"
(`:258-259`). TMDB's `/watch/providers` returns, per country, provider ids / names / logos and one `link` to TMDB's own watch page
— no Netflix / Prime / Disney+ title ids — so the deep-link branch is effectively dead and almost every "Watch on" lands on the
service's in-app search. Fix: say so in Decisions and H11, tell Jeremy the ruled behaviour becomes "opens <service> searching for
the title" (phase 20 already states this for music, phase-20:67-69); keep the deep-link branch only for forms keyed on an id TMDB
does return (TMDB / IMDb id), recorded by BS-4.

**F21 · SHOULD-FIX · §Decisions "R11 gates FINAL", "Pivots"; Approximations Y1–Y12; Build tasks 5–7, 14** — R11 photos /
camera / movies-tv not applied.
Evidence: "none written as of 2026-09-23" (`:161-164`), every Y row "pending" (`:372-383`). R11: Movies & TV never used pivots on
the phone — it used the **≡ pane** in 2015 and 2017 (r11/movies-tv.md gaps "Navigation form"); the library is a **112-epx tile
grid, 2 across** at 360 epx (not app-list rows); the player's scrubber is its own (not Groove's) and the 2017 skips are −10 / +30;
Browse posters 2 across with 12-epx gutters (Y8's 3-across / 2-epx conflicts). Photos: 3 columns on the S25U canvas (Y1's
4-across conflicts); status bar **hidden on every Photos page** (not only the viewer); the one editor capture is a panel of large
tiles with "Save a copy", not a 48-epx strip (Y11); **the inbox editor did crop / rotate / auto-enhance only** — straighten,
light / colour, filters and red-eye are P4 additions and should be marked so, each with an [accept] look row (Q1 C's Decision
reads as W10M fidelity, `:111-113`). Camera: **no horizontal mode strip in any version** (Y3's stand-in), V-2017 is a 72-epx
shutter disc with 32-epx mode discs at ±60 epx and a top capsule; the pro dial is five arcs centred on the nav-bar top at W/2
(Y4); capture-intent accept / retake UI needs a P4 row. Fix: apply the three gap lists; ≡ pane for Movies & TV (agent: fidelity,
H3 [fidelity]); P4 marks on the four editor tools.

**F22 · SHOULD-FIX (question for Jeremy) · Approximations** — three version rulings R11 hands to the owner, like R8's H-M1:
Photos V-2015 (fully measured) vs V-2016+ (governing, collection only; R11 lean: V-2016+ collection + V-2015 elsewhere);
Camera V-2015 (measured) vs V-2017 (governing, portrait rotation unmeasured); Movies & TV 10586 (measured) vs 2017 (LOW; R11
lean: 10586 geometry + the 2017 −10 / +30 skips). Each is user-visible. Fix: one batched question with R11's leans; H rows.

**F23 · NOTE · Build task 1** — `SET_WALLPAPER` is missing. Photos' "set as lock-screen wallpaper" uses `WallpaperManager`
FLAG_LOCK (`:463-466`), which needs the install-time `SET_WALLPAPER`; the manifest has none and task 1's permission list omits
it. Phase 19 also lists it (phase-19:35). Fix: 17 (first user) ADDs it; 19 reuses.

**F24 · NOTE · Build task 1** — `FOREGROUND_SERVICE_CAMERA` is declared with no foreground service described, and the same line
says recording stops on screen-off (`:448-449`). Either describe the camera service (type `camera`) or drop the permission.

## Phase 18 — Files

**F25 · SHOULD-FIX · header, §Scope, §Decisions T18-1 / T18-2, Approximations Y1–Y4, E2, E3, E16** — R11 files not applied.
Evidence: header "pending on disk" (`:4`); Scope "r11/files.md settles how W10M drew its root page" (`:24`). R11: **W10M File
Explorer had no root page** — its entries (Recent, This Device, one row per volume named "<label> (D:)") live in a 256-epx ≡
pane; **no zip, no recycle bin, no free-space line, no sort by type, no cut / paste** (verbs are "Move to" / "Copy to"); rows are
64-epx two-line with **colour shell icons / thumbnails**, not MDL2 glyphs; Properties is a page; the status bar is shown
(r11/files.md summary; gaps). The doc builds a root page (`:23`, `:144-145` "its OWN entry on the root page", E2 `files_root:*`,
Y1), "W10M glyphs by type" (`:25`), and E3 asserts **sort by type** (`:318-320`). Fix: the ≡ pane with Recycle Bin as a fourth
row (P4); mark zip and the bin P4 in full (the bin has H2; add an [accept] row for the zip flows); sort-by-type becomes a stated
P4 addition or leaves E3; type icons are A10 assets; name volumes by label (with or without "(D:)" — agent call, H4).

**F26 · SHOULD-FIX (ownership, Rule 16) · Build task 8 vs phase 19** — the Storage page's bin row is built twice.
Evidence: phase 18 task 8 builds "the one-line ADD to phase 19's Storage page (the bin's size with Empty)" (phase-18:155-156,
:261); phase 19's table and task 3 build the same row (phase-19:204, :305-306). Phase 19 builds after 18 (19 waits on 04), so 18
would ADD to a page that does not exist. Fix: strike the ADD from 18 (it publishes the bin index and an Empty call); 19 builds the
row.

**F27 · NOTE · T18-1** — bin privacy. Binned files stay on shared storage under `<volume>/.Tessera/bin/`; `.nomedia` hides them
from MediaStore only, so every app with storage access (and a PC over USB) can still read "deleted" files until Empty. H2's
wording should say so ("Deleted files stay on this phone until you empty the Recycle Bin").

**F28 · NOTE · Build task 3 (FileProvider)** — scope the provider. With all-files access, a `root-path` entry (needed to reach
`/storage/<UUID>`) also covers the app's private dirs; require a canonical-path check that the file lies under a
`StorageVolume.getDirectory()` before `getUriForFile`, and add E7's negative (a private file is never shareable).

## Phase 19 — Settings front

**F29 · SHOULD-FIX · §Goal, Scope Out (struck line), Q2 Decision note, per-page table, Y5/Y6, E1, H9** — R11 settings-front not
applied; Apps is W10M's own category.
Evidence: R11 1.5 / 1.12 (HIGH): 15063 ships an **Apps** category (Apps & features · Offline maps · Apps for websites), home order
System · Devices · Network & wireless · Personalization · **Apps** · Accounts · … · Extras (r11/settings-front.md §1). The doc
calls Apps "desktop Windows 10's 2017 addition" (`:81-83`), keeps "no W10M original" in Y6 and H9 (`:258`, `:460-461`), and E1
asserts Apps **last** (`:347-349`). Also as shipped: System has Battery (not "Battery saver") and Driving mode; Devices "Bluetooth
& other devices" and Mouse; Personalization has Navigation bar; Network order Data usage · Cellular & SIM · Wi-Fi · Airplane ·
Hotspot · VPN; Update & security "Windows Update" on 15254 and Device encryption; category pages are **48-epx one-line rows**, not
the home's 64-epx rows; leaf pages are a 72.6-epx band over black; the 15063 sidebar is a W10M-native home for the fallback line;
page transition measured (replace the `PageTransition` stand-in, Y4); box 32.7 epx and text x 56–57 (E1 asserts 31 ± 2 and 55).
Fix: apply R11 §10; Apps 5th with Apps & features / Apps for websites [fidelity], Default apps and App permissions P4 additions,
Offline maps out with its reason (no Maps, R10-Q4); header "pending" struck.

## Phase 20 — Music streaming and Radio (new since round 1)

**F30 · SHOULD-FIX · header, Goal, Scope, Decisions, Build tasks 3/5/6/8/9, E1, E12–E14, E18, H3, H9, P1** — the interview is done
but the doc is still written for every answer.
Evidence: header "interview pending" (`:4`) vs INDEX "interview DONE" (INDEX.md:66) and the five ruled Decisions (`:42-69`); Goal
"by whichever forms the interview rules" (`:13`); "only if Q2 rules internet radio" (`:136`), "only if Q1 rules a catalogue"
(`:145`), "Q4 decides whether a Wi-Fi-only setting exists" (`:169-170`), "if Q3 keeps Radio inside Music" (`:175`); dead forms Q3
B / C in task 6 (`:283-287`) and E1 (`:330-335`), Q4 B / C in task 9 and E12 (`:298-299`, `:382-384`), "only with Q3 B" (E18),
H3 / H9 branches. Scope says "Listen on <service>" opens the app **at the title** (`:26-27`) and P1 expects the same (`:427-428`),
contradicting the Q1 Decision that it opens each service's **search** (`:67-69`). Fix: the round-1 stale sweep (C-11) on this doc:
keep A / A / A / A / A forms only; Scope and P1 say "search".

**F31 · SHOULD-FIX (double build) · §Decisions "phase 03 Commands ruling … Observed in code", Build task 7, E16** — J5 already
built what task 7 builds.
Evidence: the Decision says `MusicService` "does not override `onSetMediaItems` … 'play <song>' resolves nothing today" and task 7
adds `onSetMediaItems` "closing the observed phase 03 / 10 gap" (`:100-108`, `:288-292`). J5 fixed exactly that on 2026-09-23
(qa/JEREMY-QA.md:15, commit 373106a): `MusicSearch` resolves song / artist / album and MusicService.kt:151-187 overrides
`onSetMediaItems` (and the add path) with it. Fix: task 7 ADDs station / genre / favourite resolution to `MusicSearch` and the
existing override; the Decision records J5; E16's local-track clause is J5's regression.

**F32 · SHOULD-FIX (question for Jeremy) · §Decisions "phase 11 Q1 standing rule", task 6, E1, H9** — a fifth Music shortcut
never appears.
Problem: "A Radio pivot (Q3) adds 'Radio' to that list" (`:109-110`) — Music already declares Jeremy's four (Songs / Albums /
Artists / Playlists, phase-11:35-37), phase 11 shows the first four (`:105-107`), so Radio (rank 4) is never a satellite and
E1's "phase 11's shortcut-listing method shows 'Radio' beside …" (`:335-336`) cannot pass through `quick_sat_label`. Fix: ask
Jeremy which four Music shows once Radio exists (lean: Radio replaces Playlists), or state that Radio is listed by `dumpsys
shortcut` only and never bursts.

**F33 · SHOULD-FIX · §Decisions "the Radio pivot"** — pin-a-station-to-Start is undesigned and unruled.
Problem: "A hold on a station offers … pin-to-Start (the phase 11 burst carries it once phase 11 is in)" (`:180-181`). A pinned
station is a new tile kind (which `TileKey`, what face, what a tap does, whether it plays in place), no question asked for it,
no row, no H row; and bursts are a Start-tile gesture, not a Music-list one. Fix: strike it (the Music tile and favourites cover
the need) or design it fully with its rows (Rule 16: no half form).

**F34 · SHOULD-FIX (trust) · §Decisions "station directory", Build task 3, edge cases (.pls / .m3u)** — untrusted URLs reach
ExoPlayer unfiltered.
Problem: station URLs come from a community directory and playlist entries are "resolved to its first entry" (`:454-455`); the
service plays them through the `DefaultMediaSourceFactory` it already builds (`:114`), whose `DefaultDataSource` also opens
`file://`, `content://`, `asset://`, `rawresource://` and `data:` URIs. Phase 17 allow-lists schemes for VIEW (phase-17:276-280);
phase 20 does not. Fix: station and playlist-entry URLs accepted only as `http` / `https` (`[music] stream: unsupported scheme`
otherwise, JVM test); cap the directory response size and decode station logos at bounds (arbitrary hosts).

**F35 · NOTE · Acceptance preamble** — round-1 harness conventions were never applied to this doc (C-3 seeding before E17's
tile assertions, C-6 force-stop after E13 / E16 launches, C-13 "RECORDED" on E21 and the recorded P rows). Reviewer 2's lane;
named here only because 20 post-dates the triage.

**F36 · NOTE · depends-on, Y5, H5, E14, header gates** — `depends-on: [03, 10, 17]` should add 11 (the Radio shortcut is an ADD
to phase 11's part); H5 is *fidelity* "against R11 §Movies & TV's forms" but R11 says Browse / catalogue pages are P4 → *accept*;
Y5 "R11 pending" and E14 "once phase 17 writes it" are stale (phase 17 E22's fixture exists); Jeremy's list of music apps
(INDEX.md:66) is an input the doc should name as a FINAL gate.

---

## Cross-phase

**F37 · BLOCKING · INDEX.md phase table and notes, PLAN.md:254, build-prompt.md step 1** — T12-3's order swap (and 13-before-04)
is not where the build loop reads order.
Problem: the build prompt builds "the first phase not marked done" in INDEX's table (build-prompt.md, Procedure 1) and stops at
phase 04 until R4 runs. The table is numeric (04, …, 11, 12, 13, …), row 12's notes say nothing about 13 and row 13 says only
"lands before phase 04" (INDEX.md:58-59); PLAN's 2026-09-22 order line still reads "quick actions, wizard, in-app Fluent engine
(before phase 04) …" (PLAN.md:254). So (a) 12 would be built before 13 — the round-1 BLOCKING failure (a preset writes
`StartTheme.transparencyEffects`, which only 13 adds; T12-3 said INDEX and PLAN "get the swap … when this triage is applied" and
INDEX line 19 records the triage as applied), (b) 13 can never land before 04 (it sits after 04 in the table, yet PLAN and
phase 04 need it first), and (c) 11–18 and 20, none of which needs 04, all stall behind the phone-gated R4.
Fix: an explicit build-order line in INDEX that the build prompt follows — at minimum `11 → 13 → 12 → 14 → 15 → 16 → 17 → 18`,
`20` after 17, `19` after 04, none of 11–18 / 20 behind 04 (the lead places 06–10) — PLAN's order line swapped to match, and
build-prompt step 1 reading "the first phase in INDEX's build order not marked done".

**F38 · BLOCKING (trust + correctness) · phase 17 T17-1 / E13 / E20 / E22 / P14; phase 20 station playback, E3–E21, P3** —
cleartext HTTP is blocked, and neither doc designs for it.
Problem: `targetSdk = 36` (app/build.gradle.kts:22) and the manifest has neither `usesCleartextTraffic` nor a
`networkSecurityConfig` (grep of AndroidManifest.xml and res/xml: none), so Android refuses every `http://` connection —
ExoPlayer throws `CleartextNotPermittedException`, `HttpURLConnection` an `UnknownServiceException`. Most radio-browser.info
stations are plain `http://`; phase 17 plays `http(s)://` VIEWs and a Jellyfin server that on a home LAN is `http://host:8096`
(and sends the password to `/Users/AuthenticateByName` in the clear); every fixture in both docs is `http://10.0.2.2:…`
(phase-17:562-571, :686, :756; phase-20:318-323). As written, the ruled Radio feature cannot play most stations and most rows fail.
Fix: a stated network-security design, each branch adversarially reviewed (the project rule for trust changes): e.g. a
`network_security_config.xml` whose base config keeps cleartext OFF for the shell's own fixed endpoints (Open-Meteo, Nominatim,
TMDB, radio-browser, MusicBrainz — all HTTPS) and permits cleartext only on the media paths that need it (a station stream, a
user-added media server, a VIEW from another app) — which in practice means cleartext on at the base config with the fixed
endpoints pinned to HTTPS in code, stated as such; the Jellyfin sign-in warns before sending a password over `http://` to a
non-private address. Add a row per doc proving an `http://` stream plays and a fixed endpoint refuses `http://`.

**F39 · BLOCKING · phase 12 Goal, lead's force-stop Decision, E1(a), E14; its instances 15 E28, 16 E26, 17 E25, 18 E15, 19 E16**
— the provisioning marker contradicts the wizard's own rows and the no-bypass claim.
Problem: the lead's line says provisioning **finishes** the wizard ("provision.sh taps through, or writes the same done marker")
so a force-stop — which makes Android deselect the keyboard (phase 05 N-01, INDEX.md:93) — sends "Keyboard selected" to the
checklist instead of summoning the wizard (phase-12:326-331). Consequences not propagated: (1) the Goal still says "no existing
QA driver needs a bypass: the harness provisions the grants, and the wizard has nothing to do" (`:18-20`) — writing the marker is
the bypass, and "taps through" is impossible because with every grant held the wizard never shows; (2) E1(a) force-stops a
provisioned AVD and asserts `[wizard] not shown: core held` (`:451-453`) — false either way (without the marker the keyboard row is
MISSING and the wizard shows; with it the line is `not shown: finished`); (3) E14 — "`pm clear` → `provision.sh` → revoke → Home: the
dump has `wizard_step:…`" (`:563-567`) — cannot happen once provisioning sets `finished`, and the five instances inherit it
(e.g. phase-15:639-647 also expects "Skip setup" and `not shown: core held` after provisioning).
Fix — pick one and re-cut all six docs: **A (lean)** provisioning writes no marker; the shared restart helper (C-6's force-stop +
Home, `layout_restore`'s restart) re-selects the keyboard with `ime set app.tileshell/.ime.KeyboardService` before Home, so core
stays held, the Goal stays true and E1(a) / E14 hold as written. **B** keep the marker: strike the Goal's sentence, E1(a) expects
`not shown: finished`, every E14 instance starts by deleting `shared_prefs/setup_wizard.xml` and its second half expects
`finished`.

**F40 · SHOULD-FIX · status-bar height (task item 5)** — 28 epx (R3 C4) vs 24 epx (R11, R8).
Facts: R3 C4 read Start's status bar as 28 epx (S1 14393, 112 phys) / 29 epx (S2 15063) (w10m-measurements.md:335); phase 01
built `BarMetrics.STATUS_EPX = 28` (bars/SystemBars.kt:79; phase-01:88, E19 :204). Every in-app measurement since reads 24:
Groove (r8:117), Clock (r11/clock.md:117), Calculator on two devices (r11/calculator.md:99), People (P0.2), Files, Movies & TV
library, and Settings on **the same S1 / S2 footage R3 used**, now at 1080p (r11/settings-front.md:26-29; §10 suspects R3 read
Start's bar where it overlays the wallpaper). Phase 10 already chose 28 over Groove's 24 so "one app is not the odd one out"
(phase-10:259-262) — with every app at 24, that rationale inverts.
Evidence that settles it: on R3's own S1 / S2 Start frames at 1080p, (i) the status band's bottom edge by column profile over a
flat tile column and (ii) the vertical centre of the status glyphs (12.4 epx in a 24-epx bar per R8; ≈14 in a 28-epx bar), with
the same two readings on an in-app frame of the same video. Start = 24 → R3 C4 misread (the extra 4 epx belong to Start's grid top
margin): set 24 everywhere, keeping the grid where it is measured. Start = 28 while apps = 24 → a real per-surface difference and a
Jeremy ruling (uniform 28, as phase 10 chose, or 28 on Start / 24 in apps).
Owner: **phase 01** (FINAL; the constant, its Decision and E19), through the INDEX Change Log, with the re-measure as an R3 C4
re-check in INDEX's research table. Phases 11–20 cite "phase 01's drawn status bar (`BarMetrics.STATUS_EPX`)" instead of the
literal 28 (phase-11:89, E7 :311; phase-12 E8 :521; phase-15:123-124, E10 :517, E13, E23, E24; phase-16:156-157, E19 :581;
phase-17:165; phase-18:122) so the ruling needs no doc edits.

**F41 · SHOULD-FIX · phase 11 per-activity query × phases 17 / 18 dynamic shortcuts (C-9)** — dynamic shortcuts land on the
Music tile.
Problem: shortcuts are now selected per activity (`ShortcutQuery.setActivity(the tile's component)`, phase-11:189-195). A dynamic
shortcut published without `ShortcutInfo.Builder.setActivity(...)` is attached to the package's **first** MAIN / LAUNCHER activity —
here `MusicActivity` (AndroidManifest.xml:111-123 precedes Weather and Settings). Phase 17's "Media server", "Panorama", "Slow
motion" and phase 18's "SD card" never say `setActivity` (grep: no hit in 17 / 18), so as written they burst on the **Music** tile
and never on Movies & TV / Camera / Files; 17 E23 and 18 E2 would fail. Fix: C-9's rule and both docs: every dynamic shortcut
calls `setActivity(<its app's launcher activity>)`; E23 / E2 also assert the Music tile's burst is unchanged.

**F42 · SHOULD-FIX · INDEX Change Log** — J5, J6, J7 changed FINAL parts in code with no Change Log line.
Evidence: all three are "fixed" (qa/JEREMY-QA.md:15-17; J6 7/7, J5 8/8 at 373106a) and touch phases 03 and 10; the Change Log's
only 2026-09-23 lines are the A11 amendment and P6 (INDEX.md:86-87). The triage made them the lead's to write "when each lands"
(triage §3), and phase 16 names "the INDEX Change Log line for J6 … the record of the ADD" (phase-16:276-277). Fix: three lines,
as the 2026-09-22 LIVE-QA entry did for J2–J4.

**F43 · NOTE · depends-on across 14–20** — rows need phases the headers omit. 15–20 run phase 11 bursts (15 E27, 16 E25, 17 E23,
18 E2 / E16, 19 E17, 20 E1) and phase 12's E14 template (15 E28 … 19 E16); 14 relies on phase 12's wizard gate (T14-7). None lists
11 or 12 (phase-15:5, phase-16:5, phase-17:5, phase-18:5 `[01, 02, 10, 17]`, phase-19:5, phase-20:5, phase-14:5). F37's order
makes it true in practice; the headers should say it.

**F44 · NOTE · PLAN.md** — A11 in "Working assumptions" (PLAN.md:46-48) and the feature list's "(A11 stands, R10-Q4)" (:71-73)
still read as the pre-amendment rule, and "every phase in INDEX.md; 19 as of 2026-09-22" (:98) is stale (20, and 21 for TV
channels). Annotate A11 in place ("AMENDED 2026-09-23, see Rulings").

**F45 · NOTE · round-1 resolution ledger (task item 1)** — for the record. Applied and verified in the docs: T11-1…T11-5,
T12-1, T12-2 (except F3's Q7–Q9 collapse), T12-4, T12-5, T13-1, T13-2, T14-1…T14-3, T15-1…T15-5, T15-7…T15-9, T15-13, T16-1…T16-3,
T17-1 (design; see F19), T17-2…T17-8, T18-1…T18-3, T18-6, T19-1…T19-3, C-1…C-10 in 11–19. Not resolved: **T12-3** (its INDEX /
PLAN half — F37). Code facts re-checked today: LayoutStore guard absent as the docs say; `LocalCalendar` at feeds/LocalCalendar.kt
(ACCOUNT_NAME `:21`, `id` `:24`, find `:26-34`, colour `:52`, lines `:55`, `:57`) and ActionLayer.kt:485-487 as phase 16 cites;
SpeechService.kt:100-102 / :323-327 / :355 as phase 15 cites; StartActivity.kt:123 / :149 / :187-190 as phase 14 cites;
ShellSettings.kt:72-82 and StartPage.kt:413 as phase 12 cites.

---

## Agent calls that decide something user-visible — for Jeremy (task item 6)

1. Phase 15: the ring surface — W10M toast vs full-screen page, and the unlocked-in-use surface (F9).
2. Phase 17: the TMDB key route, with the corrected premise (F18).
3. Phase 12: shipping R12's Microsoft wallpapers in the CI-published APK (F6); HAL red's swatch under Q7 A (F4); a preset tap
   discarding the user's own picture (F5).
4. Phase 15: Date calculation, recorder pause / markers / search, Clock compare / expanded views / secondary tiles (F11).
5. Phase 16: People's Groups pivot (F16).
6. Phase 17: Photos / Camera / Movies & TV version rulings (F22); "Watch on" lands on the service's search, not the title (F20).
7. Phase 20: which four Music shortcuts once Radio exists (F32); station pin-to-Start, only if kept (F33).
8. Status bar: only if the Start re-measure confirms 28 while apps are 24 (F40).
