# Phases 11–19 — design / correctness review (Reviewer 1, 2026-09-23)

Scope: the nine DRAFT docs phase-11 … phase-19 after their 2026-09-23 interviews, against PLAN.md (P1–P6, the
A-rulings incl. "A11 AMENDED"), INDEX.md (phase table, R11/R12/R13, Change Log, "In scope but NOT split"),
review/2026-09-22-r10-triage.md, the FINAL docs 01 / 02 / 03 / 05 / 10, the DRAFT phase 04, and the code under
`app/src/main/kotlin/app/tileshell/`. Testability / evidence is the other reviewer's lens and is not repeated here.

Severity: **BLOCKING** = the doc cannot go FINAL as written · **SHOULD-FIX** · **NOTE**.
Counts: **16 BLOCKING · 17 SHOULD-FIX · 32 NOTE** (65 findings; 15-5 and 12-3 are per-doc pointers to the
cross-phase items X-1 and X-3, so there are 14 distinct BLOCKING problems).

The pattern the lead predicted holds: every doc's Build tasks / Acceptance rows / Edge cases were written on
2026-09-22 for the interview LEANS, and the 2026-09-23 answers (several of which are wider than the lean) were
recorded as dated Decisions without re-cutting what sits below them. Each per-doc section opens with a
"stale after the interview" list (every place found), then the correctness findings.

---

## Phase 11 — tile quick actions

Interviewed 2026-09-22 (Q1 A, Q2 A); both answers are applied through Decisions, Build tasks and rows. No stale
line found. Three cross-phase items land on it from later docs:

**11-1 NOTE — phase 13 Q3 now ADDs Fluent light to the satellites; phase 11 says the Q6 press setting.**
Phase 11 Decisions "Satellite (P4 design…)": "A satellite's pressed look follows the Q6 press setting like any
Start tile; whether Fluent's touch light is ADDed to satellites is phase 13's decision". Phase 13 Q3 (b) now rules
"border light AND a radial light … on the transient surfaces' pressed items (menu rows, pane items, phase 11's
satellites)". A satellite would therefore show Q6 press feedback (scale / tilt / overlay) AND two Fluent lights.
Fix: phase 11 adds a dated line: satellites take phase 13's lights (an ADD by phase 13) and their Q6 pressed look
is the "interior" phase 13 preserves — or satellites take the lights INSTEAD of the Q6 style. Either is an agent
call; record it so H3 judges one thing.

**11-2 NOTE — phase 13 still carries "burst backdrop, if phase 11 names one".** Phase 11 Decisions: "The burst
has no backdrop of its own: edit mode's measured dim is the backdrop, and no acrylic applies". Phase 13's surface
table row, tag `acrylic:burst`, Build task 4 clause and Scope "the burst" as a live source are resolved: none.
Fix in phase 13 (see 13-3).

**11-3 NOTE — "page 0" for Start.** E5: "`KEYCODE_HOME` → page 0". Phase 14 moves Start to pager index 1
(POD_BAY / START / APP_LIST). Phase 11 lands first, but its rows are re-run at the end-of-build pass. Fix: say
"Start's page" (or `start_page` in the dump), not an index. Same for phase 14's renamed diagnostics line (14-3).

**11-4 NOTE — phase 19 replaces the Start-settings shortcut set Jeremy ruled here.** See 19-4.

---

## Phase 12 — setup wizard

### Stale after the 2026-09-23 interview (Q1 B, Q2 C, Q3 C + presets, Q4 C live, Q5 six presets, Q6 AI pictures)

| Place | Text that no longer matches | Ruling it misses |
|---|---|---|
| Goal | "ends with the accent step" | Q3–Q5: ends with six live-applied presets + individual items |
| Scope In | "the personalisation step (accent, writing phase 01's `ShellSettings` accent, the X26 setting)" | Q3–Q6 |
| Scope Out | "Tess's own checklist rows … this stands unless interview Q2 says otherwise" | Q2 C: every Tess row joins |
| Decisions "Visibility rule" | "Which rows are core is interview Q1" and the rule quotes the Setup rows only | Q1 B + Q2 ("a missing Tess row also summons the wizard") |
| Decisions "Steps" | "the checklist rows … (today: Default Home … Keyboard selected) — then the personalisation step" | Q2: then Tess's rows |
| Decisions "Personalisation step" | "Its contents are interview Q3; the agent call is the accent only … a 'Done' button" | Q3–Q6 |
| Decisions "The microphone is not a step (Scope)" | contradicts Q2 outright | Q2 |
| Decisions "Diagnostics" / "Test tags" | only `[wizard] accent <name>`, `wizard_accent`, `wizard_done` | Q3–Q6 (presets), Q1 (why line) |
| Decisions "Harness" | provision list = notification access, usage access, IME | Q2: Tess's grants too |
| Build task 1 | step list "derived from the checklist rows" | Q2 |
| Build task 2 | no "why" line | Q1 |
| Build task 3 | "pull the accent grid out … write the one `ShellSettings` accent" | Q3–Q6 |
| Build task 4 | "provision.sh grants the core rows and the keyboard" | Q2 |
| E1(b) | adb grant list lacks RECORD_AUDIO, READ_CONTACTS, WRITE_CALENDAR, SEND_SMS, CALL_PHONE, ACCESS_FINE / BACKGROUND_LOCATION, READ_CALL_LOG, READ_SMS and the ASSISTANT role | Q2 |
| E2, E3, E4, E5, E7 | step sets and N count Setup rows only; E3 walks the accent step only; E5 "`wizard_done` on the accent step" | Q2, Q3–Q6 |
| P1 | "the accent chosen shows on a tile" | Q4 (a preset shows on everything) |
| H1–H5 | H3 "The accent step last, and its look with the shared grid"; no H row for the six looks | Q3–Q6 |
| Edge cases | "The accent step with an accent already changed on the same install" | Q3–Q6 |

### Findings

**12-1 BLOCKING — Q2 (all of Tess's grants join) is recorded and nothing below it changed.** The stale rows above
plus four things the Decision line itself leaves open:
(a) Which Tess rows are steps. `cortana/CortanaChecklist.kt:37-122` has 14 rows: `assistant`, `microphone`,
`contacts`, `calendar`, `sms_send`, `call_phone`, `exact_alarms`, `background_location`, `call_log`, `sms_read`,
`models`, `service`, `speech_process`, `person_triggers`. The Q2 line names nine; `exact_alarms` (granted at
install, phase 03 F1-m7), `models`, `service`, `speech_process` and `person_triggers` are observations / liveness,
like the Setup checklist's probe rows. Say so, as the Steps line does for `samsung_badges` / `listener`.
(b) Id collision: the Setup checklist has `calendar` (READ_CALENDAR, `onboarding/Checklist.kt:105`) and Tess's
checklist has `calendar` (write, `CortanaChecklist.kt:56`). `wizard_step:<row id>` is ambiguous and E4's "no
`wizard_step:photos` anywhere in the run" style assertions break. Namespace the tag (`wizard_step:setup:<id>` /
`wizard_step:tess:<id>`).
(c) `background_location` ("Location, all the time") cannot be granted from a runtime dialog on API 30+: the
platform only grants it from the app's permission page, after foreground location. The doc's two step kinds
(runtime dialog / Settings page) need a third — a step whose action is the app-permission page and whose
auto-advance reads the grant on resume — and the walking order must put Tess's foreground location (FINE) before
it. `CortanaPermissionActivity` presumably already does this; name it.
(d) The order of the merged list (Setup rows, then Tess's rows, then personalisation) and "Skip setup" covering
both lists.
Fix: rewrite Scope Out, the Visibility / Steps / Harness Decisions, Build tasks 1–2 and 4, E1(b), E2–E5, E7, and
add an E row that walks one Tess step through its real sheet (the assistant role sheet on the AVD;
`cmd role add-role-holder android.app.role.ASSISTANT` as the adb form).

**12-2 BLOCKING — Q3–Q6 (six live-applied theme presets with bundled pictures) have no design below the
Decision.** Missing entirely: a preset model (a table: preset × {accent, Dark / Light, picture, X5 transparency,
press style, Tess ring / lens colours, keyboard colours, Transparency effects} with the six values); the "Custom"
rule (any item changed → the page reads Custom; re-tapping a preset restores all of it); the live-apply rule and
what "Not now" / Back after a preset tap leaves (the preset stays — the preview IS the change — say it); the
picture pipeline (art/themes/ → crop / scale to 1440 × 3120 + the 1.3× parallax headroom → bundled drawables; the
APK delta — four full-size pictures are 2–6 MB and every other phase has a budget row; this doc has none); the
ADDs to FINAL parts (phase 01 Start + theme gains the preset row — an INDEX Change Log line; phase 03's persona
gets themeable ring / lens colours; phase 05's keyboard gets themeable colours — and phase 05 E3's ± 32-level
colour check must then run on the Default preset, stated); test tags (`wizard_preset:<name>`,
`theme_preset:<name>`, `theme_preset_custom`); diagnostics (`[wizard] preset <name>`, `[theme] preset <name>`);
E rows (each preset tapped → `start_theme.xml` keys read back + one tile pixel + the app-list backdrop + a Tess
ring pixel + a keyboard key pixel; one item changed → Custom; persistence across `am force-stop`; the same on
Start + theme); NEEDS-HUMAN rows for the six looks and for label readability over each picture on the phone (the
Q6 line promises one). R12 gating FINAL is in INDEX; the art input is in theme-art-brief.md — both fine.

**12-3 BLOCKING (cross-phase order) — a preset sets phase 13's "Transparency effects" switch, and phase 13 is
ordered AFTER phase 12.** INDEX / R10 triage order: quick actions → wizard → Fluent engine. Phase 12's own
Decision (from phase 13 Q2): "a preset can turn acrylic off (e.g. Midnight)". Phase 12 says it builds presets "in
their final form" (Rule 16), which needs `StartTheme.transparencyEffects` — a field phase 13 adds
(`prefs/ShellSettings.kt:17-23` has no such field today). depends-on `[01, 03, 05, 10]` omits 13. Fix: order
11 → 13 → 12 → 14 (13 does not depend on 12) and add 13 to phase 12's depends-on; or, if the order must stay, the
transparency item is phase 13's ADD to the preset table (recorded), and phase 12's "final form" claim is softened.
The first is cleaner.

**12-4 SHOULD-FIX — Q1's "every step says why" has no tag, task or row.** The Decision says "the acceptance rows
check that every step shows one"; no row does, `wizard_why` is not in the tag list, Build task 2 does not mention
it. Fix: tag `wizard_why`, an E2 assertion (every `wizard_step:*` dump carries a non-empty `wizard_why`), H1 covers
the wording.

**12-5 NOTE — E2 asserts "no `start_page` node" while Start keeps running underneath.** Decisions "Host": "Start's
tiles keep running underneath and are not drawn over it". A composed-but-covered Start still yields semantics
nodes in a uiautomator dump. Either the wizard removes Start from composition (then "keep running" is only the
feeds), or E2 asserts the wizard is on top by bounds / z-order. Spec contradiction; the evidence reviewer owns the
row form.

**12-6 NOTE — "Lumia" and the W10M stock pictures are Microsoft names / assets.** A10 puts Microsoft names and
assets in the swappable branding module; the preset name "Lumia" and the "Windows 10 Mobile (original)" pictures
should be `Brand` strings / branding-module assets, and the doc should say the original preset is ABSENT (not a
placeholder) if R12 finds no shippable picture (no v1 → v2).

**12-7 NOTE — Persistence rule vs Jeremy's intent.** "A core row ADDed by a later phase … does not summon it" is
the agent's call against "feel like setting up a new phone"; H4 exists and it is marked overrulable — fine, but
phases 15–19 each add grants (see X-5) and only phase 18 says "and a wizard step". Make the rule one sentence in
every later doc.

---

## Phase 13 — Fluent materials

### Stale after the 2026-09-23 interview (Q1 A, Q2 A, Q3 B)

| Place | Text | Ruling it misses |
|---|---|---|
| Decisions "Reveal on touch (per Q3; lean A)" | "Form: a 1-epx border ring on the pressed item … present from touch-down to touch-up with no motion" | Q3 B: border light PLUS a radial light under the finger, following it |
| Build task 5 | "the 1-epx border ring at press" | Q3 B |
| E7 | ring only; static `DOWN` | Q3 B |
| H4 | "The border light on press" | Q3 B |
| Surface table / Build task 4 / tags / Scope | "Burst backdrop (phase 11), if its doc names one", `acrylic:burst` | phase 11 names none |

### Findings

**13-1 BLOCKING — the radial light is unspecified and contradicts the doc's own pressed-fill samples.** Q3 B adds
"a soft radial light centred on the touch point inside the item, following the finger while it is down". Nothing
gives its radius, colour, peak alpha, falloff or motion. E3 / E5 / E7 sample the pressed item's INTERIOR and
require R7 §3.6.3's (79,84,80) ± 2 and §3.1.6's (63,68,64) — a light inside the item breaks those samples, and
Q1 A ("measured colours are kept") was answered on the border-only lean. Fix: specify the light (e.g. a white
radial gradient, radius R epx, peak alpha a at the touch point, 0 at R, redrawn on every MOVE; values P4, one
H row); state where the measured pressed fill is sampled (a patch farther than R from the touch point, or the
light excluded from the patch); add an E7 sub-row with a MOVE (`input motionevent DOWN … MOVE … UP`, screencap
between) showing the light's centre following the finger; extend H4.

**13-2 SHOULD-FIX — the Q2 "agent reading" creates the 12 / 13 order conflict.** See 12-3; record the resolution
here too (Transparency effects exists before phase 12 builds presets, or the item is phase 13's ADD).

**13-3 NOTE — drop the burst-backdrop row.** Phase 11: "no acrylic applies" to the burst. Remove the table row,
`acrylic:burst`, Build task 4's clause and the Goal's "phase 11's burst backdrop"; keep satellites in the Reveal
list (11-1).

**13-4 NOTE — `StartPage.rememberBackground` is `private` (`start/StartPage.kt:987`).** The static source "decoded
as `StartPage.rememberBackground` decodes it" needs the decode exposed or moved — an ADD to phase 01's part to
name in Build task 2 and the Change Log line.

**13-5 NOTE — platform claims checked.** `disable_window_blurs` gating cross-window only, `PowerManager
.isPowerSaveMode`, `ActivityManager.isLowRamDevice`, `GraphicsLayer.renderEffect` on BOM 2026.06.01
(`gradle/libs.versions.toml:4`), minSdk 34 (`app/build.gradle.kts:21`) — all consistent. Fluent's 30-px blur and
0.8 in-app tint opacity match Microsoft's acrylic recipe (H2 covers).

**13-6 NOTE — light theme.** "tint white at 0.8; the app list stays readable" has no measured light-theme app-list
fill; H1 covers. Fine.

---

## Phase 14 — pod bay

### Stale after the 2026-09-23 interview (Q1 B, no widgets)

| Place | Text |
|---|---|
| Scope Out | "real Android widgets through `AppWidgetHost` unless interview Q1 rules them in (then the interview adds the build task and rows named under Q1)" |
| Build task 7 | "Only if Q1 is A or C: the Widgets pod kind …" |
| P4 | "(only if Q1 is A or C) Samsung's own widgets bind …" |
| Edge cases | "If widgets are in (Q1 A or C): provider uninstalled …" |

**14-1 SHOULD-FIX — strike the four conditionals above.** Q1 is B; leave "out for this plan" in Scope Out.

**14-2 NOTE — depends-on `[01, 03, 13]`.** The Now playing pod uses `MusicFeed.send(Transport)`
(`feeds/MusicFeed.kt:101`, phase 01's part after the 2026-09-21 tile-controls change) and its tap opens "the
MUSIC slot app (the shell's Music app, phase 10 Q5)". The pod works with any session, so 10 is a soft dependency;
say so in one line rather than leaving it implicit.

**14-3 NOTE — renamed diagnostics line.** E1 expects `[start] home: page START, scrolled to top`; today's line is
`home: page 0` (`StartActivity.kt:123`). Renaming a line phase 01 / 02 drivers may grep is a harness change;
either keep the text or list the drivers that read it under the harness note. (The scroll-to-top expectation
itself is right: `homeEvents.tryEmit(inFront)` at `StartActivity.kt:311` passes `inFront = true` whenever the
activity is in front, so from the pod bay Home scrolls Start to the top as it does from the app list today.)

**14-4 NOTE — `CortanaService.showSession` is the platform's `VoiceInteractionService.showSession(args, 0)` called
from `CortanaService.open(mode)` (`cortana/CortanaService.kt:86-88`).** The "destination argument" is a new
`EXTRA_*` on that bundle; name it as an ADD to phase 03's session arguments (the doc already says "an ADD",
just the wrong method name).

**14-5 NOTE — a pending pod-bay request while phase 12's wizard shows.** `PodBayRequests` is consumed in
`onResume` and animates the pager; the wizard locks the pager for touch, but `animateScrollToPage` is
programmatic, so the pane would open behind the wizard. State the rule: the request waits until the wizard
finishes (the `SecondaryTiles` band already waits for Start to be visible — reuse that gate).

**14-6 NOTE — Agenda pod after phase 16.** "when phase 16's Calendar takes the slot, the pod follows with no change
here" — right; and the Birthdays calendar (16 Q3) appears in the pod through `CalendarFeed` unfiltered. Fine.

---

## Phase 15 — Alarms & Clock, Calculator, Voice Recorder

### Stale after the 2026-09-23 interview (Q1 A, Q2 A by condition, Q3 A, Q4 A, Q5 A)

| Place | Text | Ruling |
|---|---|---|
| Goal | "(plus the converter if Q4 rules it in)" | Q4 A |
| Scope In | "the converter per Q4", "Tess's arithmetic per Q5", "storage per Q3; the Music cross-effect per Q2" | ruled |
| Scope Out | "a Currency converter with live rates … (A11; R10-Q4)" | A11 amended (see X-6) |
| Build task 5 | "Q5's arithmetic command if ruled in" | Q5 A |
| Build task 6 / 7 | "the converter per Q4" / "MediaStore publish per Q3 … the Music predicate per Q2" | ruled |
| E9 | "(pending Q1, written for its lean A …)" | Q1 A |
| E12 | "(and the converter groups per Q4) … the converter (if in)" | Q4 A |
| E14 | "(Q3 A)" | ruled |
| E20 | "does NOT list the recording under Q2 A (or DOES under B; the row asserts whichever was ruled, never 'unruled')" | Q2 A |
| H11 | "the converter's unit list if Q4 rules it in" | Q4 A |

**15-1 SHOULD-FIX — strike every conditional above and write the ruled form.**

**15-2 BLOCKING — Q5 (Tess does arithmetic through the Calculator engine; principle P6) has no design.** The only
trace is Build task 5's stale clause. Missing: the phrase forms the matcher takes ("what's 15 % of 80", "what is
2 plus 2", "12 times 7", "square root of 81"; whether unit conversion ("5 miles in km") is in — the converter is
now ruled in, so say yes or no); number words → digits (`TimeWords` exists for times only); the reply rule (P6: the
engine's exact display string, e.g. "15 % of 80 is 12."; an engine error is spoken as Windows' string: "Cannot
divide by zero"); the grammar hotwords for the ASR pass; the `LockGate` entry (`cortana/action/LockGate.kt:29-41`
is an exhaustive `when` — a new `Request.Arithmetic` must be listed; deterministic and touching no personal data,
it should be allowed while locked, stated); a card form (phase 03's response card); diagnostics; an acceptance
row (spoken + typed, ≥ 10 cases incl. one error and one over the keyguard; expectations computed on the host like
E11's); the INDEX Change Log line for phase 03. Fix: a Q5 Decision block, Build task 5 detail, E26, an H row for
the phrasing.

**15-3 SHOULD-FIX — the Q2 agent extension (list EVERY `IS_RECORDING` file, whichever app made it) needs a write
path for other apps' files.** READ_MEDIA_AUDIO reads them; rename ("changes `DISPLAY_NAME`"), delete ("removes the
MediaStore row and the file") and trim need `MediaStore.createWriteRequest` / `createDeleteRequest` consent on
API 30+ for a file the shell did not create (phase 17 already uses that path for Photos). Phase 18's all-files
access would bypass it but lands later. Fix: state the consent path (or show foreign recordings read-only), add an
E row with a fixture recording inserted by another package (tileclient-a writing an `IS_RECORDING` row), and add a
list line (`[recorder] list: n recordings (m by other apps)`). Also an H row: showing and deleting Samsung
Recorder's files from the shell is user-visible (Jeremy chose "unless there is another way to play them"; listing
everything is the agent's reading).

**15-4 SHOULD-FIX — `MicArbiter` is in-process; the recorder is in `:recorder`.** `cortana/speech/MicArbiter.kt:16`
is `class MicArbiter<K : Any>` with an `owner` and `ownerPid`, held by the speech service in `:speech`; the
keyboard (`:ime`) reaches it as a `SpeechService` client (INDEX Change Log 2026-09-22 "THE SPEECH PROCESS SERVES
SEVERAL CLIENTS WITH ONE MICROPHONE"). "The recorder takes the microphone through the shell's `MicArbiter` as a
third owner" needs the same binder route (bind the speech service's client interface to acquire / release; release
on binder death so a killed `:recorder` frees the microphone). Name it in Decisions and Build task 7; E17 already
proves the behaviour, E18 should also assert the token is released after the kill.

**15-5 BLOCKING (cross-phase) — depends-on 17 vs build order.** See X-1.

**15-6 NOTE — `LockGate`'s comment goes stale with Q1.** `LockGate.kt:25-27` allows alarms / timers "because they
are set without any UI (`EXTRA_SKIP_UI` on a voice activity)"; after Q1 they are set in-process. The rule stands,
the reason changes — say so in Build task 5.

**15-7 NOTE — `exact_alarms` is a Tess-checklist row (`CortanaChecklist.kt:57`), not a Setup-checklist row.** "the
checklist's `exact_alarms` row goes red" — name the checklist.

**15-8 NOTE — platform claims checked.** `ReminderReceiver` exists (`cortana/reminders/ReminderScheduler.kt:116-123`,
manifest line 268) and timed reminders use `setExactAndAllowWhileIdle` (`:60`) as the doc says; `setAlarmClock`
under `USE_EXACT_ALARM` is correct; `IS_RECORDING` / `DIRECTORY_RECORDINGS` are API 31+ (minSdk 34, fine); the
`systemExempted` FGS type for exact-alarm holders is rightly a build-start check.

---

## Phase 16 — Calendar and People

### Stale after the 2026-09-23 interview (Q1 A, Q2 D — Jeremy's own design, Q3 A)

| Place | Text | Ruling it misses |
|---|---|---|
| Goal | "shows and EDITS the phone's calendars … every calendar the phone has, plus a local one … interview Q2" | Q2 D: reads all, writes only the local one |
| Goal / Scope Out | "nothing here reaches the internet (A11)", "(A11)" | A11 amended (X-6) |
| Scope In (Calendar) | the editor's "calendar" field; "edit one occurrence / this and following / all; delete" with no local-only limit | Q2 rules 1–2 |
| Decisions "Calendar data (agent, pending Q2; written for its lean A)" | "The app reads AND WRITES `CalendarContract` — every calendar the provider holds"; "A calendar whose access level is below CONTRIBUTOR is shown read-only" (now every account calendar is read-only in the app) | Q2 D |
| Decisions "Birthdays (pending Q3 …)", "People over the provider (agent, pending Q1 …)" | "pending" | ruled |
| Build task 3 | "event write / edit / delete with recurrence exceptions" — no Sync action, no "Can sync to" store, no copy mapping | Q2 rules 3–4 |
| Build task 4 | settings page = first day of week only | Q2 rule 4 ("Can sync to") |
| E3 | "(Q2 A)"; "the editor's calendar picker omits it [the READ-level one]" — the picker now holds the local calendar only | Q2 |
| E17 | "(Q3 A)" | ruled |
| P2 | "an event written here to a synced calendar appears in that account's other client after its sync adapter runs" — direct writes are forbidden; this is the Sync push | Q2 |
| H5 | "if Q3 rules it in" | Q3 A |
| Edge | "An account (and its calendar) removed while its event's editor is open: the save fails" — the editor writes local only; this becomes the Sync failure case | Q2 |

**16-1 BLOCKING — Q2 D has no rows, tasks or mechanics below the Decision.** The Decision itself promises
"Acceptance rows prove each rule on the AVD with a local calendar and two sync-adapter-created account calendars
(one allowed, one not): no write reaches the disallowed one by any path" — no such row exists. Missing: Sync's
mechanics (the copy is a normal-app insert into the target calendar, then the mapping local id → {copy id,
calendar id} must live in the shell's own store — `Events.SYNC_DATA*` / `_SYNC_ID` are sync-adapter-only
columns; re-Sync updates the copy through that mapping); what a local delete does to the copy (one-way push:
nothing? delete it? — decide); the copy edited or deleted on the other side (the next Sync overwrites /
recreates — decide); the allowed calendar removed from the phone (the Sync refuses with a notice); recurring
events with exceptions and reminders (copied or not); the "synced to <calendar>" marker's look (P4, H row); the
"Can sync to" page (keyed on the provider's calendar `_ID` + account name / type so a re-added account with new
ids starts NOT allowed again); test tags (`cal_sync`, `cal_sync_target:<id>`, `cal_synced_marker:<id>`,
`cal_settings_can_sync:<id>`); diagnostics (`[calendar] sync event=<id> -> calendar <id>: ok|updated|failed
<err>|refused (not allowed)`); E rows E22–E24 (Sync to the allowed QA calendar → a copy read back with `content
query`; re-Sync after an edit → the copy changed; the disallowed calendar's event count unchanged after every
path — editor, Tess, Sync; the picker lists only allowed calendars); P2 re-cut to the push.

**16-2 BLOCKING (trust) — Tess would write into a Google (possibly work) calendar today, and the doc keeps that
path.** `cortana/action/ActionLayer.kt:647-662` `writableCalendarId()` returns the IS_PRIMARY calendar or the
first with access ≥ CONTRIBUTOR — on the phone that is an account calendar, which Q2 rules 2 and 4 forbid ("I dont
want to add anything to my work calander from my phone ever"). The Decision "Calendar data" still says "Phase 03's
`ActionLayer.writableCalendarId` then finds it (CONTRIBUTOR or better)", and E9 only tests the AVD, where the local
calendar is the only one. Fix: re-point `writableCalendarId` to the shell's LOCAL calendar id only (create it if
absent; if it cannot, Tess refuses with a spoken notice — never a fallback to another calendar), record it as the
ADD to phase 03 the Q2 line names, and add an E row with the QA account calendar present: Tess's event lands in the
local calendar and the QA calendar's event count is unchanged. P2 gains the same on the phone.

**16-3 SHOULD-FIX — the empty allow-list is a seam Jeremy did not ask for.** Jeremy: "I can click sync and it
will ask which connected calander to sync with". The agent's rule 4 makes every calendar NOT allowed until enabled
in settings, so the first Sync shows an empty picker and a trip to settings (a P2 seam). It is marked overrulable
but has no H row. Add one ("H14 [accept]: the Sync picker lists only calendars enabled once in Calendar settings,
so the work calendar can never be chosen by a mis-tap") so Jeremy sees the trade before it is built.

**16-4 NOTE — the reminder receiver notifies for every calendar's alerts.** `ACTION_EVENT_REMINDER` fires for alerts
on account calendars the shell never wrote (and on the Birthdays calendar if it carries default reminders). The
doc says "one notification per alert" — so the shell notifies for Samsung Calendar's events too, doubling P1's
double. State it; it is the rule, not a defect. (Platform note, moderately certain: the provider sends the
broadcast with `FLAG_RECEIVER_INCLUDE_BACKGROUND` so a manifest receiver gets it, as the doc assumes.)

**16-5 NOTE — depends-on `[01, 02, 03, 17]`: the `assignSlotOnce` guard must precede phase 17's seed.** See X-2.

---

## Phase 17 — Photos, Camera, Movies & TV

### Stale after the 2026-09-23 interview (Q1 C, Q2 C, Q3 superseded → Q3b A, Q4 A, Q5 A) and the A11 amendment

| Place | Text | Ruling it misses |
|---|---|---|
| Header | "interview pending" | done |
| Goal | "Nothing here uses the internet (A11)" | A11 amended; Q3b online |
| Scope In | "modes per Q2", "editing per Q1", "(and videos, Q4)", "per Q5", "a Videos library per Q3" (Q3 is superseded by Q3b: the hub) | ruled |
| Scope Out | "streaming or any http source (A11, R10-Q4: an `http://` VIEW is refused)" | Q3b: streaming hand-off + media server |
| Decisions "2026-09-22 R10-Q4 … no online catalogue, no streaming: the video player refuses network URIs" | not marked superseded | A11 amended, Q3b |
| Build task 4 / 5 / 6 | "(and videos, Q4)" / "the editor per Q1" (no trim) / "the Q2 mode set" (auto modes only) | Q4, Q1 C, Q2 C |
| Build task 7 | "Q3's library …; an explicit refusal for non-file schemes (A11)" | Q3b (a media server streams over http) |
| E3 | "With Q4 A" | Q4 A |
| E6 | "Editing (Q1 B / C)" — crop + rotate only | Q1 C |
| E9 | "(Q5 A)" | ruled |
| E13 | "the A11 negative": an `http://` VIEW refused, "no ExoPlayer source is created" | Q3b |
| E18 | `[video] refused scheme=<s>` | Q3b |
| Y5 / H3 | the player only; nothing for the hub | Q3b |
| P1 | front camera, flash, HDR / Night, zoom only | Q2 C (pro dial, panorama, slow motion, Living Images) |

**17-1 BLOCKING — Q3b (Movies & TV as the online hub) has no design, tasks, rows or trust lines.** The App
Shortcuts line names "Browse" and "Media server" screens that exist nowhere else in the doc. Missing: the
catalogue source (which public film database — TMDB needs an API key and attribution, and the key's storage in a
repo / APK is a decision; OMDb / Wikidata are alternatives; P5 forbids Google's); the "where to watch" source
(TMDB's watch-providers are JustWatch data with their own terms); the streaming hand-off table (per service: the
deep-link scheme or web URL, what "at the title" means, and `queryIntentActivities` to list installed services);
the media-server client (Jellyfin REST / Plex, token storage, which is a credential the shell keeps — a trust
line); offline behaviour (P6 / "offline preferred": a cache; what the hub shows with no network); the hub's pivots
and their R11 §Movies & TV need (the store / hub pivots, not only the player); diagnostics; build tasks; E rows
(a local HTTP fixture server for the media-server rows; recorded catalogue responses — E rows never hit the live
network); P rows (real streaming apps on the S25U); H rows; the APK / dependency line (an HTTP client — OkHttp is
already transitive through Media3? state it); and the interface phases 20 (Music streaming) and 21 (TV channels)
reuse — the doc says they reuse "the streaming hand-off", so name it. Fix: a "Movies & TV online" Decision block
+ tasks + rows, or split the online half into its own phase doc (INDEX already hangs 20 / 21 off it) — the
lead's call; and mark the 2026-09-22 R10-Q4 line SUPERSEDED in place.

**17-2 BLOCKING — Q2 C (every Windows Camera mode) has tasks and rows for the automatic modes only.** Build task 6
covers photo / video, flash, timer, grid, focus, zoom. Nothing for: the pro dial (manual focus / ISO / shutter /
white balance / exposure through CameraX's Camera2 interop — `Camera2CameraControl` — and the capability check
`CameraCharacteristics` reports); panorama (no platform API; a stitcher is code the shell ships — the Decision
says "licence checked at build start, P5", but P5 is the pinch-zoom row; a stitcher such as OpenCV is a NEW
dependency, which contradicts "CameraX is the only new dependency" and the APK budget line); slow motion
(high-speed capture — CameraX's support depends on the pinned version and may need Camera2's constrained
high-speed session; uncertain, must be a build-start check); Living Images (a short clip + still — the file
form: Samsung Motion Photo container or a sidecar, and how Photos shows it). Fix: per-mode Decision lines
(mechanism, file output, capability gate → hidden with a diagnostics reason), Build tasks 6a–6d, P rows per mode
(the AVD cannot), H rows, the stitcher's licence and dependency lines, the APK delta.

**17-3 SHOULD-FIX — Q1 C's editor set and video trim have one row (crop + rotate).** Build task 5 names the editor
but not trim. Add rows for straighten, auto-enhance, light / colour, filters, red-eye (each a saved copy; the
original's md5 unchanged), and video trim (Media3 Transformer — part of Media3, no new library — output duration
± 0.1 s, original kept); edge: an edited HEIC's output format.

**17-4 SHOULD-FIX (trust) — the capture-intent contract needs its guards.** The Camera answers `IMAGE_CAPTURE` /
`VIDEO_CAPTURE` and writes to the caller's `EXTRA_OUTPUT` from `:camera`, which runs with the shell's own
permissions (READ_MEDIA_*, and MANAGE_EXTERNAL_STORAGE after phase 18). State: accept `content://` only, refuse
`file://` (`[camera] refused output scheme=file`); write only through `contentResolver.openOutputStream(uri)` under
the caller's URI grant, never by path; strip EXIF location for a third-party caller (Android's own camera does);
no DCIM copy of an intent capture; `RESULT_CANCELED` leaves no `is_pending` row (E9 has this). Add the `file://`
negative to E9.

**17-5 SHOULD-FIX — the "re-pointed slot is left alone" claim is wrong by the code, and E1 does not test the case
that matters.** Decisions: "A slot the user re-pointed before this build installs is left alone (the marker path
only ADDs to `explicitSlots` when the marker has not run — an assignment made by hand is not one the marker
made …)". `tiles/LayoutStore.kt:127-139` checks only the marker and then writes `explicitSlots + (slot to
component)`, which REPLACES an existing entry. Phase 16 found exactly this ("`assignSlotOnce` has no guard for an
existing explicit assignment"). E1's upgrade half re-points PHOTOS AFTER the seed ran and checks `already run` —
the marker path, not the pre-existing-choice path. On Jeremy's phone PHOTOS / CAMERA are very likely hand-assigned
(Samsung Gallery / Samsung Camera), so the phase-17 build would overwrite them on install. Fix: correct the
Decision; build the guard before the seed (X-2); re-cut E1's upgrade half — assign PHOTOS by hand on the pre-17
build, install the 17 build, assert `kept user's`.

**17-6 NOTE — "Media server" is a conditional shortcut → dynamic.** See X-4.

**17-7 NOTE — platform claims checked.** `MediaController.getTag()` is API 29; `createWriteRequest` /
`createDeleteRequest` API 30; `FOREGROUND_SERVICE_CAMERA` API 34; CameraX Extensions as the public route to vendor
extensions — all right. The `com.android.cameraextensions` package on the AVD is the stub, so E rows cannot prove
any extension; the doc already routes those to P1.

---

## Phase 18 — Files

### Stale after the 2026-09-23 interview (Q1 A, Q2 C, Q3 C, Q4 A)

| Place | Text | Ruling |
|---|---|---|
| Header | "interview pending" | done |
| Goal | "Its storage model is ruled in Q1 and every row depends on it"; "Nothing here uses the internet (A11)" | Q1 A; A11 amended |
| Scope In | "Q2 rules the functional set", "delete per Q3", "open-with routing per Q4", "The storage grant per Q1" | ruled |
| Scope Out | "a recycle bin unless Q3 rules one; zip handling unless Q2 rules it" | Q3 C, Q2 C |
| Decisions | "written for Q1's lean and are re-cut if it goes the other way"; "Q4 asks whether this is the behaviour Jeremy wants" | ruled |
| Build task 1 / 3 / 4 | "(Q1 A)", "delete per Q3", "per Q4" | ruled |
| Acceptance header | "Written for Q1 A; re-cut if Q1 rules otherwise" | ruled |
| E4 | "delete `c.bin` after the confirmation (Q3 A) → `ls` fails" | Q3 C: it goes to the bin |
| E6 | "(Q4 A)" | ruled |
| H2 | "the delete rule (Q3) and its confirmation wording" | Q3 C |

**18-1 BLOCKING — the Recycle Bin (Q3 C) has one paragraph and no design below it.** Missing: the bin folder's
name and place per volume (a dot-folder? the app hides dot-files by default per its own edge case, so the bin must
be its own root / entry — the App Shortcut "Recycle Bin" implies a screen that does not exist in the tasks);
MediaStore consequences — a binned photo or song must vanish from Photos, Music and the Photos tile (a `.nomedia`
in the bin, or MediaStore's `IS_TRASHED`; otherwise the tile keeps cycling a "deleted" picture), and the scan after
the rename; the index (original path + deletion time per binned file; where it lives; what happens if it is lost
or the bin folder is edited by another file manager); Restore (clash → ask; original folder gone → recreate or
ask); Delete permanently and Empty (confirmations; H wording); a bin on a removable volume that is pulled; the
Storage page's view of the bin (phase 19); delete INSIDE the bin = permanent (state); test tags (`files_bin`,
`files_bin_row:<name>`, `files_bin_restore`, `files_bin_empty`); diagnostics (`[files] bin <op> <path>:
ok|failed`); E rows (delete → present in the bin, gone from the folder, gone from `content query` on the images
collection; restore → back with md5; clash; empty; permanent); an H row for the bin's look (P4 — W10M had none).

**18-2 BLOCKING — zip and Recent (Q2 C) have no tasks, rows or edges.** Missing: opening a zip as a folder (a
virtual root over `java.util.zip` — no new dependency; say so), extract with progress through the same foreground
service, create from a selection; Recent (which MediaStore collections, `DATE_MODIFIED`, the cap, files written by
path that MediaStore has not scanned yet); tags and diagnostics; E rows; and the security edges — zip-slip
(entries with `../` or absolute paths are refused; the extract root is enforced), zip bombs (declared vs actual
size; a cap or a warning before extract), encrypted zips (unsupported, said so), nested zips, zip64 (> 4 GB),
unicode names, extract onto a full volume (the temp-and-rename rule), a zip on a removable volume pulled
mid-extract.

**18-3 SHOULD-FIX — Q1's wizard step is stated and not built.** Decisions: "The grant is a Setup checklist row AND
a setup-wizard step with its 'why' line". Build task 1 has the checklist row only. Add the step (its "why" line;
the action is `MANAGE_ALL_FILES_ACCESS_PERMISSION`, a Settings page, so phase 12's Settings-page step kind) and an E
row in phase 12 E2's form, noting phase 12's rule that a finished install is not re-summoned.

**18-4 NOTE (platform, moderately certain) — "a rename or move by path does not update MediaStore by itself" is
too strong on Android 11+.** Shared storage is FUSE-backed and MediaProvider updates its database on path renames
and deletes of files it indexes. The scan is harmless belt-and-braces; soften the claim and let E9 record which
case actually needed it.

**18-5 NOTE — there is no `FileProvider` today** (manifest providers: `LiveTileProvider` line 163,
`KeyboardConfigProvider` line 262). Share needs one — an exported provider with `grantUriPermissions`, so it is an
allow-list ADD with its reason; say so in Build task 3 and E10.

**18-6 NOTE — "SD card while one is inserted" is a dynamic shortcut.** See X-4.

---

## Phase 19 — Settings front

### Stale after the 2026-09-23 interview (Q1 A, Q2 C, Q3 C, Q4 A)

| Place | Text | Ruling it misses |
|---|---|---|
| Header | "interview pending" | done |
| Scope In | "Helper rows: Wi-Fi, Bluetooth, mobile data and airplane mode … further verbs only per Q3"; "the launcher label per Q1"; "falls back per Q4" | Q3 C; ruled |
| Scope Out | "an 'Apps' category (desktop Windows' 15063 addition, not W10M's)" listed as OUT | Q2 C |
| Per-page table | "(helper only if Q3 adds it)" ×2, "(helper only per Q3)" ×3; no Apps rows | Q3 C, Q2 C |
| Build task 2 / 5 | "per Q1" / "per Q4" | ruled |
| Build task 4 | "per Q3, further verbs are phase 04 ADDs (its allow-list, its R4 proof, its review), not this phase's" | Q3 C names no owner |
| E1 / E2 / E8 / E13 | "the set per Q2", "per Q1" ×3, "(Q4 A)" | ruled |
| E9 | four toggles only | Q3 C |
| H7 | "the app-list result of Q1" | ruled |
| Edge cases | "Two 'Settings' entries (Q1 C) in the app list" | Q1 A |

**19-1 BLOCKING — the five new helper verbs (Q3 C) are a trust change with no owner, no rows and no gate.** The
Decision says the helper gains them, each "proven on the S25 Ultra by R4's method, a named verb in the allow-list,
and the adversarial review phase 04 requires"; Build task 4 says they are "phase 04 ADDs … not this phase's"; phase
04 is DRAFT with a four-verb set from Q13 and its own R4 gate; INDEX's R4 row lists only the four toggles to probe.
Fix: (a) name the owner — phase 19 builds the five verbs as an ADD to phase 04's helper (one allow-list entry per
toggle, no generic command), recorded in 04's Change Log when built, with the adversarial review 04 mandates;
(b) extend INDEX's R4 row with the five probes (battery saver `cmd power set-mode 1` / `settings put global
low_power`; location `cmd location set-location-enabled`; NFC `svc nfc enable|disable`; hotspot — `cmd wifi
start-softap` is a LOCAL-ONLY hotspot, tethering may need another path, so the probe must say which; auto-time
`settings put global auto_time`) each run as the shell uid on One UI 8; (c) E9 gains five sub-rows with their
observables and a "verb R4 could not prove → the row is a deep-link with the reason" row; (d) the table's Battery
saver, NFC, Mobile hotspot, Location and Date & time rows become "helper (04 ADD) + link".

**19-2 SHOULD-FIX — the Apps category (Q2 C) is still OUT in Scope and absent from the table.** Strike the Out
line; add rows — Apps > Apps & features (`MANAGE_ALL_APPLICATIONS_SETTINGS`; per app `APPLICATION_DETAILS_SETTINGS`
with `package:`), Default apps (`MANAGE_DEFAULT_APPS_SETTINGS` — today filed under System > Phone / Messaging;
pick one place), Apps for websites (today under System; Windows 10 puts it under Apps), Permissions (the Privacy
pages), Uninstall (the app list's hold-menu path, INDEX 2026-09-22 UNINSTALL) — with their AOSP resolutions
recorded and E8 covering them; E1's category count follows.

**19-3 SHOULD-FIX — relabelling Android's Settings entry is a phase 01 app-list change, and the rule it cites is
not findable.** "the 2026-09-22 slot-label rule extended to this one app entry" — no entry by that name exists in
INDEX.md, PLAN.md, phase 01 or phase 10 (searched); cite the actual Change Log line or drop the reference. The
label override for `com.android.settings` is an ADD to phase 01's app list (record it; the jump grid and search
must find "Android settings" under A and both under "settings"); the edge case "Two 'Settings' entries (Q1 C)"
must become the Q1 A form (search "settings" returns ours first, then Android's — state the order).

**19-4 SHOULD-FIX — an agent line overturns a Jeremy-ruled list.** Phase 11 Q1 (Jeremy: "A") ruled "Start settings
declares Start + theme / Tile apps / Setup / Diagnostics" as its App Shortcuts. Phase 19's agent line replaces
them with "System, Personalisation, Network & wireless, Apps". Either ask Jeremy, or record it as superseding
phase 11 Q1's list with an H row, and make sure the intent extras phase 11 builds keep opening the re-homed pages
(`settings_start_theme` etc. move under categories in E2).

**19-5 NOTE — where phase 12's presets and phase 13's Transparency effects go.** The Personalization split (H6)
lists phase 01's rows only. Windows 10 puts Transparency effects under Personalization > Colors and presets under
Personalization > Themes — say so (a Themes page = phase 12's preset page).

**19-6 NOTE — depends-on `[01, 04]`.** The table lists phase 18's Files (System > Storage) and, after 19-5, phase
12 / 13 rows; "the front lists what exists at build time" covers it — say it once in depends-on's comment.

**19-7 NOTE — platform claims checked.** `TIME_12_24`, `screen_off_timeout`, `RingtoneManager
.setActualDefaultRingtoneUri` all sit behind the `WRITE_SETTINGS` appop; DND through notification-policy access;
`ACCESSIBILITY_COLOR_CONTRAST_SETTINGS`, `TEXT_READING_SETTINGS`, `REGIONAL_PREFERENCES_SETTINGS`,
`APP_OPEN_BY_DEFAULT_SETTINGS`, `BIOMETRIC_ENROLL` exist at minSdk 34 and were resolved on the AVD. `SettingsActivity`
signs `testTagsAsResourceId` (`settings/SettingsActivity.kt:65`) and the three `res/xml` services point at it, as
stated.

---

## Cross-phase

**X-1 BLOCKING — the inbox-app build order contradicts every depends-on line.** INDEX (R10 triage: "inbox apps in
groups") and the numeric table build 15 → 16 → 17, but phases 15 and 16 both `depends-on: 17` for its build task 2
(tag-keyed live-tile routing). The dependency is real: `start/StartPage.kt:150,173,230,232` hand any tile whose
app is `app.tileshell` the content under `packageKey("app.tileshell")`, and `feeds/MusicFeed.kt:159-171` publishes
the now-playing face under the session owner's package and grows every tile of that package (`ActiveTiles
.setPackage`, `tiles/ActiveTiles.kt:51`). A pinned Alarms & Clock tile (15) or the seeded Calendar / People tiles
(16) carry Music's face on any build without 17's task 2. Rule 16 (each part built once, by one phase) plus the
order means the fix belongs to whichever inbox phase builds FIRST. Fix, the lead's pick: (a) re-home the routing
fix to phase 15 as its build task 0, with 16 and 17 depending on 15 (smallest change; 17 keeps its rows E16 as a
re-run), or (b) reorder to 17 → 15 → 16 (17 is the largest phase and now carries the online hub, which delays the
two cheap ones). Every depends-on line and the INDEX phase notes follow the choice.

**X-2 BLOCKING — the `assignSlotOnce` guard is phase 16's build task 1, but phase 17 seeds PHOTOS and CAMERA
before phase 16 exists.** `tiles/LayoutStore.kt:127-139` overwrites an existing explicit assignment (verified; phase
16 says so; phase 17 says the opposite — 17-5). Whichever phase first seeds a slot after phase 10 needs the guard
in place. Fix: the guard lands with the routing fix (X-1's first phase) or in phase 17's build task 3 before its
seed; phase 16's task 1 then only claims its markers and runs its upgrade row. The Change Log line for phase 01
moves with it.

**X-3 BLOCKING — phase 12 ↔ 13 order** (12-3 / 13-2): presets set a phase 13 setting; order 13 before 12.

**X-4 SHOULD-FIX — the App Shortcuts standing rule says "static"; two docs declare conditional shortcuts.** Phase 11
Q1's rule ("declares its own top-level screens as static App Shortcuts") and phase 11's selection rule (manifest
before dynamic, then rank, first four) assume static declarations. Phase 17 ("Media server — only when one is set
up") and phase 18 ("SD card while one is inserted") need dynamic shortcuts through `ShortcutManager`. Amend the rule
in phase 11 (static for fixed screens; dynamic, published and removed by the app, for screens that exist
conditionally) so the selection rule's "manifest before dynamic" ordering is a stated choice, and phases 17 / 18
say "dynamic".

**X-5 SHOULD-FIX — "every later grant adds a wizard step" is stated in phase 12 and honoured by phase 18 only.**
Phase 12 Q2's agent reading: "every later phase that adds a grant … adds its step to the wizard the same way each
phase adds its checklist row". Phases 15 (Full-screen alarms), 16 (WRITE_CONTACTS on Tess's `contacts` row), 17
(Camera, Videos), 19 (Modify system settings, Do not disturb access) add checklist rows and say nothing about the
wizard; only phase 18 does. Each doc needs the one line (row + wizard step + why) or phase 12's reading is wrong.

**X-6 SHOULD-FIX — the A11 amendment ripples through five docs that still cite the OLD A11 as a ruling.** Phase 14
("no network beyond Weather's own refresh (A11)"), 15 (Goal "nothing here is internet-connected (A11)"; Out: the
world-clock map, the Currency converter, downloaded alarm sounds "(A11; R10-Q4)"), 16 (Goal and Out "(A11)"), 17
(17-1), 18 (Goal and Out "(A11, R10-Q4)"). Under PLAN.md's 2026-09-23 amendment those exclusions are now "offline
preferred" agent choices, not rulings. Re-tag each ("out: offline preferred, A11 as amended; Jeremy can ask") so a
later reader does not treat a withdrawn ruling as the reason. Phase 15's Currency converter was excluded by old
A11 alone; keeping it out is a legitimate P6 / offline-preferred call but should say so.

**X-7 NOTE — three things now live in `StartActivity`'s root and none states the z-order.** Phase 11's burst layer,
phase 12's wizard page, phase 14's pod-bay page (+ phase 13's app-list backdrop). The wizard locks the pager and
sits above the pivot; a burst cannot coexist with it (edit mode is under the wizard); the pod bay is a pager page
under the wizard. Consistent, but one line in phase 12 ("above the pager, the burst layer, the pin band and the
picker") saves a build-time question.

**X-8 NOTE — `(agent)` lines that decide user-visible behaviour without an H row.** Phase 15: listing and deleting
other apps' recordings (15-3). Phase 16: the empty Sync allow-list (16-3). Phase 18: the bin's form (18-1). Phase
19: the Start-settings shortcut set replacing a Jeremy ruling (19-4). The rest (phase 12 H4, phase 14 H8 / H9,
phase 17 H8) already carry one.

**X-9 NOTE — INDEX "In scope but NOT split" already hangs phases 20 (Music streaming / Radio) and 21 (TV channels)
on phase 17's "streaming hand-off".** Until 17-1 names that interface, those two rows point at nothing buildable;
resolve 17-1 before either doc is written.
