# Phase 15 — Stage A review round 3 (the last allowed) — reviewer brief, 2026-09-23

You are one of two independent reviewers of ONE draft phase doc in a phased build. Round 3 is the cap: after it, the
doc goes FINAL once nothing BLOCKING is left. Your job is to find what would make the phase unbuildable, wrong, or
un-gradeable, so it can be fixed before the build starts. Your report is data for a triage; it is not shown to the owner.

## Where everything is (read-only for you)

- Repo (a git worktree, branch `phase-15`, at main's 298a9d7): `/home/jeremyking/projects/metro-launcher-p15`
- The doc under review: `docs/plan/phase-15-inbox-clock-calculator-recorder.md` (1,120 lines). Read ALL of it.
- Rules it must obey: `docs/plan/PLAN.md` (principles P1–P6, rulings incl. A11 AMENDED 2026-09-23, RV1–RV13 — RV9, RV11,
  RV12, RV13 especially), `docs/plan/INDEX.md` (Build order line, Change Log — top entries are 2026-09-23), the process
  rules in `~/.claude-work2/skills/phased-build/SKILL.md` ("Hard Rules", especially 1–6 and 16: phases are parts, not
  releases; no interim form of a part).
- Its measurement sources: `docs/plan/r11/clock.md`, `docs/plan/r11/calculator.md`, `docs/plan/r11/voice-recorder.md`
  (index `docs/plan/r11-inbox-apps.md`); earlier ones it cites: `docs/plan/w10m-measurements.md` (R3),
  `docs/plan/r6-measurements.md`, `docs/plan/r7-measurements.md`.
- Earlier review rounds on this doc (do NOT re-raise a finding these already triaged, unless the applied text is wrong,
  incomplete, or contradicts something): `docs/plan/review/2026-09-23-phases11-19-triage.md` (round 1, ids `T15-n`,
  `C-n`) and `docs/plan/review/2026-09-23-phases11-20-r2-triage.md` (round 2, continues the same ids).
- Neighbouring docs the phase depends on or feeds: `docs/plan/phase-03-cortana-commands.md`,
  `phase-10-media-player.md`, `phase-11-tile-quick-actions.md`, `phase-12-setup-wizard.md`, `phase-16-…`, `phase-17-…`.
- The code the doc cites (Kotlin): `app/src/main/kotlin/app/tileshell/…` — check that every `file:line` cite and every
  symbol the doc names exists and says what the doc claims.
- The QA driver floor: `docs/plan/qa/phase-03/scripts/lib.sh`, `provision.sh`, `audio.sh`, `utterances.py`, `j7.sh`;
  `docs/plan/qa/phase-02/scripts/layout.sh`, `regress.sh`; `docs/plan/qa/phase-05/README.md` (the gesture driver).

A live AOSP emulator (API 36, 1080x2340 @ 450 dpi, the shell provisioned) is available to you as **`emulator-5556`
ONLY**, for READ-ONLY feasibility checks of commands the doc's rows rely on (e.g. `adb -s emulator-5556 shell cmd alarm
help`, `appops get app.tileshell USE_FULL_SCREEN_INTENT`, `dumpsys …`, `content query …`, `cmd shortcut …` help). Put
`$HOME/Android/Sdk/platform-tools` on PATH. Never address `emulator-5554` (another session owns it), never install,
uninstall, grant, revoke, set, clear, reboot, jump the clock, change settings, or start activities. If a check needs a
state change, say so in the finding instead of doing it.

## Facts established before this round (evaluate them; do not re-discover them)

1. **Q-E is ANSWERED: A** (Decisions line ~100, Jeremy "(a)", 2026-09-23): alarms and timers ring as W10M's toast
   everywhere — the locked toast through a full-screen-intent activity, the in-use toast as a `TYPE_APPLICATION_OVERLAY`
   window behind "Display over other apps". Yet the frontmatter (line 4) still says Q-E "is with Jeremy", and Scope, build
   task 3, E2, E4, E4b, E6, E10, E21, E23, E28, P1, H1, H5 still carry "pending Q-E" wording and B / C branches. Say which
   of those texts are now wrong or dead, and what the ruled-A-only text must be. Jeremy's ruling itself is not reopened.
2. **This phase will be built IN PARALLEL, on its own branch, before phases 11, 12, 13 and 14 exist** (INDEX Change Log
   2026-09-23, "BUILD PHASE 11 NOW, AND PHASE 15 IN PARALLEL"; Build order line puts 15 after 11 → 13 → 12 → 14). The
   branch is rebased onto main after phase 11 lands and phase 15's rows are re-run there. The clearance said "phase 15
   needs nothing from 11"; the doc's own `depends-on` (C-23) is `[01, 02, 03, 10, 11, 12]`. Verified today in the worktree:
   - `qa/phase-03/scripts/lib.sh` has NO `record`, `ring_mark`, `ring_since`, `reply_since`, `fill_volume`,
     `unfill_volume`; `row_end` does not save ring slices. Phase 11's build task 7 owns `ring_mark` / `ring_since` /
     `reply_since` / `row_end`'s slices (phase-11 doc ~l.254-259); this doc's build task 8 owns `fill_volume` /
     `unfill_volume` (C-27). Find who owns `record` (C-26) and say what that means here.
   - No setup-wizard code exists in the app (`grep -rli wizard app/src/main/kotlin` → nothing); `provision.sh` writes no
     finished marker (C-15's mechanism is phase 12's). Android itself reports `device_provisioned=1`.
   - E27 needs phase 11's burst and its `[quick] shortcuts for …` line; E28 needs phase 12's wizard.
   Say, for every row and helper, what this branch can run and prove, what can only run after the rebase (phase 11 on
   main), and what can only run once phase 12 is built — and what the doc must say so the phase's gate is well-defined on
   this branch. Rule 16 applies: no interim version of a part (e.g. no second copy of phase 11's helpers).
3. Codex is out for this round (usage limit); both reviewers are Fable with different lenses. Jeremy's dated rulings
   (Decisions lines quoting Jeremy) are not reopened; if one is contradicted by a fact or is impossible, say so as a
   finding and the triage turns it into a question.

## Your lens

(Stated in your dispatch message: Reviewer 1 = design / correctness; Reviewer 2 = testability / evidence integrity.)

**Design / correctness:** contradictions between Goal, Scope, Decisions, Build tasks, rows and H rows; stale or dead text
after the Q-E ruling and the round-2 edits; impossibilities on Android 16 (API 36) AOSP and One UI 8 (full-screen intent,
overlay windows over the keyguard, `setAlarmClock`, foreground-service types — `systemExempted` eligibility,
`microphone` FGS from a background start, MediaStore `IS_RECORDING` / `OWNER_PACKAGE_NAME` semantics, `MediaCodec`→ADTS
→ mux recovery, `AudioRecordingCallback`); every code cite correct; trust-touching surfaces the doc adds (the overlay
window; the full-screen intent; the `ISpeech` ADD `holdMicrophone` / `releaseMicrophone` — who may call it, is the
service exported or permission-guarded; the recorder service; `ACTION_SEND` content URIs; the secondary-tile path) and
whether each is named for the adversarial review the build prompt requires; Rule 16; the parallel-build dependency facts
above; whether each R11 value is applied as R11 states it (section, value, tolerance, confidence); anything the doc
leaves for the builder to guess.

**Testability / evidence integrity:** every acceptance row and edge case — can it fail? does it assert what it claims
(no vacuous pass: text read off a parent, absence read from the wrong ring or slice, a check that cannot see the failure)?
can it run on this AVD with the commands it names (check them read-only on emulator-5556)? does each helper it calls
exist, and if not, which task owns it and is that task in THIS phase or another? restore steps (RV12)? clocks (the
`[motion]` / `[timer]` / `[stopwatch]` lines vs the host)? every approximation covered by an H row? any self-grading
(expectations from the app's own engine)? rows that depend on phase 11 / 12 code (fact 2).

## Output — write it to a file, then reply with only its path and your counts

Reviewer 1 → `docs/plan/review/2026-09-23-phase15-r3-design.md`; Reviewer 2 →
`docs/plan/review/2026-09-23-phase15-r3-testability.md`. (Writing that one file is the only write you may make.)

Per finding: an id (`D1…` for Reviewer 1, `V1…` for Reviewer 2), severity — **BLOCKING** (the phase cannot be built or
gated as written, or a row passes when the product is wrong), **SHOULD-FIX** (wrong or ambiguous text a builder could
misread; a row weaker than it claims), **NOTE** (small) — the doc line(s), the evidence (quote the doc, the code at
`file:line`, the emulator output), and the smallest fix as replacement text or an exact instruction. No praise, no
summaries of what is fine. End with `BLOCKING: n · SHOULD-FIX: n · NOTE: n`.
