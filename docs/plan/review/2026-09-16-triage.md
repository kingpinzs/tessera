# Review round 1 triage (2026-09-16)
> SUPERSEDED IN PART by review/2026-09-16-r2-triage.md and PLAN.md rulings Q12-Q14 / RV3-RV5 / RV9-RV10 (codex R3R2-01).
> Rows marked [SUPERSEDED] below keep their history; PLAN.md governs.
Inputs: review/2026-09-16-fable.md (B1-B8, M1-M12, m1-m9) and review/2026-09-16-codex.md (R2-01..R2-16).
Dispositions:
- ASK = plan-level gap, asked of Jeremy before the split
- AGENT = resolved by design, recorded in PLAN.md "Review resolutions"; Jeremy can overrule
- PHASE-N = asked in phase N's Stage A step 4 interview
- FACT = a verified limit, added to the plan text

## ASK (before the split, one at a time)
| Q | Findings | Gap |
|---|---|---|
| Q9 | B2, R2-03 | Phase 1 "animated live tiles" has no real data until phase 3. A fake feed breaks Rule 4 and a throwaway engine breaks Rule 16. Where does the tile engine first land, and on which real feed? |
| Q10 | B8, R2-02 | 24 R1 values are NOT FOUND (flip/peek timings, gutters, launch motion, Cortana ring). Where do those numbers come from, and what is the acceptance baseline? |
| Q11 | B1, R2-01, M1 | Scaling: Lumia 950 proportions vs 950 physical size. Sets the design tokens for every phase. |

## AGENT resolutions (recorded in PLAN.md)
| Findings | Resolution |
|---|---|
| B3, R2-15 | Settings is not a phase. Phase 1 creates a permanent W10M Settings hub (Start + theme page); each later phase adds exactly its own page. |
| B6, M9 | Phase 1 creates a permanent onboarding/health checklist (HOME role + the status of each grant + service liveness); each later phase adds its own rows. Install path = adb (USB or Wireless debugging): the dev loop already uses it, and it avoids Android 13+ restricted settings for sideloaded APKs. |
| B5, R2-04, R2-05 | [STT wording SUPERSEDED by amended RV3: grammar pass + open pass on the same runtime is allowed] Rule 16 guard: the split names ONE permanent engine per part at the phase that introduces it (STT, TTS, keyboard route, motion). R2's "v1 -> v2" swap suggestions are rejected. The Cortana STT must be open-vocabulary from its first phase (phase 3 after the Q9 re-cut), with commands matched on the transcribed text; a grammar-locked recognizer can't feed the later LLM. Unmatched utterances go to a permanent "not understood" handler, and the LLM layer adds itself there. |
| M10, R2-05 | One APK holds every part (launcher, settings, overlays, IME, dialer, SMS, assistant). No part depends on a separately installed app. The keyboard is a shell-owned IME; reusing Apache-2.0 FlorisBoard code inside it is allowed. |
| M2 | Motion primitives are written in-house. No motion dependency; MangoTile at most for list/pivot controls. Values follow RV9 (round 2): the legacy WP7/8 tilt/turnstile numbers are candidates until R3 corroborates them against W10M footage. |
| M1 | Tile size = f(column count, canvas width, gutter, edge), never a constant. |
| R2-08 | Cortana commands permanently act through real Android intents and apps. Later shell parts (dialer, SMS) register as those intents' handlers, so no command is rebuilt. |
| R2-07, M11 | Every phase doc gets two QA lists, "emulator" and "phone-only" (S25 Ultra), plus NEEDS-HUMAN rows. Assumed: the S25 Ultra with its SIM is available for the phone-only rows. |
| m2 | compileSdk/targetSdk 36; the OS version gets confirmed by adb on first phone connect. |
| m6 | The Cortana tile belongs to the Cortana commands phase (phase 3 after the Q9 re-cut), not phase 1. |
| M12 | Start-layout persistence + export/import file + app uninstall/update handling are added to the feature list, in phase 2 (edit mode). |

## FACT (added to plan text)
| Findings | Fact |
|---|---|
| M3 | The recents/app switcher stays Samsung's. |
| B7, R2-12 | [SUPERSEDED by PLAN.md Android limits / N-09: under BOTH nav modes, plus the system splash] Under gesture nav, the return-to-Start animation is drawn by Samsung's launcher module. Ours: tiles leaving Start + the requested app-entry animation. The return animation is a NEEDS-HUMAN accept row. |
| m1 | The "older note" launcher-only limits in PLAN.md are superseded. |
| M8 | Side-key assistant registration is phone-only QA; the fallback trigger is the assist gesture. |

## PHASE-N interview topics (renumbered 2026-09-16 after Q9 merged old phase 3 into phase 1)
| Phase | Topics (finding) |
|---|---|
| 1 Start screen + live tiles | (+ round 2: N-01 liveness edge cases, RV10 density/font QA rows, N-09 transition NEEDS-HUMAN rows, N-11 weather tap target + location/units, N-12 pivot/jump-grid vendor-vs-in-house) navigation mode as QA precondition (B7); system bars on Start: hide + draw W10M ones? (M4); light/dark theme (m3); column default (B1 part 3); Secure Folder apps in app list (m5); notification semantics: active notifications vs badge counts, empty/grouped/dismissed/redacted (R2-09); real-data seeding for QA (R2-10); weather provider, e.g. Open-Meteo, no key (m7) |
| 2 Edit mode | layout export/import format (M12) |
| 3 Cortana commands | (+ round 2: N-01 liveness, N-08 settings toggles follow Q13, helper introduced here if commands include toggles) "Hey Cortana" wake word yes/no (B5); command list (R2-08); open-vocab STT + TTS engine pick (B5); model provisioning: adb push vs first-run download (R2-11); W10M sounds vs stock (m4) |
| 4 Action center + volume | helper per-toggle matrix + R4 spike result (Q13; replaces ~~Shizuku yes/no (B4, R2-06)~~); shade replaced everywhere vs Start only (M5); volume QA matrix (R2-13) |
| 5 Keyboard | IME on keyguard edge case (m9) |
| 6 Dialer + SMS | (+ round 2: N-10 second phone number for real call/SMS QA) RCS loss accept (M7); OpenDialer toolkit check (m8) |
| 7 Glance | overlay-kept-on vs keep Samsung AOD (M6, R2-14) |
| 8 Cortana LLM | S25U measurements + thresholds (R2-16); model delivery vs A11 internet rule (m7) |

Q9 ruled 2026-09-16: C (all live tiles in phase 1). This resolves B2 / R2-03: the engine lands together with every real feed.

## Review round 2
Runs after Q9-Q11 are ruled and PLAN.md is updated. Cap: 3 rounds.
