BLOCKING: 0 · SHOULD-FIX: 0 · NOTE: 3

# Phase 11 re-judge, round 3: Reviewer 2 (testability and evidence), 2026-09-24

Scope: pass 7 (`qa/phase-11/SUITE.txt`, the row directories, UNIT.txt, UNIT-results/), the driver fixes in 9805f5e with the
smokes and probes kept beside them (EDGE-smoke3-round2, EDGE-callsmoke, EDGE2-probe, FLING-probe), the docs commits
7f46c4a1 and 2783ef71, and my round-2 findings EV-1 to EV-15. This review was read-only. I did not run adb or an emulator, did
not touch metro-launcher-p15, and pushed nothing. Locally I ran `git show`, `git diff`, `git hash-object`, and `python3` over
the committed dumps, rings, unit XMLs and PNGs. I treated every README, INDEX and doc line as a claim to check, not as
evidence. Paths are relative to `docs/plan/` unless they start with `app/`.

**Verdict.** The phase 11 gate and the L11-1 fix's own gate can be called passed as evidenced. What remains is Jeremy's:
H1-H11, the phone rows, and the two readings. Each of the three findings below is an assertion that cannot fail, sitting
next to a check that can. None of them changes a result.

## Findings

id | sev | file:line | finding | evidence | exact fix
---|---|---|---|---|---
EV3-1 | NOTE | qa/phase-11/scripts/edge.sh:111 · EDGE/EDGE.txt:39, :42-43 · index EDGE.txt:166 | **The fling sub-step's "no burst" cannot fail, because the press lands on a folder.** A folder never bursts, and it logs `no burst on`, which the check (`[quick] burst on`) does not match. The bullet still stands on "no hold" (edge.sh:110), the travel bounds and the positive control, and each of those can fail. | The press point (540,900) is on `tile:folder:qa` (EDGE.txt:42). The positive control's own ring slice reads `[edit] hold 783ms on folder:qa: edit mode on`, then `[quick] no burst on folder:qa: folder` (EDGE/ring-launcher.txt:340-341). The same pattern was EV-12 (1) in round 2. | Change the check to `assert_absent "no burst decision (neither burst on nor no burst on)" "burst on" "$S"`. The fixed string `burst on` matches both `[quick] burst on` and `[quick] no burst on`, and a folder's hold logs the second, so the check can then fail. As an alternative, aim the press at a tile that has shortcuts.
EV3-2 | NOTE | qa/phase-11/scripts/edge.sh:466, :478 · EDGE/EDGE.txt:156-157 | **RV10 font13's "the change applied" passes on the settings read-back alone.** The text searched is `"$rb $(cfg)"` = `font_scale=1.3 1.3 450dpi`, and the target `1.3 ` already occurs in `font_scale=1.3 ` (the read-back plus the joining space). So the check does not need the global configuration to change. EDGE.txt:156 records the global configuration at `1.3 450dpi`, so pass 7's result stands. d560 is sound: `560dpi` occurs only in `cfg` (EDGE.txt:152-153). No in-app positive control is possible: Scale.kt:44 builds `Density(Scale.pxPerEpx(context), fontScale = 1f)`, so by design nothing in the shell scales with density or font scale. The global configuration is therefore the right proof. | EDGE.txt:156 `RV10 font13 read back: font_scale=1.3 · global configuration: 1.3 450dpi` · app/src/main/kotlin/app/tileshell/ui/tokens/Scale.kt:17, :44 | For the `d*` and `font*` tags, assert against the global configuration alone: `assert_contains "RV10 $tag: the global configuration shows it" "$want" "$(cfg)"`. Keep the read-back as a note. size720 needs no change, because dividing its dump by 720 already checks that it applied (0.50 epx).
EV3-3 | NOTE | qa/phase-11/scripts/e13.sh:117-121 · E13/E13.txt:49-55 · E13/ring-launcher.txt:568-601 | **For E13's press_none, nothing in the files shows the press arrived.** Its four checks all hold when no touch reaches the app: the held tile EQUAL to rest, no burst, no hold, nothing launched. EDGE-smoke1 showed this failure mode for raw sendevent touches. For tilt and p4, the DIFF over the tile proves arrival (E13.txt:59-60, :67-68). The ring logs no touches, so its silence between the NONE style change (:568, 17:48:57.545) and the c6 restart (:601) cannot tell the two cases apart. The row was already shaped this way in pass 6. 9805f5e changed only the tile the press lands on. | E13.txt:49-55 · ring :568, :573, :601 | Add the fling sub-step's positive control after each style's press: once c6 has run, send the same `mt` DOWN at (px,py), `sleep 1.0`, then UP. Assert `[edit] hold 783ms on app:app.tileshell.testclient.b/app.tileshell.testclient.VerbActivity:0` in a fresh slice, then run c6 again. That shows the mechanism reaches that tile under that style, so the absence checks can fail.

## Round-2 findings

id | status | evidence
---|---|---
EV-1 (BLOCKING) | **resolved** | **(a) The keyguard.** EDGE.txt:71-72 reads `isKeyguardShowing=true` after the wake and `false` after the PIN. keyguard.png shows the lock screen (padlock, clock, notification stack). EDGE2-probe.txt:12 reads `false` at rest, so the flag varies; it is not stuck at true. :70 records the lock screen back to `disabled=true` after `clear`.<br>**(b) The fling.** The control travels 865 px (EDGE.txt:32-33), above the 500 threshold and below the page end of 995 (FLING-probe.txt: the 40-ms and 80-ms swipes both stop at 995). With the press it travels 650, inside the bounds 440 < 650 < 835 (:36-37). I recomputed both travels from folder:qa's unclipped tops: 1507 → 642 in fling-control-before/after.xml, and 1507 → 857 in fling-before/after.xml. The positive control holds on the tile under the point (:42-43; ring :340). The numbers reproduce: smoke3 849 / 680 (EDGE-smoke3-round2/EDGE.txt:32-37), EDGE2-probe 849 / 760.<br>**(c) The second finger.** The control closes the burst (EDGE.txt:52). Between `burst on` (ring :452) and that `tap elsewhere` (:466), the ring has no close.<br>**Wording.** The "every bullet" claim is gone: the doc reads "all 42, indexed; two readings wait on Jeremy" (:687), and README:32 and INDEX:59 say "all 42". |
EV-2 | **resolved** | L11-1.txt:52 asserts `from=music faces=0` after the forget. uninstall-sequence.txt:4-6 shows the forget at 16.656, then the null publish and `[music] forgot …` at the same ms (16.671). L11-1.txt:53 and README:31 say the race window was not produced. The other MUSIC-null writers (MusicFeed.kt:167-168, the session-moved path, and :214-216, `clear()`) log no `[music] forgot`. So the same-ms pairing ties this line to 9f790b7's `forget()` (MusicFeed.kt:201). |
EV-3 | **resolved** | The ring assertion reads 1 for each theme (EDGE.txt:119, :123; ring :1640 `theme=LIGHT`, :1722 `theme=DARK`, a real change each time). The page ground is 223 (Light) and 0 (Dark). The label rect [145,228][323,276] lies wholly over the dimmed People tile in both captures (I cropped theme-light.png and theme-dark.png). In Light, the ink min 0 is below the tile's 122 − 60. In Dark, the ink max 255 is above 51 + 120. A wrong-colour label would fail either way. |
EV-4 | **resolved** | (i) The new sub-step passes: EDGE.txt:54-60. The ring shows `burst closed: tap elsewhere` (:551) and then `[edit] tap on the held tile …: exit` (:556), with no `[edit] hold` between. The disc bounds `661 443 754 536` are unchanged and non-empty. (ii)-(iv) The index lines are re-worded (EDGE.txt:176, :178, :181, :185). |
EV-5 | **resolved** (see EV3-2) | Each change is read back and the global configuration noted: 450 → 560dpi and 1.0 → 1.3 (EDGE.txt:147-159). By design no in-app control exists (Scale.kt:44). |
EV-6 | **resolved** | EDGE.txt:113 asserts `qa_blank: startShortcut ok`. The box-size check is dropped. labels.png shows "An unusu…". |
EV-7 | **resolved** | EDGE.txt:28. The open's rest set lands at 36.451 (ring :140-143), before the MARK. Flips follow at :145, :150 and :155, with no second rest set before `stop` (:162). |
EV-8 | **resolved** | EDGE.txt:82. The killed process's `burst on` (18:08:45.493, ring :983) is kept before the new `process start` (:992). |
EV-9 | **resolved** | e6.sh requires `entry_ms` not `none`, and e6_frames.py prints `window_gaps 0` with no entry. Attempt 1 was rejected (max gap 19.2); attempt 2 was accepted with `entry_ms 1876.0`, 19 gaps and max 17.0 (E6.txt:53-58). |
EV-10 | **resolved** | E14.txt:12-29 counts only the 17 gate rows by name (17 files). All 25 patterns are ≥ 1. |
EV-11 | **resolved** | All 62 XMLs in UNIT-results/ carry `timestamp="2026-09-24T23:14:3x Z"` (17:14:36-37 local). That is after the suite's start at 17:14:33 (SUITE.txt:1), so they are this run's own. There is no build cache and no `ignoreFailures`. I recomputed 651 / 0 / 0. L11-1.txt:15 prints the XML's own timestamp. |
EV-12 | **resolved** (see EV3-3) | The press is on tileclient-b's tile, which has a shortcut (E13 ring :12-13, `onShortcutsChanged … n=1`). A hold there would log `burst on`, so "no burst" can fail. The label says what t1 − t0 measures (E13.txt:49, :57, :65). |
EV-13 | **resolved** | EDGE.txt:134 and :140 assert the stored values 9.920635E-4 and 0.9980159. |
EV-14 | **resolved** | INDEX:88 records the three corrections (42 cases, round 1's 7 NOTEs, the cold-jank figures). The older lines are kept, as an append-only log. The round-1 triage is corrected (2981b06). E10's title is fixed (E10.txt:2). e6.sh's comment no longer says "throughout". |
EV-15 | **resolved** (code; not exercised) | run_all.sh:28-33 folds the `unit:` line into the appended verdict. The regex matches SUITE.txt:4's format. run_all.sh:21 moves a replaced row aside. Pass 7 had no appended section, so neither path ran. |

## What I checked and found right

- **One APK.** All 18 row logs read `apk installed 50129ec4bf4de7f8`, `apk match yes`. `apk built` is the same file's sha256 (`736ce3543e0caf05`), and both hashes match the APK on disk. Its mtime is 13:44:46, unchanged from round 2, where the classes5.dex check placed 9f790b7 in it. `git diff 9f790b7 HEAD -- app testapps` is empty, and SUITE.txt:2 reads 0 uncommitted files under app/.
- **One driver set.** Every row's `driver … blob` equals `git rev-parse 9805f5e4:<driver>`, all 18 including E14, and lib.sh is `f296b3d`. `git diff 9805f5e4 HEAD -- scripts` is empty, and the working tree is clean apart from an untracked `__pycache__`. q.sh, e6_frames.py and edge_index.tsv are not stamped per row, but their mtimes (11:04, 17:01, 17:01) predate the run (17:14:33). 7f46c4a1 was committed mid-run (17:47, during E13) and touched no driver. SUITE-pass6-f8cd00a.txt is byte-identical to b0311b3's SUITE.txt.
- **Exit codes and order.** All 18 rows exit 0 (SUITE.txt:6-23), unit exits 0, and there is no appended section. E14 started at 18:14:59, when EDGE finished (18:05:03 + 596 s). Its union holds the gate rows only (E14.txt:12-29). No row log carries a `fresh_burst: no burst`, `could not get back`, `(dump failed)` or `found=no` line, and no console carries a traceback or shell error.
- **The EDGE sub-steps 9805f5e changed.** The flip's rest wait is not asserted, but if the rest set never came, the only effect is a failure of the re-rest check, which is the safe direction. **Keyguard:** the burst's `stop` (ring :731, 18:07:52.706) precedes the wake, so the close itself is screen-off's. What this sub-step adds is the keyguard really showing and no burst after the unlock (keyguard-after.xml). That is the shape round 2's fix asked for; whether it matches the bullet's meaning is the design lens's call. **Call:** the first try answered (EDGE.txt:65-68; EDGE-callsmoke 7/7). **Process death:** EDGE.txt:82-85. **Still press in edit mode:** Tess's centre is asserted clear of every satellite. `…opened no second burst` counts `burst on`, which would also match a `no burst on shell:cortana` line, so it can fail on a tile with no shortcuts. **X5:** EDGE.txt:130-143; pass 7's x5-100.png shows the same glyphs through One and Two as INDEX:88 and README:91 describe.
- **L11-1.** TileSourcePrecedenceTest reads 12 / 0 / 0 from this run's XML (L11-1.txt:15-19). (a), (b), (c) and (e) keep their positive controls (L11-1.txt:25-27, :36-38, :67-69, :89-94). (d) keeps one shell pid (14193) from the uninstall to the read, with "Music Player" and no "An Ending" (:56-59).
- **E6.** It asserts five warm opens: peak 100, overshoot 6.5, maxGap 16.7, settle 266 ≤ 266.8, and t0 − entry 16-17 ms (E6.txt:17-51).
- **E13.** Phase 02 E7 exits 1 because of its motion sub-rows, which are excluded by T11-6 and recorded as such in README:27. Its bracket and geometry sub-rows pass (E13.txt:21-25).
- **The docs.** The doc's QA evidence table (:660-694) matches SUITE.txt row for row. 2783ef71 touches only that section (hunks at :659 and :679). The Edge Cases text is unchanged. INDEX row 11 (:59) and README:13-32 carry pass 7's counts. The README's EDGE row lists the sub-steps without the new still press in edit mode, but the index maps it (EDGE.txt:167).
