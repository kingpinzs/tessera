# Phase 13 gate — Reviewer B (testability and evidence, adversarial), 2026-09-26

Scope: the brief `review/2026-09-26-phase13-gate-brief.md`. Read: the FINAL doc (`phase-13-fluent-materials.md`), the
INDEX Change Log lines naming PHASE 13 (2026-09-25 / 2026-09-26), ledger rows L13-1 / L13-2 and row 13,
`qa/phase-13/README.md`, every row log and its kept earlier runs, the drivers in `qa/phase-13/scripts/` (with `lib.sh`,
`p13.sh`, `edge.py`, `dumpq.py`, `acrylic_expect.py`, `acrylic_check.py`, `lights.py`, `motion_frames.py`), phase 03's
E15 logs, phase 05's frame-spacing rule. Nothing was run on the emulator. Host-side re-derivations over saved captures
are marked "(re-derived)". Default verdict per row: not proven until a captured line shows it.

## The three builds — the Change Log's claim checked

`git diff --name-only f1b05c0b 3448c92f -- app/` = `ui/fluent/StaticBackdrop.kt` only. `git diff --name-only c73c32cf
3448c92f -- app/` = `applist/AppListMenu.kt`, `applist/AppListPage.kt`, `music/MusicCollectionPage.kt`,
`ui/components/ModalOverlay.kt`, `ui/fluent/StaticBackdrop.kt`, `test/.../ModalOverlayTest.kt`. No reminder-menu,
pane or show-line code, so E3 / E5 / E6 on 15aa7f5f stand. 3448c92f changes only when the layer exists (`LaunchedEffect`
keyed on `state.on`, drop when off) and which form the form line reports, so E7 / E8 / E9 on 120e52ec stand (E7's
off-presses compare D to the same-pixel U; E8 sets no picture; E9 records). The claim holds. E12 asserts the same list
itself (E12/E12.txt:23).

## Acceptance rows

| Row | Verdict | Evidence (file:line) | Notes |
|---|---|---|---|
| unit | PASS 11/11 | `app/build/test-results/testDebugUnitTest/TEST-app.tileshell.ui.fluent.FluentMaterialTest.xml` (tests="11" failures="0", 06:05:47Z) | Rule on all 8 combinations incl. low-RAM precedence. Build artefact, not kept under qa/ (N14). |
| E1 | PASS | E1/E1.txt:12 start; :17-22 saver (precondition, line 15 ms after BS_MARK, sharp 0.8 px, 0 / 51); :23-26 saver off; :28-34 switch off (110 ms, sharp, 0 / 51); :36-37 on; :38-40 disable_window_blurs | Every step's own slice kept (slice-*.txt); slice-6 holds a line, so its absence check read a live ring. |
| E2 | PASS | E2/E2.txt:13 Start sharp; :14-16 tag inside page; :19 130.2 px; :20 profile worst 2.7 vs host oracle; :21-22 text sharp; :23-26 off; :28-37 720-wide 94.4 px | Off pass uses a pref write, not E1's control (N9). |
| E3 | PASS | E3/E3.txt:15-17 fixture; :21 show line; :23 held row (63,68,64) in the closed capture; :24-28 precondition patch + (40,40,40); :29-30 border; :33 overlap asserted; :39 profile worst 1.4; :43 sharp outside; :44-49 off (40,40,40) | Oracle from the post-dismiss capture, never the judged one. (1) cannot discriminate by design (Q1 A); (2) does. |
| E4 | PASS | E4/E4.txt:17-18 tag + show line; :22 profile 1.0; :24 patches 0.8; :25 noise 3.12; :26 text sharp; :29 off 0; :37-38; :41-44 square under the menu, worst 2.1; :46 held square sharp; :49-50 off; :58-63 now-playing; :66 off | (1) discriminates (re-derived: expected 1.9–7.9 over 671 columns; a flat band fails by ≥ 6.4). (3) proves the form only (N7). |
| E5 | PASS | E5/E5.txt:16-22 (a); :24-28 persona under the pane and pulsing; :30-35 (e2) live; :38-42 (e3) off identical; :50-53 (b) title, never (14,19,13); :54-58 (c) (17,23,15); :60-63 (d) off | Live re-cut is weaker than needed; the doc's own ≥ 2 clause is met on the kept captures (N1). |
| E6 | PASS | E6/E6.txt:15-17 precondition + σ 3.21; :19 identical 1 s apart; :22-23 off σ 0 | Identity needs a redraw between captures; E5 (e2) supplies it (N8). |
| E7 | PASS | E7/E7.txt:21-33 (a); :37-49 (b); :51-60 off; :66-84 (d) incl. :78 not pinned; :89-108 (e) incl. :101-102; :113-161 (c) tilt / p4 / off; :164-165 restore + C-3; :167-172 controls | Re-derived margins: a missing light would miss by 11–77 levels at every sampled point (e.g. (a) ring 108 vs U 45; (b) ring (77,161,227) vs accent). `pin to Start` is a real product line (AppListPage.kt:270; E11/2-slice-pin.txt:2), so (d)'s absence check can fail. |
| E8 | **FAIL** (corroboration clause) | shell clock PASS: E8/E8.txt:15-53; corroboration: :54-57 and E8/corroboration-rerun.txt:2-5 | See B2. |
| E9 | recorded only (4 facts) | E9/E9.txt:12-18 | Workload not evidenced (N5). |
| E10 | PASS | E10/E10.txt:14-18 toggle survives force-stop; :30 rebuilt line; :32 C − B 9466 KB; :36 A2 − A 1549 KB; :37 one pid | Controls faithful; one vacuous precondition and a run mislabel (N4). |
| E11 | PASS (under the 2026-09-26 amendment) | E11/E11.txt:13-46 + 1-e15-compare.txt (no REGRESSED, no FAIL-BOTH-MOVED, no NEW); :47-59 Pin; :61-73 MUSIC8; :74-93 E12 / E19; :94 exports rc 0 | Before / after reads the same e15.sh blob (92ae4883) on both builds. Weaker than the letter (N2); gaps against E15's words (N3). |
| E11_MENU | PASS | E11_MENU/E11_MENU.txt:15-26 (243.33 × 107.0 epx, 23.33 epx, no `cortana_card:delete_confirm`, 3 → 2 rows, the held row gone) | The card tag exists (Card.kt DELETE_CONFIRM → ResponseCardView.kt:95), and the row-count check can fail. |
| E12 | PASS | E12/E12.txt:12-23 builds + diff; :24-43 five surfaces' show lines in their own saved slices; :44-60 E1's reasons in step order | |
| E13 | PASS | E13/E13.txt:15 control; :16 file gone; :17-19 failed line for the checker's URI, `(fallback)`, no rebuilt line (slice-new-process.txt: `FileNotFoundException ... ENOENT`); :21-23 (0,0,0), no edge | Strip re-cut faithful (see below). |
| BS_STEP | PASS | BS_STEP/BS_STEP.txt:14 rebuilt for this URI; :16-21 128.8 px, σ 50.25, plateaus 2.0 / 51.6 | ±20 % band is loose (N10). |
| phase 03 E15 | 21 / 6 (dependency) | ../phase-03/E15/E15.txt (apk db078d6a) vs E15-run2-a437acd5 | Used by E11 (1) only. |

## Edge cases

| Case | Verdict | Evidence (file:line) | Notes |
|---|---|---|---|
| Battery saver while a menu is open | PASS | EDGE_SAVER_MENU/EDGE_SAVER_MENU.txt:15 precondition; :17-19 form=fallback 22 ms after acrylic=off; :20 (40,40,40) recorded; :21; :23-24 toggled back from its own MARK | |
| File deleted under the shell (E13) | PASS | E13/E13.txt:16-23 | |
| Toggle changed while the app list shows (E1) | PASS | E1/E1.txt:28-34 (Settings over the app list, Back, fallback read), :36-40 (blurred again) | |
| Background changed / removed while the app list shows | PASS | EDGE_BG_CHANGE/EDGE_BG_CHANGE.txt:18-25 changed (rebuilt for media 149, 0.2 × 128, no edge); :27-30 removed (no rebuilt line, key gone, (0,0,0)) | |
| Huge 8000 × 8000 | PASS | EDGE_HUGE/EDGE_HUGE.txt:17 set; :20-21 rebuilt, 87 ms; :23 C − B 9258 KB; :24 pid | B itself is 212 MB (N13). |
| No background image | PASS | EDGE_NO_IMAGE/EDGE_NO_IMAGE.txt:13-16; ring-launcher.txt:6-71 | The asserted slice misses the process start; the saved ring covers it (N6). |
| Light theme, bright wallpaper | PASS | EDGE_LIGHT/EDGE_LIGHT.txt:15-21 | Tint recorded, not asserted (N11). |
| Edit mode | PASS | EDGE_EDIT_MODE/EDGE_EDIT_MODE.txt:14-21 | |
| Burst | dropped (T11-8) | — | |
| Surface over an acrylic surface (E4 (1)) | PASS | E4/E4.txt:22-25 | |
| Screen off, then wake | PASS | EDGE_SCREEN_OFF/EDGE_SCREEN_OFF.txt:19-28 (Asleep → Awake; patches 87.5 / 82.1 / 88.4 unchanged; σ 3.22) | |
| Process death | PASS | EDGE_PROCESS_DEATH/EDGE_PROCESS_DEATH.txt:15 band open; :17-22 (kill -9, new pid, no menu, switch Off survived) | |
| RV10 | PASS | EDGE_RV10/EDGE_RV10.txt:13-45 (1440 wide 170.1 px at r 120; d420 / d560 / font 1.3 unchanged; band 84.0 epx); 720 × 1560 = E2/E2.txt:28-37 | |
| disable_window_blurs (E1) | PASS | E1/E1.txt:38-40 | |
| Low-RAM (unit) | PASS | FluentMaterialTest `ruleAndPrecedenceOnAllEightCombinations` | |
| Accessibility | PASS (asserted parts) | EDGE_A11Y/EDGE_A11Y.txt:13-14 Remove animations; :15-22 high text / inversion set, captured, restored | The inversion capture does not show inversion (N11). |
| Rapid open / close | **FAIL** (not proven) | EDGE_RAPID/EDGE_RAPID.txt:13-18 | See B1. |
| 300+ packages | PASS | EDGE_MANY_APPS/EDGE_MANY_APPS.txt:15-18; gfxinfo-scroll.txt | Re-derived: the list did scroll (84 601 px changed in x 150..900) while the right margin stayed identical (max 0, spanning the checker's 0.4–52 levels). |

## The driver re-cuts this session

- **E10's two measurement controls** — faithful. The checker pushed before A and A taking the Settings route first make
  A and A2 differ only in the Start background and the switch; a leaked layer (≈ 9.3 MB, the measured C − B) would
  still fail the 2 MB bound. The Photos precondition is asserted vacuously (N4).
- **E11 (1) before / after** — faithful to the intent (no phase-13 regression), weaker than the letter ("each passing
  with its own numbers"); an explicit agent-call amendment (N2). E11_MENU fills the menu steps E15 never reached.
- **E13's strip** — faithful: the doc says "E2's strip", which is x 675..945; the full width read row icons.
- **motion_frames.py burst window** — the first cut was wrong and the burst idea is right, but the conclusion drawn from
  it weakens the doc (B2): the tool's own safeguard (compare window_ms with the [motion] settle) is not implemented by
  any caller, its 0.5-level threshold truncates the motion's tail (windows 135.2 / 215.3 ms against settles 243 /
  250 ms), and a rejected capture is not retaken.
- **BS_STEP's MARK** — faithful; the rebuilt-line match names this run's URI, so a stale line cannot satisfy it.
- **E5's live threshold (2026-09-25)** — the controls are sound; the re-cut was unnecessary on the kept captures (N1).

## Findings

### BLOCKING

**B1 — EDGE_RAPID: no captured line shows that any of the 10 holds opened a menu.** `scripts/edge.sh` `rapid)` runs
`input swipe ... 850` + Back ten times and asserts only the host time and PSS (EDGE_RAPID.txt:15, :17). It never asserts
a band opened, and the loop's ring is lost: `clear_background` force-stops before `row_end`'s `ring_save`, so
EDGE_RAPID/ring-launcher.txt starts at the restart (`[app] process start` 00:50:49) and holds 0
`[fluent] applist_menu source=live` and 0 `[motion] applist_menu` lines. The holds are 850 ms against the band's 783-ms
threshold (E8's `[motion] applist_menu`), so the band probably opened, but "PSS returns within 2 MB of C" passes
equally if no live layer was ever allocated: a precondition never asserted. Fix (driver only): MARK before the loop,
save `ring_since MARK` before `clear_background`, assert 10 `[fluent] applist_menu source=live` lines in it, re-run the
one case.

**B2 — E8's corroboration clause is unmet, and the Change Log reads C-5 backwards.** The doc (E8, :460-462; preamble
C-5, :323-327) has a screenrecord of each motion corroborate "under phase 05's frame-spacing rule", and phase 05's rule
is "a capture that fails it is rejected and retaken" (qa/phase-05/README.md:41-43). All three animated captures fail it
(corroboration-rerun.txt:2-4: pane 18.4, reminder menu 36.6, pivot 18.9 ms). None is retaken: `e8.sh:97-108` takes one
recording per motion and only records it. The fourth, the app-list band, has window_frames=1, a jump with nothing to
corroborate. The 2026-09-26 "DRIVER FIXES" line says the `[motion]` lines therefore "stand alone, as C-5 has it". C-5
says the opposite, and that line is framed as a driver fix, not an amendment of the doc.

The rejected reminder-menu capture is also not neutral. Re-derived from `E8/rec-reminder_menu.mp4`, the grow's first two
source frames are 37 ms apart (1405 → 1442 ms), above C-31's 33.4 ms, while the shell's line reads `maxGapMs=17`. That is
exactly the disagreement corroboration exists to settle, and it went unexamined.

The shell-clock clauses themselves pass (E8.txt:15-53). Fix: a retake loop as phase 11 E6 used (retake until the spacing
inside the motion is ≤ 18.2 ms, up to N tries) on db078d6a, with window_ms checked against the `[motion]` settle. Or a
Change Log amendment dropping E8's corroboration on this AVD, for Jeremy to rule. With that amendment, B2 becomes
NON-BLOCKING, and the verdict still rests on B1.

### NON-BLOCKING

- **N1 — E5 live (e2):** the doc's own threshold (patch-mean |Δ| ≥ 2) is met on the kept captures. Re-derived from
  live-e2-1.png / -2.png: max patch-mean |Δ| over the persona box = 2.0 (R). All 90 952 changed pixels lie inside the
  pane (x 279–767, y 473–1011). The driver can assert the doc's clause; the ≥ 1 per-pixel re-cut is weaker than the
  evidence needs. The raw-capture channel order is correct: pane pixel (14.28,19.28,13.28) in both the PNG and the raw.
- **N2 — E11 (1):** two of the six clauses that still fail are on the ≡ pane, a phase-13 surface. They are "Settings sits
  172 epx above the screen bottom" (192 on both builds) and "Choosing Settings…". The amendment turns "passing" into
  "unchanged". It is sound because both read identically before phase 13, but Jeremy should know the pane's E15
  geometry is not met. The Change Log's list of the seven pre-13 failures names "the fade 317 vs 291.5 ms". The clause
  that failed was "black for 283-300 ms" (read 317), and "the page fades up" passed on both builds
  (1-e15-compare.txt:12, :14).
- **N3 — gaps against E15's and E19's words:** E15's "Delete … in one frame 333 ± 33 ms … and `dumpsys alarm` no longer
  lists that reminder's alarm" is checked by neither e15.sh nor e11_menu.sh. E15's pane fill (14,19,13) is not an e15.sh
  clause; E5 (a) covers it. E19's "screencap pixels show the drawn W10M bars" is checked on the app list by dump bounds
  only (e11.sh E19 block).
- **N4 — E10:** e10.sh:72 asserts that the last `[photos]` line contains `photos=`, which is also true of `photos=6`. The
  RECORD (E10.txt:22) shows 7; assert the count. The Change Log's E10 line labels "E10-run2-photos-face-db078d6a
  (4412 KB)", but run2 read 12 675 KB, and 4412 KB is run3 (`E10-run3-settings-code-db078d6a`).
- **N5 — E9:** the workload is not evidenced. The ring was discarded by `clear_background`'s force-stop before
  `row_end` (E9/ring-launcher.txt: 0 reminder_menu show lines, 0 pivot lines), and the fixture's reminder id is never
  asserted (e9.sh:16-17). The row is recorded-only; apply B1's fix.
- **N6 — EDGE_NO_IMAGE:** the absence slice starts after the Home restart (slice.txt has no `[app] process start`), which
  is BS_STEP's MARK-late fault. The saved ring-launcher.txt:6-71 covers the whole process with no rebuilt or failed
  line, so the claim stands. Move the MARK before `clear_background`. "E10's memory figure unchanged" is a RECORD
  pointer (":17 see E10"), not a measurement.
- **N7 — E4 (3):** over now-playing's black page 0.2·B ≤ 4.2 (patches-nowplaying.txt), so the mean clause cannot tell a
  band blurring the page from one blurring nothing. The noise discriminator (:63) proves only the acrylic form. E4 (2)
  proves the same `MusicMenu`'s live backdrop (square edge, expected up to 30.6 in B).
- **N8 — E6:** pixel identity on an idle screen does not show that a frame was drawn between the captures. E5 (e2) shows
  it: the pane re-rendered while the persona pulsed, and its pixels outside the persona's blur footprint did not change.
- **N9 — E2 off pass:** it writes `transparency_effects` with the shell stopped (e2.sh:77-79) instead of E1's control, and
  its MARK is unused. E1 proves the live toggle's fallback on the same strip (E1.txt:32-34).
- **N10 — BS_STEP:** the ±20 % band cannot tell HWUI's σ (52.46 px) from σ = r/2 (45 px → 115 px, inside
  107.6–161.4). The recorded −4.2 % does, and so does E2's 720-wide 94.4 px (r/2 predicts 76.9). The T13-18 check
  (offscreen route = on-screen width) is not recorded as a result; its numbers exist: static 128.8 px (BS_STEP) and live
  133.3 px (E3.txt:40).
- **N11 — EDGE_A11Y / EDGE_LIGHT:** applist-inversion.png is pixel-identical to applist-noanim.png (0 px differ by more
  than 2; re-derived), so H1 has no capture showing the material inverted. The high-text capture differs (137 364 px)
  and is usable. EDGE_LIGHT records the white tint (:16) instead of asserting it; the ≥ 200 patches imply T ≥ ~195.
- **N12 — E7 (c):** the off point is x = −48, off-screen (L − 200 with L = 152, E7.txt:116). A = U holds whether the
  press ended by leaving or by cancel, which the doc treats alike, so the outcome is proven but the "left the item" path
  specifically is not.
- **N13 — EDGE_HUGE:** B = 212 180 KB with the 8000² picture, against E10's B of 99 307 KB. The Photos tile refreshed to
  `photos=7` at the push (EDGE_HUGE/ring-launcher.txt). Phase 13's bound (C − B) holds, but this matters for P2's PSS
  bound and for phase 01's Photos tile.
- **N14 — unit:** the only evidence is `app/build/...` (a rebuild overwrites it). The L13 rows kept theirs under
  qa/phase-13/.
- **N15 — README E8:** "band 6 ms" is the recorded warm-up; the asserted runs read 8, 21 and 9 ms (E8.txt:46-52).
- **N16 — C-3 seeding:** E2 (`start_gap`) and EDGE_EDIT_MODE (holds a Start tile) read Start's grid without
  `layout_restore` of phase 02's baseline and its zero-assigned assert. Low risk, since each needs only a gap or a
  present tile.

## Phone rows and NEEDS-HUMAN: captures attached?

- **P1** — nothing from the AVD is needed; R4's results do not exist yet (information for phase 04). Owed.
- **P2** — no phone capture. `e9.sh` records gfxinfo only. P2 also needs `dumpsys display` before and after, and PSS with
  the layer built, against phase 03 P4's baseline, which is not recorded in qa/phase-03/README.md. Owed; the baseline
  is a prerequisite.
- **P3** — phone only; nothing attached. Owed.
- **P4** — the AVD side is attached: E2/applist-fhd.png, E3/menu-tomorrow.png and menu-photo.png, E4/band.png,
  plmenu.png and npmenu.png, E5/pane-home.png and pane-rem.png. Phone screencaps are owed.
- **H1** — every table surface has an AVD screencap (as P4), plus EDGE_LIGHT/applist.png and
  EDGE_A11Y/applist-hightext.png. The inversion capture does not show inversion (N11).
- **H2** — attached: values (E1/E2 widths, E6 σ 3.21, BS_STEP σ 50.25) and the captures above.
- **H3** — on / off attached (E2 applist-fhd.png / applist-off.png; E1 on-0.png, saver.png). The "wallpaper moving with
  the pivot" half has no capture: E8's rec-pivot.mp4 was recorded with no picture set.
- **H4** — attached: E7 U / D / M / A for (a), (b), (c-tilt), (c-p4), (d), (e); the controls ctl-tile-*.png and
  ctl-row-*.png; explore/lights-*.png.
- **H5** — dumps only (E1/settings-theme.xml has "Effects" / "Transparency effects"); no screencap of the Settings page.
- **H6** — attached: E1/saver.png, EDGE_SAVER_MENU/menu-saver.png.
- **H7** — nothing to attach (no surface left solid).
- **H8** — phone (P3). Owed.
- **H9** — phone (P2, P4). Owed.

VERDICT: FAIL
