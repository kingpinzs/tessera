BLOCKING: 0 · SHOULD-FIX: 2 · NOTE: 5

Phase 11 QA gate — Reviewer 1 (design / correctness of the built code against the FINAL spec). Read-only review of commits
d34d7bb, 4657886, bd2256c, 939c86f, a53a51e, 1950b89, be98994 on main (HEAD 9312f47) against docs/plan/phase-11-tile-quick-actions.md.
Paths are repo-relative; lines are HEAD's.

id | sev | file:line | finding | evidence | exact fix
---|---|---|---|---|---
G-D1 | SHOULD-FIX | app/src/main/kotlin/app/tileshell/start/StartPage.kt:536-539 (and :415, :729-731); app/src/main/kotlin/app/tileshell/start/EditGestures.kt:469 | The prefetch is `scope.async(Dispatchers.IO) { targetOf(); load() }` on `rememberCoroutineScope()` (a plain `Job`, not a supervisor). Any uncaught exception inside that block fails the child and cancels the page's scope for the rest of the composition's life: `scrollLoop`'s `scope.launch { scroll.scrollTo }` (EditGestures.kt:469) silently stops scrolling Start in edit mode, and every later `prefetch` returns an already-cancelled Deferred, so `QuickOpener` (StartPage.kt:728-730) rethrows `CancellationException` and leaves `edit.quick.pending` set — `edit.quick.active` stays true and the next tap in edit mode is eaten as a "burst close" with no line. The `catch (e: Exception) → query failed` at :731 can only run after the scope is already dead, so it is not the safety net the `query failed` reason (Decisions "No burst") promises. `QuickRule.decide` (QuickRules.kt:73-79) wraps only host / quiet / query; not wrapped: `platform.infos.getValue` and `info.shortLabel` (QuickSource.kt:93-94), `Bitmap.createBitmap` / `drawable.draw` (QuickSource.kt:118-129), and the slot resolve behind `entryOf` (StartPage.kt:136-137 → SlotResolver.kt:47-73: unguarded `pm.queryIntentActivities`, `defaultDialerPackage`, `slot.category!!`). | Structured concurrency: a failed `async` child cancels a non-supervisor parent `Job`; `rememberCoroutineScope` creates `Job(parent)`. Reasoned from the code; no device repro (no AVD step throws today — the same reason E14 proves `query failed` on the JVM). | In the async body wrap everything: `try { … } catch (e: CancellationException) { throw e } catch (e: Exception) { QuickLoad.None(NoBurstReason.queryFailed(e)) }` so the Deferred always completes normally (or run the async under `SupervisorJob()` in its own context). Then the opener's :731 catch becomes reachable only for cancellation and the `pending` clean-up at :734-735 always runs. Add a JVM test with a throwing `resolveSlot` through `QuickSource.targetOf`.
G-D2 | SHOULD-FIX | docs/plan/INDEX.md (no diff since 24469b3); docs/plan/phase-11-tile-quick-actions.md:4, :39, :62-65, :110-111, :185, :291-296, :458-459 | Every ADD and correction this build made to a FINAL doc is required to carry an INDEX Change Log line "when built" (T11-39 lists five parts; Q1 A says the phase 01 / 10 ADDs are "each recorded in the INDEX Change Log when built"; the doc header says changes after FINAL go through the Change Log), and the doc's own post-FINAL edits in 9312f47 cite "INDEX Change Log" at :110-111 and :458-459. None exists: `git diff 24469b3..HEAD -- docs/plan/INDEX.md` is empty. Missing lines: the consumed-DOWN fix in phase 02's `EditGestures` (T11-38, d34d7bb); `MusicActivity`'s `EXTRA_PIVOT` + `Role.Tab` selected semantics (phase 10, 939c86f / be98994); `SettingsActivity`'s shortcuts meta-data (phase 01, 939c86f); `AppCatalog.onShortcutsChanged` (phase 02 task 5, bd2256c); the three "corrected 2026-09-23 at build" doc changes (WIDE-tile geometry, `MEDIUM -> SMALL`, ASSISTANT role). | `git diff --stat 24469b3..HEAD -- docs/plan/INDEX.md` → nothing; `git show 9312f47 -- docs/plan/phase-11-tile-quick-actions.md` shows the post-FINAL edits. | Add the dated Change Log lines to docs/plan/INDEX.md (one per item above, naming the phase amended, the commit, and the row that proved it) before the gate closes; the L11-1 finding (README "Findings recorded") belongs there too as a recorded phase 01/10 defect.
G-D3 | NOTE | app/src/main/kotlin/app/tileshell/StartActivity.kt:322 vs :267-268; app/src/main/kotlin/app/tileshell/start/StartPage.kt:669, :672, :901 | A satellite launch sets `exitTappedId = burst.key.id`, but the Start exit's tapped-tile extra fade compares against `tile.model.id`, which for a bottom-row tile is `"dock:" + key.id` (idPrefix at :669). A tile tap on a row tile passes `tile.model.id` (:267-268) and gets the 67-ms extra; a satellite launch from a row tile does not — the held tile fades with its row instead of last. Grid tiles are unaffected (no prefix); band members never take `exitAlpha` (StartPage.kt:1055 passes 1f, phase 02). | Decisions "Satellite tap": "the Start exit (R3 A11) plays as for a tile tap with `exitTappedId` = the held tile's id". | In `launchSatellite` pass the model id the tile was drawn with: `if (burst.key in dockKeys) "dock:" + key.id else key.id` — simplest is to carry the drawn model id on `OpenBurst` at open (StartPage.kt:762 knows `inRow`).
G-D4 | NOTE | app/src/main/kotlin/app/tileshell/StartActivity.kt:329-336; app/src/main/kotlin/app/tileshell/apps/AppCatalog.kt:205-209; app/src/main/kotlin/app/tileshell/applist/NewAppStore.kt:39 | `launchShortcut` runs `LauncherApps.startShortcut` directly and never notifies `AppCatalog`'s launch listeners, so an app first opened through a satellite keeps its "New" caption in the app list (X11 clears it on the first shell launch). The spec's bookkeeping list (UseCounts, pendingRecent, returningFromLaunch) is met; this is `AppCatalog.launch`'s bookkeeping, one layer below `launchApp`. | Decisions "Satellite tap" names `launchApp`'s bookkeeping only; NewAppStore.kt:39 is the one listener. | On the Ok branch call the same listener path (`AppCatalog.notifyLaunched(entry)` for the held tile's `AppEntry`, or have `QuickSource.start` go through a catalog method that notifies).
G-D5 | NOTE | app/src/main/kotlin/app/tileshell/start/QuickRules.kt:178 (line: `t = tile.t - g - s` / `tile.b + g`); app/src/main/kotlin/app/tileshell/start/EditGestures.kt:345-347; app/src/main/kotlin/app/tileshell/start/EditMode.kt (DISC_EPX = 31) | In the LINE arrangements (every bottom-row tile; the top-left tile; a WIDE tile in row 1) the satellites sit one gutter (≈13 px) from the tile's edge while the discs' 31-epx (93-px) boxes centred on the tile's right corners extend 46.5 px past that edge, so a satellite is drawn over ≈33 px of the unpin disc (line above) or the resize disc (line below), and the satellite-first hit rule hands a tap on that covered part to the satellite. The corner arrangement's 16-epx stand-off was chosen to avoid exactly this; the line's "one gutter" was not. Built as the spec words it, so not a divergence — a design consequence for H4. | Decisions "Arrangement" (LINE: "one grid gutter above its top edge"), "Where the burst lives" (satellites tested before the discs); geometry from `QuickRulesTest.aBottomRowTileAlwaysTakesTheLineAboveInsideTheMargins`. | None required; if Jeremy dislikes it at H4, use `standOffPx` (16 epx) instead of `gutterPx` for a line's distance from the tile in `QuickGeometry.line` (one constant) and re-run E7's row / top-left cases.
G-D6 | NOTE | app/src/main/kotlin/app/tileshell/start/QuickBurst.kt:118-125 | A close that lands while the burst is still `pending` writes `[quick] burst closed: <reason>` only if the load has already completed `Ready`, and never writes the held-back `shortcuts for` / `burst on` lines first, so the ring can hold a `burst closed: drag` with no `burst on` before it (the hold-and-drag-as-one-gesture case, phase 02 E1 in E13) — and nothing at all when the load is still running at 783 ms. E13 passes because the load is normally done by the hold; the line is timing-dependent rather than a contract. | Decisions "Harness contracts" lists `burst on` and `burst closed` as the pair; `close()`'s own comment says a ready-but-undrawn hold "IS a burst". | Either write the `shortcuts for` and `burst on` lines from `close()`'s pending path when `ready` (so the pair is always complete), or make the pending path silent and let E13 assert `[edit] drag start` alone; record the choice in the doc's Harness contracts line.
G-D7 | NOTE (reviewer 2's lens) | docs/plan/qa/phase-11/SUITE.txt:5-10; docs/plan/qa/phase-11/README.md:5, :9-26 | The README says the whole gate ran on one APK through `run_all.sh` → SUITE.txt ("FINAL PASS below") and every row cell says "see SUITE.txt", but SUITE.txt (commit 9312f47) records unit + E1–E6 only; E7–E16 have no line in it, and E10 is BLOCKED (L11-1) so the consumed-DOWN fix (d34d7bb) is verified by code reading only (the reasoning holds: TileView.kt:304-310 consumes the control's DOWN in the Main pass, which runs child-first, so the parent's `awaitFirstDown(requireUnconsumed = false)` at EditGestures.kt:74 sees `isConsumed` before the hold race at :84). | File contents at HEAD. | Re-run `run_all.sh` to completion (or append the E7–E16 lines from their per-row logs with the same APK id) so the evidence index's claim matches the file; note E10's status in SUITE.txt explicitly.

## Checked and conforming (item 1 of the brief — no divergence found)

- Selection rule: enabled-only, manifest before dynamic, rank, id, first four — QuickRules.kt:20-23; per-activity query with `setPackage` + `setActivity`, `FLAG_MATCH_MANIFEST or FLAG_MATCH_DYNAMIC`, under the tile's user — QuickSource.kt:149-153; `isDeclaredInManifest` / `rank` / `isEnabled` mapped at :155.
- Tile-kind targets (T11-22): Folder → `folder`, Secondary → `secondary tile`, slot resolved first else `no app`, AppTile via catalog (quiet profile asked anyway, 1950b89), Weather / Settings → fixed components under `Process.myUserHandle()`, cortana → `no shortcuts` unqueried — QuickSource.kt:64-79; Unassigned → `no app` (:67-68).
- Reasons exact (QuickRules.kt:27-36) and close reasons exact (:39-50); host check before quiet before query, everything else caught into `query failed <exception>` — QuickRules.kt:72-83; `shortcuts for <pkg>/<activity>/<userId>: n (k shown: …)` — :85-87, `describe` = `flattenToShortString()/user.hashCode()` (QuickSource.kt:158-159).
- Off-main load from the DOWN, cancelled on a broken hold, edit mode entered without waiting, burst opened in the first frame the result is in hand — EditGestures.kt:87-99, StartPage.kt:534-541, :722-767; held-back lines written only on a hold (QuickSource.kt:39-47, StartPage.kt:736).
- Arrangement decided once against the edit-mode rest rectangle (centre contracted by PITCH_SCALE about the fixed point, size unchanged; dock tile as placed) — StartPage.kt:788-804; page area = below `BarMetrics.STATUS_EPX`, above dock top minus one gutter, inside the grid margins — :773-781; corner / line-above / line-below with clamping, never re-clamped after open — QuickRules.kt:120-202, OpenBurst.restAt QuickBurst.kt:53-58; `off screen` only for grid / band tiles (a53a51e) — StartPage.kt:744-751.
- Spring `spring(0.65f, 1500f)`, alpha = min(1, 2p), scale 0.5→1 on the first half, close = same spring back with alpha 0 at the halfway point and the satellites gone there — QuickRules.kt:209-245, QuickBurst.kt:186-254; `[quick] motion open|close … frames= maxGapMs=` from `withFrameNanos` — QuickBurst.kt:212-213, :251-252; `rest=` once per settle (open spring done, contraction done, bounds still) and again after each scroll — :214-231.
- Tracking: the held tile's slot read outside TileView's own face layer (the `onGloballyPositioned` sits at the end of GridTile's modifier chain, StartPage.kt:977-979, before TileView's own `graphicsLayer`, TileView.kt:281-290), re-read every frame against the `start_page` gesture Box (QuickBurst.kt:88-95, StartPage.kt:546), last bounds kept when the node leaves (:87-91, `isAttached` guarded — no detached-coordinates crash).
- Coordinate spaces: pointer positions, `pageCoords`, the satellites' offsets and the rest rectangles are all local to the same Box (StartPage.kt:543-547, :687-695, QuickBurst.kt:263-296); the pager offset never enters; `Coords` and `quickRestRect` share the fixed point (EditGestures.kt:50-59, StartPage.kt:789-795).
- Hit test: satellite (square ∪ label) before the discs while a burst is open and no drag — EditGestures.kt:343-347, QuickBurst.kt:141-150; acts on the release only if the finger lifted on the same satellite — EditGestures.kt:112-121; a stale burst cannot be launched (`satelliteAt` returns null when closed / closing; `open` re-read at the UP).
- Taps and events while open: satellite runs; disc acts and closes (`unpin` / `resize`) — :313-333; another tile / held tile / empty / folder-name strip → `tap elsewhere` only, selection kept, no `[edit] tap on the held tile` line — :129-167; press that moves on the held tile → `drag` — :102-106, :130-135; empty-space move scrolls with the burst riding — :159; Back closes the burst first — StartActivity.kt:149; Home closes both — :134-136; stop closes at once — :364; `removed` via the layout effect — StartPage.kt:702-705; `shortcuts changed` / `removed` from the one `LauncherApps` callback in `AppCatalog` — AppCatalog.kt:80-84, :97-99, StartActivity.kt:94-101; no hold timer in edit mode (T11-23) — EditGestures.kt:129-146.
- Launch path: `close(LAUNCH, animate = false)` (vanishes in the exit's first frame), `requestExit()`, Start exit with `exitToken`, `startShortcut(pkg, id, satelliteBounds, options with SPLASH_SCREEN_STYLE_SOLID_COLOR, user)` from `pendingLaunch` after the exit — StartActivity.kt:316-324, :237-238, :327-329, QuickSource.kt:135-137; Ok → `returningFromLaunch`, `UseCounts.record(heldTile)`, `pendingRecent` applied in onResume — :330-336, :394-403; the three failure exceptions → `startShortcut failed <exception>`, `returningFromLaunch = false`, entrance at once — QuickRules.kt:253-269, StartActivity.kt:337-344.
- Consumed-DOWN fix (T11-38): `if (down.isConsumed) return` before the hold race, not-in-edit branch only — EditGestures.kt:84.
- Races / leaks: no path exits edit mode with a burst left behind (every `requestExit` caller closes first or is guarded — EditGestures.kt:136-140, :160-164; StartActivity.kt:134-136, :149-151, :318-319 — and `finishExit` → `quick.clear()`, StartEditState.kt:114); the open, tracking, rest and close frame loops all live in `QuickBurstLayer`, which returns before them once `burst` is null (QuickBurst.kt:169), so no frame loop outlives the burst; the shortcuts listener is added in onCreate and removed in onDestroy (StartActivity.kt:107, :369); process death persists nothing (state is in-memory `StartEditState`).
- Promoted / dock / band: the promoted tile is held in its grid cell with the promotion suspended (StartPage.kt:482-485, EditGestures.kt:378-388), its burst opens there against `placements`; a dock tile takes the line above with its rest rectangle as the row places it and is never `off screen`; a band member takes the corner arrangement from its member placement, drawn above the band (StartPage.kt:685-695).
- Scope (Hard Rule 3) and Hard Rule 16: every source change since 24469b3 is inside the doc's In list or T11-39's FINAL-parts allowance (`git diff --stat 24469b3..HEAD -- app/src`: EditGestures / StartPage / StartEditState / QuickBurst / QuickRules / QuickSource; StartActivity's Back, Home and launch path plus the listener lifecycle; AppCatalog's callback; MusicActivity's pivot extra + `Role.Tab` selected semantics; the two activities' shortcuts meta-data, xml, strings and glyph drawables). No press feedback, no acrylic, no "Edit" satellite, no pinned shortcuts, no hooks for phase 13 (satellites draw the plain fill, QuickBurst.kt:277); L11-1 was recorded, not fixed. The one duplication — QuickSource.icon (QuickSource.kt:107-132) re-implements TileIcons.load's monochrome rule (TileIcons.kt:24-48) instead of sharing it — changes no behaviour and is not listed above.
- Phase 01 / 02 / 10 behaviour beyond T11-39: none found. The app list's and Music's hold menus are untouched (E11 is the control row); the only phase 10 change is the pivot extra and the header semantics; the only phase 02 gesture change outside phase 11's branches is the consumed-DOWN return at EditGestures.kt:84.
