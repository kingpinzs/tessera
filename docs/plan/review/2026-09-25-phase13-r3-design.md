BLOCKING: 4 · SHOULD-FIX: 5 · NOTE: 5

| id | sev | doc line(s) | finding | evidence (file:line / API fact) | exact change |
|---|---|---|---|---|---|
| R3D-1 | BLOCKING | 74-77, 237-239 (task 3), 276-279 | The live source's blur can be read two ways, and both give a wrong build. (a) `renderEffect` set on the recorded layer itself also blurs the page, because the effect is a property of the layer wherever it is drawn and the page draws that same layer to show itself. That is the "element blurs itself" design l.66-68 reject. (b) `renderEffect` set on a surface-sized layer ("clipped to its own bounds") blurs only the pixels inside the surface, clamped at its edge. But E3 / E4 / E5 expect B = a Gaussian of the whole capture at each pixel. Every pixel of the app-list band lies within 3σ of an edge, so a build that follows the text fails E4 everywhere, and fails E3 / E5 within 157 px of each edge. Also unstated: at every call site the surface sits inside the node that draws the page background, so recording "the content under" at that node records the surface itself. | `GraphicsLayer.getRenderEffect` / `setRenderEffect` are members of `androidx.compose.ui.graphics.layer.GraphicsLayer`: one property per layer (javap of ui-graphics 1.12.1 in the Gradle cache). Android `RenderNode.setRenderEffect` applies to "the end result of the contents of this RenderNode". HWUI draws such a node into an offscreen layer of the node's own width × height, so content outside that layer is not blur input (platform docs; HWUI layer promotion). Band item: `padding(AppMenuMetrics.INSET)` 12 epx around a 20-epx body line (`AppListMenu.kt:38,164`, `ui/tokens/Type.kt:15`) = 132 px, so one band is 132-264 px tall; 3σ = 3 × 52.46 = 157 px. The surface sits inside the node that draws the page background at each call site: `AppListPage.kt:257` / `:291`, `MusicCollectionPage.kt:191` / `:286`, `RemindersPage.kt:334` / `:408-414`, `CortanaSessionRoot.kt:74` / `:100-107`. | Decisions (b) l.74-77, from "and the surface draws that layer" to "over it", becomes: "…into a `rememberGraphicsLayer()` layer L that carries NO effect. L is recorded from ONE child holding the page's background and content, a sibling drawn below the surface. At each call site the background moves from the surface's parent into that child, so the surface is never inside what L records, and the page draws L to show itself, unchanged. The surface owns a second layer S, whose bounds are the surface's bounds grown by 3σ on every side (clamped to the window). S has `renderEffect = BlurEffect(r, r, TileMode.Clamp)` and records L translated by −S's origin. The surface draws S clipped to its own bounds, then the tint and noise. Every surface pixel is therefore blurred from the real backdrop around it; only the window edge is clamped." Task 3 gains the same text. Preamble l.276-279: "B = GaussianBlur_host(σ …) of the WHOLE backdrop capture, edge mode `nearest` (TileMode.Clamp at the window edge), read at the same pixel". |
| R3D-2 | BLOCKING | 169-170, 288-294, 309-316 (E2), 324-327 (E4), 333-334 (E5), 430-431 | The checkerboard is too fine for the edge-spread rule. 2.563σ is the 10-90 % width of an ISOLATED step. With 8 squares across (135 px) and σ = 52.46 px, neighbouring edges overlap and the rows of squares above and below scale the contrast down, so E2 cannot pass on a correct build. On the best strip (a square's vertical centre) the blurred row spans 10.3-40.7 (0.2-scaled), so the 10 % and 90 % levels of the 0-51 step are never reached. Measured against the row's own extremes, the width is 79.6 px, outside the 107.6-161.4 pass band. On a checker row boundary the row is flat (25.3-25.7). The same ratio holds at 720 px (90-px squares, r = 60 px). E2's "white squares read ≈ 51 far from an edge" is false (best case 40.7). E4's band is blurred TWICE, because the live source blurs the captured app list, which is already acrylic. On this checker the band swings 2.7 levels under ± 5 noise, so "with the blurred edge width" cannot pass. The edge case l.430-431 ("never a second blur of the wallpaper beyond the first") contradicts both the mechanism and E4's own B. E5's "title's edge … blurred width" measures text, which is not a step: a 3-px near-white stroke blurred at σ 52.46 peaks at 5 levels, 1 level after × 0.2. | Host computation (numpy / scipy `gaussian_filter`, mode `nearest`) on the fixture as specified: 8-across gives the numbers in the finding. With 4 squares across, the width is 129.8 px at FHD+ (pass band 107.6-161.4) and 86.8 px at 720 wide (72.1-108.1), at every strip offset tested (square centre, ± 60 px, a quarter). The double blur on 4-across swings 7.6 levels with a width of 153.5 px. | Fixture l.288: "a 4-square-across black / white PNG at the display size (270-px squares at FHD+, 180 px after `wm size 720x1560`)". Edge spread l.292-294: "…= the 10-90 % width of the step between the values at the two adjacent square CENTRES on the same row (the row's own plateaus, not 0 and 255), read on the column mean of ≥ 8 strip rows clear of text. Expected 2.563 · σ, σ = 0.57735 · r + 0.5, r = 30 epx · px/epx (r = 90 px, σ = 52.46 px, width ≈ 134.5 px at 3 px/epx), pass ± 20 %; 'sharp' = width ≤ 2 px". E2 l.312: "(white-square centres read ≈ 50, black ≈ 1)". Build-start check l.169-170: "…a blurred isolated step (a half-black / half-white layer)…". E4 l.326: strike "with the blurred edge width" (the band re-blurs the acrylic app list, and its pixels already test the blur against B). E5 l.333-334: strike "and the title's edge under the pane has the blurred width". Edge case l.430-431: "…the live layer records the app list including its blurred backdrop, and the band blurs that again (0.8·T + 0.2·blur(app list)), exactly as E4's B computes; no special path". Phase 12 E11 and phase 14 E14 cite the fixture by name and follow unchanged. |
| R3D-3 | BLOCKING | 338-353 (E7), 264-267, 184-185, 368-369 | E7 cannot run against the built items, and T11-31 is not applied. (1) A reminder-menu item draws no fill while held: (79,84,80) appears only after the tap completes (R7 §3.6.6's "after a choice" frames). So the held item's F is the menu's own noisy material, and an UP on the item runs Complete / Delete. (2) A pane item acts at touch-down. Any destination except Home-while-on-Home sets `paneOpen = false` at once, so no pane item can be held showing (63,68,64). Home-on-Home stays open but draws the accent, because the pressed fill is suppressed on the current item. (3) An UP on a satellite launches its shortcut. So "UP, screencap: neither … on the item" photographs a closed menu or a launch. (4) T11-31 (carried): the "60 px along the diagonal" sample lands on the glyph, and the off tolerance reads "rest fill ± 2" rather than phase 11's "rest pixels ± 2". (5) The satellite sub-row needs phase 11's fixture tile and baseline, but the Seeding line says phase 02's file is complete for this build. (6) E11's "every pressed-fill sample taken under T13-1's sample rule" has nothing to apply to: e15.sh samples no pressed fill. | `RemindersPage.kt:619-620` (`MenuItem(…, chosen == true) { chosen = true; onChoose(true) }`), `:634-635` (`background(if (pressed) MENU_PRESSED_FILL …)`, `detectTapGestures { onClick() }`), `:231` (the choice deletes / completes). `CortanaNavPane.kt:210-211` (`awaitFirstDown(requireUnconsumed = false)` → `onDown()` → onSelect), `:193-194` (`showPressed = !showAccent && …`). `CortanaModel.kt:449-453` (Home-on-Home returns early; otherwise `paneOpen = false`). `CortanaSessionRoot.kt:100` (the pane is composed only while `paneOpen`). `EditGestures.kt:117-125` (an UP on the same satellite → `onSatelliteTap`). phase-11 E5 l.475-486 (press at (left + 10, top + 10); "MOVE off the satellite and UP (nothing runs, burst stays)"; (left + 70, top + 10); "every one of those pixels equals the rest pixels ± 2"). `qa/phase-11/baseline_layout.json` pins `app:app.tileshell.testclient.a/…:0` MEDIUM. `e15.sh:112-113` is its only pixel read (the page background). | Replace E7 (l.338-353) with: "E7 The two lights on touch (Q3 B; T13-1's values; T13-8 / T11-31's points). For each item: capture U (unpressed); `input motionevent DOWN` at the stated point, capture D; `MOVE` +30 px in x (still inside the item), capture M; `MOVE` off the surface (outside the menu / pane / burst), then `UP`, which runs nothing, capture A. Let F_p = pixel p of U, so the material's deterministic noise cancels. In D: the ring, where it lies ≥ 126 px from the touch point, reads F_p + 0.30·(255 − F_p) ± 4; the touch point reads F_p + 0.10·(255 − F_p) ± 4; the stated 0.5 r point reads F_p + 0.05·(255 − F_p) ± 4; the stated far point reads F_p ± 2. The centroid of (M − U) inside the item, ring excluded, lies 30 ± 3 px right of the centroid of (D − U). In A, every sampled pixel equals U ± 2. (a) Reminder menu: E15's setup, long-press the tomorrow row, press the Complete item's centre (0.5 r point 60 px left of it; far point 200 px right of it on its centre line). The built item draws no fill while held ((79,84,80) only after a choice, R7 §3.6.6), so F_p is the menu's material. (b) ≡ pane: opened over Home, press `cortana_pane_item_home` at its centre. It is the current item, so the pane stays open (R7 §3.1.11) and F_p is the accent; any other item closes the pane at touch-down. (c) Satellite: `layout_restore qa/phase-11/baseline_layout.json` with phase 11's fixture APKs installed (its E5 setup). Hold the tileclient-a tile and press `quick_sat:0` at (left + 10, top + 10) px of its `rest=` bounds, never its centre (T13-8). Samples: the ring on its right and bottom edges; the touch point; the 0.5 r point 60 px along the TOP edge, at (left + 70, top + 10) px, outside the centred glyph box and clear of the 3-px ring (T11-31); the far point 12 px inside the bottom-right corner (≈ 200 px away). No Q6 style under `press_tilt` (T11-7); phase 11's E5 / E7 sub-rows re-run here. Acrylic off (E1's control), the same three presses: D equals U ± 2 at every sampled pixel (one tolerance with phase 11 E5, T11-31). Controls unchanged: a Start tile with press style None (phase 01 E10's zero change); a Settings row (X19's 15 % white, neither light)." Seeding l.266 gains: "— except E7(c), which restores phase 11's `baseline_layout.json` (its fixture tile)". E11 l.368-369: strike "every pressed-fill sample taken under T13-1's sample rule (≥ r + 2 epx from the touch point, or after UP)". l.184-185 "cited by E7 and by E11's run of phase 03 E15" → "cited by E7". |
| R3D-4 | BLOCKING | 327-329 (E4 Music) | E4's Music sub-row cannot run: no Music hold menu can open over album art. Rows on the albums pivot take no hold. The only holds are on songs (drawn transport marks, not art), playlists (accent squares) and tracks inside a playlist. Now-playing's `•••` band rises from above the transport row, far below the art. | `MusicCollectionPage.kt:423` (`AlbumRow(item) { onTap(item) }`, no onHold); `:262-277` (holds: `SongItem`, `PlaylistItem`, `else -> Unit`); `:425-426`; `:514` (SongRow draws `drawTransportMark`); `MusicNowPlaying.kt:231-240` (the band rises from above the transport row). | E4's Music part: "Music: on the playlists pivot, with three playlists made by MUSIC8's steps, hold the first playlist row → `music_menu` hangs over the next row's accent square (the square's dump bounds intersect `music_menu`'s). Under the band that square's edge reads 0.2·B ± 4 (patch means; B from `acrylic_expect.py` over the pivot captured with the menu closed); the held row's own square, outside the band, is sharp. Acrylic off: the theme background ± 1. Delete the three playlists." |
| R3D-5 | SHOULD-FIX | 93-94, 310-312 (E2), 318-321 (E3), 324-326 (E4), 330-337 (E5, E6) | (1) The material adds ± 5 levels of per-pixel noise from a hash the host cannot reproduce. Yet E2 checks "pixel values … ± 3", E4 "the pixels equal … ± 4", and E5 "(14,19,13) ± 2" / "(17,23,15) ± 2", all per pixel, so a correct build fails them. (2) The Q1 A clauses that put a fill over its measured backdrop (E3 (40,40,40) ± 2, E5 (14,19,13) ± 2, E6's "flat backdrop") hold only where the blurred backdrop equals B_m. In the built layouts it rarely does within 3σ (157 px). The reminder menu always sits over rows: their near-white titles, and the held row's (63,68,64), which that row keeps while the menu is open. The pane over Home overlaps the accent persona ring (70 epx wide at x 540 px, y 244 epx; the pane spans x 0-768 px). (3) E3's "captured with the menu closed" is ambiguous: a capture from before the long-press has no pressed row. | l.93-94 (the noise is "± 5 levels … from a hash of the pixel position"; the hash is not given). `RemindersPage.kt:222`, `:236`, `:376` (the long-press sets `pressedRowId`, and `onMenuGone` does not clear it). `CortanaPageChrome.kt:300` (`LONG_PRESS_FILL_MS = 300`). `Persona.kt:49-52` (idle ring 70 epx, centre y 244 epx). | Preamble, after l.279: "**Acrylic-on colour reads:** every acrylic-on colour clause compares the MEAN of a ≥ 10 × 10 px patch, or for an edge profile the column mean of ≥ 8 strip rows — never one pixel, since the ± 5 noise cannot be reproduced on the host. Single pixels are compared only with acrylic off, on opaque fills, or against the same pixel of a reference capture (E7). A clause that promises a measured fill over its measured backdrop (Q1 A: E3's (40,40,40), E5's (14,19,13), E6's flat patch) puts its patch where `acrylic_expect.py`'s B is within ± 1 of B_m over the whole patch, and asserts that first as a precondition (no such patch → the row fails loudly, not the product)." E3 l.321: "(B from `acrylic_expect.py` over the Reminders page captured after the menu is dismissed by a tap outside it — the held row keeps its (63,68,64) fill, asserted in that capture — never a capture from before the long-press; T13-2)". |
| R3D-6 | SHOULD-FIX | 69-73, 154-156, 231-232 (task 2) | Two things about the static source are unspecified, and E2's expected values depend on both. (1) Geometry: Start draws the picture `ContentScale.Crop` at 1.3× with a 0.25 parallax. Nothing says whether the app list copies that or draws the picture 1:1, so a builder has to guess, and `acrylic_expect.py` cannot map fixture pixels to screen pixels (at 1.3× the expected 134.5-px width becomes ≈ 175 px). (2) Kernel: "blurred once … off the main thread" does not say by what. A CPU box or stack blur, the usual off-thread choice, is not HWUI's Gaussian, and then σ = 0.57735·r + 0.5 and the host expectations do not hold. | `StartPage.kt:568-575` (`ContentScale.Crop`; `translationY = -scroll.value * 0.25f`; `scaleY = 1.3f; scaleX = 1.3f`). l.276-279 (the static-source B is computed from the pulled fixture with HWUI's σ). | Decisions (a), after "and cached": "The picture is placed `ContentScale.Crop` to the app-list page's own size at scale 1, with no parallax and no 1.3× (those are Start's own, X15). It is blurred at that size by HWUI's own blur, `RenderEffect.createBlurEffect(r, r, CLAMP)`, rendered once into the cached bitmap (never a CPU box or stack blur), so both sources share one σ = 0.57735·r + 0.5 px. `acrylic_expect.py` maps the fixture the same way (Crop to the page's dump bounds)." Task 2 says the same. |
| R3D-7 | SHOULD-FIX | 27-29, 113-122, 432 | Scope applies the table to "every existing transient surface", but the table names five and the code has more. Two of them share the table's own components. `MusicMenu` has a second call site, now-playing's `•••` band, so acrylic built into `MusicMenu` dresses that band with no row, no B_m and no E-row. The reminder menu is identified by `CortanaUi.MENU_FILL`, which Cortana's combo drop-down also uses. The pin confirmation band (H22) and the two jump grids (X8) are transient overlays the doc neither dresses nor excludes. The edge case at l.432 treats the pin band as a transient surface, though since T11-8 nothing in it is acrylic. | `MusicNowPlaying.kt:235` (`MusicMenu(anchorPx = 0f, riseFromPx = rise, …)`); `ReminderDetailPage.kt:520-531` (`OptionList` … `.background(CortanaUi.MENU_FILL)`, H25); `StartActivity.kt:210` (`SecondaryPinPrompt`); `AppListPage.kt:567-572`, `MusicCollectionPage.kt:736` (jump grids). | Scope l.28: "…the surface table below — the complete list — applied to its surfaces…". The table's Music row becomes: "Music menus (`MusicMenu`: the collection's hold menus and now-playing's `•••` band, `MusicNowPlaying.kt:235`) \| 10 MUSIC8 \| live \| theme background \| the page under each (black) \| (0,0,0) \| theme background". Out gains: "the pin confirmation band (H22), the app-list and Music jump grids (X8), Cortana's combo drop-down (`OptionList`, H25), and the name boxes and pickers keep their fills (not measured as menus; Jeremy can ask)". Strike the edge case at l.432. |
| R3D-8 | SHOULD-FIX | 126-129, 157-158 | `reason=` has no precedence, and "on every change" does not say whether a change of reason alone writes a line. With the switch off AND battery saver on, both `setting` and `battery-saver` apply. Phase 12's edge case already expects `reason=battery-saver` for a preset applied under battery saver, including one that turns the switch off. A builder who reports `setting` first therefore fails another phase's row. | `phase-12-setup-wizard.md:775-778` ("phase 13's rule wins — `[fluent] acrylic=off reason=battery-saver`"). | l.157-158: "`[fluent] acrylic=on\|off reason=<the first false term, in the order low-ram, battery-saver, setting>\|none`, written at process start and whenever the value OR the reason changes". Task 1's JVM test covers the precedence on all 8 combinations. |
| R3D-9 | SHOULD-FIX | 69-73, 165-168, 364-366 (E10), 374-380 (E13) | The doc never says when the static layer is built, and the built pager makes it matter: page 1 is composed beside Start. A layer built when the app list composes is built at Home, so in E13 the `failed` line is stamped before "MARK, swipe to the app list". The row then fails on a correct build, and its "no `rebuilt` line" check cannot fail. E10's "(one screen layer + noise)" reads as if the layer existed only on the app list. | `StartActivity.kt:170` (`beyondViewportPageCount = 1`); `AppListPage.kt:257-261` (the page's own `visible` test); `Diagnostics.kt:13-26` (a per-process ring, empty after a force-stop). | Decisions (a) and task 2: "built when the image is set and at process start (the pager composes the app-list page beside Start, so the layer exists before the first swipe and no unblurred frame shows), kept until the image changes or is removed". E13 l.375-376: "…`am force-stop app.tileshell`, MARK, Home, swipe to the app list: the slice holds …" (the new process's ring starts empty). E10 l.365-366: "(the layer is built with Start; 14 MB bounds a second copy)". |
| R3D-10 | NOTE | 242-243 (task 5), 258-260 | The satellites are draw-only. The gesture branch that sees a satellite press consumes it and keeps no touch position, so task 5 cannot attach the lights to the satellite composable the way it can for menu and pane items. Phase 11 is FINAL and done, its part changes here, and task 7's Change Log list leaves it out. | `QuickBurst.kt:223-226` ("DRAW-ONLY — every touch is decided in [startEditGestures]"); `EditGestures.kt:117-126`, `:470-477` (`waitForUpConsuming` keeps only the release point). | Task 5 gains: "the satellite's press point (DOWN, each MOVE, cleared at UP or cancel) is set on `QuickBurstState` by `EditGestures`' `Hit.Satellite` branch and drawn by `QuickBurstLayer`". Task 7's Change Log list gains "11 (satellites draw the two lights; the press point in `EditGestures`)". |
| R3D-11 | NOTE | 71, 115, 235, 254-257, 281 | Stale cites and clauses. `rememberBackground` is now at `StartPage.kt:1148`, not `:987`. Task 7's `record` clause ("if this phase is built before phase 15's branch merges…") is overtaken: phase 15 has merged, and `lib.sh` has `record` and the recorded-only `row_end`. l.281 still points to "build task 7". "`start_page`'s pager page 1" becomes page 2 once phase 14 inserts the pod bay. | `StartPage.kt:1148`; `lib.sh:388-395` (`record`, "phase 15 owns this helper"), `:140-145` (`recorded only`); phase-11 l.468 (phase 14 moves Start to pager index 1, T11-9). | l.71 and l.235: ":987" → ":1148". l.254-257 → "`record <name> <value>` and `row_end`'s `recorded only` line (C-26) are phase 15's and already in `lib.sh` (`:388-395`, `:140-145`; T15-21 / C-33); this phase builds nothing for them". l.281: "(build task 7)" → "(phase 15's `lib.sh` helper)". l.115: "`start_page`'s pager page 1" → "the app-list pager page (`app_list`)". |
| R3D-12 | NOTE | 247-249 (task 7), 356-357 (E8) | A writer for the `[motion]` line already exists: phase 15's `MotionClock` / `MotionTrace` emits C-5 / C-31's exact format. Task 7's ADDs should call it rather than build a second writer. E8's `[motion] applist_menu` "settle 0" is 0 by construction, so that clause cannot fail. | `ui/MotionClock.kt:9-49` (`animate`; `jump(name, inputUptime, firstFrameUptime)`, whose settle = input to first frame). | Task 7: "…the `[motion]` lines … through `ui/MotionClock.kt` (`MotionTrace`, phase 15), one format". E8: "`[motion] applist_menu` is written by `MotionClock.jump` from the hold's 783-ms uptime to the band's first frame: settle ≤ 33.4 ms", replacing "settle 0". |
| R3D-13 | NOTE | 305-306, 367-371 (E11) | Gate wording under the 2026-09-25 QA ruling. "runs at phase 12's gate" and "a dependency of this gate" read as whole-gate runs. E11 also names all of phase 01 E12 (its "New"-caption clock-forward steps included), where only the app-list sub-steps cover what this phase changes. | INDEX.md:92 (QA ruling: a session runs "the specific rows, sub-steps and unit tests that cover what it built or changed"). | l.305-306: "runs at phase 12's gate because…" → "run by phase 12's build session as one of the specific tests its presets touch, because…". l.367-368: "a dependency of this gate" → "a dependency of E11". l.369: "phase 01 E12 and E19" → "phase 01 E12's swipe / search / jump-grid sub-steps and E19's app-list bar checks". |
| R3D-14 | NOTE | 13-14, 146-147, 179-180 | Which items take the lights is stated two ways. The Goal says "the items on those surfaces", and those surfaces include the app-list backdrop, so app-list rows would be lit. Q3 B's ruled list is menu rows, pane items and satellites. "Pod cards if a later phase makes them pressable" is a hook for a later phase, which Out excludes; phase 14 names no lights. | l.42-44 (Q3 B's list); l.39 (Out: "Hooks for later phases"); `phase-14-pod-bay.md` has no "light" / "Reveal" text (grep). | Goal l.13-14: "for the items on those surfaces and phase 11's satellites" → "for the pressed items of the menus and the ≡ pane and phase 11's satellites (Q3 B's list; the app list's rows keep X19's 15 % white)". l.147: strike ", pod cards later". l.180: strike "and pod cards if a later phase makes them pressable". |

Checked and correctly applied: T13-2, T13-3, T13-4, T13-5, T13-8, T11-7, T12-3, T14-11, C-5, C-13, C-18, C-20, C-25, C-31.
