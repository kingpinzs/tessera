BLOCKING: 7 · SHOULD-FIX: 9 · NOTE: 5

| id | sev | doc line(s) | finding | evidence (file:line / API fact) | exact change |
|---|---|---|---|---|---|
| R3T-1 | BLOCKING | 33-35, 70, 128-130, 160, 233, 332-333, 366-367, 447 | Jeremy's Q2 A (l.33-35, option text l.233) and the Taps line (l.129-130) say a disc tap acts AND the burst closes. E5 (l.332-333) asserts `quick_burst` PRESENT after the resize tap with the satellites on the new corners, Tracking (l.70) and the edge case (l.447) keep the burst open across a resize, and the close-reason list (l.160) has no reason for a resize (nor for E8(c)'s uninstall, l.366-367). A build that follows the ruling fails E5; a build that passes E5 breaks the ruling. Raised under rule 1, not reopened. | doc l.33-35 "(unpin removes the tile and the burst with it; resize changes the size) and the burst closes"; l.233 "A. One tap: … resize changes its size) and the burst closes"; l.332-333 "`quick_burst` present, and every `quick_sat:i` sits at the new corners"; `start/EditGestures.kt:288` logs `[edit] resize <id> A -> B`. | Apply the ruling. E5: "Tap `edit_disc:resize` → the tile's size cycles (`[edit] resize <id> A -> B`), `quick_burst` absent, `edit_disc:*` on the new corners, edit mode on, and the slice has `[quick] burst closed: resize`". l.160's list gains `resize` and `removed` (the tile left Start; E8(c) then asserts `[quick] burst closed: removed`). l.70: strike "a resize (the discs jump to the new corners, §1.2.7, and the satellites re-anchor)". l.129-130 → "resize cycles the size) and the burst closes (`resize`)". l.447 "a resize that changes the arrangement (medium → wide at the right edge: corner → line)" → "a resize with the burst open (the burst closes, Q2 A)". If the lead reads Q2 A as keeping the burst on a resize, that is a question for Jeremy, not an edit. |
| R3T-2 | BLOCKING | 133, 334-335 | E5's Home step cannot pass on this AVD. `KEYCODE_HOME` sent while the shell is already the resumed home activity is never delivered (no `onNewIntent`), so the burst and edit mode stay, and `[quick] burst closed: home` (l.160, needed by E14) is never written. | `phase-02-edit-mode-folders.md:117-120` ("Home while Start is showing … never fires on this AVD … Android does not re-deliver the home intent"); `qa/phase-02/README.md:73-75`; `qa/phase-02/scripts/gestures.sh:28-32`. `StartActivity.kt:308-312`: onNewIntent is the intent path. `StartActivity.kt:198-201`: the drawn Windows key runs `homeEvents.tryEmit(true)`, the same exit with no intent, and that is how `qa/phase-02/scripts/regress.sh:94-121` (step 7) proves Home. | E5: "`KEYCODE_HOME`" → "a tap on the drawn Windows key (`tap_node nav_windows`; `KEYCODE_HOME` is not re-delivered to the resumed home activity on this AVD, phase 02 l.117-120) → `start_page` in the dump, no `quick_burst`, no `edit_disc:*`, and the slice has `[quick] burst closed: home` and a `[start] home:` line". Phone-only gains "P6 `KEYCODE_HOME` (the phone's Home) with a burst open → the same", because that is the intent path the AVD cannot drive. |
| R3T-3 | BLOCKING | 273-275, 278, 352-359, 412-413 | (a) Placing the fixture "right after `slot:CALENDAR`" puts it on the grid's RIGHT edge, not "clear of every edge": the tile after the WIDE Calendar first-fits units 4–5. Its corner burst reaches 212 px past each side (164 + 48). In edit mode the drawn tile spans x ≈ 686–1029, so the right pair would span x ≈ 1077–1241 and is clamped inward. E7's "inner corner 48 ± 3 px … on both axes" then fails for satellites 1 and 3 on a correct build, and E16 inherits the failure. (b) The tall variant cannot run. `make_tall.py` makes EVERY tile WIDE, so the fixture itself becomes a left-edge WIDE tile and its left pair is clamped. Twelve WIDE rows (≈ 4270 px) scroll by at most ≈ 2400 px, so "scrolled 1.5 screens" (≈ 3500 px) is impossible and would push the fixture (row 5) off the screen. | `qa/phase-01/RECENT0922/fresh.xml`: `tile:slot:CALENDAR [9,439][708,782]` and `tile:slot:PHOTOS [721,439][1064,782]` (the tile after Calendar); `tiles/GridPack.kt:19-47` (first fit from row 0); `qa/phase-02/scripts/gestures.sh` `edit_point` (centres contract to 0.90 about (540, 1111.5)); `qa/phase-02/scripts/make_tall.py:9-12` (`o["size"] = "WIDE"` for every key). | Seeding l.273-275 → "tileclient-a MEDIUM in a MIDDLE-column cell. Order: `slot:PEOPLE`, `slot:BROWSER`, `slot:MAIL`, `slot:PHOTOS` (MEDIUM), tileclient-a (MEDIUM), tileclient-b (SMALL), `slot:CALENDAR`, …. First fit puts the fixture at units 2–3 of the second row (x 365–708, y 439–782 on this AVD), where its 212-px corner reach clears both screen edges, the status bar and the bottom row. E1 first asserts those rest bounds ± 1, so a mis-seeded file fails loudly." Tall (l.278, E7 l.358-359) → "`baseline_layout-tall.json` = the main file with every tile AFTER the fixture set WIDE (make_tall.py's rule applied only to those keys), so the fixture keeps its cell and Start scrolls. E7 scrolls Start with a slow drag (`input swipe 540 1700 540 1580 1500`, no fling) and reads the scroll from the fixture's dump bounds (Δy 60–120 px; more pushes the top pair's labels into the status-bar clamp). It then holds the fixture and asserts that every `rest=` rectangle, relative to the fixture's drawn bounds, equals the main corner case ± 1 px." |
| R3T-4 | BLOCKING | 310-311 | "`glide +40 px; UP` → … the tile moved (bounds diff)" cannot pass. A 40-px drag never leaves the tile's own cell, so the drop is "nothing to do", and after UP the tile is back where it started. | `start/EditGestures.kt:207-223`: the target is the cell under the dragged tile's centre, and over the tile's own gap the result is "nothing to do". `:264-268`: the commit uses `previewOrder`, which is null here, so nothing moves. A medium tile is 343 px wide, so a 40-px move keeps its centre inside it. `:144` logs `[edit] drag start <id> size=…`. | E2 → "`DOWN; sleep 0.9; glide +40 px` → a dump (dump route) taken BEFORE the `UP` has no `quick_burst`, and the fixture is drawn ≥ 30 px from its E1 held bounds (the dragged tile follows the finger); the slice has `[edit] drag start <fixture id>` and `[quick] burst closed: drag`; after `UP` the fixture is back in its own cell (E1's held bounds ± 1 px)". |
| R3T-5 | BLOCKING | 109-111, 174-175, 363-367 | E8 has three problems. (1) (b)'s expected "`… 1 (0 shown)`" can never appear. When the app disables a dynamic shortcut, the platform DELETES it unless it is pinned, and a pinned one loses its dynamic flag, so the shell's MANIFEST\|DYNAMIC query returns 0. The `isEnabled` filter cannot be reached on the device. (2) `am start … disable` brings VerbActivity to the front. Start stops, so `burst closed: stop` (l.134-135) races `shortcuts changed`, and the hold after it has no Home step (Start is covered). (3) (c) uninstalls tileclient-b and restores nothing. The next row's `layout_restore` compares the order, fails 3× on the missing tile, and every later row fails with it. | AOSP `ShortcutPackage.deleteOrDisableWithId` (services/core/…/pm/ShortcutPackage.java): on disable, a shortcut that is neither pinned nor cached is deleted with `forceDeleteShortcutInner`; a pinned one is kept with FLAG_DYNAMIC and FLAG_MANIFEST cleared (known from the AOSP source; the row can confirm it with `cmd shortcut get-shortcuts`, which exists on emulator-5554). Doc l.111: "the shell pins none". `qa/phase-02/scripts/layout.sh:30-45`: the restore compares `order` keys, tries 3 times, then reports FAILED. | (b) → "`am start` tileclient-b with the `reset` verb, then C-6. Send the `disable` verb with no window: `adb shell am broadcast -n app.tileshell.testclient.b/app.tileshell.testclient.VerbReceiver --es verb disable` (an exported receiver in tileclient-b; `disableShortcuts` is not rate-limited in the background). Hold → no burst; the slice has `[quick] shortcuts for app.tileshell.testclient.b/app.tileshell.testclient.VerbActivity/0: 0 (0 shown)` and `[quick] no burst on <id>: no shortcuts`; the saved `cmd shortcut get-shortcuts --flags 9 app.tileshell.testclient.b` output lists nothing. `reset` + C-6, open a burst on the tile, broadcast `disable` → `StartActivity` is still resumed and the slice has `[quick] burst closed: shortcuts changed`. The `isEnabled` filter is proved by the selection rule's JVM test only." (c) ends "restore: `adb install` tileclient-b's debug APK, `reset` + C-6, `layout_restore`". The QA fixtures Decision (l.174-175) adds the receiver. |
| R3T-6 | BLOCKING | 117, 372-374, 419, 437 | E9's quiet-mode step cannot run. `pm set-quiet-mode` does not exist on this AVD. The fallback, `am stop-user`, stops the profile without turning quiet mode on, so `AppCatalog.isQuietMode` stays false and the reason is not `profile quiet`. E9, and E14's `profile quiet`, fail on a correct build. The "AVD refuses a managed profile → P4" branch never applies, because phase 01 created one here. The locked private space (l.117, edge case l.437) has no row, although this AVD supports private space. | `adb shell pm help` on emulator-5554 (API 36 userdebug) lists no quiet-mode command (create-user, remove-user, rename-user …). `apps/AppCatalog.kt:142` (`isQuietModeEnabled`). Phase 01 E18 (phase-01 l.203; `qa/phase-01/E18/E18.txt:5-36`, `:38-55`) created a managed profile, set QUIET_MODE through the shell's own `profile_lock:work` (`applist/Profiles.kt:87-89` `requestQuietModeEnabled`), and created and locked a private space (`--user-type android.os.usertype.profile.PRIVATE`, `profile_lock:private`). | E9: "`adb shell pm set-quiet-mode true --user <id>` (or `am stop-user <id>` …)" → "Open the app list and `tap_node profile_lock:work` (phase 01 E18's route). Assert that `adb shell dumpsys user` shows `QUIET_MODE` for <id>, press Home, and assert the work tile is still in the dump. Hold → no burst, `[quick] no burst on <id>: profile quiet`. `profile_unlock:work` → hold → four satellites again (edge case l.437, 'then unquieted')." Add: "Private space: `pm create-user --profileOf 0 --user-type android.os.usertype.profile.PRIVATE qa_private`, `am start-user`, `pm install-existing --user <p> app.tileshell.testclient.a`, pin from the private group, `tap_node profile_lock:private` → hold → `[quick] no burst on <id>: profile quiet` (the same `isQuietMode` check, l.117). Restore with `pm remove-user` for both." Strike "If the AVD refuses a managed profile, the row records it and moves to P4" and P4 (l.419). |
| R3T-7 | BLOCKING | 115-118, 122-127, 134-135, 157-160, 256-259, 282-284, 390-393 | E14 cannot pass as written. (1) No row produces `[quick] burst closed: stop`: none turns the screen off or brings a window over an open burst. (2) No row produces `startShortcut failed <exception>`: none taps a satellite that cannot start, and E8(b)'s disable closes the burst first through onShortcutsChanged (l.126-127). (3) Lines written before a mid-row `am force-stop` are wiped before `row_end` saves the slice: E3's `icon failed` (l.316-319, then `reset` + C-6), E4's `tap satellite … ok` / `burst closed: launch` (each followed by C-6) and E15's two `shortcuts for` lines. The union therefore misses them, and `row_end` has no `ROW_MARK` source. (4) The `no burst` reasons have no literals in Decisions (l.115-118), and no row drives a query exception. | `diag/Diagnostics.kt:14-26`: one in-memory ring per process, so a force-stop empties it. `qa/phase-03/scripts/lib.sh:58-82`: `row_begin` takes no mark. `:125-137`: `row_end` saves nothing today. Doc l.282-284: C-6 force-stops after every launch. | (1) Task 7 and the Ring reads line: "`row_begin` sets `ROW_MARK=$(ring_mark)`; `ring_save [launcher]` appends `ring_since $ROW_MARK` to `<row>/ring-<name>.txt`; every in-row `am force-stop app.tileshell`, `layout_restore` and `pm clear` is preceded by `ring_save launcher` (C-6's step reads `ring_save launcher; am force-stop app.tileshell; Home`), and `row_end` calls it last". (2) Decisions l.115-118 names the literals: `folder` · `no shortcuts` · `not the shortcut host` · `profile quiet` (a quiet work profile and a locked private space alike) · `query failed <exception>`. (3) E5 gains: "with a burst open, `input keyevent KEYCODE_SLEEP`, then `wake_device` → `Awake` (C-25) → no `quick_burst`, and the slice has `[quick] burst closed: stop`". (4) E8 gains (d): "tileclient-b's `deadtarget` verb (`am start`, then C-6) adds `qa_dead` ("Dead", rank 1), whose intent is `ACTION_VIEW` to the explicit component `app.tileshell.testclient.b/app.tileshell.testclient.Missing` (declared nowhere). Hold, then `tap_node quick_sat:1` → nothing resumed, edit mode off, and the slice has `[quick] tap satellite 1 app.tileshell.testclient.b/qa_dead: startShortcut failed android.content.ActivityNotFoundException` (LauncherApps.startShortcut throws it when the platform cannot start the shortcut's intent). Then `reset`." (5) E14: "`query failed` is proved by the shortcut source's JVM test with a throwing fake (build task 1), named in this row; every other line and literal comes from the saved ring files". |
| R3T-8 | SHOULD-FIX | 174-175, 268-270, 313-315 | tileclient-b's `qa_dyn` exists only after the app has run, because a dynamic shortcut is published by the app at run time. Seeding only installs the app, and E3 holds its tile without launching it first. On a fresh install E3's "one satellite" reads 0 and fails, so whether E3 passes depends on what ran before. | Doc l.174-175: "publishes ONE dynamic shortcut … on launch". l.268-270: install plus `layout_restore` only. `ShortcutManager.setDynamicShortcuts` is an app-side call. VerbActivity takes its verbs as `--es verb <name>` (`testapps/common/src/main/kotlin/app/tileshell/testclient/VerbActivity.kt:37`). | Seeding: "after installing, run `adb shell am start -n app.tileshell.testclient.b/app.tileshell.testclient.VerbActivity --es verb reset` once (it publishes `qa_dyn` alone), then C-6's force-stop + Home". E3's tileclient-b sub-step starts with the same `reset` + C-6, so it never depends on an earlier row. |
| R3T-9 | SHOULD-FIX | 158, 176-178, 315-318, 452-453 | The fixture's missing-URI icon does not make `getShortcutIconDrawable` throw. LauncherApps catches the FileNotFoundException and returns null, the same value it returns for a shortcut with no icon, and Decisions never say what the shell logs on null. Whether E3's `icon failed` line appears is therefore a builder guess. Also, "no glyph drawn" on `quick_sat:1` proves nothing unless `quick_sat:0` draws one, and `qa_dyn`'s icon is unspecified. | AOSP `LauncherApps.getShortcutIconDrawable`: for an icon-URI shortcut, `openFileDescriptor` sits inside `catch (FileNotFoundException e) { Log.e(TAG, "Icon file not found: " …); return null; }` (AOSP source). `testapps/common/…/TestImageProvider.kt:35-40` throws FileNotFoundException for any name except `peek*` and `big.png`. The authority `app.tileshell.testclient.b.images` exists (`testapps/tileclient-b/src/main/AndroidManifest.xml:32-36`), so `missing.png` reaches the provider and gets that exception. | Harness l.158 / the T11-14 line: "a null drawable or an exception from `getShortcutIconDrawable` counts as a failed icon: `[quick] satellite <i> icon failed <pkg>/<id>: null drawable` or `…: <exception>`". QA fixtures: "every fixture shortcut except `qa_noicon` carries an icon (`android:icon` on `qa_one`…`qa_five`, `Icon.createWithResource` on `qa_dyn`)". E3's sub-step also asserts that `quick_sat:0`'s interior is NOT uniform fill (a glyph is drawn), beside `quick_sat:1`'s uniform fill. |
| R3T-10 | SHOULD-FIX | 92-93, 131-133, 328-339, 441 | E5 leaves out the taps whose handling this phase CHANGES in `EditGestures`, so a build that keeps today's behaviour passes: a tap on the held tile (today it exits edit mode), a tap on the folder-name strip (today it opens the name box), and a tap on a satellite's LABEL (edge case l.441: it counts as the satellite). "Tap another tile" is unnamed, yet satellites overlap neighbours by design (l.92-93), so a tile centre under a satellite runs a shortcut instead. The scroll sub-step cannot fail: the main baseline is shorter than the page, Start does not scroll, and a Δy of 0 satisfies "moves by the tile's Δy". The press sub-step leaves `press_p4` selected, against RV12. | `start/EditGestures.kt:103-105` (`hit.key == edit.selected` → `requestExit`); `:112-118` (a strip tap sets `edit.naming = true`); `:421` (the scroll is clamped to `scroll.maxValue`). The packed main baseline ends near y 1500 (four medium rows from y 84 at a 355-px pitch, RECENT0922 `fresh.xml`), above the bottom row's top at 1935, so `maxValue` is 0. `start/StartPage.kt:923-930` (`folder_name_placeholder:<id>` carries the name or "Name folder"). `prefs/ShellSettings.kt:23` (the press style defaults to NONE). | E5 gains these sub-steps, each from a fresh burst with a MARK. (a) "Tap the held tile's centre → `quick_burst` absent, `edit_disc:*` still on it, `[quick] burst closed: tap elsewhere`, and no `[edit] tap on the held tile` line." (b) "On the band variant, tap `folder_name_placeholder:qa` → burst closed, no `folder_name_box`." (c) "`tap_node quick_sat_label:0` → satellite 0 runs (`shortcut_id` = `qa_one`), then C-6." "Tap another tile" → "tap `tile:shell:cortana` (assert first that its centre lies outside every `quick_sat:*` rectangle)". The scroll sub-step runs on `baseline_layout-tall.json` and first asserts the tile's Δy ≥ 60 px. The press sub-step ends by re-selecting `press_none`. |
| R3T-11 | SHOULD-FIX | 89, 93-95, 276-281, 355-358, 381-382 | E7 has five gaps, and E12 one. (a) "Show more tiles off" is not part of any layout file; it is a Start + theme pref, so `baseline_layout-wide.json` alone cannot give the 2-column grid. (b) `bottom_tile_row`'s own node covers the whole page, so "bottoms one gutter above the row's top", read from that node, compares against y 0. (c) The top-left case holds `slot:PEOPLE`, which on this AVD bursts with ONE satellite, and asserts only containment. (d) No E7 case asserts the Decision's "never the held tile" (l.93), so a build that overlaps the held tile passes. Also, for a line BELOW the tile, "labels above" (l.89) lands the labels in the one-gutter gap on the tile. (e) In the band and bottom-row variants the fixture's dump id is not `tile:<key>` (l.280-281). (f) E12's "a bottom-row tile" is unnamed; the baseline's row holds PHONE, MESSAGING and CAMERA, AVD apps whose shortcuts are not seeded. | `settings/StartThemePage.kt:101` (`theme_show_more_tiles` → `mediumColumns`) and `prefs/ShellSettings.kt:62` (the `columns` pref); `layout.sh:21-24` (the restore writes only `files/start_layout.json`). RECENT0922 `fresh.xml`: `bottom_tile_row [0,0][1080,2196]`, `tile:dock:slot:PHONE [9,1935][352,2182]`; `StartPage.kt:642` (`fillMaxSize()…testTag("bottom_tile_row")`). `StartPage.kt:152` (`id = idPrefix + key.id`; the band passes `"member:"`, the row uses `dock:`). `dumpsys shortcut` on emulator-5554: com.android.contacts has one manifest shortcut, `shortcut-add-contact`, on PeopleActivity; phase 02's E7 evidence shows PEOPLE = `com.android.contacts/.activities.PeopleActivity`. | (a) The wide sub-step turns `theme_show_more_tiles` off in Start + theme before `layout_restore baseline_layout-wide.json`, turns it back on afterwards (RV12), and asserts the fixture spans the grid width before the hold. (b) "The row's top = the smallest top of the `tile:dock:*` nodes." (c) A fifth variant, `baseline_layout-topleft.json` (the fixture first in the order), replaces `slot:PEOPLE`: per l.94, four satellites in a line BELOW the tile, all inside the page area. (d) Every E7 case, and E16, asserts that no `quick_sat:i` or `quick_sat_label:i` rectangle intersects the held tile's drawn bounds; the lead settles where a below-line's labels go. (e) l.280-281 adds "`tile:member:<key>` inside an expanded band, `tile:dock:<key>` in the bottom row". (f) E12: "restore `baseline_layout-bottomrow.json` and hold the fixture tile in the row; the satellite tap resumes `ShortcutActivity` with the tapped id". Task 7: "four variant baselines" → five. |
| R3T-12 | SHOULD-FIX | 411-413 | "the fixture tile at its grid-cell bounds (the baseline dump's ± 1 px)" compares an edit-mode dump with a dump taken at rest. In edit mode the grid contracts, so at this cell the held tile's drawn centre moves ≈ 50 px, and the row fails on a correct build. | `qa/phase-02/scripts/e7_geometry.py:9-12` ("A dump taken while a tile is held carries the graphicsLayer transform … the held tile keeps its size"); centres contract to 0.90 about (540, 1111.5) (`gestures.sh` `edit_point`; `EditGestures.kt:47-56` `Coords`). The fixture's centre y moves from 610.5 to 660.6. | "the fixture tile at the bounds E1's held dump gives it (edit mode, grid cell) ± 1 px". |
| R3T-13 | SHOULD-FIX | 103-104, 184-185, 347 | `alpha0 ≤ 100 ms` cannot tell the Decision's close (alpha reaches 0 at the halfway point) from a fade that ends on arrival. The close `settle ≤ 250 ms` is met by any fade, because settle counts "or gone". Neither close number can fail. | For ζ = 0.65 and k = 1500 (ω0 = 38.73 rad/s, ωd = 29.43 rad/s), the travel fraction 1 − e^(−25.17t)(cos 29.43t + 0.855 sin 29.43t) passes 0.5 at 36.0 ms and first reaches 1 at 77.5 ms (computed). Both times are ≤ 100 ms. | E6 close → "`alpha0` = 36 ± 17 ms (the spring's progress passes 0.5 at 36.0 ms; RV11 + one frame) and `settle` = `alpha0` ± 1 frame (the satellites are gone)". |
| R3T-14 | SHOULD-FIX | 110, 153-156, 210-216, 394-404 | Shell tiles carry no component and no `AppEntry`, yet the query runs "under the tile's own `AppEntry.user`" with `setActivity(the tile's component)`, and E15 asserts component-keyed lines for `shell:settings`. `shell:cortana` has no activity at all. The builder has to invent the mapping, and a fallback to a package-wide query would burst Tess with Music's shortcuts. | `tiles/TileModel.kt:59` (`ShellTile(name)` carries only an id); `StartActivity.kt:276-280` (weather and settings are started by class; cortana goes to `CortanaService.open`). | The per-activity Decision gains: "A shell tile's component is fixed: `shell:weather` → `app.tileshell/.weather.WeatherActivity` and `shell:settings` → `app.tileshell/.settings.SettingsActivity`, both under `Process.myUserHandle()`. `shell:cortana` has no activity and is not queried; it logs `[quick] no burst on shell:cortana: no shortcuts` directly. A slot tile queries its resolved app's component and user." E15's Weather check adds `[quick] shortcuts for app.tileshell/.weather.WeatherActivity/0: 0 (0 shown)`. |
| R3T-15 | SHOULD-FIX | 402 | `page_header` never "reads 'Diagnostics'". The tag sits on the header Row, and the title is a child text node drawn in upper case, so reading the text of the `page_header` node returns "" on a correct build. | `settings/SettingsWidgets.kt:52-55` (`Row(…testTag("page_header"))` with a child `BasicText(title.uppercase(), …)`); `settings/DiagnosticsPage.kt:57` (`PageHeader(Glyph.DOCUMENT, "Diagnostics")`); `lib.sh` `node_text` and `nodetext.py` read only the tagged node's own `text=`. | "`page_header`'s subtree holds the text `DIAGNOSTICS` (the first `text=` among the `page_header` node's descendants; PageHeader draws `title.uppercase()` on a child)". |
| R3T-16 | SHOULD-FIX | 434-435 | The edge case adds a timed hold IN edit mode ("a hold in edit mode on another tile … opens that tile's burst"). `EditGestures` has no such hold today: in edit mode a still press waits for the release and then moves the selection. No row drives the new hold, so a build without it passes the gate. | `start/EditGestures.kt:97-111`: an edit-mode tile press goes through `awaitPress` and has no `HOLD_MS` timer. | E5 gains: "from edit mode with no burst open, `DOWN` on `tile:shell:settings`, sleep 1.0 → the discs move to it, `quick_burst` shows Start settings' four labels, and the slice has `[quick] burst on shell:settings: 4 satellites`; `UP`. Then `DOWN` on the held tile, sleep 1.0, `UP` → the slice has no second `[quick] burst on` line." If the lead strikes the edge case instead, strike it at l.434-435. |
| R3T-17 | NOTE | 305 | "`UP` → a second dump is identical": the flipping tiles change the XML between dumps (l.285-286), so a whole-dump comparison fails on a correct build. | `qa/phase-02/scripts/dumpdiff.py:2-13` compares tile rectangles only. | "… a second dump passes `dumpdiff.py` (IDENTICAL), and its `quick_sat:*`, `quick_sat_label:*` and `edit_disc:*` bounds match the first dump ± 1 px, with equal label texts". |
| R3T-18 | NOTE | 307-308 | E2's 740-ms negative ("no `quick_burst`, no `edit_disc:*`") is read from a dump with the fixture app in front, where it holds on any build. | `start/EditGestures.kt:80` writes `[edit] hold …: edit mode on` when a hold fires. | Add: "the slice from a MARK taken before the swipe has no `[quick] burst on` and no `[edit] hold` line". |
| R3T-19 | NOTE | 375-376 | There is no `…:PAUSE` tag or log line; the pause control is `PLAY_PAUSE`. | `tiles/engine/TileContent.kt:61` (`Transport { PLAY_PAUSE, STOP, NEXT }`); `start/TileView.kt:527` (the tag); `:316` (`[tile_control] tile=<id> <transport> (no launch)`). | "`tile_control:slot:MUSIC:PLAY_PAUSE` … `[tile_control] tile=slot:MUSIC PLAY_PAUSE (no launch)`". |
| R3T-20 | NOTE | 168-170, 324 | `app.tileshell.testclient.a/.ShortcutActivity` is only `dumpsys`'s short form for a class inside `app.tileshell.testclient.a`. tileclient-a compiles the shared `app.tileshell.testclient` sources (its launcher is `app.tileshell.testclient.VerbActivity`). Also, `shortcut_id` appears in a dump with its package prefix, and VerbActivity builds its views in code with no ids. | `testapps/tileclient-a/src/main/AndroidManifest.xml:21`; `testapps/common/src/main/kotlin/app/tileshell/testclient/VerbActivity.kt:81-103` (views have no resource ids). | Fixtures: "`app.tileshell.testclient.ShortcutActivity`, whose TextView has the resource id `shortcut_id` (`@+id/shortcut_id`)". E4 and E9 assert `app.tileshell.testclient.a/app.tileshell.testclient.ShortcutActivity` resumed and read `resource-id="app.tileshell.testclient.a:id/shortcut_id"`. |
| R3T-21 | NOTE | 384 | E13's new "830-ms line also asserts `quick_burst`" depends on the AVD's Contacts app. Phase 02 E7 holds `slot:PEOPLE`, which is `com.android.contacts/.activities.PeopleActivity` here, and that activity publishes exactly one shortcut. | `qa/phase-02/scripts/run_all.sh:44` (`e7.sh … slot:PEOPLE`); `dumpsys shortcut` on emulator-5554: `shortcut-add-contact`, activity PeopleActivity. | E13: "phase 02 E7's held tile `slot:PEOPLE` bursts with Contacts' one manifest shortcut; the row saves `dumpsys shortcut` for `com.android.contacts` first and expects `quick_sat:0` only". |

Round-2 items checked and correctly applied: T11-11, T11-12, T11-13, T13-8, C-3, C-5, C-6, C-9, C-10, C-17, C-21, C-25, C-31
