BLOCKING: 7 · SHOULD-FIX: 6 · NOTE: 2

| id | sev | doc line(s) | finding | evidence (file:line / API fact) | exact change |
|---|---|---|---|---|---|
| R3T-1 | BLOCKING | 288–294, 309–334, 430–431 | The isolated-step blur threshold cannot measure the specified checkerboard, arbitrary photo edges, or thin text. E4 additionally applies the single-blur width to an already blurred backdrop while forbidding a second blur. | `phase-13-fluent-materials.md:288–294` specifies 135-px squares and σ = 52.4615 px. Direct Gaussian integration gives interior checker extrema approximately 0.318–0.682, so original black/white 10–90% crossings do not exist. Normalizing to local extrema instead changes the expected width. At `:325–326`, B is the blurred app-list capture, so applying the prescribed live Gaussian adds another blur; `:431` contradicts that operation. | Replace the width fixture with a display-sized vertical black/white split at x = W/2, sampled away from display boundaries and foreground ink; retain the eight-square checker only for full-image host comparisons. Apply `2.563·σ ±20%` only to that isolated step. For E3–E5 compare the complete sampled profile against the host-blurred closed-surface capture, rather than assigning every feature the isolated-step width. Replace `:431` with: “The live source blurs the composited app list once, including its already blurred wallpaper; E4’s host oracle performs that same additional convolution.” |
| R3T-2 | SHOULD-FIX | 93–95, 276–279, 311–334 | Colour assertions omit the specified noise. Individual pixels may legitimately differ by five levels before rounding, exceeding E2’s ±3 and E3–E5’s ±4; “patch reads” also leaves mean versus individual-pixel comparison unspecified. | `phase-13-fluent-materials.md:94–95`: “noise of ± 5 levels” drawn after tint. Its expected-value formula at `:276–278` contains only blur and tint. E6 separately requires nonzero pixel variance at `:335–337`. | Add: “Acrylic colour measurements use the per-channel mean of a named 10×10 foreground-free patch, compared with the mean host prediction over the identical coordinates. The host prediction includes the specified noise, rounding and channel clipping. Publish the noise hash and coordinate origin with `acrylic_expect.py`. Individual acrylic-pixel comparisons without noise prediction use ±6; opaque fallback and pressed-fill tolerances remain unchanged. E6 separately verifies noise amplitude and determinism.” |
| R3T-3 | SHOULD-FIX | 317–323, 330–334, 367–369 | The referenced E15 driver does not supply E3’s three-reminder/photo fixture. Without a specified image and touch coordinate, builders will also obtain different overlap and blur measurements. | `qa/phase-03/scripts/e15.sh:24–32` creates only `reminder_time` and `reminder_no_time`; the first explicitly depends on the hour. It neither attaches a photo nor creates the specified third reminder. `cortana/ui/RemindersPage.kt:172–176` positions the menu from the actual touch point. | Add a shared phase-13 reminder-fixture helper: fix device time to noon using RV12’s restore procedure; create uniquely named reminders for today at 18:00 with a generated 1970×1000 black/white split PNG, tomorrow at 08:00, and Whenever through the reminder UI. Assert all three rows and the photo node before testing. For the photo case, hold at the photo’s horizontal centre and 10 px above its bottom; assert the required overlap before sampling. Capture the closed-menu control first and delete all three reminders during restore. Explicitly state that existing `e15.sh` is not this fixture helper. |
| R3T-4 | BLOCKING | 327–329 | E4’s Music operation has no target: the albums pivot contains albums, not holdable tracks. Its named fixtures also contain no embedded artwork. | `music/MusicCollectionPage.kt:246–277` opens album detail on tap and handles holds only for `SongItem` and `PlaylistItem`. `:216–218` supplies track holds inside detail. `qa/phase-01/scripts/music_lib.sh:14–20` names the six MUSIC6 MP3s; read-only `ffprobe` inspection of all six found audio streams only, with `attached_pic=0`. | Replace the Music clause with: “Ship two uniquely named phase-13 tracks in one album, each with deterministic high-contrast embedded artwork; scan them into MediaStore and grant audio access. Open that album’s detail page, hold its first track, and assert that `acrylic:music_menu` overlaps the second track’s artwork. Compare foreground-free pixels with the host-blurred closed-menu capture, verify menu text remains sharp, and verify the opaque fallback. Fail if artwork or overlap is absent; remove the tracks and rescan during restore.” |
| R3T-5 | BLOCKING | 344–351 | The satellite sub-row still samples the glyph, inherits an UP that launches the shortcut, and incompletely names phase 11’s required press-style and containment checks. | `review/2026-09-23-phase11-r3-triage.md:50` carries T11-31 explicitly to this review. `phase-11-tile-quick-actions.md:475–486` specifies the top-edge sample, both `press_tilt` and `press_p4`, cancellation before UP, and ±2 when off. `:523–525` checks neighbouring satellites and the held tile. `start/QuickBurst.kt:350–352` centres a glyph occupying 52% of the square. `start/EditGestures.kt:117–124` launches on UP over the same satellite. | Replace the diagonal sample with `(left + 70, top + 10) px`. Seed phase 11’s corner fixture and obtain bounds from fresh `rest=` lines, corroborated by its dump route. Run once under `press_tilt` and once under `press_p4`; restore `press_none`. After held captures, MOVE outside every satellite’s square **and label**, then UP; assert the burst remains and no shortcut launched. Keep one off tolerance, ±2, for all four samples. Add phase 11 E7’s unchanged-held-tile and unchanged-`quick_sat:1..3` comparisons, ±1. |
| R3T-6 | BLOCKING | 338–343 | “The brightest pixel inside the item” cannot locate the radial light: stationary text and the stronger border ring are brighter. A correct moving light fails this assertion. | The document itself sets ring alpha to 0.30 and radial peak alpha to 0.10 at `:340–341`. `cortana/ui/RemindersPage.kt:640–643` draws foreground text over the item. Thus neither the whole-item maximum nor its position measures the radial peak. | Replace the brightest-pixel clause with: “Use a foreground-free interior ROI excluding the 3-px ring. Fit the specified radial profile to the held capture’s colour increment over F; after MOVE +30 px, the fitted centre advances 30±3 px. Assert the expected intensities at the old and new touch coordinates in both captures. Save the ROI and fit residuals; fail if no valid ROI or radial profile exists.” |
| R3T-7 | BLOCKING | 108–109, 240–243, 338–350, 367–369 | E7 assumes sustained measured pressed states that the current reminder menu and pane do not provide. This needs an explicit prerequisite repair; adding lights alone cannot satisfy the row. | `cortana/ui/RemindersPage.kt:619–635` supplies the pressed fill from `chosen`, set by `onClick`, rather than DOWN. `CortanaNavPane.kt:123,136,152,208–211` selects on DOWN; `cortana/CortanaModel.kt:448–453` immediately sets `paneOpen=false`, and `CortanaSessionRoot.kt:100–106` removes it. The already specified 334-ms accent/350-ms transition constants exist at `CortanaPageChrome.kt:122–129`. | Add to task 4: “Repair the existing phase-03 pressed-state/selection lifecycle required by Q1 A: reminder-menu items expose their measured pressed fill while DOWN; pane selection retains the already specified pressed/accent/350-ms transition rather than removing the pane immediately.” Re-cut E7: use the current Home item over Home for sustained pane DOWN/MOVE/UP checks, with F equal to its selected accent; test the unselected `(63,68,64)` state separately with device-timed captures before 334 ms, correlated with the shell’s motion clock. Preserve the FINAL transition values and fail captures outside that interval. |
| R3T-8 | SHOULD-FIX | 240–243, 338–353 | E7 omits the app-list and Music menu items even though task 5 installs lights on them. An implementation that skips both call sites passes all named light checks. | `phase-13-fluent-materials.md:242–243` requires transient-surface items generally. E7 at `:349` explicitly tests only “the three.” The other item implementations are distinct: `applist/AppListMenu.kt:142–162` and `music/MusicCollectionPage.kt:169–184,260–275`. | Add E7 sub-rows for `applist_menu_pin` and `music_menu_new`: DOWN at a foreground-free point, verify the 3-px ring and radial profile against their underlying pressed fill, MOVE +30 px, then cancel outside the item and UP. Repeat with Transparency effects off, verifying the existing pressed fill and absence of both lights. Restore any fixtures and settings. |
| R3T-9 | SHOULD-FIX | 271–273, 372–373 | E12 does not specify where its cross-row evidence survives restarts. Reading the current ring cannot establish E1’s sequence or all E2–E5 surface lines. | `diag/Diagnostics.kt:14–25` stores an in-memory bounded ring. `qa/phase-03/scripts/lib.sh:169–174,209–219` explicitly requires saving before force-stop/layout restore. Music’s existing opener force-stops the shell at `qa/phase-01/scripts/music_lib.sh:24–26`. | Replace E12 with: “Validate the saved, action-scoped `ring_since MARK` slices from E1–E5’s selected runs on this build, with matching APK stamps. Save slices before every restart, fixture rewrite or layout restore. Each surface’s show action must supply its own expected line; validate E1’s reasons in its saved step order. Missing source evidence fails; do not search an unsliced current ring or evidence from an earlier build.” |
| R3T-10 | BLOCKING | 374–380 | E13 takes its MARK after the operation that can produce the required failure. A correct cached failure can therefore be excluded from the slice. | `StartActivity.kt:164–186` uses `beyondViewportPageCount = 1`, composing the app-list page while Start shows. The static source is cached per image/theme at `phase-13-fluent-materials.md:231–233`; nothing requires another build attempt when swiping to that already composed page. | Replace the sequence with: “Delete the fixture; save existing ring evidence; take MARK immediately before force-stop and Home; then swipe to the app list. Read the new process’s slice from that MARK for the failure and absence of a successful rebuild.” Do not require the failure to occur after the swipe. |
| R3T-11 | BLOCKING | 165–168, 364–366, 422–424, 443 | E10’s memory control cannot isolate the cache and its release target includes the image being removed. Precomposition can allocate the cache before the Start measurement, letting an oversized cache pass the page-difference check. Correct release can then fall more than 2 MB below that image-bearing baseline. | `StartActivity.kt:170` precomposes the neighbouring page. `phase-13-fluent-materials.md:165–167` budgets approximately 10 MB for the static bitmap; `:365–366` measures Start with the checker already set and then compares removal against that figure. | Replace the memory sequence with three explicit controls in one process: “Warm Start and the app list with no picture and record baseline A. Set the fixture with acrylic off, warm both pages and record B. Enable acrylic, wait for the successful rebuild and record C; require C−B ≤14 MB. Remove the picture, return through the same warmed pages, and require PSS within 2 MB of A. Use the median of three samples one second apart in each state; never force-stop between states. Use A for the no-image and released-live-layer checks.” |
| R3T-12 | SHOULD-FIX | 173–175, 367–371 | E11’s blanket “re-run unchanged” wording does not delimit the regressions to this phase’s changes. It also imports E15’s screenrecord clock despite the phase’s own-clock rule. | `INDEX.md:92` requires specific affected rows/sub-steps, reserving whole gates for Jeremy. `phase-01-start-live-tiles.md:197,204` includes unrelated install/clock-aging and Weather checks. `qa/phase-01/scripts/music8.sh:32–40,139–148` includes playlist persistence work. `qa/phase-03/scripts/e15.sh:68–90` derives timings from `ffmpeg -vf fps=60`. | Replace the blanket clause with: “Run the affected sub-steps: phase 03 E15 pane/menu fills, geometry, selection and motion; phase 02 E2 hold-menu display and Pin action; MUSIC8 menu opening, selection and dismissal; phase 01 E12 app-list navigation/search/jump and foreground geometry; E19 chrome on Start and the app list. Preserve their expected values, using this phase’s shell-clock method for affected motion. Run the exported-component check. Broaden only for further changed code; whole gates remain Jeremy’s end-of-project run.” Apply the same scope to `:173–175`. |
| R3T-13 | SHOULD-FIX | 74–82, 237–239, 317–337, 360–363 | No asserted row distinguishes a live per-frame backdrop from a snapshot taken when the surface opens. All visual inputs remain static; E9 only records cost. | `phase-13-fluent-materials.md:74–82` requires recording each frame. E3–E6 assert settled images; E9 at `:360–363` deliberately has no performance verdict. A frozen initial capture satisfies those checks. | Add an instrumentation sub-row using the production live-source composable: “While one surface remains open, change a deterministic backdrop from a vertical black/white split to its inverse and back on specified shell frames. Capture each settled state and compare with the independent host oracle; assert the surface was neither dismissed nor recreated and its foreground stayed sharp. Repeat off, requiring an unchanged opaque fallback. Save shell frame timestamps and captures; fail a frozen initial layer.” |
| R3T-14 | NOTE | 383–385 | P1 refers to this phase’s nonexistent `reason=unsupported` branch and can be read as a dependency on the pending R4 probe. | The phase’s rule at `:126–140` explicitly excludes cross-window capability; its diagnostic reasons at `:157–158` contain no `unsupported`. `INDEX.md:41` leaves R4 pending, while `:45` says phases 11–18 do not wait for it. | Replace P1 with: “If available, attach R4’s cross-window blur results as recorded information for phase 04. Phase 13 has no `reason=unsupported` branch and does not wait for R4; its in-app rule is tested independently.” |
| R3T-15 | NOTE | 254–257 | Task 7 retains the obsolete pre-merge contingency for `record`. | `INDEX.md:94` records the phase-15 merge. `qa/phase-03/scripts/lib.sh:177–206` already supplies ring helpers; `:391–395` supplies `record`, and `:140–145` handles recorded-only rows. The file contains no `battery_saver_on/off` definitions. | Replace `:254–257` with: “Reuse the merged phase-03 `lib.sh` implementations of `ring_mark`, `ring_since`, `reply_since`, `record`, and recorded-only `row_end` handling. Task 7 adds only the two battery-saver helpers described above; do not create duplicate helper implementations.” |

- T11-8
- T12-3
- T13-4
- T13-5
- T13-7
- T13-9
- T14-11
- C-3
- C-5
- C-18
- C-25
- C-26
- C-31
