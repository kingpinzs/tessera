BLOCKING: 2 · SHOULD-FIX: 11 · NOTE: 6

| id | sev | doc line(s) | finding | evidence (file:line / API fact) | exact change |
|---|---|---|---|---|---|
| R3D-1 | BLOCKING | 115-118, 174-175, 364-366 (E8 b), 134-135 | E8(b) cannot pass on a correct shell, for two reasons. (1) `… 1 (0 shown)` can never be printed: Android deletes a disabled shortcut that is not pinned, so the query returns 0 and the `isEnabled` filter never sees it. (2) "with a burst open on it, the verb" delivers the verb with `am start` of tileclient-b's activity. That puts a window in front of Start, and this doc's own Decision (l.134-135) closes the burst on Start stopping (`stop`) before `onShortcutsChanged` arrives. So `burst closed: shortcuts changed` is never produced, and E14 needs that line. | API: `ShortcutManager.disableShortcuts`: per developer.android.com "Disable shortcuts", it "removes the specified shortcuts from the static and dynamic shortcuts list and disables any pinned copies". The shell pins none (l.26, l.111). The verbs live in `VerbActivity`, an Activity (`testapps/common/src/main/kotlin/app/tileshell/testclient/VerbActivity.kt`). tileclient-b's manifest declares only that activity and a provider (`testapps/tileclient-b/src/main/AndroidManifest.xml`). | E8(b) → "(b) a disabled shortcut: `am start` tileclient-b with the `disable` verb, Home, MARK, hold its tile → no burst. The slice holds `[quick] shortcuts for app.tileshell.testclient.b/app.tileshell.testclient.VerbActivity/0: 0 (0 shown)` and `[quick] no burst on <id>: no shortcuts` (Android drops a disabled, unpinned shortcut from the dynamic list, so the `isEnabled` filter is proven by the selection rule's JVM test, not here). Then run the `reset` verb. With a burst open on tileclient-b's tile, run `adb shell am broadcast -n app.tileshell.testclient.b/app.tileshell.testclient.ShortcutVerbReceiver --es verb disable` (no window comes to front) → `[quick] burst closed: shortcuts changed`, edit mode stays on. Then `reset` (RV12)." Decisions "QA fixtures" gains: "tileclient-b declares an exported `ShortcutVerbReceiver` that runs `disable` / `badicon` / `reset` with no UI." |
| R3D-2 | BLOCKING | 133, 334-335 (E5) | E5's `KEYCODE_HOME` sub-step cannot pass on this AVD. Phase 02's FINAL gate recorded that pressing Home while Start is the resumed home activity never reaches the shell here, so no burst closes and edit mode stays. The `home` close reason that E14 needs is also never produced on the AVD. | `qa/phase-02/README.md:73-75`: "this AVD does not re-deliver the home intent, so `onNewIntent` is never called". Also `qa/phase-02/scripts/gestures.sh:29-31`. `regress.sh:93-96` drives the same path with the drawn Windows key (`nav_windows`, `bars/SystemBars.kt:201`), which calls `homeEvents.tryEmit(true)` (`StartActivity.kt:200`) → the collector that exits edit mode (`StartActivity.kt:114-117`). | E5: "`KEYCODE_HOME` → …" becomes "tap the drawn Windows key (`tap_node nav_windows`, which sends the same home event as the HOME intent, `StartActivity.kt:200` / `:114-117`) → Start's page (…), no burst, no edit mode, `[quick] burst closed: home`". Phone-only gains "P6 system Home (`KEYCODE_HOME`) with a burst open → no burst, no edit mode, `[quick] burst closed: home`" (this AVD never re-delivers the home intent to a resumed Start). Decisions l.133: "Home → StartActivity's existing exit closes both (the HOME intent and the drawn Windows key reach the same `homeEvents` collector)". |
| R3D-3 | SHOULD-FIX | 434-435 (edge), 128-135 | The edge case "a hold in edit mode on another tile (moves the selection and opens that tile's burst; a hold on the held tile in edit mode is a drag)" has no Decision behind it and contradicts the built gestures. In edit mode `EditGestures` has no hold timer: a still press settles only when the finger moves or lifts. So a long still press on another tile is a tap on release, and a still press on the held tile is a tap that EXITS edit mode (R6 §1.5.1(a)), not a drag. Building "opens that tile's burst" would need a new 783-ms timer in phase 02's edit-mode branch, and no ruling asks for one. | `start/EditGestures.kt:97-111` (edit-mode `Hit.Tile` → `awaitPress` → Moved / Tap / Cancelled only), `:103-105` (tap on the held tile → `requestExit`), `:387-395`. | Edge case → "A press in edit mode, with or without a burst open, has no hold timer, as phase 02 built it (`EditGestures.kt:97-111`). A still press on another tile, however long, is a tap on release: with a burst open it only closes the burst (Decisions 'Taps and events'); without one it moves the selection (R6 §1.5.1). A still press on the held tile is also a tap: it closes an open burst, and with no burst it exits edit mode (§1.5.1(a)). A press that moves drags. Only the hold that enters edit mode opens a burst." |
| R3D-4 | SHOULD-FIX | 122-127, 245 (task 4), 323-327 (E4) | The satellite launch never says where `startShortcut` runs relative to the Start exit, and the failure path as written can leave Start blank. A tile's launch runs in `pendingLaunch` AFTER the exit's last frame. If a satellite's `startShortcut` runs there and throws, nothing pauses Start, so no entrance plays and Start stays on the exit's last frame (rows faded, grid scaled) until the next Home. Two more gaps: calling "`RecentApp.opened(heldTile)`" at tap time moves the tile out from under the exit, which `launchApp` deliberately avoids; and `null` options drop the N-09 splash request that tile launches send. | `StartActivity.kt:207-217` (exit loop, then `pendingLaunch?.invoke()`), `:244-248` (tile tap), `:253-271` (options bundle; `UseCounts.record`; `pendingRecent` "recorded here but APPLIED on the way back, in onResume … the tile must not leave the grid under the finger"), `:329-336` (onResume applies `pendingRecent` and plays the entrance; nothing else does). | Decisions "Satellite tap" becomes: "`startShortcut(pkg, id, satelliteBounds, <launchApp's options bundle>, user)` runs where a tile's launch runs: in `pendingLaunch`, after the Start exit's last frame (`exitTappedId` = the held tile's id). It uses `launchApp`'s bookkeeping: `UseCounts.record`, `pendingRecent` = the held tile's key (so `RecentApp.opened` applies in onResume, never at tap time), and `returningFromLaunch`. The burst vanishes in the exit's first frame; edit mode exits meanwhile with R6 §1.5's exit. On `ActivityNotFoundException` / `IllegalStateException`: nothing launches, the `failed` line is written, `returningFromLaunch` is cleared, and Start plays its entrance at once (`StartAnimation(entranceElapsedMs = 0f)` + `entranceToken++`, as onResume does), so it is never left on the exit's last frame and is not in edit mode." Build task 4 follows. The failure branch is proven by a JVM test of the launch path with a throwing `startShortcut` fake, and E14 exempts the `startShortcut failed` pattern on that basis. |
| R3D-5 | SHOULD-FIX | 16-17, 110, 115-118, 157, 210-216, 390-393 (E14) | The query is keyed on "the tile's own `AppEntry.user`" and "the tile's component". Those exist only for `TileTarget.App`. Start also has Shell tiles (including the Start settings tile that E15 holds, which has no AppEntry), Unassigned slot tiles and Secondary tiles, and no Decision says what they query or which reason they log. The reason strings are never listed either. The rows use `no shortcuts`, `folder`, `not the shortcut host` and `profile quiet`; "any exception from the query" has no string, and no row can produce it. So E14's "each `[quick] no burst` reason appears at least once" is undefined, and cannot pass. | `start/StartPage.kt:94-102`: `TileTarget` = App(entry) / Unassigned(slot) / Shell(name) / Folder / Secondary(owner, tileId). `StartActivity.kt:276-280`: `weather` → WeatherActivity, `settings` → SettingsActivity, `cortana` → a voice session with no activity. | Decisions "No burst" gains: "What a tile queries: `TileTarget.App` → its AppEntry's component and user. `TileTarget.Shell` → the activity its launch opens (`StartActivity.kt:276-277`: `weather` → `app.tileshell/.weather.WeatherActivity`, `settings` → `app.tileshell/.settings.SettingsActivity`), under the shell's own user. `cortana` opens no activity → `no shortcuts`. `TileTarget.Unassigned` → `no app`. `TileTarget.Secondary` → `secondary tile` (it stands for one item inside its app, not the app; agent). The reasons, exactly: `folder` · `no app` · `secondary tile` · `off screen` (R3D-8) · `not the shortcut host` · `profile quiet` (quiet mode, or a locked private space) · `no shortcuts` · `query failed <exception>`." Harness l.157 lists them. E14: "each reason at least once, except `query failed`, which the shortcut source's JVM test drives with a throwing fake". E3 gains an unassigned-slot tile and a secondary tile (phase 02 E5's `secondary.requestCreate`), each with its reason. |
| R3D-6 | SHOULD-FIX | 78-80, 454 (edge) | The label is drawn in white OUTSIDE the satellite, on the dimmed page. In the light theme that page is near-white, so the label is unreadable. The edge case contradicts the Decision: "the satellite fill and label follow the theme like tiles", whereas a tile's label is white on accent in both themes. | `ui/tokens/Palette.kt:27` (light background #FFFFFF); `start/EditMode.kt:103-104` (light-theme wallpaper dim = (0.659,0.659,0.659) at α 0.37) → the page under a label is ≈ (223,223,223), and white text on it is ≈1.3:1 contrast. R6 §1.1.6. Phase 02 gives the discs the theme foreground (R6 §1.2.4). | Satellite l.78-80: "…`getShortLabel()` in the 12-epx caption class, in the theme's text colour (`ShellColors.text`: white on Dark, black on Light — the label sits on the dimmed page, not on the tile, just as the discs take the theme foreground, R6 §1.2.4), one line …". Edge l.454: "(the satellite fill is the accent, as on a tile; its label takes the theme's text colour)". H3 gains "the label in both themes". |
| R3D-7 | SHOULD-FIX | 79, 88-89, 63-66, 441 (edge) | In the line arrangement, neighbouring labels overlap. Each label box is "the satellite's width plus one gutter each side", but the satellites sit "one gutter apart", so adjacent boxes overlap by one gutter and two long labels collide. Separately, the edge case says "a tap on a satellite's label counts as the satellite", but no Decision puts the label in the hit area: `hitTest` gains "a satellite hit" only. | Doc l.79 with l.88-89. At 1080 px one gutter = 13.1 px (`ui/tokens/StartGrid.kt:11`, 17.5/1440 of the width). | l.79: "ellipsised at the satellite's width plus half a gutter each side (neighbours one gutter apart in the line arrangement then never overlap)". "Where the burst lives" gains: "a satellite's hit rectangle is its square plus its label's box". |
| R3D-8 | SHOULD-FIX | 68-71, 89-95, 141-146, 335-336 (E5), 443 (edge) | Clamping is under-specified in three ways. (1) The rest rectangle is recomputed from the tracked bounds every frame, and clamping is part of that. So a scroll that carries the tile near an edge re-clamps the satellites and can switch corner → line mid-scroll, with the rest jumping and no spring. Yet E5 asserts the satellites move by exactly the tile's Δy. (2) The horizontal bound has two values: "the screen's left or right edge" (l.91-92) against "inside the grid margins" (l.443). (3) Since J4, holding the promoted tile returns it to its stored grid cell, and nothing scrolls that cell into view. A cell below the fold leaves four satellites clamped against the page edge with no tile beside them. | `start/StartPage.kt:477-479` (edit mode suspends the promotion). The only scrolls in edit mode are the band's (`StartPage.kt:513-519`) and `DragAutoScroll` (`:746-762`); `EditGestures.kt:78-81` enters with no scroll. | The Arrangement Decision gains: "The arrangement and its clamping are decided when the burst opens, from the held tile's bounds in that frame, and again when a resize re-anchors it. Between those, each satellite keeps its offset from the tile and rides it (the contraction, a scroll). The page edge and the drawn bars cover a satellite carried past them, as they cover the tile. The horizontal bound is the grid's margins (`StartGrid.leftMarginPx` / `rightMarginPx`), replacing l.91-92's 'the screen's left or right edge'. A held tile whose drawn rectangle lies wholly outside the page area when the burst would open opens no burst: `[quick] no burst on <tileId>: off screen`. Only the promoted tile can do this since J4, because its grid cell can be scrolled out of view." E16 gains a sub-row on `baseline_layout-tall.json` with the fixture tile's cell below the fold → edit mode, no `quick_burst`, and the `off screen` line. |
| R3D-9 | SHOULD-FIX | 157, 68-71, 244 (task 3), 359-360 (E7) | Nothing says WHEN `[quick] satellite <i> rest=` is written, but E7 needs "every rest= line equals the dump's bounds ± 1 px". The spring settles at ≈250 ms, while the entry contraction keeps moving the held tile far longer. A line written at the spring's settle (or per frame) is several px off the dump taken a second later. | `start/EditMode.kt:27-31` and `:108-115`: the entry scale is a saturating exponential with a 67-ms half-life, driven to `SCALE_MS = 700` (`StartPage.kt:698-704`). At 250 ms the pitch is 1 − 0.1·(1 − 2^(−250/67)) = 0.9075, not 0.90. So a tile d px from the fixed point (0.475 × 2340 = 1111.5 px) is still 0.0075·d from rest: ≈6 px for the top-left `slot:PEOPLE` (centre ≈856 px above), and >1 px for the mid-grid fixture. | Harness contracts: "`[quick] satellite <i> rest=[l,t,r,b]` is written once per satellite each time the burst comes to rest: in the first frame where the open spring has settled AND the held tile's tracked bounds equal the previous frame's. It is written again after each resize re-anchor and each scroll end, and never per frame." |
| R3D-10 | SHOULD-FIX | 102-104, 185 (T11-5), 345-347 (E6) | "settle = first frame within 1 px of rest" measures a zero crossing, not settling. The spring passes through rest before it settles, so the metric usually reports ≈180-190 ms and cannot tell a slow settle from a fast one. Under the correct definition (from then on, within 1 px), E6's own fixture settles 0.9 ms inside its 250-ms bound, so one late vsync fails a correct build. "Within 1 px of rest by 250 ms" is a property of the travel distance, not of the spring alone. | ζ = 0.65, k = 1500: ωn = 38.73, ωd = 29.43, ζωn = 25.17 s⁻¹. The deviation (1 − x) crosses zero at 77, 184 and 291 ms, and is under 1 px from ≈179 to ≈191 ms, so about three runs in four report that window. E6's MEDIUM fixture is drawn 342.75 px (`StartGrid.kt:14` at 1080 px). Its rest centre is 171.4 + 48 + 82.4 = 301.8 px out on each axis, a 426.8-px diagonal travel. The deviation falls below 1 px for good at ≈249.1 ms. A WIDE tile (≈567 px of travel) settles at ≈257 ms. | T11-5 line (l.185): "settle = the first frame from which every later frame of the motion is within 1 px of rest (written when the motion ends)". Motion l.103: "…within 1 px of rest by ≈249 ms for a medium tile's ≈427-px travel (a longer travel settles later: wide ≈257 ms)". E6: "settle ≤ 250 + 17 ms (RV11's one frame)". |
| R3D-11 | SHOULD-FIX | 167-181 (QA fixtures), 268-270 (Seeding), 300-305 (E1), 368-371 (E9) | The fixtures as written would break E1, E3 and E9 on a correct shell, in two ways. (1) The three test APKs compile ONE shared `VerbActivity`, and the verbs live there. If "tileclient-b publishes `qa_dyn` on launch" is built there, it also runs when tileclient-a launches (E2's short press, E4). `qa_dyn` then attaches to tileclient-a's only launcher activity, so the query reads `6 (4 shown …)` where E1 / E9 assert `5`. (2) A dynamic shortcut exists only after its app has run, and Seeding installs tileclient-b but never launches it, so E3's first hold on tileclient-b finds 0. | `testapps/tileclient-b/build.gradle.kts` sourceSets: "VerbActivity and TestImageProvider are shared by the three test APKs" (`kotlin.directories.add(... testapps/common ...)`). tileclient-a has one launcher activity (`testapps/tileclient-a/src/main/AndroidManifest.xml:20-28`). The `setActivity` default attaches a dynamic shortcut to the first MAIN / LAUNCHER activity (C-21's API fact). | QA fixtures: "tileclient-b's dynamic shortcuts are published only in `app.tileshell.testclient.b` (a `packageName` check in the shared `VerbActivity`, or a tileclient-b-only source set); tileclient-a publishes none." Seeding: "…installed from their debug APKs; then tileclient-b is started once with the `reset` verb (publishing `qa_dyn`), then Home." |
| R3D-12 | SHOULD-FIX | 336-344 (E5), 360-362 (E7), 167-170 | The press-light pixel "60 px from the touch point along the diagonal" falls inside the satellite's centred glyph box. The fixture shortcuts' icons are unspecified, so from phase 13's build on, the `F + 0.05·(255 − F)` sample can land on a glyph pixel on a correct build. Separately, the Transparency-off tolerance is ± 1 here but "rest fill ± 2" in phase 13 E7 (the same sub-row). | The small-tile glyph is 0.52 × the shorter side (`start/TileView.kt:398`) = 85.7 px on a 164.8-px satellite. Centred, it spans 39.6-125.3 px on each axis. The diagonal pixel is at (left + 52.4, top + 52.4), inside that span. `phase-13-fluent-materials.md:344-350` carries the same sample and the ± 2. | E5 and E7, and phase 13 E7's satellite sub-row: "the pixel 60 px from the touch point along the satellite's top edge, (left + 70, top + 10) px — outside the centred glyph box and clear of the 3-px ring — reads F + 0.05·(255 − F) ± 4". Use one Transparency-off tolerance in both docs (± 2). QA fixtures: tileclient-a's five shortcuts declare one `android:icon`, an adaptive icon with a monochrome layer, so the glyph rule's monochrome branch is exercised. |
| R3D-13 | SHOULD-FIX | 11-16, 52-54, 96-99, 239-240 (task 1) | The doc never says when, or on which thread, the shortcut source runs. Yet the satellites must start "in the first changed frame of the entry", and "nothing about the hold changes" (phase 02 E7: 783 ± 33 ms to the first changed frame). The natural reading, querying at `edit.enter`, puts binder calls and up to four icon decodes on the main thread in the very frame phase 02 measured. | `start/EditGestures.kt:78-81`: the timeout and `edit.enter` run in the pointer-input coroutine on the main thread. API: `LauncherApps.getShortcuts` and `getShortcutIconDrawable` are IPC into system_server (`ILauncherApps`), and the latter also decodes an image per icon. | Decisions "Data source" gains: "The source runs off the main thread (`Dispatchers.IO`), with icons decoded there too. It starts at the DOWN of every press on a tile outside edit mode (the hold race, `EditGestures.kt:74-79`) and is cancelled when the press ends before 783 ms. At 783 ms edit mode enters without waiting. The burst uses the result if it is in hand; otherwise it opens in the first frame after the result arrives, and its `[quick] motion open` t0 is that frame." Build task 1 names it. |
| R3D-14 | NOTE | 136-140, 252 (task 6), 375-378 (E10) | The "consumed-DOWN" build-start check already has an answer in the code: today a hold on a transport control enters edit mode, and the control never fires. The fix is therefore required, not conditional. The doc's premise "a still finger sends none" also does not describe E10's driver: `input swipe cx cy cx cy 1000` sends same-position MOVEs, and none of them is consumed either. | `start/TileView.kt:304-308` consumes only a control press's DOWN and UP; `waitForUpOrCancellation` consumes no MOVE. `EditGestures.kt:69` (`awaitFirstDown(requireUnconsumed = false)`, the parent, which gets the Main pass after the child) still receives that DOWN, and `awaitPress` checks `isConsumed` only on later events (`:392`), so the hold race times out and `edit.enter` fires. TileView's `pointerInput` is keyed on `interactive` (`TileView.kt:292`; `StartPage.kt:831`), so it restarts at entry and the control's release is lost. | l.138-140: "Recorded from the code: today a hold on a control enters edit mode (evidence above). That is a phase 01/02 defect, fixed at the producer here: `EditGestures`' not-in-edit-mode branch returns when `down.isConsumed` (a DOWN the tile's own handler already took), recorded in the INDEX Change Log for phase 02." Build task 6 drops this check. |
| R3D-15 | NOTE | 37-38, 127, 241-242 (task 2), 246-251 (task 5) | Build task 2 files Back under the `EditGestures` changes, but Back lives in StartActivity. The doc also never lists the FINAL parts it edits beyond phases 01 and 10's intent extras. And Start settings' extra already exists, so a builder might add a second one. | Back is StartActivity's `backEvents` collector (`StartActivity.kt:127-130`: `edit.active -> edit.requestExit()`). AppCatalog's `LauncherApps.Callback` (`apps/AppCatalog.kt:54-85`, phase 02 task 5) has no `onShortcutsChanged`. SettingsActivity already opens a page from `EXTRA_PAGE = "page"` in both onCreate and onNewIntent (`settings/SettingsActivity.kt:58`, `:97-100`, `:117`). | Task 2: "…the tap-elsewhere / disc / drag rules in `EditGestures`, and Back in StartActivity's `backEvents` (a first branch: burst open → close the burst only)". Task 5: "Start settings' shortcuts carry the existing `SettingsActivity.EXTRA_PAGE` (`"page"`) with the `SettingsPage` name — no new extra; phase 01's ADD is the manifest meta-data only". Add one line listing the FINAL parts this phase edits, each with its INDEX Change Log line when built: `EditGestures` / `StartPage` / `StartEditState` (02, the 2026-09-22 amendment line), StartActivity's Back and launch path (01 / 02), AppCatalog's callback (02 task 5), SettingsActivity's manifest meta-data (01), and MusicActivity's pivot extra plus `music_pivot_header` semantics (10). |
| R3D-16 | NOTE | 446 (edge) | The edge case says "the reflow closes edit mode per phase 02". Phase 02 has no such rule, and no code does it. | Edit mode exits only on the home event, Back, or a tap on the held tile / empty space: every `requestExit` caller is at `StartActivity.kt:117`, `:130` and `EditGestures.kt:105`, `:124`. A column change only re-packs (`StartPage.kt:500`). Show more tiles lives on Start + theme, another activity, so Start stops first. | l.446: "Show more tiles toggled (on Start + theme, so Start stops first: the burst closes with `stop`, and edit mode is as phase 02 left it)". |
| R3D-17 | NOTE | 68-71, 103-104, 135, 433 (edge) | Which rectangle is tracked is not pinned down. l.135 says a live flip "changes nothing", while l.433 says "the burst tracks" during a flip. When the held tile's grid node leaves the composition (a drag starts, an unpin), "run back to the tile's centre" has no tile to track. | TileView's own `graphicsLayer` squashes the tile during a flip and tilts / depresses it on a press (`start/TileView.kt:282-290`). While the tile is dragged, the grid tile is skipped and a `drag:` copy is drawn at the finger (`StartPage.kt:560`, `:580-598`). Bounds copied into state from `onGloballyPositioned` are drawn one frame later. | The Tracking Decision gains: "The tracked rectangle is the held tile's slot as StartPage places it, read outside TileView's own face layer, so a flip or a press never moves a satellite (l.433 → 'the satellites stay still; the flip continues'). When the tile's node leaves the composition (a drag starts, an unpin), the close runs back to its last reported centre. The one-frame trail that bounds copied from `onGloballyPositioned` carry is accepted (H1)." |
| R3D-18 | NOTE | 5 | `depends-on: [01, 02]` omits phases the rows and tasks use. C-23's rule is that the header says what the rows use. | The rows run on `qa/phase-03/scripts/lib.sh`, use `shell:cortana` (E15) and re-run phase 03 E5 (E13). The dump route is phase 05's gesture driver (`qa/phase-05/README.md`). Build task 5, E10 and E15 edit and hold phase 10's Music. | `depends-on: [01, 02, 03, 05, 10]`. All three precede 11 in INDEX's Build order, so nothing moves. |
| R3D-19 | NOTE | 14-15 (Goal) | INDEX's record of the ruling can be read the wrong way. "a tap elsewhere closes it and leaves edit mode" reads as an exit, but the ruling is that edit mode stays. | `INDEX.md:100` vs `PLAN.md` 2026-09-22 R10-Q3 re-ruling ("a tap elsewhere closes the burst and edit mode stays"), and this doc's l.14-15 ("leaves edit mode on"). | INDEX.md:100: "…a tap elsewhere closes it and edit mode stays" (the lead's edit, marked as a 2026-09-23 clarification). |

Round-2 items checked and found correctly applied: T11-11 · T11-12 · T11-13 · T11-14 · T13-8 · C-3 · C-5 · C-6 · C-9 · C-10 · C-17 · C-20 · C-21 · C-25 · C-31
