# Phases 11–19 — review triage (Stage A step 5, round 1, 2026-09-23)

Inputs: review/2026-09-23-phases11-19-design.md (Reviewer 1, "R1", ids `NN-k` / `X-k`) and
review/2026-09-23-phases11-19-testability.md (Reviewer 2, "R2", ids `#1`–`#60`), against PLAN.md (P1–P6, the 2026-09-23
rulings incl. A11 AMENDED, the theme-preset and TV-channels scope adds), INDEX.md (phase table incl. 20, R11/R12/R13, Change
Log), the nine DRAFT docs' Decisions (the 2026-09-23 interview answers are Jeremy's rulings and are not reopened here) and
qa/JEREMY-QA.md.

Two facts established after the reviews, applied throughout:
1. `qa/phase-02/baseline_layout.json` now carries `addedOnce = [phase03:cortana, folder:games:v1, folder:office:v1,
   slot:music:v1]`, `manualSizes` for every tile (10) and `slots = {MUSIC: app.tileshell/…MusicActivity}` (read 2026-09-23);
   `layout_restore` verifies after the shell reloads and compares sizes only for hand-set tiles. So R2 #50's MECHANISM exists;
   what is missing is the per-phase file (C-3).
2. The lead is fixing in code, outside the phase docs: Tess's calendar writes go to a shell-owned LOCAL calendar only
   (qa/phase-03/scripts/j6.sh, evidence qa/phase-03/J6/ — phase 16 Q2 rule 2 brought forward as a safety fix); Tess's
   "play <name>" resolved in the shell's own player (J5); the recogniser's silence-to-"AND" defect (J7). R1 16-2 and the E9
   half of R2 #31 ask for exactly the first of those: "being fixed in code", not doc work — but T16-2 lists what phase 16
   must say about reusing that local calendar.

Code claims verified today (every cite below is against these): `tiles/LayoutStore.kt:127-139` (`assignSlotOnce` checks only
the marker, then writes `explicitSlots + (slot to component)` — an existing hand assignment IS overwritten);
`cortana/action/ActionLayer.kt:647-662` (`writableCalendarId` = IS_PRIMARY else the first calendar with access ≥ CONTRIBUTOR;
caller at `:486`; unchanged in the working tree as of this triage — J6 is in flight); `cortana/CortanaChecklist.kt:37-122`
(14 rows; `calendar` at `:44-45`, `exact_alarms` at `:56-57`, `background_location` at `:62-71` carrying BOTH FINE and
BACKGROUND, the `permissionRow` helper at `:121`); `onboarding/Checklist.kt:90-129` (`calendar` at `:105` — the id collides
with Tess's); `cortana/CortanaPermissionActivity.kt:60-62` (already asks FINE first, then BACKGROUND) and `:27,:36,:96` (the
app-details fallback); `prefs/ShellSettings.kt:17-24` (`StartTheme` has no `transparencyEffects`); `start/StartPage.kt:150,
173,230,232` (package-keyed live content) and `:987` (`private fun rememberBackground`); `StartActivity.kt` — NOTE the file is
`app/src/main/kotlin/app/tileshell/StartActivity.kt`, not `start/` — `:110` (`pageCount = { 2 }`), `:123` (`home: page 0`),
`:149` (`beyondViewportPageCount = 1`), `:151` (`userScrollEnabled = !edit.active`), `:311` (`homeEvents.tryEmit(inFront)`);
`cortana/CortanaService.kt:74` (`open(context, mode)`) and `:87` (`service.showSession(args, 0)`);
`cortana/action/LockGate.kt:22-27` (the EXTRA_SKIP_UI comment) and `:29-41` (exhaustive `when`, no `else`);
`cortana/speech/MicArbiter.kt:16` (an in-process class); `feeds/MusicFeed.kt:101` (`send(Transport)`) and `:159-171`
(publishes under `packageKey(pkg)` + `ActiveTiles.setPackage`); `tiles/ActiveTiles.kt:51`; `start/EditGestures.kt:326-333`
(the promoted tile IS hit-tested since J4); `AndroidManifest.xml:162` (`LiveTileProvider`) and `:261` (`KeyboardConfigProvider`)
— no `FileProvider`; permissions held: INTERNET (`:32`), RECORD_AUDIO, READ_CONTACTS, WRITE_CALENDAR, SEND_SMS, CALL_PHONE,
FINE/BACKGROUND location, READ_CALL_LOG, READ_SMS; NOT held: CAMERA, WRITE_CONTACTS, READ_MEDIA_VIDEO, MANAGE_EXTERNAL_STORAGE,
USE_FULL_SCREEN_INTENT, FOREGROUND_SERVICE_MICROPHONE; `settings/SettingsActivity.kt:65` (`testTagsAsResourceId`);
`gradle/libs.versions.toml:4` (BOM 2026.06.01), `app/build.gradle.kts:21` (minSdk 34); `tiles/TileModel.kt:23-35` (`Slot`:
PHOTOS, PEOPLE, CAMERA, CALENDAR exist); `ShellApp.kt:122,185` (`claimMusicSlot`, `slot:music:v1`);
`qa/phase-03/scripts/provision.sh:35,50-51,91` (install `-g`, HOME / ASSISTANT / SMS roles; no `allow_listener`, no appops, no
`ime`); `qa/phase-01/scripts/recent0922.sh:19-21` (RecentApp in memory only); `qa/phase-02/scripts/regress.sh:120` (DOES grep
`home: page 0` — R1 14-3's worry is real); `qa/phase-01/scripts/e4_part2.sh:19` uses `com.fsck.k9` and `~/android-fixtures/`
holds BOTH `com.fsck.k9` and `eu.faircode.email` (R2 #32 is wrong); `docs/plan/r11-inbox-apps.md` is now an index and
`r11/<app>.md` are still pending; INDEX Change Log 2026-09-22 "JEREMY'S FOUR ASKS" item (3) is the "slot label" rule R1 19-3
could not find.

**Counts (merged, de-duplicated): 100 items — 17 BLOCKING · 36 SHOULD-FIX · 47 NOTE.**
**Classes: 66 DOC-UPDATE · 27 AGENT-CALL · 1 QUESTION FOR JEREMY (carrying 2 questions, §2) · 6 REJECT** (5 of the 6 are
"confirmation only, nothing to change"; 1 is wrong).

Class rule used: a finding whose fix is writable only after a design choice → AGENT-CALL (the choice is made below, one line
of reasoning); a mechanical re-cut to a dated Decision or a missing row with an obvious form → DOC-UPDATE; a change to WHAT
gets built or how it behaves for the user with no ruling → QUESTION; wrong or already handled → REJECT.

---

## 1. Merged findings

Severity is the higher of the two reviewers'. `T<phase>-<n>` per doc, `C-<n>` cross-phase.

### Phase 11 — tile quick actions

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T11-1 | SHOULD-FIX | R2 #1 | DOC-UPDATE | Add **E15** (shell tiles, Q1): hold MUSIC slot tile → `quick_sat_label:0..3` = Songs / Albums / Artists / Playlists (ranks 0–3), `[quick] shortcuts for app.tileshell/0: 4 (4 shown: …)`; `tap_node quick_sat:1` → `MusicActivity` resumed with the Albums pivot header `selected="true"` (name the tag: `music_pivot:albums`); hold `shell:settings` → Start + theme / Tile apps / Setup / Diagnostics, tap satellite 3 → `SettingsActivity` with `settings_diagnostics` in the dump; hold the Weather and Tess tiles → `edit_disc:*` present, no `quick_burst`, `[quick] no burst on <id>: no shortcuts`. Rewrite E3's "unless Q1 says otherwise" and E10's "(per Q1)" to the ruling. |
| T11-2 | SHOULD-FIX | R2 #2 | DOC-UPDATE | Decisions "The promoted tile" and the edge case are stale since J4 (`EditGestures.kt:326-333`: hit-tested "like any other"; edit mode suspends the promotion). Rewrite to J4's behaviour and add a row: hold the promoted fixture tile → edit mode, `recent_app_row` absent, the burst rests around the tile's GRID cell (bounds relative to its unpromoted bounds = E7's corner case ± 1 px), `[quick] burst on <id>: 4 satellites`. Strike "the hold on the promoted tile" from Build task 6. Sequencing: E5 must not start from E4's promoted tile — C-6's force-stop rule. |
| T11-3 | SHOULD-FIX | R2 #3, R2 #50 (11 part) | DOC-UPDATE | Fixtures are seeded through the verified restore, not pinned by hand per row: `qa/phase-11/baseline_layout.json` derived from phase 02's (tileclient-a / -b at named cells, `manualSizes` set, every `addedOnce` marker of the build under test), restored with `layout_restore` at each row's start; E7's variants (bottom row, wide at the right edge, band member, 1.5 screens tall) are named baselines. Rule in C-3. |
| T11-4 | SHOULD-FIX | R2 #4, R2 #55 | DOC-UPDATE | The Acceptance preamble names the dump route for the burst over live tiles: phase 05's gesture-driver `UiDevice.dumpWindowHierarchy` with `Configurator.setWaitForIdleTimeout(0)` (qa/phase-05/README.md); each bounds assertion takes the `[quick] satellite i rest=` line as primary, the dump as corroboration. (C-10) |
| T11-5 | SHOULD-FIX | R2 #5, #13, #18, #30, #34, #39, #49, #53 | AGENT-CALL | Adopt the shell-logged motion clock as the standing rule (C-5). Here: the burst logs `[quick] motion open <tileId>: t0=<uptime> peak=<ms> overshoot=<%> settle=<ms>` and `… close …` from `withFrameNanos`; E6 asserts those numbers; the screenrecord corroborates under phase 05's frame-spacing rule. Reason: the P02 lesson — variable-rate screenrecord cannot be the clock, and phase 11's Decisions already hold the frame maths, only t's source changes. |
| T11-6 | NOTE | R2 #6 | DOC-UPDATE | E13: scope the phase 02 E7 regression to its bracket and geometry sub-rows; its motion sub-rows stay open until C-5's method lands (INDEX row 02: E7 7/10 open on exactly that). |
| T11-7 | NOTE | R1 11-1 | AGENT-CALL | Satellites take phase 13's two lights and NOT the Q6 press style: a satellite is a transient-surface item (Jeremy's phase 13 Q3 B named "satellites" in the pressed-item set), Q6 governs Start tiles; with Transparency effects off / battery saver a satellite shows no press feedback (phase 13's lights obey the switch; W10M final's default is None anyway). One feedback, never two. Record in 11 (Decisions "Satellite", E5/E7 gain a DOWN screencap with the ring + light, H3 wording) and keep satellites in 13's Reveal list. |
| T11-8 | NOTE | R1 11-2, R1 13-3 | DOC-UPDATE | Phase 13 drops the burst-backdrop row, `acrylic:burst`, Build task 4's clause and the Goal's "phase 11's burst backdrop" (phase 11: edit mode's dim IS the backdrop, no acrylic). Satellites stay in the Reveal list (T11-7). |
| T11-9 | NOTE | R1 11-3, R1 14-3 | DOC-UPDATE | Phase 11 E5: "`KEYCODE_HOME` → Start's page" (not "page 0"; phase 14 moves Start to index 1). Phase 14 E1: the line becomes `[start] home: page START, scrolled to top` — a harness change because `qa/phase-02/scripts/regress.sh:120` greps `home: page 0`; phase 14's harness task (which already re-greps the qa tree) lists and updates that grep alongside the `ensure_start` fix. |
| T11-10 | NOTE | R1 11-4, R1 19-4, R1 X-8 (19) | AGENT-CALL | Phase 19 KEEPS phase 11 Q1's Jeremy-ruled set for the Settings tile's satellites (Start + theme / Tile apps / Setup / Diagnostics); phase 19's agent line ("System, Personalisation, Network & wireless, Apps") is withdrawn. Reason: the four are a ruling and the pages people open; a satellite that lands on a category is one tap short of them. The intent extras keep opening the re-homed pages (phase 19 E2 asserts `settings_start_theme` etc. under their categories). Phase 19 gains H8 [accept] so Jeremy can swap them for categories on the phone. |

### Phase 12 — setup wizard

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T12-1 | BLOCKING | R1 12-1 (+ its stale table), R2 #7, #11, #56 (12 part) | AGENT-CALL | Q2 (all of Tess's grants join) applied below the Decision. Agent calls: **(a)** the Tess steps are the nine rows with a grant action — `assistant`, `microphone`, `contacts`, `calendar`, `sms_send`, `call_phone`, `background_location`, `call_log`, `sms_read`; `exact_alarms`, `models`, `service`, `speech_process`, `person_triggers` are observations, said so as the Steps line says it for `samsung_badges` / `listener`. **(b)** Tags are namespaced: `wizard_step:setup:<id>` / `wizard_step:tess:<id>` (the two checklists both have a `calendar` row — `Checklist.kt:105`, `CortanaChecklist.kt:44-45`). **(c)** No third step kind: `CortanaChecklist`'s `background_location` row already carries FINE + BACKGROUND (`:62-71`) and `CortanaPermissionActivity.kt:60-62` already asks FINE first then BACKGROUND (the app's location page, as the platform requires); what changes is the wizard's PARTIAL rule — "PARTIAL counts as done" is Photos-only (a `partialIsDone` flag on the row: true for `photos`, false for `background_location`, whose PARTIAL means "FINE granted, all-the-time still owed"); such a step stays until GRANTED or Not now. **(d)** Order: Setup rows → Tess rows in `CortanaChecklist` order → the presets page; "Skip setup" covers both lists. Then the DOC re-cut: Goal, Scope In/Out, Decisions "Visibility rule" / "Steps" / "The microphone is not a step" (delete) / "Harness", Build tasks 1, 2, 4, E1(b) (add `pm grant` RECORD_AUDIO, READ_CONTACTS, WRITE_CALENDAR, SEND_SMS, READ_SMS, CALL_PHONE, READ_CALL_LOG, ACCESS_FINE_LOCATION, ACCESS_BACKGROUND_LOCATION + `cmd role add-role-holder android.app.role.ASSISTANT app.tileshell`), E2 (N = a hand-listed id set for the `pm clear` state: the Setup rows missing + the nine + 1; ORDER asserted), E3 (walk the Tess steps: the ASSISTANT role sheet tapped by bounds with `cmd role get-role-holders` read back; each runtime dialog; `background_location` → the app's location page, `appops get app.tileshell ACCESS_BACKGROUND_LOCATION` = allow on return; `[wizard] step tess:assistant: granted` lines), E4, E5, E7, P1, H2. |
| T12-2 | BLOCKING | R1 12-2, R2 #8, #12, #17 | AGENT-CALL | Q3–Q6 (six live presets, AI pictures) designed below the Decision. **Preset model** (one table in Decisions; every value H6 [accept]): preset × {accent, Dark/Light, picture, X5 transparency, press style, Tess lens/ring colours, keyboard palette, Transparency effects}. Agent picks: Default = as built (Default Blue X26, Dark, no picture, 0.5, None, HAL lens, built keyboard, On); Windows 10 Mobile (original) = R12's accent / theme / stock picture, and the persona's pre-HAL Cortana drawing (the easter egg's reveal = 1 form, which already exists); HAL = HAL red lens colour as accent, Dark, the HAL picture, 0.35, None, HAL lens, dark keyboard, On; Soft = Light, the Soft picture, an accent from the A16 grid nearest the picture's palette, 0.6, P4 press, lens in the accent, light keyboard, On; Lumia = the A16 grid's Cyan, Dark, the Lumia picture, 0.5, WP8 tilt, lens cyan, dark keyboard, On; Midnight = Dark, the Midnight picture, 0.0 (opaque tiles), None, lens dim, dark keyboard, Transparency effects OFF. **Custom rule**: `theme_preset` key in `start_theme.xml` = the name, set to `custom` by any item change; re-tapping a preset restores every item. **Live-apply**: tapping a preset IS the change (Jeremy: "auto changes the phone so they can see"); "Not now" / Back / Skip after a tap leave it applied — stated. **Pictures**: `art/themes/<name>.png` → cropped/scaled on the host to 1872 × 4056 (1440 × 3120 × 1.3 parallax headroom, portrait) → bundled WebP in the branding source set; APK delta row: ≤ 8 MB total, no single asset ≥ 2.5 MB. **ADDs to FINAL parts** (Change Log lines when built): phase 01 Start + theme gains the preset row + `theme_preset`; phase 03's persona reads two colour keys (lens hue, ring) from `ShellSettings`; phase 05's keyboard reads a palette key through the existing `KeyboardConfigProvider` (manifest `:261`) — and phase 05 E3's ± 32-level colour check is stated to run on the Default preset. **Tags** `preset:<name>`, `preset:Custom`, `theme_preset:<name>`; **diagnostics** `[wizard] preset <name>`, `[theme] preset <name> applied`. **Rows**: E11 presets (both surfaces; `start_theme.xml` keys read back incl. `transparency_effects=false` for Midnight, a tile pixel and a tile-free pixel ± 2 via `e13_pixels.py`, a Tess lens pixel, a keyboard key pixel; one item changed → Custom, other keys unchanged; `preset:Default` → the `pm clear` control values; `am force-stop` → persisted); E12 pictures (APK asset dimensions via `unzip -p | identify`; `art/themes/` sources ≥ 1024 px wide; with phase 13 present `[fluent] static backdrop rebuilt for <uri>` follows each preset); E13 the original preset (placeholder until R12, as phase 17 E19 says). **H rows** H6 each look, H7 labels readable over each picture on the phone, H8 the four pictures (no metric), H9 the original against R12. **Edges**: preset under battery saver (13's rule wins, key still written); missing picture asset (fallback + line); a preset over a user's own picture (the user's picture is NOT kept — say it); Transparency effects toggled after a preset → Custom. E3's restore becomes `preset:Default` (not "accent back through Settings"). |
| T12-3 | BLOCKING | R1 12-3, R1 13-2, R1 X-3 | AGENT-CALL | Order **11 → 13 → 12 → 14**; phase 12 depends-on becomes `[01, 03, 05, 10, 13]`. Reason: a preset writes `StartTheme.transparencyEffects`, a field only phase 13 adds (`ShellSettings.kt:17-24` has none); 13 does not depend on 12, and 12 says it builds presets "in their final form" (Rule 16). INDEX phase notes for 12 / 13 and PLAN's 2026-09-22 order line get the swap (recorded here, edited when this triage is applied). |
| T12-4 | SHOULD-FIX | R1 12-4, R2 #9 | DOC-UPDATE | Tag `wizard_why`; a table of the why lines per row id (setup + tess) in Decisions; E2 / E3 assert `wizard_why` text equals the table's line for each step seen; H1 covers the copy; Build task 2 names it. |
| T12-5 | SHOULD-FIX | R2 #10, R1 12-7, R1 X-5, R2 #43, #51, #59 | DOC-UPDATE | A named template row **E14 "wizard step added"** that later docs cite: "`pm clear` + revoke <grant> + Home → `wizard_step:<ns>:<id>` present with its `wizard_why`; grant from adb → absent; `provision.sh` gains the grant; phase 12 E1 re-run." Phase 12's persistence rule (a finished install is not re-summoned, H4) is restated in one sentence in each of 15–19 (C-4). |
| T12-6 | NOTE | R1 12-5 | AGENT-CALL | While the wizard shows, the pager is NOT composed (the feeds keep running — that is what "tiles keep running underneath" means; there is nothing to draw for); Start composes when the wizard finishes, under the X7 page motion. E2's "no `start_page` node" then holds as written. Reason: a covered-but-composed Start would be in the dump, and z-order assertions by bounds are weaker than absence. |
| T12-7 | NOTE | R1 12-6 | AGENT-CALL | "Lumia" and "Windows 10 Mobile (original)" are `Brand` strings and the stock pictures live in the A10 branding source set (like the Segoe substitute); if R12 finds no shippable picture, the original preset ships with R12's accent + theme and NO picture — still a preset, stated, not a placeholder (no v1 → v2). |
| T12-8 | NOTE | R1 X-7 | DOC-UPDATE | One line in Decisions "Host": the wizard sits above the pager, the burst layer, the pin band and the picker; a burst cannot coexist with it (edit mode is under it); a pending pod-bay request waits (T14-7). |
| T12-9 | NOTE | R2 #57 (12 part) | DOC-UPDATE | E13 (original preset) reads "not runnable until R12 lands; the doc cannot go FINAL before it" in phase 17 E19's form. |

### Phase 13 — Fluent materials

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T13-1 | BLOCKING | R1 13-1, R2 #14 | AGENT-CALL | Radial light values (P4, H4 extended): a white radial gradient centred on the touch point, radius r = 40 epx (120 px at FHD+; scales with px/epx), peak alpha 0.10 at the centre falling linearly to 0 at r, redrawn on every MOVE, gone in the first frame after UP, no motion of its own; the 1-epx 30 % border ring unchanged. **Sample rule** (stated once, cited by E11's regression of phase 03 E15): every measured pressed-fill sample is taken ≥ r + 2 epx from the touch point or after UP. Re-cut E7: DOWN at the item's centre → ring as before; pixel at the touch point = pressed fill + 0.10·(255 − fill) ± 4; at 0.5 r between the two; beyond r (clear of the ring) = R7 §3.6.3's (79,84,80) ± 2; `MOVE` +30 px → the bright centre moved (two captures); UP → gone; acrylic off → no ring, no light. Rewrite Decisions "Reveal on touch (per Q3; lean A)", Build task 5, H4 to Q3 B. |
| T13-2 | SHOULD-FIX | R2 #15 | DOC-UPDATE | E3 / E4 / E5: "expected = 0.8·T + 0.2·GaussianBlur_host(σ = 0.57735·r + 0.5 px)(fixture) at the same pixel", computed on the host from the pulled fixture / the acrylic-OFF control capture by `qa/phase-13/scripts/acrylic_expect.py`; never read from the capture under judgement. |
| T13-3 | NOTE | R2 #16 | DOC-UPDATE | E11: phase 03 E15 has never run (INDEX row 03) — "run, first time if necessary, and pass"; E15's driver is a dependency of this gate. |
| T13-4 | NOTE | R2 #17 | DOC-UPDATE | E1 sub-row: after `preset:Midnight` (phase 12, built after 13 under T12-3 — so this sub-row is written now and runs at 12's gate), `[fluent] acrylic=off reason=setting`. |
| T13-5 | NOTE | R1 13-4 | DOC-UPDATE | `StartPage.rememberBackground` is `private` (`StartPage.kt:987`): the static source needs the decode exposed (a `BackgroundDecoder` object both call) — an ADD to phase 01's part named in Build task 2 and the Change Log line. |
| T13-6 | NOTE | R1 13-5, 13-6 | REJECT | Confirmations (platform claims, light-theme H1). Nothing to change. |
| T13-7 | NOTE | R2 #18 | DOC-UPDATE | E8's four motion numbers take the shell's `[motion]` clock (C-5); screenrecord corroborates. |

### Phase 14 — pod bay

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T14-1 | SHOULD-FIX | R1 14-1, R2 #19 | DOC-UPDATE | Strike the four widget conditionals (Scope Out clause, Build task 7, P4, the last edge case); one line "ruled out 2026-09-23 (Q1 B)". |
| T14-2 | SHOULD-FIX | R2 #20 | DOC-UPDATE | E3 Weather: the expected strings for `weather_fixture.py`'s report are written by hand in the row (place, "72°", condition text, "H 80° L 60°", precipitation), as the Agenda sub-row already does. |
| T14-3 | SHOULD-FIX | R2 #21, #54 | DOC-UPDATE | Harness line: after any launch (E3 Now playing, E9, E13) `am force-stop app.tileshell` + Home before the next grid assertion (C-6). |
| T14-4 | NOTE | R1 14-2 | DOC-UPDATE | depends-on comment: phase 10 is a soft dependency — the Now playing pod works with any session; its title tap opens the MUSIC slot app. |
| T14-5 | NOTE | R1 14-3 | DOC-UPDATE | See T11-9: the renamed line and `regress.sh:120`. |
| T14-6 | NOTE | R1 14-4 | DOC-UPDATE | The Reminders pod opens Tess through `CortanaService.open(context, mode)` (`CortanaService.kt:74`), which calls the platform's `showSession(args, 0)` (`:87`); the destination is a new `EXTRA_*` on that bundle — an ADD to phase 03's session arguments (same ADD, right method name). |
| T14-7 | NOTE | R1 14-5 | DOC-UPDATE | A pending `PodBayRequests` open while the wizard shows waits until the wizard finishes (reuse the `SecondaryTiles` "Start visible" gate); stated in Decisions "Route to Start". |
| T14-8 | NOTE | R2 #22, #23 | DOC-UPDATE | E6: the driver reads `wall=` from the ring, never the host clock. E1 / E10 absence assertions: record `StartActivity.kt:149` (`beyondViewportPageCount = 1`) + `gestures.sh`'s `app_list`-absent assertion as the build-start check, and the `[start] page=<name>` line as the second source. |
| T14-9 | NOTE | R1 14-6 | REJECT | Confirmation (Agenda pod after phase 16; Birthdays through `CalendarFeed`). Nothing to change. |
| T14-10 | NOTE | R1 X-6 (14) | DOC-UPDATE | "no network beyond Weather's own refresh (A11)" → "(offline preferred; A11 as amended 2026-09-23)" (C-7). |

### Phase 15 — Alarms & Clock, Calculator, Voice Recorder

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T15-1 | SHOULD-FIX | R1 15-1, R2 #56 (15) | DOC-UPDATE | Strike every conditional in R1's table (Goal, Scope In, Build tasks 5–7, E9 "(pending Q1…)", E12 "(if in)", E14 "(Q3 A)", E20's "(or DOES under B…)", H11) and write the ruled form. |
| T15-2 | BLOCKING | R1 15-2, R2 #24 | AGENT-CALL | Tess arithmetic (Q5, P6) design: `Request.Arithmetic(expr)` matched on "what's / what is / calculate / how much is <expr>" and bare "<a> plus \| minus \| times \| divided by \| percent of \| to the power of <b>", "square root of <a>", and unit conversions "<n> <unit> in \| to <unit>" through the converter (Q4 is in; one engine); number words → digits via a `NumberWords` map beside `TimeWords` ("point", "percent", "negative"); the expression goes to the Calculator engine and the reply is its display string in a sentence — "15 % of 80 is 12."; an engine error is spoken as Windows' string ("Cannot divide by zero."); very large results are spoken as the engine displays them (e-notation read digit by digit is the engine's form — H17 judges); grammar hotwords = the operator words; `LockGate.allowedWhileLocked` → true (deterministic, no personal data) — `LockGate.kt:29-41` is exhaustive so the branch is mandatory; card = phase 03's response card with the expression and result; `[calc] tess "<expr>" -> <display \| error>`. Rows: **E26** (`say calc_percent` → `[match] … -> Arithmetic(…)`, `reply_text` "15 % of 80 is 12." with phase 03's RMS rule; `type_request "what is 2 plus 2"`; "1 divided by 0" → "Cannot divide by zero."; one over the keyguard; one conversion "5 miles in kilometres"; negatives "what's the weather like" → Weather, "calculate my life" → not understood; ≥ 10 cases host-computed in `calc-cases.tsv`'s new `tess` column; utterance ids in `utterances.py`); H17 [accept] the spoken forms; the INDEX Change Log line for phase 03; a Q5 Decision block and Build task 5 detail. |
| T15-3 | SHOULD-FIX | R1 15-3, R2 #25, R1 X-8 (15) | AGENT-CALL | Foreign recordings (Q2's agent extension): every `IS_RECORDING` file is LISTED and can be played and shared; rename / delete / trim are offered only for files whose `OWNER_PACKAGE_NAME` is `app.tileshell` — Samsung Recorder's files stay Samsung's and no `createWriteRequest` consent dialog appears (P2: no seam; the shell never edits another app's recordings). Line `[recorder] list: n recordings (m by other apps)`. Rows: E14 gains the pushed `/sdcard/Recordings/other.m4a` (+ scan; `content update … --bind is_recording:i:1` if the AVD does not flag it) → listed with `rec_duration:` 3 ± 0.5 s and NO rename/delete on its hold menu; a .m4a in `/sdcard/Music` with `is_recording` 0 is not listed here and IS in Music; E20 rewritten for A with `[music] skipped recording <id>`; P row: Samsung Voice Recorder's files appear here; H18 [accept] other apps' recordings shown read-only. |
| T15-4 | SHOULD-FIX | R1 15-4 | AGENT-CALL | `MicArbiter` is in-process (`MicArbiter.kt:16`), held by `:speech`; the recorder (`:recorder`) acquires it as a `SpeechService` client exactly as the keyboard does (bind the client interface, `acquire("recorder")` / `release`), released on binder death so a killed `:recorder` frees the microphone. Name it in Decisions and Build task 7; E18 also asserts the speech service's dump shows the microphone free after the kill. |
| T15-5 | BLOCKING | R1 15-5, R1 X-1 | AGENT-CALL | See C-1: the routing fix moves to phase 15 as build task 0; depends-on drops 17. |
| T15-6 | NOTE | R1 15-6, 15-7 | DOC-UPDATE | Build task 5: `LockGate.kt:22-27`'s reason ("set without any UI, EXTRA_SKIP_UI") goes stale under Q1 — the rule stands, the comment is rewritten ("set in-process"). "the checklist's `exact_alarms` row" → "Tess's checklist's `exact_alarms` row" (`CortanaChecklist.kt:56-57`). |
| T15-7 | SHOULD-FIX | R2 #26 | DOC-UPDATE | After E14: `adb uninstall app.tileshell` → `ls /sdcard/Recordings` still lists the take and `content query … is_recording=1` still finds it; reinstall through `provision.sh` (RV12). |
| T15-8 | SHOULD-FIX | R2 #27 | DOC-UPDATE | E12 asserts the category list equals exactly [Volume, Length, Weight, Temperature, Energy, Area, Speed, Time, Power, Data, Pressure, Angle] and "Currency" absent; `calc-cases.tsv` gains ≥ 2 host-computed cases per category with the unit factors taken from microsoft/calculator's unit tables (MIT; the engine source, so Windows' own rounding — e.g. its Data units) rather than guessed; H11 covers. |
| T15-9 | SHOULD-FIX | R2 #28 | DOC-UPDATE | E6 / E7 read ticking screens through phase 05's gesture driver (C-10) and time from the ring: add `[stopwatch] elapsed=<ms> uptime=<ms>` / `[timer] remaining=<ms> uptime=<ms>`, assert Δelapsed = Δuptime ± 100 ms. |
| T15-10 | NOTE | R2 #29 | DOC-UPDATE | E4 "vibrates" → `dumpsys vibrator_manager` lists the shell's request. |
| T15-11 | NOTE | R2 #30 | DOC-UPDATE | E10 / E24 pivot settle → C-5's clock; the App Shortcuts build task + row → C-8. |
| T15-12 | NOTE | R1 15-8 | REJECT | Confirmation (ReminderReceiver, `setExactAndAllowWhileIdle`, `IS_RECORDING` API 31+). Nothing to change. |
| T15-13 | SHOULD-FIX | R1 X-6 (15) | DOC-UPDATE | Goal "nothing here is internet-connected (A11)" and Scope Out's map / Currency / downloaded sounds "(A11; R10-Q4)" → "out: offline preferred (A11 as amended 2026-09-23); Currency stays out as a P6 / offline call, Jeremy can ask" (C-7). |

### Phase 16 — Calendar and People

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T16-1 | BLOCKING | R1 16-1, R2 #31 (Sync half) | AGENT-CALL | Q2 D's Sync designed below the Decision. Agent calls: **mapping** lives in the shell's own `calendar_sync.json` (files dir, temp-and-rename): local event id → {target calendar `_ID`, account name/type, copy event id, last-pushed hash} — `SYNC_DATA*` / `_SYNC_ID` are sync-adapter columns and are not used; **the copy** is a normal-app insert into the target (so the account's own sync adapter uploads it); re-Sync updates the copy by id, recreates it if gone on the other side (line says so); an edit made on the other side is overwritten by the next Sync (local is the source); **local delete of a synced event**: the confirmation offers "Delete here" (default) / "Delete here and from <calendar>" — the second is still a user tap, so rule 3 holds; **allowed calendar removed from the phone**: Sync refuses with "That calendar is no longer on this phone", the marker stays with a warning glyph; **recurring events** copy RRULE / DURATION / exceptions and their reminders; **"Can sync to"** rows keyed on `_ID` + account name + type (a re-added account starts NOT allowed); **marker** "synced to <calendar>" P4 (H14). Tags `cal_sync`, `cal_sync_target:<id>`, `cal_synced_marker:<id>`, `cal_settings_can_sync:<id>`; diagnostics `[calendar] sync event=<id> -> calendar <id>: ok \| updated \| recreated \| failed <err> \| refused (not allowed)`. Rows: **E22–E24** as R2 #31 writes them (two `com.google`-type QA calendars "Personal" / "Work" access 700; rule 1 both listed, neither in the editor's picker; rule 2 after editor / Tess writes both account counts 0 while the local holds them; rule 4 fresh install → picker empty → `[calendar] sync event=<id>: no calendar allowed`, enable Personal → picker lists Personal only; rule 3 Sync → one copy, marker in the dump, re-Sync after edit → updated, still one row; JVM negative on the guard; Birthdays never in the picker; edges: target deleted, local deleted after sync, allow-list cleared). P2 re-cut to the push; `dumpsys netstats --uid` unchanged. Stale lines struck: Goal, Scope In (editor's "calendar" field → the local calendar only), Decisions "Calendar data (agent, pending Q2 …)", Build tasks 3–4, E3's picker clause, the account-removed edge (now the Sync failure case). |
| T16-2 | BLOCKING (trust) | R1 16-2, R2 #31 (E9 half) | DOC-UPDATE (code fix in flight) | **Being fixed in code** (J6: `qa/phase-03/scripts/j6.sh`, evidence `qa/phase-03/J6/`; `ActionLayer.kt:647-662` still the old form in the working tree as of this triage — do not write it as doc work). What phase 16 MUST say about reusing that local calendar: (1) the local calendar J6 creates is THE shell local calendar — one finder/creator (`LocalCalendar.idOrCreate()`, phase 03's part after J6) shared by `ActionLayer` and the Calendar app; the app never creates a second, and phase 16's account_name / account_type / display name (today the doc says name "Calendar" in Decisions but `account_name=Tessera&account_type=LOCAL` in its edge case) are replaced by J6's actual constants once J6 lands; (2) the find keys on those constants, never on "any LOCAL calendar" — Birthdays (Q3) is a second LOCAL calendar and must never be picked; (3) it cannot be created → Tess refuses with a spoken notice, never a fallback to another calendar (J6's rule), and the app shows the same state; (4) E3 is inverted: under Q2 D the local calendar ALWAYS exists (it is the only write target) — "with the QA calendar inserted first … no local calendar is created" becomes "is created anyway", and `local present` is asserted when Tess created it first; the edge "recreates only if no calendar remains" → recreates always; (5) E9 cites J6's row as its account-calendar negative (re-run on the phase 16 build) instead of a new row, and P2 gains the same on the phone; (6) Build task 3's "local-calendar creation (idempotent)" → "reuse phase 03's `LocalCalendar` (J6); no second creator"; the INDEX Change Log line for J6 is the record of the ADD. |
| T16-3 | SHOULD-FIX | R1 16-3, R1 X-8 (16) | AGENT-CALL | The opt-in allow-list stays (Jeremy: "never" — only an opt-in list makes a mis-tap on the work calendar impossible), and the seam is removed: the first Sync with nothing allowed opens the "Can sync to" page directly with one line "Choose which calendars Sync may use", then returns to the picker — no dead-end empty picker. H14 [accept]: "the Sync picker lists only calendars enabled once in Calendar settings, so the work calendar can never be chosen by a mis-tap". |
| T16-4 | NOTE | R1 16-4 | DOC-UPDATE | Decisions "Event reminders": the shell notifies for every calendar's alerts, account calendars included (the app shows them all under Q2 D), so P1's double also applies to Samsung Calendar's own events; the Birthdays calendar's events carry no reminder rows (agent) so nothing fires for them. |
| T16-5 | NOTE | R1 16-5, R1 X-2 | AGENT-CALL | See C-2: the guard stays here (task 1); 16 builds before 17. |
| T16-6 | SHOULD-FIX | R2 #32 | REJECT | Wrong: K-9 (`com.fsck.k9`) IS in the fixture stash (`~/android-fixtures/com.fsck.k9`, beside `eu.faircode.email`) and phase 01 E4b already uses it as the Mail slot candidate (`qa/phase-01/scripts/e4_part2.sh:19`). E12 stands. |
| T16-7 | NOTE | R2 #33 | DOC-UPDATE | E1's upgrade pass keeps the pre-16 APK at `qa/phase-16/upgrade/<tag>.apk` (its git tag in the log); same for phase 17 E1. |
| T16-8 | NOTE | R2 #34 | DOC-UPDATE | E11's ≈333 / ≈583 ms slides use a `[people] tile event <n> t0=<uptime>` line as the clock (C-5); the 7.7-s period from the capture is fine. |
| T16-9 | NOTE | R1 stale table, R2 #56 (16) | DOC-UPDATE | Strike "pending / lean" in Decisions "Birthdays", "People over the provider"; E17 "(Q3 A)"; H5 "if Q3 rules it in"; P2 as T16-1. |
| T16-10 | NOTE | R1 X-6 (16) | DOC-UPDATE | Goal / Out "(A11)" → the amended wording (C-7); "synced calendars are other apps' sync adapters" stays true. |

### Phase 17 — Photos, Camera, Movies & TV

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T17-1 | BLOCKING | R1 17-1, R2 #35, R1 X-9 | QUESTION (2, §2) + AGENT-CALL for the rest | Two things change WHAT is built and need Jeremy: the catalogue source / key and which media server (§2). Everything else is the agent's and goes into a "Movies & TV online" Decision block + Build tasks 11–14 in THIS doc (not a split: phases 20 / 21 hang on one named interface, and a 17b would only move the gate): the interface **`StreamingHandoff`** — `installedServices()` (a per-service table of package + deep-link scheme + web URL + search URL, resolved with `queryIntentActivities`), `openTitle(service, ids)` (deep link at the title where the service has one, else its search URL with the title), `catalogue.search / lookup` (source per Q-A), `mediaServer` (client per Q-B; its token is a credential kept in `EncryptedSharedPreferences` — a trust line, adversarial review at build per the project's rule for trust changes); **offline**: a 7-day catalogue cache in the files dir; no network → the cache and the local pivot with an offline line; **external `http(s)://` VIEW** from another app now PLAYS in the shared player (`[video] playing scheme=https`; the A11 refusal is gone — the 2026-09-22 R10-Q4 Decision line marked SUPERSEDED in place; E13 re-cut); **pivots** My videos / Browse / Media server (R11 §Movies & TV measures the local half; the Store half is P4, H rows); **rows** E20 catalogue against a host fixture (`python3 -m http.server` at `10.0.2.2:<port>`, base URL through a QA-only pref via `prefs_edit.py`; search "Blade Runner" → `hub_result:<id>` rows; airplane mode → offline line, local pivot intact; 500 → `error 500`), E21 "Watch on" (a QA-Flix fixture APK declaring the table's intent filters and echoing the URI; "Not on this phone" negative), E22 media server (a Jellyfin container at `10.0.2.2:8096` with `qa-steps.mp4`; sign-in, wrong password → `unauthorised`, stopped → `unreachable`), P rows (the real services on the S25U: lands on the title or the home, recorded; Jeremy's own server), H rows (hub layout, "Watch on" row, sign-in page), diagnostics in E18 (`[video] catalogue <q>: n \| offline \| error <code>`, `[video] watch-on <service> <title> -> <intent> \| not installed`, `[video] server <host>: connected \| unreachable \| unauthorised`), the dependency line (OkHttp is transitive through Media3 — state it; no new library for HTTP). Stale lines struck: header "interview pending", Goal "Nothing here uses the internet (A11)", Scope Out "streaming or any http source", Build task 7's "explicit refusal for non-file schemes", E13, E18's `refused scheme`, Y5 / H3 (add the hub). |
| T17-2 | BLOCKING | R1 17-2, R2 #36 (camera half) | AGENT-CALL | Q2 C per mode: **pro dial** through `Camera2CameraControl` interop, each control gated by `CameraCharacteristics` (`CONTROL_AE_AVAILABLE_MODES` OFF, `SENSOR_INFO_*`, `LENS_INFO_MINIMUM_FOCUS_DISTANCE` > 0, `CONTROL_AWB_AVAILABLE_MODES`), a missing control hidden with `[camera] mode pro.<control>: unavailable (<reason>)`; **panorama** = a NEW dependency, OpenCV (Apache-2.0) `Stitcher`, arm64 only in the phone build — corrects "CameraX is the only new dependency" and adds an APK delta bound (≤ 20 MB) to E17; the licence check is numbered as build-start check BS-1 (the Decision's "P5" pointed at the pinch-zoom row); **slow motion** = Camera2 constrained high-speed session through interop where `REQUEST_AVAILABLE_CAPABILITIES` lists it, else hidden; BS-2 on the pinned CameraX version; **Living Images** = the Motion Photo container (JPEG + trailing MP4 + XMP `MotionPhoto=1`, one file, no sidecar) so Samsung Gallery reads it too; Photos shows a glyph and plays the clip on hold. Rows: E7 gains timer (`[camera] timer 3s -> shutter`, `date_added` ≥ tap + 3 s from the shell's line), grid (1/3, 2/3 lines in a screencap), tap-to-focus (`[camera] focus at x,y: <state>`, autofocus support read from `dumpsys media.camera` first), zoom 2× (the known scene object twice as wide), **mode availability both directions** (the AVD's capabilities predict the mode list; each hidden mode logs its reason); P rows: pro dial with EXIF read-back (ISO, exposure, WB), panorama (width > sensor width), slow motion (`ffprobe` r_frame_rate ≥ 120), Living Images (the container's XMP present); H rows for the editor tool UI and panorama UI; P1's list extended. Build tasks 6a–6d. |
| T17-3 | SHOULD-FIX | R1 17-3, R2 #36 (editor half) | DOC-UPDATE | E6 gains one host-checkable assertion per tool on the flat-colour fixtures (the tool's transform stated in Decisions — agent: filters / light / colour / auto-enhance as fixed matrices, red-eye = red channel ÷ 2 inside detected discs, straighten = rotate + centre crop), copy count +1, original md5 unchanged; E6b video trim `qa-steps.mp4` to 2–5 s → `ffprobe` 3.0 ± 0.1 s, first frame = colour 3, original md5 unchanged (Media3 Transformer, no new library); edge: an edited HEIC saves as JPEG. |
| T17-4 | SHOULD-FIX (trust) | R1 17-4 | AGENT-CALL | Capture-intent guards: accept `content://` `EXTRA_OUTPUT` only, refuse `file://` (`[camera] refused output scheme=file`, E9 negative); write only through `contentResolver.openOutputStream(uri)` under the caller's grant, never by path; strip EXIF location for a third-party caller; no DCIM copy of an intent capture; RESULT_CANCELED leaves no `is_pending` row (E9 has it). Adversarial review at build (the project rule for trust changes). |
| T17-5 | SHOULD-FIX | R1 17-5, R1 X-2 | DOC-UPDATE | Decisions' "A slot the user re-pointed before this build installs is left alone" is WRONG by the code (`LayoutStore.kt:127-139` replaces the entry): replace with "phase 16's guard (its build task 1, built before this phase) keeps a hand assignment; the seed here runs under it". depends-on gains 16 (C-2). E1's upgrade half re-cut: assign PHOTOS by hand on the pre-17 build, install the 17 build, assert `assignSlotOnce slot:photos:v1 PHOTOS -> kept user's <component>`. |
| T17-6 | SHOULD-FIX | R2 #37 | DOC-UPDATE | E9: the fixture also starts VIDEO_CAPTURE with `EXTRA_OUTPUT` → RESULT_OK with an mp4 `ffprobe` decodes at that URI; Back → RESULT_CANCELED, no `is_pending` row. |
| T17-7 | SHOULD-FIX | R2 #38 | DOC-UPDATE | E3: tap the video row in Photos → `VideoActivity` resumed, `[video] playing <id>`, no player node in Photos' own dump (one player surface). |
| T17-8 | SHOULD-FIX | R2 #39 | DOC-UPDATE | E5 slideshow timed from `[photosapp] slideshow next <id>` lines (add them), as E11 times from `[video] playing`. |
| T17-9 | NOTE | R1 17-6, R1 X-4 (17) | AGENT-CALL | "Media server" is a DYNAMIC shortcut published when a server is set up and removed when it is not (C-9); row: `dumpsys shortcut` before / after set-up, and a burst on a pinned Movies & TV tile shows 2 then 3 satellites. |
| T17-10 | NOTE | R1 17-7 | REJECT | Confirmation (API levels; the AVD's `cameraextensions` stub → P1). Nothing to change. |
| T17-11 | NOTE | R2 #40 | DOC-UPDATE | E1 "`pm clear` + Start" will meet the wizard → C-4's form (`pm clear` → `provision.sh` → Home). |
| T17-12 | NOTE | R1 stale table, R2 #56 (17), R2 #60 | DOC-UPDATE | Strike: Scope In's "per Q1 / Q2 / Q3 / Q4 / Q5" (Q3 → Q3b), Build tasks 4–6's "(Q4)" / "per Q1" / "the Q2 mode set", E3 "With Q4 A", E6 "(Q1 B / C)", E9 "(Q5 A)", P1's mode list (add the pro dial, panorama, slow motion, Living Images). |

### Phase 18 — Files

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T18-1 | BLOCKING | R1 18-1, R2 #41 | AGENT-CALL | Recycle Bin (Q3 C) design: one bin per volume at `<volume>/.Tessera/bin/` with a `.nomedia` marker and `.index.json` (original path, deleted-at, size; temp-and-rename); the bin is its OWN root entry in Files ("Recycle Bin", all volumes' bins in one list with the volume named), never a browsable dot-folder (dot-files are hidden by default); **delete** = rename inside the volume (instant, never a copy) + index write + a scan of the source path, so MediaStore drops the row and Photos, Music and the Photos tile lose it (`IS_TRASHED` is NOT used: media-only and auto-purged at 30 days, against "emptied only by the user"); **restore** = rename back, the original folder recreated if gone, a clash → Files' existing conflict dialog (replace / keep both / skip); **Delete permanently** and **Empty** confirm (H wording); a delete INSIDE the bin is permanent; a lost index → entries listed by their bin name and restored to `<volume>/Download/Restored/`; index rows whose file is gone are dropped; a removable volume pulled takes its bin with it; phase 19's Storage page shows the bin's size with Empty (a one-line ADD there). Tags `files_bin`, `files_bin_row:<name>`, `files_bin_restore`, `files_bin_empty`; diagnostics `[files] bin <op> <path>: ok \| failed <why>`. Rows **E4b** as R2 #41 writes them (present in the bin, gone from the folder and from `content query` on images within 3 s; restore with md5; clash; permanent; empty; a delete on E2's public volume lands in THAT volume's bin; `adb shell rm` never passes through the bin; the bin absent from Recent; `fallocate`-full volume still deletes; bin folder deleted by another app; `adb uninstall` leaves the bin). H-row [accept] the bin page and wording (H2 becomes this). |
| T18-2 | BLOCKING | R1 18-2, R2 #42 | AGENT-CALL | Zip + Recent (Q2 C) design: `java.util.zip` (`ZipFile`, zip64 supported by the platform) — no new dependency, said so; **open as folder** = a virtual root; **extract** through the same foreground service into a temp dir, renamed on completion, progress + cancel, no partial output; **create** from a selection (DEFLATE); **security**: every entry name normalised and refused if its canonical path escapes the extract root (`../`, absolute) with `[files] zip: refused entry <name>` while the rest extract; the declared uncompressed total is summed before extract and refused above free space − 50 MB; extraction stops at declared total + 1 MB (a bomb) and fails cleanly; encrypted → "This zip is password-protected" (unsupported); nested zips are files (openable again); UTF-8 flag honoured, CP437 fallback; a removable volume pulled mid-extract → the service fails with "storage removed". **Recent** = MediaStore `Files` ordered by `DATE_MODIFIED` desc, cap 100, excluding `.nomedia` folders and the bin; a Files write's scan makes it appear within 3 s; an unscanned file is absent (a rule, stated). Rows **E13 zip** / **E14 Recent** as R2 #42 writes them (host-made `qa.zip`, `qa-bad.zip` with `../../evil.txt`, `qa-corrupt.zip` = `head -c 500`; 200 MB zip through the service with cancel; `unzip -l` / `unzip -t` on a created zip; Recent's exact order from `touch -d` dates; rename rises to the top). Diagnostics in E12. |
| T18-3 | SHOULD-FIX | R1 18-3, R2 #43 | DOC-UPDATE | Build task 1 adds the wizard step (a Settings-page step kind; action `MANAGE_ALL_FILES_ACCESS_PERMISSION`; why line "Files can browse everything on this phone. Without it Files sees nothing."); a row in phase 12 E14's template (`appops set app.tileshell MANAGE_EXTERNAL_STORAGE default` + Home → `wizard_step:setup:files`; `provision.sh` gains `appops set … allow`); phase 12's finished-install rule restated (C-4). |
| T18-4 | NOTE | R1 18-4 | DOC-UPDATE | Soften "a rename or move by path does not update MediaStore by itself" (MediaProvider follows FUSE renames on Android 11+) to "the scan is belt-and-braces; E9 records which case needed it". |
| T18-5 | NOTE | R1 18-5 | DOC-UPDATE (partial reject) | Share needs a `FileProvider` (none exists: manifest providers at `:162` and `:261` only) — a manifest ADD named in Build task 3 and E10. BUT it is `exported="false"` with `grantUriPermissions="true"`, so it is NOT an exported-component allow-list ADD; E10's "no ADD" clause for the allow-list stays true. |
| T18-6 | SHOULD-FIX | R1 18-6, R2 #44, R1 X-4 (18) | AGENT-CALL | "SD card" is a DYNAMIC shortcut (C-9), published on mount and removed on unmount; E2's virtual-disk row gains `dumpsys shortcut` lists `sdcard` after mount, gone after unmount, and a burst on a pinned Files tile shows 3 then 4 satellites. |
| T18-7 | NOTE | R1 stale table, R2 #56 (18), R1 X-6 (18) | DOC-UPDATE | Strike: header "interview pending", Goal's Q1 / A11 lines, Scope In's "Q2 / Q3 / Q4 / Q1" clauses, Scope Out's "unless Q3 / Q2", Decisions "written for Q1's lean", Build tasks 1 / 3 / 4's "(Q1 A)" / "per Q3" / "per Q4", the Acceptance header "Written for Q1 A", **E4's "delete `c.bin` after the confirmation (Q3 A) → `ls` fails"** (→ it goes to the bin, E4b), E6 "(Q4 A)", H2; A11 tags per C-7. |

### Phase 19 — Settings front

| Id | Sev | Sources | Class | Ruling / exact change |
|---|---|---|---|---|
| T19-1 | BLOCKING | R1 19-1, R2 #45 | AGENT-CALL | The five verbs (Q3 C) are built BY PHASE 19 as ADDs to phase 04's helper — one allow-list entry per toggle, never a generic command — recorded in 04's Change Log when built, with the adversarial review 04 mandates as a build-task GATE (not a sentence). Reason: 19 is the phase that needs them and 04 is DRAFT with a four-verb set; if 04's interview (pending R4) reshapes the helper, these ride with it. INDEX's R4 row gains five probes, each as the shell uid on One UI 8: battery saver (`settings put global low_power 1` / `cmd power set-mode 1`), location (`cmd location set-location-enabled`), NFC (`svc nfc enable \| disable`), hotspot (the probe must find a TETHERING path — `cmd wifi start-softap` is a local-only AP; if none works, hotspot stays a deep-link), auto-time (`settings put global auto_time`). E9 gains five sub-rows with their observables both directions + RV12 restore (`settings get global low_power`; `settings get secure location_mode`; NFC on the AVD has no hardware → the verb reports `unsupported` and the row IS the fallback line, a negative not a skip; `cmd wifi status` soft-AP state; `settings get global auto_time`, restore 1), the allow-list negative (an unlisted verb refused and logged), each row's state with the helper stopped, and "a verb R4 could not prove → the row is a deep-link with the reason"; P3 extended to the five; the table's Battery saver / NFC / Mobile hotspot / Location / Date & time rows become "helper (04 ADD) + link"; Build task 4 rewritten. |
| T19-2 | SHOULD-FIX | R1 19-2, R2 #46 | DOC-UPDATE | Strike the Scope Out "Apps category" line; add table rows — Apps > Apps & features (`MANAGE_ALL_APPLICATIONS_SETTINGS` → `Settings$ManageApplicationsActivity`; per app `APPLICATION_DETAILS_SETTINGS` with `package:` → `.applications.InstalledAppDetails`; Uninstall through the app list's `REQUEST_DELETE_PACKAGES` path → the installer's dialog, self-package excluded); Apps > Default apps (`MANAGE_DEFAULT_APPS_SETTINGS`, MOVED here from System > Phone / Messaging); Apps > Apps for websites (MOVED here from System); Apps > App permissions (the Privacy pages' per-app link). E1's category count follows; E8 covers the new rows; H-row [accept] for a category with no W10M original (P4). |
| T19-3 | SHOULD-FIX | R1 19-3, R2 #47 | DOC-UPDATE (partial reject) | The rule IS findable: INDEX Change Log 2026-09-22 "JEREMY'S FOUR ASKS" item (3) ("Contacts should be called people" — a slot is the W10M tile; the resolved app is not its name; fixed for slots only). Cite it by that name. It labels slot TILES, so relabelling Android's Settings in the APP LIST is a different mechanism: a per-package label override in phase 01's app list (an ADD, Change Log when built; the jump grid and search find "Android settings" under A and both under "settings", ours first). E2 asserts `applist_row:com.android.settings` text = "Android settings" filed under A, ours "Settings" under S, search order, `aapt dump badging` label "Settings". The edge case "Two 'Settings' entries (Q1 C)" → the Q1 A form. |
| T19-5 | NOTE | R1 19-5 | DOC-UPDATE | Personalization > Themes = phase 12's preset page; Transparency effects (phase 13) under Personalization > Colors — Windows 10's own placement; H6 lists them. |
| T19-6 | NOTE | R1 19-6 | DOC-UPDATE | depends-on comment: "the front lists what exists at build time" (phase 18's Files under Storage, 12 / 13's rows). |
| T19-7 | NOTE | R2 #48 | DOC-UPDATE | E9 is runnable on the AVD only if phase 04's own rows run there (Wireless-debugging pairing on the emulator is phase 04's build-start question); otherwise E9 moves to P3 with an AVD-only "helper stopped" state row — mirror whatever phase 04 records. |
| T19-8 | NOTE | R1 19-7 | REJECT | Confirmation (appops, DND access, resolved actions, `SettingsActivity.kt:65`). Nothing to change. |
| T19-9 | NOTE | R2 #49 | DOC-UPDATE | Y4 motion → C-5's clock. |
| T19-10 | NOTE | R1 stale table, R2 #56 (19) | DOC-UPDATE | Strike: header "interview pending", Scope In's "further verbs only per Q3" / "per Q1" / "per Q4", the table's "(helper only … Q3)" ×5, Build tasks 2 / 5 "per Q1 / Q4", E1 / E2 / E8 / E13's "per Q1 / Q2 / (Q4 A)", H7. |

### Cross-phase

| Id | Sev | Sources | Class | Ruling / fix |
|---|---|---|---|---|
| C-1 | BLOCKING | R1 X-1, 15-5, 16-5 | AGENT-CALL | **Inbox order stays 15 → 16 → 17 and the live-tile routing fix moves to phase 15 as build task 0** ("routing by session tag — the ADD to phases 01 and 10 that phase 17's Decisions describe, built here because 15 builds first"; `MusicFeed.kt:159-171`, `ActiveTiles.kt:51`, `StartPage.kt:150,173,230,232`). Phase 15 depends-on → `[01, 02, 03, 10]`; 16 → `[01, 02, 03, 15]`; 17 → `[01, 02, 03, 10, 15, 16]`; 17's build task 2 becomes "E16 re-run on this build" (its rows stay). INDEX phase notes for 15 / 16 / 17 follow. Reason: (a) is the smallest change; 15 is cheap and has no external input, while 17 now waits on Jeremy's hub answers (§2). |
| C-2 | BLOCKING | R1 X-2, 17-5, 16-5; R2 fact | AGENT-CALL | The `assignSlotOnce` guard stays phase 16's build task 1 (16 is the first seeder in the order); 17 depends on 16, corrects its wrong "left alone" claim (T17-5) and re-cuts E1. The Change Log line for phase 01 stays with 16. |
| C-3 | BLOCKING | R2 #50, #3 | DOC-UPDATE | Rule for every phase that adds an `addedOnce` marker (16: `slot:calendar:v1`, `slot:people:v1`; 17: `slot:photos:v1`, `slot:camera:v1`) or pins fixtures (11, 15): its harness build task ships `qa/phase-NN/baseline_layout.json` derived from the previous file (markers + `manualSizes` + `slots`), keeps the previous as `baseline_layout-pre-<NN>.json`, and its regression row asserts zero `assignSlotOnce … -> assigned` lines after `layout_restore` and `addedOnce` equal to the file's. The restore itself already verifies after reload and compares sizes only for hand-set tiles (fact 1) — no driver change, only the files. |
| C-4 | BLOCKING | R2 #51, R1 X-5, R2 #10, #43, #59, R1 12-7 | DOC-UPDATE | Every 15–19 doc: (a) its `provision.sh` line(s) — 15: `appops set app.tileshell USE_FULL_SCREEN_INTENT allow` (+ RECORD_AUDIO is already `-g`); 16: `pm grant … WRITE_CONTACTS`; 17: `pm grant … CAMERA`, `READ_MEDIA_VIDEO`; 18: `appops set … MANAGE_EXTERNAL_STORAGE allow`; 19: `appops set … WRITE_SETTINGS allow`, `cmd notification allow_dnd app.tileshell`; (b) the wizard-step row in phase 12 E14's template with its why line; (c) phase 12 E1 re-run on its build; (d) every `pm clear` row reads "`pm clear` → `provision.sh` → Home" (phase 16 E1, 17 E1 as written would meet `wizard_page`); (e) phase 12's finished-install rule in one sentence. Phase 12 Build task 4: `provision.sh` grants the Setup rows, Tess's nine, and each later phase appends its line. |
| C-5 | SHOULD-FIX | R2 #53 (+ #5, #13, #18, #30, #34, #39, #49) | AGENT-CALL | Standing harness rule (one line in each doc's Acceptance preamble): every motion the shell animates logs its own clock — `[motion] <name> t0=<uptime> peak=<ms> overshoot=<%> settle=<ms>` from `withFrameNanos` — and the row asserts the logged numbers against RV11's tolerance; a screenrecord corroborates under phase 05's frame-spacing rule (source-frame spacing ≤ 18.2 ms during the motion) and is never the primary clock. Applies to 11 E6, 12 E10, 13 E8, 15 E10 / E24, 16 E11 / E19, 17 E5 / Y6, 19 Y4 — and is how phase 02 E7's open motion sub-rows close at the end-of-build pass. |
| C-6 | SHOULD-FIX | R2 #54, #21 | DOC-UPDATE | After any launch, `am force-stop app.tileshell` + Home before the next grid assertion (RecentApp is in memory only, `recent0922.sh:19-21`). One harness line in 11, 14, 16, 17, 18; the rows that launch (11 E4, 14 E3 / E9 / E13, 16 E1, 17 E1, 18 E6 / E10) do it. |
| C-7 | SHOULD-FIX | R1 X-6, R2 #60 | DOC-UPDATE | In 14, 15, 16, 17, 18 every "(A11)" / "(A11; R10-Q4)" exclusion that is still wanted is re-tagged "out: offline preferred (A11 as amended 2026-09-23); Jeremy can ask" so a withdrawn ruling is never cited as the reason; 17's are struck (T17-1). R10-Q4's no-Mail / no-browser / no-Maps stays a ruling and keeps its tag. |
| C-8 | SHOULD-FIX | R2 #52 | DOC-UPDATE | Each of 15–19 gains the build task ("static `shortcuts.xml`, ids <…>, ranks 0–3, targeting <activity> with the page extra") and the row ("pin the app's tile through the phase baseline; hold → `quick_sat_label:0..3` = [names] in rank order (phase 11 E3's method); tap each → the app resumed on that screen with its page tag selected; `dumpsys shortcut` lists the ids with those ranks") that phase 11 Q1's standing rule promised. |
| C-9 | SHOULD-FIX | R1 X-4, 17-6, 18-6, R2 #44 | AGENT-CALL | Phase 11's standing rule is amended: static App Shortcuts for fixed screens; DYNAMIC shortcuts (`ShortcutManager.setDynamicShortcuts`, published and removed by the app) for screens that exist conditionally — 17's Media server, 18's SD card; the selection rule's "manifest before dynamic" becomes a stated choice (a conditional screen ranks after the fixed ones). Phases 17 / 18 say "dynamic" and carry the row (T17-9, T18-6). |
| C-10 | SHOULD-FIX | R2 #55, #4, #28 | DOC-UPDATE | Dumps of screens that never idle (11 the burst over live tiles; 15 timer / stopwatch; 16 the People tile cycling; 17 the viewfinder / player) name the route: phase 05's gesture-driver `UiDevice.dumpWindowHierarchy` with `Configurator.setWaitForIdleTimeout(0)` — instead of "dumps follow RV13". |
| C-11 | NOTE | R2 #56 | DOC-UPDATE | The stale-wording sweep per doc is listed under each phase above (T11-1, T12-1, T14-1, T15-1, T16-9, T17-12, T18-7, T19-10). |
| C-12 | NOTE | R2 #57 | DOC-UPDATE | R11 is now an index (`docs/plan/r11-inbox-apps.md`) with `r11/<app>.md` pending; 15–19 cite `r11/<app>.md` by name; R12 pending (12); R13 pending (the TV-channels doc). "Written once R11 lands" rows stay honest placeholders. |
| C-13 | NOTE | R2 #58 | DOC-UPDATE | Every "recorded, not gated" row (13 E9, 15 E21's last clause, 16 E12's resolver note, 17 E14 / P rows) ends its PASS/FAIL line with "RECORDED" so `lib.sh` `row_end` never counts it as a pass. |

Folded pointers (not counted twice): R1 X-3 → T12-3; R1 X-7 → T12-8; R1 X-8 → T15-3 / T16-3 / T18-1 / T11-10; R1 X-9 → T17-1; R1 13-2 → T12-3; R1 13-3 → T11-8; R1 14-3 → T11-9; R1 16-5 → C-2; R2 #11 → T12-1; R2 #12 / #17 → T12-2 / T13-4; R2 #40 → T17-11; R2 #43 → T18-3.

---

## 2. Questions for Jeremy (Stage A shape; load-bearing first)

Only two true forks survived: both change WHAT the Movies & TV hub is built on and neither is covered by a ruling (Q3b A
said "a public film database" and "Jellyfin or Plex" without choosing). Everything else in this triage is an agent call
under P3 with an H row where it is user-visible.

**Q-A (phase 17 — the film database).** Movies & TV's online catalogue needs a film database. The good one (TMDB) is free
but needs your own key — an input from you, like the theme pictures. Which way?
- A. TMDB with your own free key: best titles, artwork and "where to watch"; you sign up once and the key ships only in
  your personal build, never in the repo (lean)
- B. Keyless sources only (Wikidata, TVmaze): no key to get, thinner data, no "where to watch", so "Watch on" becomes a
  search inside each streaming app
- C. TMDB, but you paste the key once into Settings instead of it being built in
- D. Other / let me clarify.

**Q-B (phase 17 — your media server).** The hub's media-server part: which server do you actually run at home?
- A. Jellyfin: self-hosted, no cloud sign-in, open API (lean)
- B. Plex: needs a plex.tv sign-in for its token
- C. Both
- D. Other / let me clarify.

---

## 3. Per-doc work list (for the phase writers)

Apply the T-items above; this section is the checklist form. Nothing here reopens a 2026-09-23 ruling.

**phase-11-tile-quick-actions.md** — T11-1 (E15; E3 / E10 wording) · T11-2 (promoted-tile Decision + row; strike from
task 6) · T11-3 (`qa/phase-11/baseline_layout.json`; E7 variant baselines) · T11-4 / C-10 (dump route line) · T11-5 / C-5
(`[quick] motion …` lines; E6 re-cut) · T11-6 (E13 scope) · T11-7 (satellite pressed look = phase 13's lights; E5 / E7 sub-row;
H3) · T11-9 ("Start's page") · C-6 (force-stop after E4) · C-9 (the standing rule gains the dynamic-shortcut clause) ·
T11-10 (note that phase 19 keeps this Q1 set).

**phase-12-setup-wizard.md** — T12-1 (Q2 below the Decision: rows a–d, the whole stale table, E1(b) grants, E2 N + order,
E3 Tess steps, E4 / E5 / E7 / P1 / H2) · T12-2 (preset table, Custom, live-apply, pictures + APK row, ADD lines for 01 / 03 /
05 incl. phase 05 E3 on Default, tags, diagnostics, E11–E13, H6–H9, edges, E3 restore) · T12-3 (depends-on `[01, 03, 05, 10,
13]`; order note) · T12-4 (`wizard_why` + table + E2 / E3 + H1) · T12-5 (E14 template row; Build task 4's `provision.sh` list)
· T12-6 (Host: pager not composed while showing) · T12-7 (Brand strings; original preset without a picture is still a preset) ·
T12-8 (z-order line) · T12-9 (E13 placeholder wording) · C-4 (a).

**phase-13-fluent-materials.md** — T13-1 (radial light values; sample rule; E7 re-cut; H4; Decisions / task 5 to Q3 B) ·
T11-8 (drop the burst-backdrop row, `acrylic:burst`, task 4 clause, Goal clause; satellites stay in the Reveal list) · T13-2
(`acrylic_expect.py`) · T13-3 (E11 / E15 wording) · T13-4 (E1 Midnight sub-row) · T13-5 (`rememberBackground` ADD) ·
T12-3 (a line: presets land after this phase and write `transparencyEffects`) · C-5 (E8).

**phase-14-pod-bay.md** — T14-1 (strike widgets ×4) · T14-2 (Weather strings by hand) · T14-3 / C-6 (force-stop) · T14-4
(soft 10) · T11-9 / T14-5 (`home: page START`; `regress.sh:120` in the harness task) · T14-6 (`CortanaService.open` /
`showSession`) · T14-7 (request waits under the wizard) · T14-8 (`wall=`; absence build-start check) · C-7 (A11 tag).

**phase-15-inbox-clock-calculator-recorder.md** — C-1 (build task 0 routing fix; depends-on `[01, 02, 03, 10]`; E1's Music
negative stays) · T15-1 (conditionals) · T15-2 (Q5 block, task 5, E26, H17, Change Log line) · T15-3 (foreign recordings
rule; E14 / E20; P row; H18) · T15-4 (`MicArbiter` route; E18) · T15-6 (LockGate comment; "Tess's checklist") · T15-7 (E14
uninstall) · T15-8 (E12 categories; microsoft/calculator unit tables) · T15-9 / C-10 (E6 / E7 route + lines) · T15-10
(`dumpsys vibrator_manager`) · C-3 (`qa/phase-15/baseline_layout.json` for the pinned tiles) · C-4 (a–e) · C-5 (E10 / E24) ·
C-7 / T15-13 (A11 tags; Currency as P6 / offline) · C-8 (shortcut task + row) · C-12 (`r11/clock.md` …) · C-13.

**phase-16-inbox-calendar-people.md** — T16-1 (Sync design block; tags; diagnostics; E22–E24; P2; strike the stale lines)
· T16-2 (the six local-calendar lines; E3 inverted; E9 cites J6; task 3 reuse) · T16-3 (first-Sync route to "Can sync to";
H14) · T16-4 (reminders for all calendars; Birthdays carries none) · C-1 / C-2 (depends-on `[01, 02, 03, 15]`; task 1's
precondition becomes "phase 15's task 0 is built") · T16-7 (`qa/phase-16/upgrade/<tag>.apk`) · T16-8 / C-5 (E11 clock) ·
T16-9 (strike pending / lean) · C-3 (baseline with `slot:calendar:v1`, `slot:people:v1`) · C-4 (a–e; E1's `pm clear`) · C-6
(E1) · C-7 · C-8 · C-10 (People tile dumps) · C-12.

**phase-17-inbox-photos-camera-video.md** — T17-1 (the "Movies & TV online" Decision block; `StreamingHandoff`; tasks
11–14; E20–E22; P / H rows; E13 / E18 re-cut; R10-Q4 line SUPERSEDED; §2's answers slot in) · T17-2 (per-mode lines; BS-1 /
BS-2; OpenCV dependency + APK delta; tasks 6a–6d; E7 / mode-availability rows; P rows; H rows) · T17-3 (E6 tools; E6b trim)
· T17-4 (capture guards; E9 negative; adversarial review) · T17-5 / C-2 (claim corrected; depends-on `[01, 02, 03, 10, 15,
16]`; E1 upgrade half) · T17-6 (VIDEO_CAPTURE) · T17-7 (E3 hand-off) · T17-8 (slideshow lines) · T17-9 / C-9 (Media server
dynamic + row) · C-1 (task 2 → E16 re-run) · T17-12 (stale sweep) · C-3 (baseline with `slot:photos:v1`, `slot:camera:v1`) ·
C-4 (a–e; E1) · C-5 (E5 / Y6) · C-6 · C-8 · C-10 (viewfinder / player dumps) · C-12 · C-13.

**phase-18-files.md** — T18-1 (bin design; tags; diagnostics; E4b; H-row; the phase 19 Storage ADD line) · T18-2 (zip +
Recent design; E13 / E14; E12 lines) · T18-3 (wizard step; E14-template row) · T18-4 (MediaStore claim) · T18-5
(`FileProvider`, non-exported) · T18-6 / C-9 (SD card dynamic + row) · T18-7 (stale sweep incl. E4's Q3 A clause) · C-4 (a–e)
· C-6 (E6 / E10) · C-7 · C-8 · C-12.

**phase-19-settings-front.md** — T19-1 (owner = this phase; INDEX R4 probes; E9 sub-rows; table rows; task 4; the review
as a gate) · T19-2 (Apps rows; moves; E1 / E8; H-row) · T19-3 (cite the Change Log line; app-list label ADD; E2 assertions;
edge case) · T11-10 (R1 19-4: keep phase 11's four; H8) · T19-5 (Themes / Colors) · T19-6 (depends-on comment) · T19-7 (E9
AVD / P3) · T19-10 (stale sweep) · C-4 (a–e) · C-5 (Y4) · C-8 · C-12.

**INDEX.md / PLAN.md (when this triage is applied, not now)** — the 11 → 13 → 12 → 14 order (T12-3) in the phase notes and
PLAN's 2026-09-22 order line; phase 15 / 16 / 17 notes for C-1 / C-2 (15 carries the routing fix; 16 the guard; 17 depends
on both); the R4 row's five helper probes (T19-1); the review-roster line for round 1's triage.

**Code (already in flight, outside the docs)** — J6 (Tess writes to the local calendar only), J5 ("play <name>" in the
shell's player), J7 (silence → "AND"). The INDEX Change Log lines for J5 / J6 / J7 are the lead's, written when each lands.

---

## 4. Counts

- Merged findings: **100** (from R1's 65 and R2's 60, overlaps merged as cited per row).
- Severity as merged: **BLOCKING 17 · SHOULD-FIX 36 · NOTE 47.**
- Class: **DOC-UPDATE 66 · AGENT-CALL 27 · QUESTION FOR JEREMY 1 (two questions, §2) · REJECT 6** (T13-6, T14-9, T15-12,
  T17-10, T19-8 are confirmations with nothing to change; T16-6 is wrong — K-9 is in the stash; two partial rejects sit inside
  DOC-UPDATE items T18-5 and T19-3). R1 19-4 is folded into T11-10 and not counted twice.
- BLOCKING by phase: 12 ×3 · 13 ×1 · 15 ×2 · 16 ×2 · 17 ×2 · 18 ×2 · 19 ×1 · cross-phase ×4.
- Of the 17 BLOCKING: 1 is code in flight (T16-2), 2 wait on Jeremy (T17-1's two questions), the other 14 are writable now.
