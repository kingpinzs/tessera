# Phase 13 gate, round 3 (the last): Reviewer B (testability and evidence, adversarial), 2026-09-26

## What I read

I worked from the brief, including its "Round 2" and "Round 3" sections. I read:

- both round-2 reports;
- the INDEX Change Log lines "PHASE 13 GATE ROUND 2" (`INDEX.md:91`) and "… ROUND 2 (B) AND AFTER" (`:90`), and row 13 (`:61`);
- the FINAL doc's Acceptance preamble, E1–E13 and Edge cases;
- `qa/phase-13/README.md`, and every current row log with its slices;
- the runs kept from before the decode fix, EDGE_RAPID's run3, run4 and back-race probe, and `unit/`;
- the drivers changed since 6b20eb10 (`e10.sh`, `e12.sh`, `edge.sh`);
- the code: 9c4d049f (`start/SingleFlight.kt`, `start/BackgroundDecoder.kt`, `SingleFlightTest.kt`), the decode's three callers (`start/StartPage.kt:1144-1149`, `ui/fluent/StaticBackdrop.kt:70-73` and `:181-192`), `StaticBackdrop.build`, and `ui/components/ModalOverlay.kt`.

I ran nothing on the emulator. "Re-derived" means I recomputed the figure on the host from saved captures: `sha256sum -c` of E8's captures, python over PNGs and logs, and `git diff --name-only`, `git cat-file`. Paths are relative to `docs/plan/qa/phase-13/`; code paths are relative to `app/src/main/kotlin/app/tileshell/`.

**The snapshot.** `main` at 08de290a. `git diff --name-only 9c4d049f HEAD -- app/` is empty, and `git status app/` is clean. `app/build/outputs/apk/debug/app-debug.apk` is sha256 `0eb36905…` / md5 `44b46cc9…`, built at 02:26:53. 9c4d049f was committed at 02:27:25, and `SingleFlightTest`'s green XML is stamped 02:26:54 local. The red runs at about 06:04 did not rebuild the APK (its mtime is unchanged). Every row counted on 0eb36905 reads `apk match yes (44b46cc9…)`.

## Round 2's BLOCKING finding (A-B1, the Start picture held twice): RESOLVED

**The fix is at the producer.** `BackgroundDecoder.decode` (`start/BackgroundDecoder.kt:32-50`) now works in three steps:

1. It returns the finished decode if it has one (`:33`).
2. Otherwise it joins the one in-flight run for that URI (`:35`, `SingleFlight.run`).
3. That run re-checks the cache before it decodes (`:36`).

The run belongs to a process scope (`SupervisorJob() + Dispatchers.IO`, `:29`), not to its first caller (`SingleFlight.kt:18`). Its entry is removed on completion, and only if it is still the current entry (`:20`). All three callers pass the same `backgroundUri` string as the key:

- `StartPage.kt:1147`;
- `StaticBackdrop.kt:190`, the app list's picture;
- `StaticBackdrop.kt:71`, the static build.

**On the device, the steady state now holds one decode** (acrylic off, Start and the app list decoding in the same frame):

| Build | Where | A (KB) | B (KB) | B − A (KB) |
|---|---|---|---|---|
| 509f6e49 | `E10-run-509f6e49-pre-decode-fix/E10.txt:23`, `:28` | 78 325 | 98 877 | 20 552 |
| db078d6a | `E10-run4-db078d6a/E10.txt:23`, `:28` | 78 428 | 99 307 | 20 879 |
| 0eb36905 | `E10/E10.txt:23`, `:28` | 78 539 | 89 518 | **10 979** |

- A is stable across builds (78.3–78.5 MB), so B's drop of 9 359 KB is the second copy gone. One decode is 1080 × 2340 × 4 = 9 872 KB (re-derived).
- The new clause, `E10/E10.txt:29`, asserts B − A ≤ 9 871 + 3 072 KB. Its bound comes from the fixture's size (`scripts/e10.sh:102`), not from the capture under judgement. Two copies (≈ 20 MB) fail it, so the clause discriminates.
- C − B = 9 239 KB (`E10/E10.txt:33`) ≈ one 1080 × 2196 layer (9 264 KB, re-derived). So the static build took the held decode and made no third copy. The rebuild took 56 ms (`E10/slice-C.txt`), a cache hit.
- A2 − A = 986 KB (`:37`), in one pid (`:38`).

**The huge picture.** EDGE_HUGE's B is 150 443 KB on 0eb36905 (`EDGE_HUGE/EDGE_HUGE.txt:18`), against 212 180 KB on db078d6a (`EDGE_HUGE-run-db078d6a-pre-decode-fix/EDGE_HUGE.txt:18`). The drop, 61 737 KB, is one 4000 × 4000 decode (62 500 KB, re-derived). B is recorded, not asserted, and the case has no A, so this is corroboration across runs (N3).

**Process start with acrylic on (three callers), re-derived.** EDGE_RAPID's C is taken by an identical driver prefix on both builds:

- 105 982 KB on 509f6e49 (`EDGE_RAPID-run2-509f6e49-no-gpu-record/EDGE_RAPID.txt:13`);
- 95 547 / 95 761 / 95 648 KB on 0eb36905 (`EDGE_RAPID/EDGE_RAPID.txt:13`, run3 `:13`, run4 `:13`).

That drop, about 10.3 MB, is one decode, so the start path shares too. No row asserts this path (N3).

**Unit tests.** `unit/TEST-app.tileshell.start.SingleFlightTest.xml` reads tests=4, failures=0:

- one run shared for one key (`assertSame`, runs = 1);
- different keys run separately;
- a cancelled caller does not cancel the run for the others. A run owned by its first caller would hand the stayer a CancellationException, so this test tells the two designs apart;
- a finished run is not reused.

The red run, `unit/red-SingleFlightTest-non-sharing-stub-gradle.txt`, shows the sharing case failing (AssertionError) and the cancel case failing (ComparisonFailure), 2 of 4. That is exactly what a `run = make()` stub produces: test 1 gets two objects; test 3's stayer runs its own make and returns "second run". The stub's source is not kept (N6).

**Is 9c4d049f right?**

- **Sharing:** yes, as above.
- **A cancelled waiter:** `await()` throws into the caller, and the run continues (`SingleFlight.kt:18`, `:23`). `StaticBackdrop.build` still rethrows the cancellation, so a7ef430b's behaviour holds (`StaticBackdrop.kt:83-86`). `runCatching` inside the run cannot swallow a cancellation of its own, because its scope is never cancelled.
- **Failure:** the run returns `Result.failure` as a value, every waiter gets it, and it is not cached, so the next call retries. On the device, E13 on 0eb36905 logs exactly one `static backdrop failed … FileNotFoundException … ENOENT (fallback)` (`E13/slice-new-process.txt:55`) and the fallback (`E13/E13.txt:17-22`).
- **Change of picture:** runs are keyed per URI. The old `LaunchedEffect` / `produceState` is cancelled before it assigns, so a page never shows the old picture. On the device: `EDGE_BG_CHANGE/EDGE_BG_CHANGE.txt:19-25` (rebuilt for media 173, no stale checker) and `:27-30` (removed: no rebuild, (0,0,0)).
- **Phase 01's Start page:** it now receives the same `ImageBitmap` object as the app list. Nothing recycles or mutates it: the only `recycle()` in the path is on the rendered hardware copy (`StaticBackdrop.kt:154`). Start still draws the picture, sharp: `E2/E2.txt:13`. I re-derived the checker's 0/255 steps at x = 188, 540 and 890 on `E2/start-fhd.png` row 1536, so the check reads the wallpaper, not a tile edge.
- **The input path:** no decode call runs on a hold or a Back. The `LaunchedEffect` keys (`StaticBackdrop.kt:184`, `:196`) do not change when the band opens.

The fix leaves one race in place (N9); it was there before 9c4d049f.

## Driver changes since round 2: faithful or weakening?

- **E10 "the picture is held once": faithful**, an agent addition that strengthens the row (`INDEX.md:91`). It is one-sided (N2).
- **EDGE_LIGHT's MARK and form proof: faithful.**
  - The MARK now precedes `set_pref` (`scripts/edge.sh:252-255`).
  - The slice holds this process's start (`EDGE_LIGHT/slice.txt:3`) and its rebuilt line (`:59`). The ring is per process, so that line cannot come from the intermediate process between the two `set_pref`s.
  - The form is asserted from the capture: edge spread 155.4 px inside 134.5 ± 26.9 (`EDGE_LIGHT/EDGE_LIGHT.txt:19`). The fallback would read ≤ 2 px.
  - Re-derived from `EDGE_LIGHT/applist.png`, y 599–607, x 675–945: width 155.4, plateaus 240.6 / 253.1, an exact match. The assert at `:15` still reads the form-agnostic show line, but it no longer carries the proof.
- **EDGE_RAPID's gap after Back: faithful to the doc's words, but it steps around a product race whose cause is not controlled (N1).** 10 holds took 11.9 s ≤ 15 s (`EDGE_RAPID/EDGE_RAPID.txt:16-17`), and 10 of 10 opened the band (`:15`).
- **EDGE_RAPID's GPU record: faithful**, and recorded, as the doc keeps PSS as the check. Re-derived: 28.56 → 30.88 MB total, but the non-purgeable part goes 3.24 → 3.15 MB (`:14`, `:19`). So no leaked render target shows; the rise is purgeable cache.
- **E12's apk-match check: faithful.** It reads each counted row's own `apk match` field: `E12/E12.txt:13`, `:16`, `:19`, `:22`, `:25`. A row reading `NO` fails it.
- **E12's file-list assert** (`E12/E12.txt:28`) equals my `git diff --name-only c73c32cf 9c4d049f -- app`. That diff is 10 files: L13-2's five (with ModalOverlayTest), StaticBackdrop.kt + test, BackgroundDecoder.kt, SingleFlight.kt + test.

## Do the earlier-build rows stand? (git diff checked)

`git diff --name-only` between the build commits:

| From | To | App files changed |
|---|---|---|
| a7ef430b | 9c4d049f | BackgroundDecoder.kt, SingleFlight.kt, SingleFlightTest.kt |
| 3448c92f | 9c4d049f | the above + StaticBackdrop.kt + StaticBackdropTest.kt |
| f1b05c0b | 9c4d049f | the same five |
| c73c32cf | 9c4d049f | the same five + L13-2's five |

Which rows set a picture (prefs-in and the drivers):

- **E8** sets none, so the decode never runs. It stands.
- **E9** sets the checker, but it is recorded only. It stands as a record.
- **E11, E11_MENU, SAVER_MENU, SCREEN_OFF and PROCESS_DEATH** set none. They stand.
- **EDIT_MODE, RV10, A11Y and MANY_APPS** set the checker. They assert blur widths, band heights, rebuild counts and scroll timing. None of those depends on how many copies of the decode are held: the sampling, the key and the bitmap content are unchanged. They stand. Round 2 already checked their rings for a cancelled-build line and found none.
- **E3, E5 and E6** set no picture and touch no L13-2 code (Cortana only). They stand.
- **E7** sets the checker and compares D with the same pixel's U, so the number of copies cannot move it. It stands.

The claim is sound. But the README does not "say why each earlier-build row stands": only E3's row gives a reason (N5).

## Acceptance rows

| Row | Build | Verdict | Evidence |
|---|---|---|---|
| unit | 9c4d049f / a7ef430b | PASS 18/18 | `unit/TEST-…SingleFlightTest.xml` 4/0; `…StaticBackdropTest.xml` 3/0; `…FluentMaterialTest.xml` 11/0. Reds: `unit/red-*.txt` (N6) |
| E1 | 0eb36905 | PASS 24/0 | `E1/E1.txt:12` start on; `:15` 134.0 px; `:17-22` saver: 13 ms after BS_MARK, sharp 0.80, 0 / 51; `:24-26` saver off, blurred; `:28-34` switch off, 112 ms, sharp, 0 / 51; `:36-37` on; `:38-40` disable_window_blurs: no off line (slice-6 is a live, non-empty slice), still blurred |
| E2 | 0eb36905 | PASS 22/0 | `E2/E2.txt:13` Start sharp (re-derived above); `:14-16` tag inside `app_list`; `:19-22` 130.2 px, profile 2.7, text sharp; `:23-26` off; `:28-37` 720 wide 94.4 vs 90.1; `:38-39` restored |
| E3 | 15aa7f5f | PASS 34/0 | `E3/E3.txt:21-24` show line, dismissed, (63,68,64) in the closed capture, precondition patch; `:38-40` edge 1.40 from the oracle. Stands (E12.txt:28) |
| E4 | 0eb36905 | PASS 48/0 | `E4/E4.txt:17-26` band (profile 1.00, patches 0.80, noise 3.12, text sharp); `:27-31` off; `:33-52` playlists (2.10, held square sharp, off); `:54-68` now-playing (noise 1.77 is what discriminates over its black backdrop) |
| E5 | 15aa7f5f | PASS 45/0 | `E5/E5.txt:18-22` (14.3,19.3,13.3) with its precondition; `:33` live sub-step; `:42` off identical; `:52`, `:57`, `:62`. Stands |
| E6 | 15aa7f5f | PASS 11/0 | `E6/E6.txt:15-17` precondition, σ 3.21; `:19` identical 1 s apart; `:23` off 0. Stands |
| E7 | 120e52ec | PASS 144/0 | `E7/E7.txt:21-24` (a); `:64-66` (d); `:164-172` controls. Stands (above) |
| E8 | 509f6e49 | PASS 32/0 | `E8/E8.txt:14-53` shell clock; `:54-68` corroboration. `sha256sum -c E8/captures.sha256`: 7/7 OK against the local mp4s. Stands (no picture). Round 2's N1 / N4 flake risks are unchanged (N8) |
| E9 | 509f6e49 | recorded only (7 facts), as worded | `E9/E9.txt:12-18` |
| E10 | 0eb36905 | PASS 14/0 | `E10/E10.txt:14-18` toggle survives force-stop; `:21` Photos 7 before A; `:29` held once; `:31` rebuilt line; `:33` C − B 9 239 KB; `:37` A2 − A 986 KB; `:38` one pid |
| E11 | db078d6a | PASS 47/0 (the before / after amendment, INDEX Change Log) | `E11/E11.txt:43-46`, `1-e15-compare.txt`; `:97`. Sets no picture; stands |
| E11_MENU | db078d6a | PASS 13/0 | `E11_MENU/E11_MENU.txt:15-27` (`:19` the whole menu on screen) |
| E12 | 0eb36905 (+ E3 / E5 on 15aa7f5f) | PASS 53/0 | `E12/E12.txt:12-26` builds and apk match; `:28` file list; `:29-48` each show line in its own slice; `:49-65` E1's reasons in step order |
| E13 | 0eb36905 | PASS 10/0 | `E13/E13.txt:15` control 134.0 px; `:16` file gone; `:17-19` failed line (ENOENT, `slice-new-process.txt:55`, after the process start at `:6`), `(fallback)`, no rebuilt line; `:21-23` (0,0,0), no edge |
| BS_STEP | 0eb36905 | PASS 7/0 | `BS_STEP/BS_STEP.txt:14` rebuilt for this run's URI; `:16-21` 128.8 px, σ 50.25, plateaus 2.0 / 51.6 |

## Edge cases

| Case | Build | Verdict | Evidence |
|---|---|---|---|
| Battery saver while a menu is open | db078d6a | PASS | `EDGE_SAVER_MENU/EDGE_SAVER_MENU.txt:14-19` fallback line 22 ms after acrylic=off; `:20` (40,40,40) recorded as corroboration; `:21`; `:23-24` form=acrylic, still open |
| File deleted | = E13 | PASS | `E13/E13.txt:16-23` |
| Toggle changed while the app list shows | = E1 | PASS | `E1/E1.txt:28-37`; `E1/slice-5-switch-on.txt` has the rebuilt line and `applist form=acrylic` |
| Background changed / removed | 0eb36905 | PASS | `EDGE_BG_CHANGE/EDGE_BG_CHANGE.txt:14` control; `:19-25` rebuilt for media 173, 0.2 × 128, no edge; `:27-30` removed |
| Huge 8000 × 8000 | 0eb36905 | PASS | `EDGE_HUGE/EDGE_HUGE.txt:20` rebuilt for media 174; `:23` C − B 9 072 KB; `:24` pid unchanged |
| No background image | 0eb36905 | PASS | `EDGE_NO_IMAGE/EDGE_NO_IMAGE.txt:13-16` (process start inside the slice) |
| Light theme, bright wallpaper | 0eb36905 | PASS | `EDGE_LIGHT/EDGE_LIGHT.txt:16` rebuilt; `:17` tint (255,255,255); `:19` blurred, 155.4 px (re-derived); `:20-24` ≥ 200, text 0. H1 capture: `EDGE_LIGHT/applist.png` |
| Edit mode | db078d6a | PASS | `EDGE_EDIT_MODE/EDGE_EDIT_MODE.txt:14-21` (locked, no rebuilt line, layout unchanged) |
| Burst | — | dropped (T11-8) | — |
| Surface over acrylic | = E4 (1) | PASS | `E4/E4.txt:21-24` |
| Screen off, then wake | db078d6a | PASS | `EDGE_SCREEN_OFF/EDGE_SCREEN_OFF.txt:18-28` |
| Process death | db078d6a | PASS | `EDGE_PROCESS_DEATH/EDGE_PROCESS_DEATH.txt:14-24` |
| RV10 | db078d6a | PASS | `EDGE_RV10/EDGE_RV10.txt:13-45`; 720 wide in `E2/E2.txt:28-37` |
| disable_window_blurs | = E1 | PASS | `E1/E1.txt:38-40` |
| Low-RAM | unit | PASS | FluentMaterialTest `ruleAndPrecedenceOnAllEightCombinations` |
| Accessibility | db078d6a | PASS on the asserted parts; the inversion capture is owed to H1 | `EDGE_A11Y/EDGE_A11Y.txt:13-22` |
| Rapid open / close | 0eb36905 | PASS | `EDGE_RAPID/EDGE_RAPID.txt:15` 10 / 10 shows; `:16-17` 11.9 s; `:20` PSS +1 382 KB; `:21` one pid; `:14`, `:19` GPU recorded. See N1 |
| 300+ packages | db078d6a | PASS | `EDGE_MANY_APPS/EDGE_MANY_APPS.txt:15-22` |

## Findings

### BLOCKING

None. Round 2's one blocker is resolved by the captured evidence above. No row or edge case is unmet or unproven.

### NON-BLOCKING

**N1: EDGE_RAPID. The hold lost after Back appeared across a build change, and its cause is not controlled.**

- The same driver cadence (swipe 850 ms, then Back, no gap) gave different results on the two builds:
  - 10 of 10 on 509f6e49 (`EDGE_RAPID-run2-509f6e49-no-gpu-record/EDGE_RAPID.txt:14`, 8 848 ms);
  - 1 of 10, twice, on 0eb36905 (`EDGE_RAPID-run3-0eb36905-back-race/EDGE_RAPID.txt:15`, 8 832 ms; `-run4-…:15`).
- The Change Log (`INDEX.md:90`) blames a timing race in the band / Back handling (phase 02's part, L13-2's ModalOverlay). Nothing re-ran the no-gap cadence on 509f6e49 to rule out the build.
- Its details have no kept capture:
  - "the pager logs a `[motion] pivot` instead": run3's and run4's slices hold no pivot line; after the first hold they hold nothing at all;
  - "by hand 3 of 4 and 4 of 4": the probe (`EDGE_RAPID-probe-back-race-0eb36905/`) holds only dumps, no log, and in every dump the hold opened the band (b, d, f1–f3).
- **My reading of the code.** 9c4d049f is not a plausible cause, because no decode runs on a hold or a Back. Run3 and run4 ran right after the ~06:04 gradle red runs, so host load is a likely cause of the timing.
- **For this gate.** The driver's 0.3 s per cycle keeps the doc's "10 holds in 15 s" (11.9 s), and the 10-show assert keeps the case able to fail. So the case is met as worded.
- **For Jeremy.** Rule on the race. Keep a no-gap probe in the end-of-project run.

**N2: E10's "held once" clause is one-sided.**

- `scripts/e10.sh:103` bounds B − A only from above, so a build that never decoded the picture would pass it.
- The recorded 10 979 KB (≥ 9 872) and E1 / E2 on the same build (picture drawn) show the decode is held, so the evidence stands.
- Add a floor: B − A ≥ one decode − 3 MB.

**N3: two memory figures are corroboration only, not assertions.**

- EDGE_HUGE's B is recorded and the case has no A. The 212 → 150 MB comparison is across runs.
- The acrylic-on process start (three callers) has no asserted memory figure. It is shown only by EDGE_RAPID's C across builds (re-derived above).
- The doc asks for neither, so this is not a gate failure.

**N4: four edge.sh blobs named in row headers are not in git.**

- `EDGE_LIGHT.txt:4` names edge.sh blob 4512c61a. The db078d6a rows name others: SAVER_MENU ffa7bb94, PROCESS_DEATH aec108c7, RV10 / A11Y 74def838.
- `git cat-file -t` fails for all four: they were working-tree states between commits.
- Their assertion labels and tolerances match HEAD's case sections (checked label by label), so the evidence reads the same. But the exact driver behind those logs cannot be recovered.
- Round 2 A's "every current row header's driver blob equals its HEAD blob" is not true for these rows.

**N5: the README does not say why earlier-build rows stand.**

- Only E3's line gives a reason. E5, E6, E7, E8, E9, E11 and the db078d6a edge cases carry none.
- `INDEX.md:91` says they "stand where the README says".
- I verified the claim with git diff myself (section above). Add one reason per row.

**N6: the red runs are console output without the code they ran against.**

- Neither the "non-sharing stub" nor the "old handling" is kept.
- The failing tests and lines are consistent with those descriptions: SingleFlight tests 1 and 3; StaticBackdropTest at `:29`, the cancelled-build test.
- Keep the stub diff next to each red file.

**N7: EDGE_LIGHT's blur check has little margin.**

- 155.4 px sits 6 px inside the 161.4 upper bound.
- The step is only 12.5 levels (plateaus 240.6 / 253.1), so the ±5 noise pushes the 10–90 % crossings outward. Dark-theme rows read 128.8–134.0.
- This is a flake risk for Jeremy's run. Use a higher-contrast bright fixture (for example 128 / 255) or more strip rows.

**N8: carried from round 2, unchanged and open for Jeremy (`INDEX.md:90`).**

- E8's reminder-menu band rejects about one correct open in six (t0 is stamped inside the first frame; round 2 B-N1).
- The pivot sits 0.7 ms inside its band (B-N4).
- E8's reminder menu is clipped at the window's top (B-N9).

**N9: a pre-existing stale-cache race in `BackgroundDecoder`, not introduced by 9c4d049f.**

- An older URI's decode that finishes after a newer one calls `remember(old)` (`start/BackgroundDecoder.kt:49`, `:54`). It evicts the current picture from the one-entry cache and pins the old bitmap until the next `forgetAllBut`.
- This is reachable only when the picture is changed twice within one decode. The old code had the same order.
- Fix at the producer: `remember` should skip a URI that is no longer the latest asked for.

**N10: the ADD-line and doc-drift items from rounds 1–2 stay recorded (`INDEX.md:92-94`).** INDEX row 13 is now current (`INDEX.md:61`), so round 2's N7 is closed.

## Phone rows and NEEDS-HUMAN rows: are their captures attached?

- **P1:** nothing attached; R4 is pending. Information only.
- **P2:** no phone capture. `scripts/e9.sh` is ready. Phase 03 P4's PSS baseline is still unrecorded. The double decode that A expected to push P2 over its bound is fixed.
- **P3:** phone only; nothing attached.
- **P4:** the AVD references are attached: `E2/applist-fhd.png`, `applist-off.png` (0eb36905); `E3/menu-*.png`; `E4/band.png`, `plmenu.png`, `npmenu.png` (0eb36905); `E5/pane-*.png`. The phone captures are owed.
- **H1:** the P4 set, `EDGE_LIGHT/applist.png` (acrylic now asserted) and `EDGE_A11Y/*.png`. The inversion capture does not show the inversion.
- **H2:** the values are attached (E1 / E2 widths, E6 σ 3.21, BS_STEP σ 50.25).
- **H3:** blurred and unblurred are attached (`E1/on-0.png`, `E1/saver.png`, `E2/applist-fhd.png`, `applist-off.png`). The "wallpaper moves with the pivot" half has no capture with a picture set (E8 sets none).
- **H4:** E7's U / D / M / A captures and `explore/lights-*.png`.
- **H5:** a dump only (`E1/settings-theme.xml`); no screencap.
- **H6:** `E1/saver.png`, `EDGE_SAVER_MENU/menu-saver.png`.
- **H7:** nothing to attach.
- **H8, H9:** phone rows; owed.

VERDICT: PASS
