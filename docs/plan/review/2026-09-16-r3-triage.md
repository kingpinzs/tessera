# Review round 3 triage (2026-09-16), final allowed round
Inputs: review/2026-09-16-r3-codex.md (0 BLOCKING / 0 MAJOR / 1 MINOR) and review/2026-09-16-r3-fable.md (0 BLOCKING / 2 MAJOR / 6 MINOR).
Both reviewers: every round-2 BLOCKING and MAJOR item RESOLVED. No new BLOCKING. **The plan is review-cleared for the split.**

| Finding | Disposition |
|---|---|
| R3R2-01 (codex) | FIXED: round-1 triage marked superseded in part; round-2 topics merged into its per-phase list. |
| R3F-01 | PHASE (helper's): the helper edge-case set (reboot on trusted Wi-Fi / Wi-Fi off / unknown network; Wireless debugging toggled off; adb key revoked after 7 days; APK reinstalled while the helper runs; killed by Device care) + precondition rows (Developer options, Wireless debugging paired, adb authorization timeout disabled, WRITE_SECURE_SETTINGS granted, helper running). Owner behaviour question there: what the Wi-Fi tile does when the helper is down (default: "helper not running, tap to start", no silent fallback). FIXED now: RV1 wording. |
| R3F-02 | AGENT: R4 spike (phone-only, before the helper phase's interview) added to PLAN.md research tasks. Earlier phases must not pre-build helper rows. |
| R3F-03 | FIXED: helper added to the feature list; stale triage row replaced. At the split: a phase 3 with no toggle commands (added in the helper's phase) or the helper introduced in phase 3; no panel placeholder either way. |
| R3F-04 | FIXED: Cortana feature line = ring states + waveform listening (Q12). |
| R3F-05 | FIXED: PLAN.md R3 entry lists the scope extensions; RV9 FINAL tag includes build + doc-URL/approximation cases. |
| R3F-06 | PHASE-1: pin the AVD system image (Google Play image, so real Messages/Gmail/Calendar/Photos/Chrome feeds exist); W10M -> Android default-tile mapping (owner question: stand-ins for Mail, Music, Maps, Store, Photos, People, Camera; default = Google app, role holder where a role exists); missing-app slot rule (edge case). |
| R3F-07 | PHASE-1: phone precondition row "Auto Blocker OFF" plus USB debugging. |
| R3F-08 | FIXED: RV10 uses the display's portrait width (maximumWindowMetrics); HD+ = 2 px/epx added. |
