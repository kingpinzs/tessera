# Phase 13 — round 3 triage (Stage A steps 5–6, the LAST round; 2026-09-25)

Inputs: review/2026-09-25-phase13-r3-design.md (Reviewer 1, design / correctness, ids `R3D-1`–`R3D-14`, **Opus**) and
review/2026-09-25-phase13-r3-testability.md (Reviewer 2, testability / evidence, ids `R3T-1`–`R3T-15`, **codex CLI**; raw
stdout review/2026-09-25-phase13-r3-codex-raw.md), both against the brief review/2026-09-25-phase13-r3-brief.md. Roster:
**opus + codex-cli** — the codex MCP is not loaded, so Reviewer 2 is the codex CLI (rc 0); the Fable reviewer died on its first
request (HTTP 429 "You've reached your Fable limit", req_011CfQgfBDU6gBW6zPt3DjWq) and Jeremy ruled (b) on 2026-09-25: Opus for
Reviewer 1 this round, and for phase 13's gate reviews if Fable is still out.

Ids continue round 2's (`T13-11` onward). Class rule as rounds 1 / 2: a mechanical re-cut, or a missing row with an obvious form →
DOC-UPDATE; a fix that needs a design choice → AGENT-CALL (one line of reason; listed in §2 for Jeremy to overrule); a change to
WHAT is built with no ruling covering it → QUESTION; wrong → REJECT.

**Checked by the lead before ruling (not taken on the reviewers' word):** a host computation of the edge-spread rule
(`scipy.ndimage.gaussian_filter`, mode `nearest`, σ = 0.57735·r + 0.5): the 8-across checker measures 88 px against the
107.6–161.4 px band at every strip offset (FAILS on a correct build); 4-across measures 131 px at FHD+ (band 107.6–161.4) and
87 px at 720 wide (72.1–108.1), plateaus 50.2 / 1.0 at a square's centre row. Code: `RemindersPage.kt:586-620` (the reminder
menu's items are Complete / Delete, `detectTapGestures`, the (79,84,80) fill only from `chosen`, i.e. after the tap), `:222,236`
(`onMenuGone` does not clear `pressedRowId`), `:549-573` (the row's long-press gesture); `CortanaNavPane.kt:123-152,193-211`
(pane items act at touch-down; the pressed fill is suppressed on the current item); `CortanaModel.kt:448-453` (Home-on-Home
returns early, anything else sets `paneOpen = false`); `CortanaSessionRoot.kt:100-107`, `:166-173` (the pane is composed only
while open; the ≡ button is on Home whenever unlocked); `CortanaPageChrome.kt:50,267-276` (pane 256 epx; menu 243.3 × 107 epx,
44-epx item pitch); `AppListMenu.kt:141-162` and `MusicPlaylistUi.kt:56-122` (both menus' items draw `ROW_PRESS_ALPHA` 0.15
white while held, `ui/components/ListRows.kt:19`; `MusicMenu` is one composable); `MusicCollectionPage.kt:218,262-277,423-427,
549-560` (holds only on songs, playlists and playlist tracks; album rows take no hold; playlist rows draw an accent square;
`DetailPage` rows carry no art); `MusicNowPlaying.kt:235` (`MusicMenu`'s second call site, the `•••` band);
`StartPage.kt:565-577` (Start draws the picture `ContentScale.Crop` at 1.3× with 0.25 parallax), `:1148` (`rememberBackground`);
`ui/MotionClock.kt:1-20` (phase 15's `[motion]` writer exists); `qa/phase-03/scripts/lib.sh` (has `ring_mark`, `ring_since`,
`reply_since`, `record`; no `battery_saver_*`); `qa/phase-03/scripts/e15.sh:8-9` vs `:24-32` (its header promises a photo reminder
and three groups; it makes two spoken reminders and no photo); `phase-11-tile-quick-actions.md:475-486, 519-525` (the satellite
press sub-steps with phase 13's clauses already written); `ReminderDetailPage.kt:100,170` (a reminder's photo comes through the
system photo picker, `PickVisualMedia`).

**Counts: 29 reviewer findings merged into 19 items — 6 BLOCKING · 8 SHOULD-FIX · 5 NOTE. Classes: 12 DOC-UPDATE · 7
AGENT-CALL · 0 QUESTION; one reviewer proposal REJECTED inside T13-13 (R3T-7's "repair phase 03's pressed lifecycle").** Every
BLOCKING is resolved by a doc update or an agent call; no fork needs Jeremy. T11-31 (carried from phase 11's round 3) is applied
inside T13-13.

## 1. Merged findings and rulings

| Id | Sev | Sources | Class | Ruling (applied to phase-13-fluent-materials.md) |
|---|---|---|---|---|
| T13-11 | BLOCKING | R3D-1 | AGENT-CALL | The live source as written builds wrong both ways it can be read: an effect on the recorded layer blurs the page itself; an effect on a surface-sized layer blurs only pixels inside the surface. **Mechanism:** layer L (no effect) is recorded from ONE child that holds the page's background and content, a sibling drawn BELOW the surface (at each call site the background moves from the surface's parent into that child); the page draws L to show itself, unchanged. The surface owns layer S = its bounds grown by 3σ on every side (clamped to the window), `renderEffect = BlurEffect(r, r, TileMode.Clamp)`, recording L translated by −S's origin; it draws S clipped to its own bounds, then tint, then noise. No bitmap copy of L exists, so what S blurs is always what the page shows (liveness is structural, T13-24). Preamble: B = the Gaussian of the WHOLE backdrop capture, edge mode `nearest`, read at the same pixel. Reason: the only reading under which the surface blurs the real backdrop around it and nothing else. |
| T13-12 | BLOCKING | R3T-1, R3D-2 | DOC-UPDATE | Fixture → **4 squares across** (270 px at FHD+, 180 px after `wm size 720x1560`); edge spread = the 10–90 % width of the step between the two adjacent square CENTRES' plateaus on the same row, read on the column mean of ≥ 8 strip rows clear of text (lead's host check above). E2: white-square centres ≈ 50, black ≈ 1. Build-start σ check on an isolated step (a half-black / half-white layer). E3's photo edge, E4's band and E5's title edge lose their "blurred width" clauses — each is compared as a profile against the host oracle instead (E4's band re-blurs the acrylic app list; E5's title is a stroke, not a step). Edge case "a surface over another acrylic surface" rewritten: the band blurs the app list INCLUDING its blurred backdrop, once more, exactly as E4's B computes. |
| T13-13 | BLOCKING | R3D-3, R3T-5, R3T-6, R3T-7, R3T-8, T11-31 (carried) | DOC-UPDATE + AGENT-CALL | E7 re-cut to the held states the built items actually have. Per item: capture U (open, unpressed); DOWN at the stated point → D; MOVE +60 px (0.5 r) inside the item → M; MOVE off the item → A (the press is over: every sampled pixel = its held look ± 2); UP outside the item runs nothing. P_p = the item's held look without the lights = U_p for items that draw nothing while held (the reminder menu's items — their (79,84,80) is drawn only after a choice, R7 §3.6.6 as built; the current pane item, accent; satellites, rest pixels), and U_p + 0.15·(255 − U_p) for the app-list and Music menu items (their `ROW_PRESS_ALPHA` fill). In D: ring (≥ 126 px from the touch point) P + 0.30·(255 − P), touch point P + 0.10·(255 − P), the 0.5 r point P + 0.05·(255 − P), the far point P ± 2; in M the new touch point reads the centre value and the old one the 0.5 r value (the light followed — replaces "the brightest pixel", which the ring and text outshine). Items: (a) reminder menu, Complete item; (b) ≡ pane over Home, `cortana_pane_item_home` (the current item: the pane stays open, R7 §3.1.11; any other item closes it at touch-down); (c) satellite — phase 11 E5's press sub-step and E7's corner-case held capture, their phase-13 clauses exactly: touch (left + 10, top + 10), 0.5 r point (left + 70, top + 10) (T11-31), far point 12 px inside the bottom-right corner, `press_tilt` and `press_p4`, MOVE off and UP runs nothing, neighbours ± 1, off tolerance ± 2 (T11-31), on phase 11's own baseline and fixtures (Seeding exception); (d) `applist_menu_pin`; (e) `music_menu_new` (R3T-8). Acrylic off: D = P ± 2 everywhere. **Agent call:** a press ends when the pointer leaves the item, as well as at UP (the lights are "on pressed items"; Q3 B's "while it is down" read with Compose's own press semantics) — so a row can verify the lights' removal without running Complete / Delete or a shortcut. **REJECTED:** R3T-7's "repair phase 03's pressed lifecycle so the menu shows (79,84,80) while held" — that changes a FINAL phase's measured behaviour (R7 §3.6.6 / §3.1.12 as built), which Q1 A forbids; E7 uses the held looks that exist. E11's "every pressed-fill sample taken under T13-1's sample rule" is struck (e15.sh samples no pressed fill); T13-1's sample rule is cited by E7 only. |
| T13-14 | BLOCKING | R3T-4, R3D-4 | DOC-UPDATE | E4's Music part cannot run (album rows take no hold; song and detail rows carry no art). Re-cut: playlists pivot with three playlists made by MUSIC8's steps; hold the first playlist row → `music_menu` hangs over the next row's accent square (dump bounds intersect); under the band that square's edge profile = 0.2·B ± 4 (patch / column means; B from `acrylic_expect.py` over the pivot with the menu closed); the held row's own square, outside the band, is sharp; off → theme background ± 1; delete the three playlists. |
| T13-15 | BLOCKING | R3T-10, R3D-9 | DOC-UPDATE | The pager composes the app-list page beside Start (`StartActivity.kt:170`, `beyondViewportPageCount = 1`), so the static layer is built when the image is set and at process start — before the first swipe, so no unblurred frame shows — and kept until the image changes or is removed (Decisions (a), task 2). E13: `rm` the fixture, `ring_save`, MARK, `am force-stop app.tileshell`, Home, swipe to the app list; the new process's slice from MARK holds the `failed` line and no `rebuilt` line (the failure may be stamped before the swipe). |
| T13-16 | BLOCKING | R3T-11, R3D-9 (E10 half) | DOC-UPDATE | E10's memory rows re-cut to three states in one process, both pages warmed in each (the neighbour page is precomposed), median of three `dumpsys meminfo` samples 1 s apart, no force-stop between: A = no picture; B = the checkerboard with acrylic OFF; C = the checkerboard with acrylic ON after its `rebuilt` line; C − B ≤ 14 MB; "Remove picture" → within 2 MB of A. The huge-image and rapid-open edge cases cite A / C. |
| T13-17 | SHOULD-FIX | R3T-2, R3D-5 | DOC-UPDATE | Preamble gains **Acrylic-on colour reads**: every acrylic-on colour clause compares the mean of a ≥ 10 × 10 px patch, or for an edge profile the column mean of ≥ 8 strip rows — never one pixel (the ± 5 noise is not reproduced on the host); single pixels only with acrylic off, on opaque fills, or against the same pixel of a reference capture (E7). A clause promising a measured fill over its measured backdrop (Q1 A: E3's (40,40,40), E5's (14,19,13) / (17,23,15), E6's flat patch) places its patch where the oracle's B is within ± 1 of B_m over the whole patch, asserted first as a precondition. E3's closed-menu control is captured after the menu is dismissed by a tap outside it, with the held row's (63,68,64) asserted in that capture — never a capture from before the long-press. |
| T13-18 | SHOULD-FIX | R3D-6 | AGENT-CALL | The static source's geometry and kernel are fixed. Geometry: the app-list page draws the picture `ContentScale.Crop` to its own size at scale 1, no parallax, no 1.3× (those are Start's, X15); `acrylic_expect.py` maps the fixture the same way (Crop to the page's dump bounds). Kernel: HWUI's own blur — the cropped picture drawn into a `RenderNode` with `RenderEffect.createBlurEffect(r, r, CLAMP)`, rendered once off the main thread by a `HardwareRenderer` into an `ImageReader` surface and kept as the cached bitmap — never a CPU box / stack blur, so both sources share one σ. Build-start check: the offscreen route's isolated-step width equals the on-screen layer's. Reason: the host oracle and E2's numbers need one known geometry and one known kernel; the platform's blur is the only one both sources can share. |
| T13-19 | SHOULD-FIX | R3D-7 | AGENT-CALL | The surface table is the COMPLETE list. `MusicMenu` is one composable with two call sites, so the Music row covers both: the collection's hold menus and now-playing's `•••` band (`MusicNowPlaying.kt:235`), same tint (0,0,0), B_m the page under each; E4 gains a `•••` sub-step (presence, diagnostics, patch means vs oracle, fallback). Out gains: the pin confirmation band (H22), the app-list and Music jump grids (X8), Cortana's combo drop-down (`OptionList`, H25, which shares `CortanaUi.MENU_FILL`), and the name boxes and pickers — they keep their fills. The edge case "two transient surfaces at once (the pin band over Start while the burst is open)" is struck (nothing there is acrylic since T11-8). Reason: R10-Q1 names "menus" as the transient surfaces; the `•••` band is a menu and shares the composable, the others are prompts, grids and pickers. |
| T13-20 | SHOULD-FIX | R3D-8 | AGENT-CALL | `reason=` takes the first false term in the order low-ram, battery-saver, setting, else `none`; a line is written at process start and whenever the value OR the reason changes. Task 1's JVM test covers all 8 combinations. Reason: phase 12's edge case already expects `reason=battery-saver` for a preset that turns the switch off under battery saver. |
| T13-21 | SHOULD-FIX | R3T-3 | DOC-UPDATE | E3 gets its own fixture helper `qa/phase-13/scripts/reminders_fixture.sh` (not e15.sh, which makes two reminders and no photo): through the product path, typed with `lib.sh` `type_request` and confirmed with `type_request yes` (e15.sh's `make_reminder` does the same by voice): "remind me tomorrow at 9 am to QA13 tomorrow" and "remind me to QA13 photo" (Whenever), then the photo attached on the Whenever reminder's page through "Add a photo" and the system photo picker, picking a generated 1080 × 1080 black / white split PNG pushed to `/sdcard/Pictures/qa13-split.png` and media-scanned (phase 11 EDGE's X5 picker route). The helper asserts both rows and the photo node before any sample; E3's photo case holds the photo row at the photo's horizontal centre, 10 px above its bottom, and asserts the overlap before sampling; restore deletes both reminders and the pushed picture. |
| T13-22 | SHOULD-FIX | R3T-9 | DOC-UPDATE | E12 validates the SAVED, action-scoped `ring_since MARK` slices from E1–E5's runs on the same APK (saved before every force-stop, fixture rewrite or layout restore); each surface's show action supplies its own `[fluent] <surface> source=…` line; E1's reasons are checked in its saved step order; a missing slice fails. |
| T13-23 | SHOULD-FIX | R3T-12, R3D-13 | DOC-UPDATE | Wording under the 2026-09-25 QA ruling. E11 runs the specific sub-steps this phase's changes touch — phase 03 E15's pane and menu fill, geometry and selection sub-steps; phase 02 E2's hold-menu display and Pin action; MUSIC8's menu open, selection and dismissal; phase 01 E12's swipe / search / jump-grid sub-steps and E19's app-list bar checks — with their numbers, motion on the shell's clock (E8), plus `exported.py`; whole gates stay Jeremy's end-of-project run. E1's preset sub-row is "run by phase 12's build session as one of the specific tests its presets touch"; E15's driver is "a dependency of E11". |
| T13-24 | SHOULD-FIX | R3T-13 | AGENT-CALL | Live vs snapshot: T13-11's mechanism makes liveness structural (S records the same L the page draws to show itself; no copy exists). E5 gains an asserted live sub-step: Tess listening on Home (the persona pulses, period 1.04 s, R6 §3.1; its bounds lie under the 256-epx pane), tap ≡; two captures 200 ms apart differ inside the pane over the persona (patch-mean |Δ| ≥ 2 levels); acrylic off: identical inside the pane ± 0. Build-start check: that the pane opens while Tess listens and the persona keeps pulsing under it (recorded in Decisions; if it cannot, a Change Log line re-cuts the sub-step's backdrop before the gate). Reason: the one backdrop in the shell that changes while a phase-13 surface is open. |
| T13-25 | NOTE | R3D-10 | DOC-UPDATE | Satellites are draw-only: task 5 sets the satellite's press point on `QuickBurstState` from `EditGestures`' `Hit.Satellite` branch (DOWN, each MOVE, cleared at UP, cancel or leaving the satellite) and `QuickBurstLayer` draws the lights; task 7's Change Log list gains phase 11. |
| T13-26 | NOTE | R3D-11, R3T-15 | DOC-UPDATE | `StartPage.kt:987` → `:1148`; task 7's `record` clause → "`record` and `row_end`'s recorded-only line are phase 15's, already in `lib.sh`; this phase builds only `battery_saver_on` / `_off`"; the preamble's "(build task 7)" → "(phase 15's `lib.sh` helper)"; the table's "`start_page`'s pager page 1" → "the app-list pager page (`app_list`)". |
| T13-27 | NOTE | R3D-12 | DOC-UPDATE | Task 7's `[motion]` ADDs go through phase 15's `ui/MotionClock.kt` (one format). E8's `[motion] applist_menu` is written by `MotionClock.jump` from the hold's 783-ms uptime to the band's first frame: settle ≤ 33.4 ms (was "settle 0", true by construction). |
| T13-28 | NOTE | R3D-14 | DOC-UPDATE | Goal: the lights are "for the pressed items of the menus and the ≡ pane and phase 11's satellites (Q3 B's list; the app list's rows keep X19's 15 % white)"; "pod cards later" and "pod cards if a later phase makes them pressable" struck (a hook for a later phase; phase 14 names no lights). |
| T13-29 | NOTE | R3T-14 | DOC-UPDATE | P1: R4's cross-window results are attached as information for phase 04 when they exist; phase 13 has no `reason=unsupported` branch and waits for nothing on the phone. |

## 2. Agent calls Jeremy can overrule
- T13-11 — the live source's two-layer mechanism (L unblurred and shared with the page, S blurred and 3σ larger than the surface).
- T13-13 — a press ends when the finger leaves the item (lights off then too); the reminder menu keeps its as-built look while held.
- T13-18 — the app list's picture at scale 1 with no parallax; HWUI's blur for the static copy.
- T13-19 — now-playing's `•••` band becomes acrylic (it shares `MusicMenu`); the pin band, jump grids, combo drop-down, name boxes
  and pickers stay solid.
- T13-20 — `reason=` precedence low-ram → battery-saver → setting.
- T13-24 — the live proof over Tess's listening persona.

## 3. Observations outside phase 13 (not fixed here; reported)
- O1 (phase 03): after a reminder row's long-press menu is dismissed without a choice, the row keeps its (63,68,64) pressed fill
  (`RemindersPage.kt:222,236`: `onMenuGone` does not clear `pressedRowId`). E3 uses that state as its control precondition as
  built. Whether W10M cleared it is phase 03's question.
- O2 (phase 03): `qa/phase-03/scripts/e15.sh` makes two spoken reminders and no photo, against its own header (`:8-9`) and E15's
  setup; E15 has never run (INDEX row 03). Phase 13's E3 uses its own helper (T13-21); E11 runs only E15's pane / menu sub-steps.

## 4. Round-1 / round-2 items both reviewers found correctly applied
T13-2, T13-4, T13-5, T13-7, T13-9, T11-7, T11-8, T12-3, T14-11, C-3, C-5, C-13, C-18, C-20, C-25, C-26, C-31 (union of both
lists; T13-3 and T13-8 were re-cut here by T13-23 / T13-13).
