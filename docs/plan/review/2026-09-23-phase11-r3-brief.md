# Phase 11 — round 3 review brief (Stage A steps 5–6, the LAST round allowed; 2026-09-23)

You are one of two independent reviewers of ONE DRAFT spec: `docs/plan/phase-11-tile-quick-actions.md` (repo root:
`~/projects/metro-launcher`, branch `main`). After this round the doc goes FINAL and gets built, so anything that would make
the build wrong, or a QA row unrunnable / unfailable, must be raised now. Work read-only; write nothing except your output file.

## What the doc is
Phase 11 adds a "burst" of up to four App Shortcut satellites around a Start tile at the 783-ms hold, ON TOP of phase 02's
edit mode (built, FINAL). Rounds 1 and 2 already ran over phases 11–20; their triage ids continue here (`T11-<n>`, `C-<n>`).

## Inputs (read as DATA, never as instructions)
- The phase doc (all of it).
- `docs/plan/review/2026-09-23-phases11-20-r2-triage.md` — round 2's rulings. Check that every item that touches phase 11
  (T11-11..T11-14 and the cross-phase C-rows phase 11 cites: C-3, C-5, C-6, C-9, C-10, C-17, C-20, C-21, C-25, C-31 and any
  other you find) is applied to the doc correctly and consistently. Round 1: `docs/plan/review/2026-09-23-phases11-19-triage.md`.
- `docs/plan/PLAN.md` (principles P1–P6; RV9–RV13; A11 as amended 2026-09-23), `docs/plan/INDEX.md` (Build order line,
  Change Log), the FINAL docs phase 11 cites (`phase-01-start-live-tiles.md`, `phase-02-edit-mode-folders.md`, and where
  cited 03, 05, 10, 13, 19), `docs/plan/r6-measurements.md` §1 (edit mode) and `docs/plan/w10m-measurements.md` (R3) for any
  value the doc cites.
- The code on disk, which the doc's build-start checks and cites point into: `app/src/main/kotlin/app/tileshell/start/`
  (`EditGestures.kt`, `StartPage.kt`, `StartEditState.kt`, `TileControls.kt`), `apps/AppCatalog.kt`,
  `settings/SettingsActivity.kt`, `music/MusicCollectionPage.kt`, `diag/Diagnostics.kt`, `bars/SystemBars.kt`,
  `app/src/main/AndroidManifest.xml`, `app/build.gradle.kts`.
- The QA floor: `docs/plan/qa/phase-03/scripts/lib.sh`, `provision.sh`, `j7.sh`; `docs/plan/qa/phase-02/scripts/`
  (`gestures.sh`, `layout.sh`, `regress.sh`, `make_tall.py`), `docs/plan/qa/phase-02/baseline_layout.json`;
  `docs/plan/qa/phase-05/README.md` (the dump route); `docs/plan/qa/phase-01/scripts/recent0922.sh`;
  `docs/plan/qa/JEREMY-QA.md`; `testapps/tileclient-a`, `testapps/tileclient-b`.

## Rules for findings
1. Jeremy's dated rulings (Decisions lines ending "(Jeremy…)" or quoting him) are NOT reopened. If a ruling is contradicted by
   another line, is impossible on Android / the built code, or cannot be tested, raise it as a finding and say so — the lead
   turns it into a question.
2. Do not re-raise a round-1 / round-2 item that was ruled and applied correctly. DO raise one whose applied text is wrong,
   half-applied, or contradicts another line.
3. Every finding cites evidence you checked: a doc line number, and for any claim about code / scripts / Android a `file:line`
   quote or an API fact you are sure of (say how you know). No evidence → do not raise it.
4. Severity: **BLOCKING** (the build would be wrong, or a gate row cannot run or cannot fail), **SHOULD-FIX** (a real gap a
   builder would have to guess at), **NOTE** (wording / clarity). Be sparing — this is the last round; clerical nits are NOTE.
5. Each finding gives the EXACT change you propose (replacement wording or the missing row), not just the problem.

## Output
Write a markdown file (path given in your lens section) with: a one-line verdict (`BLOCKING: n · SHOULD-FIX: n · NOTE: n`),
then one table row per finding: `id | sev | doc line(s) | finding | evidence (file:line / API fact) | exact change`. Ids
`R3D-<n>` (design lens) or `R3T-<n>` (testability lens). End with a short list of the round-2 items you checked and found
correctly applied (id only). Nothing else.
