BLOCKING: 2 · SHOULD-FIX: 6 · NOTE: 7

# Phase 11 re-judge: Reviewer 2 (testability and evidence), 2026-09-24

Scope: pass 5 (`docs/plan/qa/phase-11/SUITE.txt`, commit aaf7adc), the L11-1 fix's own gate list, the E6 method re-cut
(4769695), and my earlier report (review/2026-09-24-phase11-gate-evidence.md). This was a read-only review. I did not run
adb or an emulator, did not touch metro-launcher-p15, and pushed nothing. I did run `ffprobe`/`python3` locally over
committed `.pts` files and dumps. I treated every commit message, README line and INDEX line as a claim to check, not as
evidence.

## Findings

id | sev | file:line | finding | evidence | exact fix
---|---|---|---|---|---
R2-1 | BLOCKING | docs/plan/phase-11-tile-quick-actions.md:626-658 (Edge cases), :660 (QA evidence, empty) · qa/phase-11/README.md (no Edge-cases row) | **The phase doc's Edge Cases list was never run as a list.** The phased-build gate rule says: "Exercise the phase doc's Edge Cases list explicitly — happy path alone never passes the gate" (~/.claude/skills/phased-build/SKILL.md:83; Hard Rule 5, :105: "QA executes them"). Phases 01 and 02 each closed theirs with an Edge-cases row (phase-01 doc:303 `Edge cases \| EDGE/ … \| PASS`; phase-02 doc:103 `Edge cases \| PASS \| qa/phase-02/EDGE/EDGE.txt`). Phase 11 has no EDGE row, and neither the README nor the doc maps its Edge Cases to any evidence. These bullets have no device row, no named JVM test and no mapping: a hold during a live flip, where the satellites should stay still (:628); a hold while Start is flinging (:629); an incoming call, a notification-listener restart, and process death with a burst open (:636-637); a second finger (:638); Show more tiles toggled while a burst is open, which should close it with `stop` (:644-645); unpinning the last tile of a two-tile folder with a burst open (:646-647); a satellite with no short label, and very long labels (:652); Light / Dark label colour and X5 transparency at 0 % and 100 % (:656-657); and RV10's `wm size` / `wm density` / `font_scale` rows (:658). The earlier gate reviews missed this too, mine included. | `grep -rli` over qa/phase-11/scripts/*.sh finds no match for fling, font_scale, `wm density`, `wm size`, second finger / POINTER_DOWN, telecom / call, allow_listener, am kill / process death, Light theme, transparency / X5, ellips, dissolve / last tile. `set_more_tiles` (q.sh:190-198) is used only by E7's wide case, with no burst open. Covered bullets: transport hold E10 · no hold in edit mode E5 · disable / uninstall / unstartable E8 · quiet profiles E9 · screen-off / Home E5 · slide-off and label tap E5 (E5.txt:98,101) · folder / band / bottom row / promoted / WIDE / top-left E3/E7/E12/E16 · not host / off screen / unassigned / secondary E8/E16/E3 · query failed, equal ranks JVM (QuickRulesTest `aThrowingQueryIsCaughtAndBecomesTheReason`, `equalRanksBreakTiesById`) · icon fail E3. | Add an `EDGE` row (qa/phase-11/EDGE/EDGE.txt as its index, as phase 02 did). Map every bullet at doc:627-658 to one of three homes: an existing row or JVM test, cited by file:line or test name; a new sub-step, recorded; or a phone / NEEDS-HUMAN row, recorded with an INDEX Change Log line (the doc is FINAL). Any bullet that stays unmapped keeps row 11 short of `done`. Candidate new sub-steps: listener restart with the burst open (`cmd notification disallow_listener` then `allow_listener`, reading the close reason); `am kill` / force-stop with the burst open, then Start comes back with no `quick_burst`; a `KEYCODE_CALL`-style front window, which should close with `stop`; Show more tiles toggled while the burst is open, closing with `stop`; unpin inside `folder:qa`; RV10's three dumps using phase 01 E3's method.
R2-2 | BLOCKING | phase-11 doc:550-554 (E10), :627-628 (edge) · scripts/e10.sh:39-46 · README.md:23 | **E10's second sub-step never holds the art of a tile that shows the strip.** The row reads "the same hold on the tile's art above the strip → edit mode and Music's four satellites". The driver force-stops Fossify (e10.sh:39), restores the plain baseline (:40), and holds `tile:slot:MUSIC` at a quarter of its height (:42-44). That tile is a 165-px tile with no strip and nothing playing, so this is the same hold E15 proves. That matters now for two reasons. L11-1 is fixed, so the strip can be present: L11-1 (c) shows `tile_control:slot:MUSIC:PLAY_PAUSE` on the shell's own player (L11-1.txt:47-49). And d34d7bb ("return when `down.isConsumed`") is exactly the change that could swallow an art hold on a grown tile. This sub-step is that fix's only device-level negative control, and it has not run. README:23 says "the art hold bursts" with no qualifier. I flagged this gap in my earlier report (E10 section, gap 2) while E10 was blocked, and it is still open. | E10/music-rest.xml: `tile:slot:MUSIC` bounds `[899,439][1064,604]`. `grep -c tile_control:slot:MUSIC` gives 0 in both music-rest.xml and art-held.xml. E10.txt:25-26 is the only art evidence. | In e10.sh, play the shell's own player the way l11_1.sh:92-96 does. Assert `tile_control:slot:MUSIC:PLAY_PAUSE` is present, then hold the art above the strip for 1.0 s: x at the tile centre, y at tile top + (the top of `tile_controls:slot:MUSIC` − tile top) / 2, and assert that point lies outside every `tile_control:*` rect. Then assert `edit_disc:*` present, `[edit] hold` in the slice, and labels Songs / Albums / Artists / Playlists. Do this before the control hold pauses the player (or resume play first). Optionally repeat on Fossify's tile, asserting only edit mode on and its `[quick] shortcuts for org.fossify…` line.
R2-3 | SHOULD-FIX | scripts/l11_1.sh:82-86 · L11-1/L11-1.txt:39-43 · README.md:30 ("a reinstall … inherits no face (F-2)") | **The rewritten (d) still cannot fail on F-2.** The shape is new; the question is still whether it can fail. The reinstall-never-happened case is now caught: :73 asserts the uninstall, :79 the reinstall, and :85 asserts "Tap to choose" is absent. L11-1-smoke3-fossify-missing/L11-1.txt:40 shows the exact state :85 now rejects. But the tile check at :86 runs after `restore baseline_layout-player.json` (:82). `layout_restore` force-stops `app.tileshell` twice (qa/phase-02/scripts/layout.sh:21,24). Everything F-2 concerns lives only in memory: `LiveTileEngine.state` / `packageSources` (LiveTileEngine.kt:40,56) and `MusicFeed.published` (MusicFeed.kt:47). A fresh process holds no inherited face with or without 7cb3742, so "no inherited track face" passes on the pre-fix code too. The same assertion already passed while the player was missing altogether (smoke3 L11-1.txt:41). What does prove F-2 on the device is the pair of log lines at :75-76, `[engine] forget … (every source)` and `[music] forgot …`. Those can fail: pre-7cb3742 code emits neither. | L11-1.txt:36-37 (forget lines, real) · :41-43 (tile check after the restore) · smoke3 L11-1.txt:41 (`PASS … no inherited track face` with Fossify absent) | Keep one shell process from the uninstall to the read. Record `adb shell pidof app.tileshell` before :72 and again at the read, and assert they are equal. Re-pin the reinstalled player without `restore`: phase 02 E2's app-list pin route, or `layout_restore` only after the read. Then sample its tile for "An Ending". Also assert the positive label ("Music Player") at :85 rather than only the absence of "Tap to choose". Until then, word README:30 and INDEX:88 as "the engine and MusicFeed forget the package (log lines); no inherited face after a shell restart".
R2-4 | SHOULD-FIX | scripts/l11_1.sh:15,21,30 · L11-1/L11-1.txt:18 | **(a)'s "…not the notification" checks for text the notification never carried.** `verb_a` passes `"$@"` through `adb shell` (:15), and the remote shell splits it on spaces. So `--es title "Note title" --es text "note text"` (:21) reaches the app as title `Note` with no text, and the app falls back to `setNumber test` (testapps/common/.../VerbActivity.kt:334). The needle "Note title" (:30) appears in no dump, so this assertion cannot fail. The sub-step still holds: the ring line `from=notifications faces=1 … -> shows api` and "APIqueue" in 7 of 8 samples carry it, and I confirmed from the files that no api-over-notif-*.xml contains `setNumber test`. | L11-1/notif-after-clear.xml, fixture tile texts: `Note / setNumber test / Tile client A / 2`. `grep -c "Note title" L11-1/*.xml` gives 0 everywhere. `grep -l "setNumber test"` finds only notif-after-clear.xml. | Pass single-word extras (`--es title NoteTitle --es text NoteText`), or quote for the remote shell (`--es title "'Note title'"`). Add a positive control: assert the needle is present in notif-after-clear.xml, where the notification does show.
R2-5 | SHOULD-FIX | scripts/l11_1.sh:134-143 · L11-1/L11-1.txt:66,72 · README.md:30 · INDEX.md:88 | **"Phase 02 E5 exits 0" says nothing about F-1.** F-1's defect is that a notification rescan cleared a secondary tile's content. Phase 02's e5.sh never reads a secondary tile's content and never triggers a rescan. Its 14 checks cover pinning, launch arguments, decline, duplicate id, folder and uninstall (qa/phase-02/scripts/e5.sh:61-165). It passed before 7cb3742 as well; the bug was pre-existing. Yet the section header says "(the rescan no longer clears their content)" (L11-1.txt:66), and README:30 files the run under F-1. The fix review asked for this re-run as a check that nothing regressed, and that request is met. But the only evidence that F-1 is fixed is the JVM test `aSecondaryTilesKeyIsNeverClearedByTheArbiter`, and see R2-6 on that. | qa/phase-11/L11-1/p02-E05/E05.txt:5-56 (no content or face check, no rescan) · `grep -n "texts\|rescan\|listener" qa/phase-02/scripts/e5.sh` returns nothing | Either add a device step: pin st1 (E5 step 2), push content to it through the Live Tile API, force a rescan with `cmd notification disallow_listener` then `allow_listener app.tileshell/.feeds.TileNotificationListener`, and assert the secondary tile still shows its content, with a pre-rescan sample as the positive control. Or re-word L11-1.txt:66, README:30 and INDEX:88 to "phase 02 E5 regression exits 0; F-1 is proved by the JVM test", and do R2-6.
R2-6 | SHOULD-FIX | UNIT.txt:1 · SUITE.txt:4 · scripts/e14.sh:37 · README.md:32 | **The fix gate's first item, the JVM precedence and race tests, has only an aggregate count as evidence.** Nothing under qa/ names `TileSourcePrecedenceTest` except README:32, and its "12" comes from the source file. E14 runs a filtered `gradlew … --tests QuickRulesTest` last (e14.sh:37), and Gradle then clears the suite's XML results. After pass 5, build/test-results/testDebugUnitTest holds only TEST-app.tileshell.start.QuickRulesTest.xml, so UNIT.txt's `TOTAL 651` can no longer be re-derived. The arithmetic does fit: pass 3 was 639 (SUITE-pass3:4), pass 4 was 646 (+7, ca73663), pass 5 is 651 (+5, 7cb3742). Even so, F-1's proof (R2-5) and part of F-2's (R2-3) rest entirely on this class. | `grep -rl TileSourcePrecedenceTest docs/plan/qa` returns README.md only · `ls app/build/test-results/testDebugUnitTest` shows one XML | In run_all.sh, copy `TEST-app.tileshell.tiles.engine.TileSourcePrecedenceTest.xml` into L11-1/ right after the unit run and before any row. In the L11-1 row, assert `tests="12" … failures="0"` and assert the three testcases by name (`notificationNullsNoLongerWipeAPlayingFace`, `aSecondaryTilesKeyIsNeverClearedByTheArbiter`, `aForgottenPackageKeepsNothing`), following E14's QuickRulesTest precedent (e14.sh:39-44). Better still, have run_all keep a copy of the whole results directory.
R2-7 | SHOULD-FIX | INDEX.md:89 · scripts/e6.sh:2-6 · scripts/e6_frames.py:56-58 · E6-pass4-83ms/ | **The E6 re-cut is acceptable, but the reason it records is not what the files show, and the recording check can pass on a clipped window.** INDEX:89 says "every unrecorded open before had stayed at or under 33.3 ms", and e6.sh:3 says "ten unrecorded-era opens". But every warm open in the kept E6 logs is a recorded attempt: E6-pass2…, E6-rerun1…, E6-smoke…, pass 3's E6 at 0f9d116, E6-pass4…. Across those, recorded opens read ≤ 33.3 in all but one. So the files hold no unrecorded baseline to compare against. What the pass-4 files do show about the 83.3-ms open (attempt 2): its edit entry landed 3757.6 ms into the recording, against 1785.6–1818.2 ms for attempts 1, 3 and 4, so the input arrived about 2 s late. Its 4-s recording (e6.sh:79, `--time-limit 4`) ends at 3890.5 ms (open-2.pts, 111 frames), inside the 266-ms motion. So the recorder shut down mid-motion, and the host was stalled. Either could explain the gap, and host contention would hit an unrecorded open too. `e6_frames.py` quietly clipped that attempt's spacing window to 10 of its 19 gaps (:57 `hi = min(n-1, entry+18)`), so its `max_gap_ms 19.3` does not measure the motion. Nothing rejects a clipped window. | E6-pass4-83ms/E6.txt:25,29 (`maxGapMs=83.3`), :32 · recording-1..4.txt `entry_ms` · open-2.pts (entry idx 101 of 111) · pass-5 accepted attempt 4 covers idx 30-49 of 75 (max 18.178 ms), so pass 5 is not affected | Correct INDEX:89 and e6.sh:2-6 to the facts above: late input plus the recording ending mid-motion, and no unrecorded baseline. Have e6_frames.py print `window_gaps`, and have e6.sh retake any recording with fewer than 19 gaps in the window. Optionally keep asserting `maxGapMs` on recorded opens whose window was complete and whose `entry_ms` is within 200 ms of the run's median, so a recurrence is not silently noted away.
R2-8 | SHOULD-FIX | phase-11 doc:660 | **The phase doc's `## QA evidence` section is an empty heading**, the last line of the file. Phases 01, 02 and 03 fill theirs (26, 38 and 26 lines; phase-02 doc:86 onward, for example), and the skill's rule 15 puts the NEEDS-HUMAN rows there (SKILL.md:115). The re-judge brief itself points reviewers at "the doc's QA evidence section". | `awk '/^## QA evidence/{f=1;next} /^## /{f=0} f' phase-11-…md \| grep -c .` returns 0 | Through an INDEX Change Log line (the doc is FINAL), write the evidence table: rows E1–E16, L11-1, EDGE (R2-1), unit, each with result and path, as phase 02's doc does (its `## QA evidence` at :86, the Edge-cases row at :103). Add the NEEDS-HUMAN and phone rows with their status.
R2-9 | NOTE | scripts/e6.sh:107 · app/src/main/kotlin/app/tileshell/start/QuickBurst.kt:257-258 · QuickRules.kt:255-257 | **"close settle = alpha0 ± 17 ms" cannot fail.** The close line prints the same value into both fields: `alpha0=${alpha0.toInt()} settle=${alpha0.toInt()}`. The doc's "a fade that ends on arrival fails" is carried by the alpha0 bound (36 ± 17, e6.sh:106) alone, and the drawing uses the same `QuickMotion.closeAlpha` (QuickBurst.kt:266), so that bound is real. | E6/E6.txt:64-66 (`alpha0=50 settle=50`) | Drop the settle assertion, or log settle as the frame the layer stops drawing (the frame `quick.clear()` runs), and say which in the doc.
R2-10 | NOTE | E6/E6.txt:17-51 · phase-11 doc:59-60, :124 · QuickBurst.kt:204-213 | **What E6 can and cannot fail on.** (a) In all five asserted opens, and in every warm open of every kept run, `t0 − entry = 16–17 ms`: the burst starts one frame after the entry's first frame, every time, right at the ±17 edge. The doc says the burst opens in "normally that frame" (doc:60), which the device does not show. That is Reviewer 1's call. (b) peak, overshoot and settle are the spring's own values sampled at frame times (QuickBurst.kt:209-213), not drawn pixels. E6 fails on a changed spring, on jank (maxGap on 5 opens) and on late alignment. It cannot fail on a layer that draws without using `quick.progress`. With the pixel detector retired (G-E6-4), drawn motion is only H1 / H2. | E6.txt:23,30,37,44,51 (`17.00 vs 0.00 +/- 17.0`) | (a) Reviewer 1 / lead: either re-word doc:60 or look at why t0 is always the next frame. (b) Record in the README E6 row that the drawn motion rests on H1 / H2.
R2-11 | NOTE | fix plan "Its gate" (review/2026-09-24-L11-1-fix-plan.md:42) · scripts/l11_1.sh:45-49 | **"Phase 01 E8's playback face re-verified" rests on the saved files, not on an assertion.** The fossify-1..3 dumps do show the face (`texts=An Ending / Brian Eno` on `tile:app:org.fossify…Green:0`, which I checked with qa/phase-01/scripts/nodes.py), but the row asserts only the strip and the publish lines. | L11-1/fossify-1.xml … fossify-3.xml | Assert "An Ending" in the tile's texts in at least one of the three dumps.
R2-12 | NOTE | scripts/l11_1.sh:92-110 | **(c) never asserts the shell's player state.** Play is shown by the strip plus the notification count, and pause (:108-110) only by the strip's absence. (b) asserts `state=PLAYING(3)` and `PAUSED(2)` through `session_state` (:43,60). If `KEYCODE_MEDIA_PAUSE` missed and the strip vanished for some other reason, :110 would still pass. | L11-1.txt:44-53 | `assert_contains … "state=PLAYING(3)" "$(session_state app.tileshell)"` after :96, and `PAUSED(2)` after :108.
R2-13 | NOTE | scripts/e10.sh:28-33 · phase-11 doc:551-552 | **E10's control dump is saved but never asserted.** The spec's own checks (no `quick_burst`, no `edit_disc:*`) are left to ring absences (`[edit] hold`, `[quick] burst on`), which are sound. The saved control-held.xml agrees: it has 0 `edit_disc` or `quick_burst` nodes. | E10/control-held.xml | Assert both absences on control-held.xml, next to a positive `start_page` check.
R2-14 | NOTE | README.md:69-73 · INDEX.md:95 · COLDJANK/COLDJANK.txt:14-20 · E6-smoke-fullsize-recording/E6.txt:17,33 | **Two README / INDEX claims are wider than their captures.** (1) "janks in phase 02's own edit-mode entry … a burst or not": COLDJANK shows the folder at 25.45 % and 27.27 % janky frames, but the fixture, where the burst opens, at 41.67 % and 37.50 %, so the burst adds to the cold jank. INDEX:95's "30-36 %" for the folder is not in COLDJANK, which was captured the next day. (2) "opens whose own frame clock read 16.7 ms throughout": smoke attempts 1 and 3 read `maxGapMs=33.3`. | as cited | Reword both against COLDJANK.txt and the smoke log, and tell P2 the cold burst open is the one to read on the phone.
R2-15 | NOTE | L11-1/p01-E15/E15.txt:1 · L11-1/p01-E17/E17.txt:1 · scripts/e13.sh:60-61 | **Two provenance gaps.** Phase 01 e15.sh and e17.sh write no driver blob, unlike phase 02 e5.sh (E05.txt:2-3) and E13's sub-drivers. The scripts are unchanged from aefe4b9 to HEAD and the tree is clean, so no harm is shown. Separately, E13's `contacts-shortcuts.txt` is the whole `dumpsys shortcut` (its note lists `albums`, `checklist`, …), not Contacts' section; the ring line `shortcuts for com.android.contacts/.activities.PeopleActivity/0: 1 (1 shown: shortcut-add-contact)` is what carries it. | E13/E13.txt (Contacts' shortcuts note) · E13/ring-launcher.txt | Stamp `git hash-object` for the phase 01 drivers in the L11-1 log. Filter the Contacts dump by package (the awk section filter e8.sh:43 uses), or drop the note.

## 1. Pass 5: one APK, one driver set, every row

- **One run.** SUITE.txt:1 starts at 12:33:58 and E14 finishes it at 13:20 (E14.txt:3). There is no `--append` section: all 17
  rows plus unit ran once, E14 last. Every row log E1–E16 and L11-1 stamps `apk installed 114511e7de8bb2e0`, `apk match
  yes`, `apk built a982b1894cd5600b` (the sha256 of the same file; aefe4b9's message uses that id) and `lib.sh blob
  f296b3dd` (= aaf7adc's). Each `driver … blob` equals `git rev-parse aaf7adc:<driver>`; I checked all 17. The row times
  run in order (E1 12:34:00 … L11-1 13:11:00, E14 13:20:34).
- **Code and drivers.** `git diff --stat 7cb3742 aefe4b9 -- app/ testapps/` is empty, so the APK is 7cb3742's code as
  README:12 says. `git diff --stat aefe4b9 HEAD -- docs/plan/qa/*/scripts` is empty, and `git status` is clean apart
  from the briefs file. The consoles (`.e*.console`, `.l11_1.console`) contain no error, traceback or adb argument error.
- **Exit codes.** SUITE.txt:5-21 shows 17 × `exit 0`, and the counts equal each log's last line and README:14-30.
- **Rows whose drivers changed since my pass-2 review** (`git diff 97697c8 aefe4b9`): e2, e3, e4, e5, e8, e11, e14 and e16
  (my findings, checked below); e6 (item 3); e10 and l11_1 (items 1-2). The others are unchanged and their pass-5 logs
  match pass 3's assertions and counts. The absence checks I sampled (quick_burst "no" in e2/e3/e5/e8/e9/e15/e16) each read
  a Start dump paired with a positive check such as `edit_disc` or `start_page`.
- **Warm and cold.** Every hold outside E6 is the first hold after a process start (I paired each `motion open` line
  with the ring's `[app] process start` lines), and each reads `maxGapMs` 33.3 or 50.0. No row outside E6 asserts
  motion, so this is consistent with COLDJANK (R2-14).
  All nine warm opens in pass 5, the five asserted and the four recorded, read 16.7.

## 2. The L11-1 fix plan's gate list (review/2026-09-24-L11-1-fix-plan.md:33-43)

| Item | Evidenced? | Where |
|---|---|---|
| JVM precedence table + the race | Tests exist (TileSourcePrecedenceTest.kt:18-100; the race is :35-44, seven NOTIFICATIONS nulls). Their pass shows only in the aggregate | UNIT.txt:1 → **R2-6** |
| L11-1 row: Fossify strip in 3 dumps over 6 s, every later publish `shows music`, pause drops the strip; the same on the Music tile | Yes. Failable: under L11-1 the last `from=notifications … null` wipes the face before the dumps (the publishes end at 13:13:01.552; the dumps start after Home + 3 s). The race actually ran (3 and 8 notification publishes after the face) | L11-1.txt:23-32, :47-53; L11-1/fossify-publishes.txt:10-14 |
| Phase 11 E10 with its control sub-step | Control hold: yes. The 1000-ms swipe is past 783 ms, so pre-d34d7bb code would log `[edit] hold` and fail. `[tile_control] … PLAY_PAUSE (no launch)`, no hold, no burst, PAUSED. Art-above-strip: **no** | E10.txt:17-22 · **R2-2** |
| Phase 01 E15 / E17 re-run | Yes, and asserted this time, since those drivers carry no verdict. Every "faces seen" section they read from is non-empty (E15.txt:4-25), so the absence checks are real | L11-1.txt:57-65 |
| Phase 01 E8's playback face | In the dumps, not asserted | **R2-11** |
| Phase 11 E14's coverage | Yes: 28/28, run last over the 15 E-rows on this APK | E14.txt:12-57 |
| Fix review F-1: re-run phase 02 E5 | Re-run, exit 0 (14/14), but it cannot test F-1 | **R2-5** |
| Fix review F-2 (not in the plan; it came from the review) | Forget log lines, real. The tile check cannot fail | **R2-3** |

**On (d), which the brief asked about directly.** The first cut's failure (no reinstall, tile "Tap to choose", PASS) cannot
recur: :73, :79 and :85 each fail in that state, and :69 pins the reinstall source by sha256. The row's headline check at
:86 is still one that cannot fail, now because of the `restore` at :82 (R2-3). I searched the rest of the row for the same
shape and found two more: (a)'s "…not the notification" (R2-4) and the F-1 run (R2-5). (c)'s pause check is weaker than
(b)'s (R2-12).

## 3. The E6 method re-cut (4769695)

Asserting only the unrecorded opens is **acceptable**. E6 still fails when the motion is wrong. Each of the five warm opens
in part 1 is asserted for peak (107 ± 17), overshoot (6.8 ± 2), `maxGapMs` ≤ 33.4, settle ≤ 249.1 + that open's gap + 1,
and t0 within one frame of `[edit] entry first frame`. A missing line fails "one open line" or can't be measured, and
`front_ok` fails an attempt that has a dialog up (e6.sh:28-41, :61-71). So a changed spring, jank, or a late burst fails
the row. What the re-cut gave up: a recorded open's clock numbers no longer fail anything (E6.txt:53-59), and the record
of why the method changed cites a comparison the kept files don't contain, while the corroboration can pass on a clipped
window (**R2-7**). E6 cannot fail on the close's settle (**R2-9**) or on drawn motion (**R2-10**). In pass 5 all four
recorded opens read 16.7 and the accepted recording covers its whole window (idx 30-49 of 75, max gap 18.178 ms), so pass
5's PASS stands.

## 4. My earlier report's findings, re-judged on pass 5

| Id | Status | Evidence |
|---|---|---|
| G-E6-1 (BLOCKING) | **Resolved** | One pass, one driver set, SUITE PASSED, E6 run inside it (E6.txt:3 12:44:20), E14 last |
| G-E6-2 | **Resolved for the asserted opens** (every part-1 open, settle bound per open). The re-cut left recorded opens unasserted | E6.txt:17-51 · R2-7 |
| G-E6-3 | **Resolved** | L11-2 recorded (README:62-66, INDEX:93); trace kept (E6-pass2…/anr_2026-09-24-08-04-57.txt); `front_ok` |
| G-E6-4 | **Resolved** (alignment moved to the shell clock; pixel lag printed only) | e6.sh:52, :98 · R2-10 (a) |
| G-E10-1 | **Resolved** (E10 runs on Fossify) | e10.sh:14-17 · E10.txt:14-15 |
| G-E10-2 | **Resolved** (the control sub-step is gated on the strip; the control's centre is asserted; README #1 now matches the evidence) | e10.sh:23-38 · E10.txt:17-22 · README:37-38 |
| G-E11-1 | **Resolved** | E11.txt:19 (ring absence) |
| G-E2-1 | **Resolved** (dump checks dropped) | e2.sh:15 |
| G-E4-1 | **Resolved** | E4.txt:15 |
| G-E5-1 | **Resolved** (dy; the discs follow the selection) | E5.txt:45-46, 70-72 |
| G-E14-1 | **Resolved** | e14.sh:22; E14 runs last |
| G-E8-1 | **Resolved** (positive control; `cmd shortcut` swapped for the package's `dumpsys shortcut` section after pass 3's control caught it) | E8.txt:22, :27 |
| G-E16-1 | **Resolved** (precondition asserted: cell bottom −223 < 84; computed with a re-implemented GridPack and backed by the product's `off screen`) | E16.txt:25-26, :29 |
| G-E3-1 | **Resolved** | E3.txt:29 |
| G-README-1 | **Resolved** (gfxinfo captured). New wording issue in R2-14 | COLDJANK/gfxinfo-*.txt |
| G-README-2, -3, -4 | **Resolved** (counts dropped; 23; WIDE mid-grid pinned by `aFullWidthWideTileMidGridKeepsAClampedCorner`, which is in E14's XML) | README:32, :75-77 |
| E10 gap 2 (in my text, not an id) | **Open** | R2-2 |

## What I checked and found right

- The provenance and exit codes in item 1.
- README claims I checked against files, and found supported:
  - README:12's header (APK, code, SUITE PASSED).
  - README:37-38: #1 was verified in pass 5's E10 (E10.txt:19-22).
  - README:51-55: pass 5's E10/l11-1-sequence.txt shows the notification publishes resolving to the playing face
    (`from=notifications … -> shows music (playing)` ×3 after the face).
  - README:57-61, the vacuous first cut: smoke2 and smoke3 are kept, and aefe4b9 asserts the reinstall.
  - README:62-66, L11-2 and its trace.
  - README:67-68 and E13.txt:61-64, the phase 03 allow-list gap.
  - README:74 (ASSISTANT host) and E8.txt:15.
  - README:78-80, the dump route: `.drv` retries.
- **L11-1 (a)'s real proof.** The notification publish with `faces=1 source=notification` resolved `-> shows api`
  (L11-1 ring, 13:11:32.900). The API's clear resolved `-> shows notifications` (13:12:16.592), and notif-after-clear.xml
  then shows the notification. So the API-over-notifications rule survived the arbiter.
- **E10's l11-1-sequence.txt** shows the fix working on the named fixture: ten `from=notifications` nulls, the three after
  the face each resolving to `shows music (playing)`.
- **UNIT arithmetic** 639 → 646 → 651 matches +7 (ca73663) and +5 (7cb3742), and TileSourcePrecedenceTest.kt has 12
  `@Test`s.
- **E14's union** holds 15 rows, all on 114511e7. Its patterns are end-anchored and `shortcuts for` is activity-keyed.
  QuickRulesTest has 23 tests, 0 failures, and `aThrowingQueryIsCaughtAndBecomesTheReason` is present (E14.txt:54-57).
