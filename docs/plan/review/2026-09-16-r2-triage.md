# Review round 2 triage (2026-09-16)
Inputs: review/2026-09-16-r2-codex.md (R2R2-01 BLOCKING, R2R2-02 BLOCKING) and review/2026-09-16-r2-fable.md
(0 BLOCKING; N-01..N-12). Round-1 blockers: all RESOLVED except B4/R2-06 (Shizuku), per both reviewers.

| Finding | Disposition |
|---|---|
| R2R2-01, N-02, B4, R2-06 | ASK Q13 (pre-split: it settles the one-APK contract, RV1). The per-toggle matrix stays in the interview of the phase that introduces the helper (phase 3 if Cortana has settings toggles, else 4). |
| R2R2-02 | AGENT: RV9 source order. R3 scope extended to check legacy tilt/turnstile values against W10M footage (message sent to R3). |
| N-05 | ASK Q12 (governing build). AGENT: R3 now records build, source device + epx canvas, fps and tolerance per value (message sent). |
| N-03 | AGENT (Q14 withdrawn per Jeremy's P3: phase placement is the agent's call). Phase 1 = code-defined default layout in the permanent tile-unit model; pinning and persistence are part of the edit-mode part (phase 2). |
| N-01 | AGENT: liveness/recovery edge cases (reboot, 24 h idle, Device care optimise, firmware update, service disabled, launcher crash-loop -> One UI Home fallback) go into phases 1 (listener + HOME), 3 (assistant / hotword service), 4 (accessibility + overlays), 5 (IME), 7 (glance), 8 (LLM). |
| N-04 | AGENT: RV10 (Screen zoom and Font size ignored by the shell; Density at every owned window; AVD FHD+ 450 dpi; phase 1 QA rows). |
| N-06 | AGENT: R3 phase-1 addendum (Settings page, app list, per-tile content layouts; message sent). |
| N-07 | AGENT: RV3 amended (open-vocab text for unmatched utterances; grammar+open pass allowed). |
| N-08 | AGENT: RV4 caveat; settings toggles via Cortana follow Q13. |
| N-09 | FACT: CANNOT list widened (return animation under both nav modes, system splash); phase 1 transition defined; NEEDS-HUMAN rows. |
| N-10 | PHASE-6: precondition = a second phone number (person or second phone) for real call/SMS QA. |
| N-11 | PHASE-1: Weather tile tap target + location/units source (default: device location + locale). |
| N-12 | AGENT: RV5 amended (no un-retimed third-party motion; vendor+retime vs in-house in the phase 1 interview). |

Q12 ruled C (final release). Q13 ruled A (helper inside our APK; feasibility check opens its phase). Q14 withdrawn (P3), resolved by the agent.
Round 3 runs after Q12-Q14 are ruled (last round allowed by the cap).
