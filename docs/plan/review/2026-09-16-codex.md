# Pre-split review: Reviewer 2 (codex MCP, read-only sandbox), 2026-09-16
Lens: testability, evidence, internal consistency. Inputs: PLAN.md, w10m-reference.md, r2-reuse-check.md.
Captured from mcp__codex__codex (threadId 01a0ac7d-c2db-7322-8c0e-66e154f2b85e), verbatim below.

R2-01 | BLOCKING
Claim: The Start-grid geometry is unresolved, and R1's asset dimensions cannot be used directly as rendered tile dimensions. Three 150-epx medium tiles already exceed either proposed 360- or 408-epx canvas before gutters. A uniform gutter also cannot satisfy both 2×71→150 and 2×150→310. The split needs one rendered-grid formula, distinct from asset sizes, with column counts, margins, density conversion and screenshot tolerances.
Evidence: docs/plan/w10m-reference.md:28: "small 71x71, medium 150x150, wide 310x150"; :49: "three-column layout by default"; :353–355: "360 epx wide", "408x884 epx canvas", "Pick one and state it in the design tokens". docs/plan/PLAN.md:50: "'show more tiles' column option".
Owner decision: Should the shell preserve the Lumia 950's screen proportions or its physical element sizes, with which medium-column counts for "show more tiles" off and on?

R2-02 | BLOCKING
Claim: "Exact W10M fidelity" has no acceptance baseline. R1 mixes Windows generations and leaves core geometry and motion unsourced; R2 substitutes another recreation's timings without evidence that they match W10M. Select a reference build and identifiable screenshots/videos, measure missing values with tolerances, and explicitly label remaining choices as approximations. Owner judgments about feel need NEEDS-HUMAN rows; implementation screenshots cannot establish their own historical correctness.
Evidence: docs/plan/PLAN.md:49: "Exact dimensions and motion come from the research task R1." docs/plan/w10m-reference.md:39: "W10M gutter / edge margin in epx | NOT FOUND"; :94: "Flip / peek / cycle durations (ms) | NOT FOUND"; :136: "W10M shell Start→app launch animation … | NOT FOUND"; :254: "Which palette … actually exposed … NOT FOUND"; :320: "Ring radii, stroke, frame timings … | NOT FOUND". docs/plan/r2-reuse-check.md:93: "copy live-tile timings … from MetroLauncherAndroid".
Owner decision: Which W10M reference build and visual artifacts should govern acceptance, and may unsourced details become explicitly approved approximations?

R2-03 | BLOCKING
Claim: Build items 1 and 3 overlap on the same part without defining an additive boundary. Item 1 must deliver a permanent animation engine with an executable real-system acceptance path before the live-data sources in item 3 exist. Sample-content animations later replaced by a production implementation would violate Rule 16; mock-only demonstrations would violate Rule 4. Define the permanent renderer, content contract and initial real content source before splitting.
Evidence: docs/plan/PLAN.md:79: "Start screen: animated live tiles"; :82: "Live tiles (notification-driven, Photos, Calendar, Music, Weather)". docs/plan/r2-reuse-check.md:41: "Not provided: live-tile flip/cycle, 2-D tile grid with resize, folders, notification binding — those are ours to write."

R2-04 | BLOCKING
Claim: R2's speech roadmap can replace a completed assistant part, and its constrained recognizer leaves the later LLM's input path undefined. Adding hotword detection is additive; replacing platform TTS with Kokoro is not automatically additive. A grammar restricted to fixed commands also does not establish that arbitrary spoken requests will reach the later LLM intact. Define permanent speech responsibilities and an unknown-utterance path before splitting, without necessarily choosing the final model now.
Evidence: docs/plan/PLAN.md:38–41: "each layer is permanent … handles whatever the fixed commands don't catch … Nothing gets rebuilt or thrown away." docs/plan/r2-reuse-check.md:197: "grammar constrained to the command list"; :201: "Pick Vosk (5.1) or this, not both"; :223: "v1 = … platform TextToSpeech … v2 = sherpa-onnx … + Kokoro TTS".
Owner decision: Should the first assistant retain its speech engines permanently with later capabilities added alongside them, or should its final speech architecture be implemented before that part is accepted?

R2-05 | BLOCKING
Claim: R2 explicitly proposes an interim keyboard that might be replaced by a fork, contrary to Rule 16. The alternatives also have different installation and QA boundaries: an independently installed FlorisBoard plus theme versus a shell-owned IME. Missing autocorrect is an acceptance limitation, not an owner ruling. Resolve the permanent delivery boundary before assigning keyboard phases.
Evidence: docs/plan/r2-reuse-check.md:137: "ship a Snygg theme/extension … user installs FlorisBoard" or "fork and strip to a W10M-only IME"; :260: "shipping a Snygg theme first … only forking if the theme engine cannot reach the W10M look. Accept 'no autocorrect' until FlorisBoard lands it." docs/plan/PLAN.md:122: "Keyboard layout … details (their own phases)".
Owner decision: Is a permanent external FlorisBoard installation without guaranteed autocorrect acceptable, or must the completed keyboard be a shell-owned IME?

R2-06 | BLOCKING
Claim: Permission willingness does not settle the action center's capability boundary. R2 leaves Shizuku versus system-settings handoffs undecided, changing deployment, reboot behavior, visible fidelity and acceptance. Its proposed direct-toggle list is explicitly unverified. Before splitting, classify each promised action by actual target-device behavior and identify any unavoidable Android UI.
Evidence: docs/plan/PLAN.md:42: "Jeremy grants everything"; :103: "Full W10M shell built from apps, no root". docs/plan/r2-reuse-check.md:113: "Shizuku mandatory … must be re-started after every reboot"; :126: "a mix of direct calls … and Settings.Panel intents … re-verify per toggle"; :258: "Decide early whether to require Shizuku for the rest."
Owner decision: Should the shell require Shizuku and its reboot setup, or permanently accept system-settings handoffs for restricted controls?

R2-07 | BLOCKING
Claim: The plan lacks a device-and-human acceptance contract for independently completed phases. Emulator execution can exercise Android components, but cannot establish carrier call/SMS delivery, Samsung side-key behavior or Samsung-specific permission and service persistence. The split needs designated emulator versus S25 tests, provisioning prerequisites, and NEEDS-HUMAN rows for owner-only setup or observations. Otherwise "emulator passed" can conceal unfinished phone acceptance.
Evidence: docs/plan/PLAN.md:31: "confirm the current OS version … when the phone is first connected"; :92–93: "Develop against the Android emulator … then sideload the same APK onto the Samsung"; :59: "phone dialer (default dialer role), messaging (default SMS role)"; :61: "possibly registered as Samsung's default assistant (side-key long-press)".
Owner decision: Will the S25 Ultra, a working SIM and a cooperating call/SMS endpoint be available for the phases that require them, with owner-only setup and observations recorded as NEEDS-HUMAN?

R2-08 | MAJOR
Claim: Cortana's deferred command list can change dependencies across several phases. Commands involving calls, messages, volume or settings may need parts scheduled later. Preserve a permanent action contract using real existing system capabilities where appropriate, and distinguish launching an app from completing an action. If a command requires a later shell component, it cannot be accepted earlier through a placeholder.
Evidence: docs/plan/PLAN.md:40–41: "calls the same phone actions"; :83–86: "Cortana: offline fixed commands", followed by overlays, keyboard, and "Phone dialer + messaging"; :118–119: "These only affect one phase each … Cortana's fixed command list".
Owner decision: May fixed commands permanently operate through existing Android apps and system actions before the corresponding W10M shell parts are added?

R2-09 | MAJOR
Claim: "Notification-driven for every app" lacks a falsifiable meaning. Notifications are not evidence of an app's complete unread state, and an app may expose no usable notification content. Define whether tiles represent active notifications, app-reported badge values or actual unread items, and specify empty, grouped, updated, dismissed and redacted notification behavior. QA needs expected results from real notifying apps.
Evidence: docs/plan/PLAN.md:75: "notification access for unread counts"; :105: "Notification-driven for every app". docs/plan/r2-reuse-check.md:51: "NotificationListenerService feeding Tile.liveMessages/liveBody/liveTitle".
Owner decision: Is notification-derived content with an explicit empty or unavailable state acceptable when an app exposes no reliable unread count?

R2-10 | MAJOR
Claim: The live-tile phase has no real-data provisioning or observable update contract. A fresh emulator cannot demonstrate Photos, Calendar or Music behavior without actual media, calendar entries and playback; Weather needs a selected real service and configuration. Phase QA must seed real providers or apps, exercise the installed shell, and verify additions, removals, empty data, revoked access, offline operation and stale weather—not inject renderer state.
Evidence: docs/plan/PLAN.md:82: "Live tiles (notification-driven, Photos, Calendar, Music, Weather)"; :76: "Weather needs an outside API"; :45: "The Weather tile is in. It's the only launcher feature that uses the internet."

R2-11 | MAJOR
Claim: Offline voice acceptance must cover the full microphone→recognition→action→audible-reply path. Text entry or an animated listening ring cannot prove voice input. Platform TTS additionally requires provisioned offline voices, and platform recognition availability is unverified. QA needs a working emulator audio route or physical-phone execution, network-disabled runs, and cases for silence, noise, permission denial, missing voice/model assets and interrupted speech. Owner judgments about intelligibility or voice character belong in NEEDS-HUMAN rows.
Evidence: docs/plan/PLAN.md:60: "voice in and out"; :95–97: "offline fixed commands … fully offline … No cloud AI". docs/plan/r2-reuse-check.md:212: "Availability is per-device/per-language … UNVERIFIED on the S25 Ultra"; :221: "offline with downloaded voices".
Owner decision: Must voice work offline immediately after sideloading, or is explicit one-time model and voice provisioning acceptable?

R2-12 | MAJOR
Claim: App launch and return transitions have neither a defined ownership boundary nor demonstrated target-device feasibility. Animating the launcher does not prove control over an arbitrary app's entry, system splash or return through Home/Back. Acceptance needs recordings of real cold and warm launches and returns under the selected navigation mode, with explicit treatment of unavoidable system animation.
Evidence: docs/plan/PLAN.md:54: "App launch and return transitions"; :79: "launch real apps with the transition". docs/plan/w10m-reference.md:136: "W10M shell Start→app launch animation … | NOT FOUND".
Owner decision: Is W10M animation limited to the shell-controlled portions of launch and return acceptable when Android controls the remaining transition?

R2-13 | MAJOR
Claim: R2's "volume panel is largely solved" conclusion exceeds its evidence. The window type is unverified and no Samsung-specific validation is reported. Reusing key capture does not prove suppression of the stock panel, gesture coexistence or survival across lifecycle events. Overlay QA must execute real volume keys and top-edge swipes, verify actual volume changes, and cover permission revocation, reboot, process death, lock/unlock and competing overlays.
Evidence: docs/plan/r2-reuse-check.md:106: "Window type not seen … UNVERIFIED"; :107: "No Samsung-specific code seen"; :257: "Volume panel is largely solved … not a research item." docs/plan/PLAN.md:84: "Action center + volume panel overlays".

R2-14 | MAJOR
Claim: "Glance screen" does not define whether acceptance means a visible lock-screen activity or an always-on/proximity-driven display. These are materially different behaviors; a black activity screenshot proves neither screen-off operation nor coexistence with Samsung's secure lock screen. Define activation, dismissal, proximity, charging and authentication behavior, then test them on the phone with measurable power and timing criteria.
Evidence: docs/plan/PLAN.md:72: "Apps CANNOT fully replace: the real lock screen security"; :87: "Glance screen". docs/plan/w10m-reference.md:304: "modes off / 30 s (proximity) / 15 min / always on". docs/plan/r2-reuse-check.md:263: "lock-screen showWhenLocked activity or overlay".
Owner decision: Must Glance provide always-on and proximity behavior, or is a W10M-style screen shown over the existing secure lock screen sufficient?

R2-15 | MAJOR
Claim: Settings cannot remain an independent late placement if earlier phases must expose and prove configurable features through the real UI. Accent selection, column changes, background selection and permission setup need permanent entry points when their owning parts are accepted. Establish the settings host early and add pages with their parts; temporary controls or debug-only mutation would leave earlier acceptance incomplete or require replacement.
Evidence: docs/plan/PLAN.md:50–51: "accent color, 'show more tiles' column option, full-screen background image"; :57: "Settings screens in Windows 10 Mobile style"; :89: "Settings screens: placement to decide at the split".

R2-16 | MAJOR
Claim: R2 provides no target-device evidence for the proposed LLM's usability or its emulator execution path. S26 benchmark numbers do not establish S25 latency, available memory, thermal behavior, runtime compatibility or correct phone-action execution. The LLM phase needs real S25 measurements and explicit quality, latency and resource thresholds, plus offline asset provisioning and tests for unsupported requests and failed actions. Runtime selection can remain deferred, but benchmark claims cannot serve as acceptance.
Evidence: docs/plan/r2-reuse-check.md:232: "GPU via OpenCL"; :243: "card benchmarks on Galaxy S26 Ultra"; :251: "LiteRT-LM … + Gemma 4 E2B"; docs/plan/PLAN.md:30: "Galaxy S25 Ultra"; :40–41: "handles whatever the fixed commands don't catch and calls the same phone actions".

BLOCKING: 7
MAJOR: 9
MINOR: 0
